# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式；版本号语义化（0.x 阶段以次要版本承载功能批，补丁号承载修复）。

## [Unreleased]

### 新增
- **CI 宿主发版探测轨（COMPAT-007，轴③ C1；DEC-025 ADR 决策②② + RB-01 窗口期披露）**：`.github/workflows/ci.yml` 新增 `host-latest-probe` job——`on.schedule` **每日一次**（`cron: '17 3 * * *'`，UTC；频率 SLO = 每日一次，RB-01 明示窗口期）＋ `workflow_dispatch` 手动触发；`if` 限定仅这两类事件（**只读网络探测不进 PR 门禁**）。**供应链白名单（硬约束，BC-05）**：job 内注释写明白名单——仅允许 `npm view <pkg> versions|dist-tags --json`（只读注册表元数据），**不 `npm install` / `npm ci` / `npx` / `npm pack`、不解包执行、不执行宿主代码、不用 `actions/cache`**；理由 = C1 若为此 install rc 版宿主闭包（200+ 包、rc 直接打 latest tag——契约 6.5）则探测轨自身即引入供应链面与「探测轨成为事故源」风险，而版本元数据已足够支撑判据。`permissions: contents: read` + 探测目标集**逐行显式**（便于白名单逐行审计）。**判据逻辑内联于 ci.yml**（heredoc → `$RUNNER_TEMP/nv-host-latest-probe.mjs`，**不新增仓库路径**，commit 恰三路径；本地验证以**同一 heredoc 提取物**运行 ⇒ 验证产物 ≡ 交付产物）。判据四条子句（逐包可归因）：① 探测面 ≡ 契约 `hostSurface.packages` 并集（**双向**：漏探=红、越界探=红——契约是探测目标集单一事实源）；② fixtures 覆盖版本 MUST 仍在上游已发布列表内（覆盖声明陈旧/被撤回 ⇒ 红）；③ fixtures 自洽（host 族包版本 MUST ≡ `hostVersion`；`versionExceptions`〔cordis/schemastery〕MUST ≠ `hostVersion`，防例外表腐化）；④ 版本车判据（已覆盖车 `major.minor.patch` 内上游最大版本 MUST ≡ 已覆盖版本，且上游 MUST NOT 出现更高车）⇒ 违反即输出 `detected new host version X, fixtures covered Y ⇒ 需审阅 + 更新 fixtures` 并 job 红；退出码 0 绿 / 1 红 / 2 输入错误。
  - **判据设计经实测订正（P-01，如实留档）**：任务书设想的「关键子包 `latest` 版本 vs fixtures `hostVersion`」直接比对**实测为恒红假阳**——`@deepseek-ai/dsh-*` 子包的 `latest` dist-tag 停在 `0.0.1-rc.X` 旧线（实测 `dsh-settings` latest=`0.0.1-rc.1`、`dsh-home-paths` latest=`0.0.1-rc.3`），宿主线实际发布在 `next`/`alpha` tag（`@deepseek-ai/dsh` dist-tags = `{latest: 0.1.5-rc.1, next: 0.1.5-rc.2, alpha: 0.1.5-alpha.2}`；子包 `next=0.1.5-rc.2`）。故判据改为**「已发布版本列表 + 版本车比较」**，与 dist-tag 命名解耦；`dist-tags` 仅作上下文报告（非判据）。受限版本口径 `X.Y.Z` / `X.Y.Z-{alpha|rc}.N`（本宿主线实测命名），未知形态**fail-closed 判红**（无法判定的版本 = 尚未审阅的版本）。**CLI（`@deepseek-ai/dsh`）单列**：fixtures 只记闭包子包、无 CLI 版本机读位 ⇒ 覆盖面以 `hostVersion` 集合为**代理**（fixture 头自述「CLI 版本闭包内子包实测版本 = hostVersion」；代理只会更早判红，不掩盖更新版本）——CLI 单独发版亦红。
  - **README：宿主版本矩阵 + RB-01 窗口期残余披露**：矩阵按 DEC-026——`0.1.5-rc.2`（现行闭包实测）完整支持 / `0.1.2-rc.1` 支持（**最低支持线**，A1 绑定 v1.0 major 边界、**当前 A3 阶段尚未生效**）/ `0.1.0-rc.7`·`0.1.1-rc.2`（0.1.x 旧表面线）最佳努力且 v1.0 起断供（旧宿主诊断提示「宿主版本低于最低支持」+ v0.5.x tag 回退）/ 未列版本未验证（`peerDependencies` 仍为 `*`）；并新增「⚠️ 探测窗口期（已知残余，RB-01）」：探测**每日一次且仅在 CI 侧** ⇒ **窗口期内（≤1 天）宿主升级仍可能先于探测到达用户**（探测不是实时防护），兜底 = 加载期 `[nv-compat]` 结构化告警 + 客户端 `apiHas` 按域降级与设置页诊断（**不白屏**）——即**「有探测」≠「无窗口」**。验证管线段补探测轨的本地等效命令（`npm view @deepseek-ai/dsh versions --json` 等）。
  - **隔离环境验证（M7.7；无 install、网络只读、构造全在 %TEMP%）**：`%TEMP%` 隔离目录 + `npm_config_cache` / `npm_config_userconfig` / `npm_config_globalconfig` 重定向（不触碰 `$HOME` 配置目录）；`npm view` 真跑 9 目标 + CLI dist-tags（退出码全 0）；**双向实证**——真实元数据 ⇒ **绿 exit 0**；隔离目录构造 9 例 ⇒ **红 exit 1**（同车新 rc / 新车 / CLI 单独发版 / 覆盖版本上游消失 / 漏探 / 越界探 / 未知版本形态 / 例外表腐化 / host 族不自洽），每例均输出可归因明细。YAML `yaml.safe_load` 通过（`on` = push + pull_request + schedule + workflow_dispatch；jobs = sanity + host-logic + host-latest-probe）；`node --check` 提取物 **0 错**；YAML 探测目标集 ≡ 契约声明面 ∪ CLI **双向一致（9 项）**；白名单审计 = 禁止词在命令/代码中 **0 命中**（仅注释内列举）。**本地无法真跑 GitHub scheduled ⇒ 以隔离等效命令验证探测逻辑，CI 侧首次定时执行仍需观测确认**（如实留档）。
  - **改动面 = 恰 3 路径**：`.github/workflows/ci.yml`（on.schedule/workflow_dispatch + host-latest-probe job）、`README.md`（矩阵 + 窗口期 + 验证管线）、`CHANGELOG.md`（本条）。**零产品代码改动**（`lib/*` 未动）；**零安装**（不 install 任何包）。
- **设置页「诊断」区：能力报告 = 契约运行时投影（COMPAT-005 D1；DEC-025 轴④ D2 的三消费方之一落地）**：把 COMPAT-004 的 `detectHostCapabilities` 报告接进客户端设置页——**报告项全部由契约 `lib/host-contract.mjs` 声明面投影而来，面板不自带状态清单**（无硬编码清单 = 无第二事实源）。
  - **数据源单一（P-10 / 仅布尔·名称·版本）**：服务端已算的报告经**既有 `webServer` 路由模式**新增 1 条**只读**路由 `/novel-writing/api/compat` 送达面板（`lib/index.js` `api('compat')`，与 `[nv-compat]` 告警**同源同函数**）。载荷边界**收紧到契约投影**：`contract.items` 只出 `item/face/kind/necessity`——**`symbol` / `file` / `line` 一律不出网关**（宿主/仓库路径零泄漏）；探测抛错如实返回 `{ ok:false, error }`（不伪造绿、不冒泡 500）。页面侧 `buildDiagReport({ api, server })` 为**纯函数投影**（无 DOM 依赖，测试直测）。
  - **契约面覆盖状态**：面 1 = 服务端探测**逐项**（`ok` / 缺面 / 未探测三态如实，未探测项带原因不静默判绿）；面 2/3 = 本面板**运行期自检**（8 项：旧 `.api` 连接载体 + `remote.*` 5 服务 + `[data-phase]` 会话根 + DOM 观察器基建）；面 4/5/6 **零探测**并以「未覆盖 29 项 = 48 − 19」数量摘要披露——**不伪造未实现面的状态**。
  - **契约登记**：`lib/host-contract.mjs` 新增顶层 `clientProbes[]`（登记面板**已实现**的客户端探测项 = 面 2 六项 + 面 3 两项，含 `probe` 键与 `mode`/`kind`），smoke 机检**三向同集**（契约 `clientProbes` ≡ `lib/client.js` `CLIENT_PROBES` 常数 ≡ 源码提取面，任一方向增删即红）+ 每项 `item` 真实在册且 `kind` 对账 + 面 4/5/6 零伪造；契约 1.8 路由数随本条新增同步为 **13**（原实测 12）。
  - **RB-03 代理信号**：`remote.*` 服务族**全缺**而旧连接载体的 `.api` 域对象仍在 ⇒ 面板输出**明确文案**「宿主版本低于最低支持（v1.0 起生效）」（判据 = 客户端实测 service 在场性，**不依赖 TP-4 宿主版本号**——报告 `host.version` 仍如实为 `null`）。代际判定落在 `makeHostApi` **同一处**（不走 `api.*` 形状反推：新表面适配层同样会填 `api.host/settings` 等域，形状反推会把新宿主误判为旧表面）。smoke 增**三形态构造断言**（旧表面触发 / 新表面不触发 / 双缺不误报，均以 apply 驱动代际标记，读数可归因）。**⚠️ 本条判据经 REVIEW-COMPAT-005-R1 F1 判定为错层并已返工**（`remote.*` 一侧原以求值于 facade 域对象 ⇒ 真实旧宿主上恒不触发 + 2.5~2.9 假绿）——现行判据与构造见上方「修复 → REVIEW-COMPAT-005-R1 F1~F9 全量返工」，本节描述仅为交付时的历史形态。
  - **F6（TP-4 交叉标注）**：面板「兼容性信号」段在未触发时显式注明 `host.version = null`（版本获取路径未证实），**不外推**为「宿主版本已证实」。**ctxGetSemantics 复核**：本轮**未取得**宿主 cordis 源码可及性或真机观测新证据 ⇒ `status` **保持 `unverified`**（不因面板落地而虚假转 `verified`；新增 `recheck` 字段留痕并写明转 `verified` 的判据 = 源码摘录或真机观测二者之一）。**BC-05 载荷机检**（沿用 COMPAT-004 D2 告警②口径）：报告无路径/token/用户数据 ∧ 无契约 `file`/`line` 泄漏。
  - **并入 REVIEW-COMPAT-004-R2 的 N3/N4（契约登记）**：item 2.13 `note` 增 N3（宿主侧槽位标签刷新依赖「宿主重渲染自身 shell」，本侧只保证**必要条件** = label 渲染期惰性取值；**该前提在本仓无证据，如实标「前提未证实」**）与 N4（locale 服务**撤离**保持最后一次快照、不回退 zh——登记为**已知边界**）。
  - **行号重基**：面板代码（约 +350 行）落在面 2 段之前 ⇒ 契约面 2/3 全部 `file`/`line` 由机械偏移重基并**逐项实读复核**（面 2 为 smoke 严格面：起止行 MUST 为真实代码行）；顺带修正 2.2（保留调用点口径）、2.6/2.9/2.11/2.12（旧值起点落在注释/无关行，⑫b 判据①③自 004-R1 起即报偏移）、3.5（起点取 `//` 互操作说明行，维持「非严格面恰 2 处注释起段」golden）。**未新增 `revisions[]` 条目**（本轮为既有条目字段订正 + 面板接入，非宿主耦合面变更——沿用 COMPAT-012/013 先例，避免 `[nv-compat]` 归因任务漂移）。
  - **实现期两处自查明缺陷（如实留档）**：① 代际标记 `hostLegacyApiSurface` 初版误置于 `makeHostApi` **之后**（该函数在 apply 期赋值 ⇒ 落入 TDZ、赋值落到全局对象，探针恒读 `false`）——已上移并在注释写明「声明位置 MUST 在 makeHostApi 之前」；② 首版 smoke 构造把旧表面**域对象**当作 `ctx.get('connection')` 的返回值传入（实际应为连接**服务** `{ api: 域对象 }`）⇒ 探针假阴性，已修正构造并在断言处写明口径要点。
  - **验证**：smoke **262 → 271 项，0 失败**（+9 项新断言：报告生成函数可达 / BC-05 载荷边界 / RB-03 三形态构造 / 三向同集 / 面覆盖台账 / 服务端路由投影 / 设置页接入 / N3+N4 登记 / ctxGetSemantics 诚实保持；历史 262 项零回归）；`node --check` ×7 **0 错**；`validate-preset` **29/29** 不劣化。**改动面 = 4 代码/测试路径 + 本 CHANGELOG（commit 恰 5 路径）**：`lib/client.js`（诊断面板 + 投影函数 + 探测常数 + i18n zh/en 成对 + 令牌派生样式）、`lib/index.js`（1 条只读路由 + 契约投影出口）、`lib/host-contract.mjs`（`clientProbes[]` + N3/N4 登记 + 面 2/3 行号重基 + 1.8 路由数 + `recheck`）、`test/smoke.mjs`、`CHANGELOG.md`。
- **宿主边界层：lib/index.js 宿主调用 100% 收口 + 能力探测 + 启动 `[nv-compat]` 自检（COMPAT-004；DEC-025 §4 轴②B1/B2 + 轴④D2，A3 依 DEC-026）**：新增 `lib/host-boundary.js`——**服务端宿主边界层**（文件头 3 句显式职责声明，REVIEW-COMPAT-001-R1 F3）。**范围口径（REVIEW-COMPAT-004-R1 F7）**：lib/index.js 宿主调用 100% 收口；`lib/tools.js` 工具行按轴①独立评估、**不在本层**（契约 1.11 标 `out-of-boundary-scope`）。`lib/index.js` 的**全部宿主调用**收口为边界导出的 14 个符号（宿主包具名导入 `Service`/`z`/`resolveDshHome`、`settings` 注册与读取、`webServer` 路由挂载、`Service` 注册名、日志·事件·清理生命周期入口、`$DSH_HOME` 预设路径解析），**产品代码零直连宿主 API**——9 类直连模式（宿主包 import / `ctx.settings` / `ctx.get(` / `ctx.effect(` / `ctx.emit(` / `ctx.logger` / `resolveDshHome` / `webServer.register` / `.agent-presets`）grep 零命中，唯一宿主入口 = `import ... from './host-boundary.js'`；`BOUNDARY_MAP` 收口映射表（键 ≡ 契约面 1 可收口 9 项，值 = 边界导出符号）由 smoke 机检「键集 ≡ 契约面 1 收口项 + file 全指边界 + 映射符号全为该模块真实导出且被 index.js 消费（14 符号）」。**行为等价重构**（纯搬运 + 命名，逻辑零改写）：smoke 231 项历史断言**零回归**全绿。宿主升级时服务端适配只改本文件（依赖方向：产品 → 边界 → 契约）。
  - **`detectHostCapabilities()` 能力探测**（轴④ D2 立即落地；005 诊断面板 / CI 轨后续消费的**单一实现**）：读取 `lib/host-contract.mjs` 声明 → 探测宿主**实际**表面 → 结构化报告。`probes[]` 逐项 = 契约 face 1 **全 11 项**投影（同序，每项标注 `sync:true`）；未探测项**如实**计入 `unprobed`（1.4 已退役 / 1.11 属 tools 行越界）而非静默判绿；面 2~6 披露 `notCovered`（客户端/DOM/安装/预设/版本面归属其他轨）。**apply 期探测全同步**（F7）：零 `await`、零动态 `import()`、零网络、零 DOM/布局读取。宿主版本如实置 `null`（TP-4 待验证——不猜）。纯读取、零副作用。
  - **启动 `[nv-compat]` 结构化告警（D2）**：apply 期自检 → 缺面**单次** `console.warn('[nv-compat]', JSON 摘要)`（缺面域名/方法名/服务名 + 缺面 item + 计数 + 宿主版本），**无缺面即静默**（不刷屏、不阻断加载）。**安全边界（BC-05）机检**：摘要仅布尔/名称/版本——无路径、token、用户数据、宿主配置值。
  - **A3 inject 收敛（DEC-026「A3 立即执行」）**：`lib/client.js` `inject = ['slots','connection','locale']` → **`['slots']`**——`connection`/`locale` 降为**可选探测**（apply 期 `ctx.get` 惰性判定；依据宿主只读闭包 `cordis/lib/index.js` L762-771：`get(name, strict)` 对未提供/未注入服务返回 `undefined` 而**非抛错**，抛 `cannot get property ... without inject` 的是**属性访问**形态 ctx.xxx——本文件不使用该形态）。**非 breaking**（inject 表收敛 = 放宽挂载条件，旧宿主旧表面回退不变）；`makeHostApi` 的 legacyApi 回退分支**零改动**（diff 实证 + smoke 断言 `connection.api` 唯一引用 + 5 域回退返回在位 + 单点收口函数在位）。smoke 增**双路径**断言：service **缺** → 仍挂载 + 6 席注册 + 降级默认 zh（走既有 apiHas/degraded 「API 不可用」提示路径，不崩溃）；service **在** → locale 服务真被消费（英文标签）。
  - **验证**：smoke **231 → 248 项，0 失败**（+17 项新断言，历史 231 项零回归）；`node --check` ×5（index.js / host-boundary.js / client.js / host-contract.mjs / smoke.mjs）0 错；`ci-mock-face.mjs` **exit 0（4/4 面）**；`validate-preset` **29/29** 不劣化。

- **宿主表面 fixtures 快照 + 离线契约对账（COMPAT-003，承接 REVIEW-COMPAT-002-R1 F1~F9 非阻断项全量落地）**：`test/fixtures/host-surfaces/` 入仓三份**真实包只读提取**的宿主表面快照——`0.1.5-rc.2.json`（宿主已安装闭包：CLI `@deepseek-ai/dsh@0.1.5-rc.1` 的子包实测 **0.1.5-rc.2**，另含 cordis 4.0.2 / schemastery 3.18.2）、`0.1.2-rc.1.json`、`0.1.1-rc.2.json`（0.1.x 旧表面线最新发布版）——每份 8/6/6 个关键子包的**导出名集合 + 类继承（extends/extendsFrom）+ 类方法名 + 三版本标记**，头部 `source` 如实标注来源与方式（`extract.mjs` 静态解析，**零动态 import、零执行宿主代码、零写入宿主目录**；**registry 可达**故无需「证据构造」降级路径）；fixture 仅含名称与形状布尔，无路径/token/用户数据（BC-05 机检）。**smoke 214 → 230（+16 项，0 失败）**：契约 `requiredExports` ⊆ 现行 fixture 实测面、消除口径（`settingsNamespace` 不在现行面 ≡ item 1.4 `eliminated`）、**三版本差异 golden**（`settingsNamespaceExported` true→false = **BUG-003** 断点；`connectionApiDomainField` true→false = **BUG-004** 断点（旧连接载体 `.api` 域对象消失）；`remoteNamespaceServicePackages` false→true = 0.1.x 线不存在可消费的 `remote.*` 服务面）、fixtures 安全边界扫描；**F1 面钉**——ci.yml 手写 dsh-settings mock 的运行时导出键集**恰** `{SettingsConflictError, SettingsProvider, default, redactSecrets}`（动态 import ci.yml 内联 mock + 静态解析两法独立互证，三方一致：mock ≡ fixtures ≡ 契约声明面）；**F5** 契约 file/line 活性（非 null `file` 全为仓库真实文件 + `line` 形态白名单 + 每项首行号 ≤ 目标文件总行数〔46 项〕+ 每面 1 项锚点抽核 6/6）；**F6/F7** necessity 九值 golden 分布（9 值全量 = 48）+ `kind` 封闭枚举机检（30 值无空置）+ `faces` 结构机检（恰 6 项、id 1..6、name/scope 非空）；**F4** 递归 own-descriptor 纯数据强化（补 ④ 的缝：JSON 往返对**函数值天然免疫**，新检查直看描述符——取值器/方法简写即刻暴露）；**F3** 事件名对账由单向改**双向**（原「四类 region 字面量双向对账」宣称超出实现：删契约项或置空数组仍全绿，属真空方向）。CI 新增 sanity 步骤 `ci-mock-face.mjs` 把 mock 面钉在 fixtures 实测面上，语法检查步骤扩至契约与 fixtures 工具。**契约侧同步**：**F1** 补录 item 3.8（findSidebar 侧栏探测：aside/nav + 宿主类名正则 + querySelector 兜底，47→48、面 3 分布 7→8，`revisions` 入册修订史）、**F2** CSS 令牌族放宽（`--dsw-alias-*` 12 → +`--dsw-shadow-lv2` = 13，关闭族外盲区）、**F9** 三处 line 精度修正（3.4 补 L4119/L4254 并注明 TK 表仅含 11 个去重名；4.5/5.5 合并行段注明 §3 原始分段来源）、**F8** 提取口径（单引号字面量 / 仅 querySelector(All) / `.nv-` 排除 / 令牌族限定 / 先剥注释）与「**新增宿主耦合字面量必须同步契约 + smoke 对账**」纪律入契约头（含纯数据守卫**能拦/不能拦**的如实披露）、`hostSurface` 机读声明（requiredExports / eliminatedExports / versionFacts / ciMock）、ci.yml 行号引用随本次 CI 改动全量重校准（9 处引用逐一校核命中）。`node --check` ×7（含契约与 fixtures 工具）0 错；validate-preset 29/29 不劣化；**演练红绿双向 ×5**（契约删项 → 228/2；fixture 篡改导出名 → 228/2 + CI 步骤 exit=1；mock 复活 settingsNamespace → 229/1 + CI 步骤 exit=1；events 置空 → 229/1〔F3 修订前该方向全绿〕；契约注入取值器 → 229/1 而 ④ 仍绿〔F4 补缝实证〕；每次恢复后均 230/0 且 sha256 字节级一致）。
- **宿主契约清单 `lib/host-contract.mjs`（COMPAT-002，DEC-025 ADR 决策①落地地基）**：六面 47 项宿主依赖的**机读声明式契约**——纯数据、零 import 宿主包、零运行时逻辑（JSON 往返无损守卫），供 CI/smoke/诊断三方消费；逐项可溯源至 COMPAT-001 分析 §3（61 处行号抽核 0 失实底稿）：面1 服务端 API 11 / 面2 客户端 API 13 / 面3 宿主 DOM·前端约定 7 / 面4 安装注册 6 / 面5 预设 manifest 5 / 面6 版本环境 5。每条目 schema = `face/item/kind/symbol/file/line/necessity`（+可选 `note/incident`），文件头含一条完整样例逐字段说明；necessity 九值口径机读化（含 F10 统一口径：自有恰 1 项〔3.7 localStorage 键，无宿主耦合〕+ 已消除恰 1 项〔1.4 settingsNamespace，necessity=eliminated 保留在册供 BUG-003 历史勾稽〕；6.4 CI mock 漂移条目注记 COMPAT-010 已修正、对账以 ci.yml 实状为准）。附 `regionLiterals` 首批对账清单（F8）：槽位名 3（settings.section/sidebar.footer.action/shell.overlay）/ 服务名（inject 表 3 + ctx.get 目标 3 + makeHostApi svc 域名 6 + internal/service 事件 1）/ CSS 令牌 `--dsw-alias-*` 去重恰 12 / 宿主耦合 DOM selector 3（.nv- 自有前缀除外）。smoke 新增 11 项断言（**203 → 214**，0 失败）：契约结构（六面分布/编号面内连续/七字段齐全/F10 口径）+ 纯数据守卫 + 三类 region 字面量（槽位名·CSS 令牌·DOM selector）与 `lib/client.js` 源码**双向对账**（提取面 ⊆ 契约 ∧ 契约 ⊆ 提取面）+ 服务名对账（其中**事件名一项当时为单向断言**——删契约项或置空数组仍全绿，属真空方向；该措辞偏差与断言强度缺口已由 COMPAT-003 F3 修订为双向对账）。可靠性演练实证：临时篡改契约槽位字面量 → 对账断言变红（exit=1，失配明细列出 src/contract 差异）→ 恢复 → 全绿。`node --check` ×4（含新文件）0 错；validate-preset 29/29 不劣化。

### 修复
- **COMPAT-016 015-R1 P2 微收口（守卫强度对齐声称；全在测试/脚本侧，产品代码零改动）**：收口 REVIEW-COMPAT-015-R1 §9 的 P2-1/P2-2 与 P3-1/P3-4/P3-6/P3-10/P3-11——**同一失效模式家族「守卫强度 < 声称」**（守卫绿，而它声称保护的面实际可被绕过）。
  - **P2-1（探针标记防伪）**：smoke ⑧c 原以**全文子串**断言 `REAL-RUN: UNVERIFIED`，而 `scripts/probe-host.mjs` **头注释**含同一串 ⇒ 只改常量取值即可让 5 处输出自述「已验证」而守卫**全绿**（README「不得静默转为已验证」的承诺可被一行改动解除）。**修复**：判据锚定**常量赋值行**（`const REAL_RUN_STATE = '…'` / `const SELF_CHECK_STATE = '…'`，与输出面解耦 ⇒ 头注释保留同串不再污染）＋ 反向排除「已验证」形态 ＋ 要求常量在输出面被真实引用（≥ 定义 + 2 处拼接）；并**内置常量篡改变异对照**（改常量值、头注释同串保留 ⇒ 旧全文子串守卫仍绿 ∧ 新守卫必红）。**真机负例**：改常量值 ⇒ smoke 红（`realRunAnchor=false`）→ 还原 → sha256 复核一致。
  - **P2-2（⑩c 正控测副本）**：smoke ⑩c 的三例正控原引用**重新声明的正则副本**（与 ⑩ 部署面文本相同、对象不同）⇒ 部署词法被改窄仍全绿。**修复**：两条词法提为**单点常量** `STALE_RE_NOW5` / `STALE_RE_EMIT5`，⑩ 部署面与 ⑩c 正控**共用同一 RegExp 对象** ＋ 对象同一性入断言 ＋ 「改窄（间隔上限 10 → 3）后同一正例必须失配」的构造对照。**真机负例**：改窄 ⑩ 部署词法 ⇒ smoke 红 → 还原 → sha256 复核一致。
  - **P3-1（F1 负例最小变异）**：`probe-face.mjs` 的 `deleteOnChild` 原为「段落式跳过」（跳过状态只在遇下一个 2 空格键行时复位）⇒ 删 `on:` **最后一个**子键时连带吞掉其后空行与 0 缩进的 `jobs:`，变异体非最小（② 的门禁与权限记录同时变红，红点不可单独归因四键断言）。**修复**：只删目标键行 + 其 ≥4 空格缩进的子行；⑦ 增**变异体最小性**机检（`jobs:` 段与三个 job 权限读数 MUST 原样保留）。
  - **P3-4（`jobSection` 镜像无交叉机检）**：`jobsStart`/`jobSection`/`cmdOf` 原在 `probe-face.mjs`（②/⑦）与 `test/smoke.mjs`（⑧b）**各写一份镜像**。**修复**：抽为共享**纯函数模块** `test/fixtures/host-surfaces/yml-jobs.mjs`（先例 = `ci-mock-face.mjs` 的 `extractHeredocs`；零副作用 ⇒ smoke 可直接 import、无需导入顶层脚本），两处消费**同一对象**，镜像面消除。
  - **P3-6（⑩b 扫描面未覆盖新增文本载体）**：原扫描面 = smoke 注释行 + CHANGELOG 当前条目。**修复**：扩至 `README.md` 全文 ＋ `scripts/probe-host.mjs` 注释行 ＋ `probe-face.mjs` 注释行；CHANGELOG 由单锚点改**锚点集**（`COMPAT-015` ＋ 本条目）；五面各设**扫描量下限**（防空面——扩面本身入机检）。**面外如实披露**：CHANGELOG 历史条目（不改写记录）；install 头部块（其约定行只写字面量不写行号，字面量已由 ⑧ 对账）。
  - **P3-11（⑧ golden 只锁数量）**：`INSTALL_GOLDEN_ENTRIES` 由「数量 5」改**有序 id 集** `['4.1','4.2','4.3','4.4','5.5']`（双向：扩 / 删 / 换项均须显式改 golden），并补换项 / 删项 / 增项**三向构造负例**（原实现下两脚本同步换 id 并写入 ∈ 新 id symbol 的字面量即可全绿——覆盖面脱靶零信号）。
  - **P3-10（记录精度：行数口径 + 计数笔误）**：① 原文「非注释行 install.ps1 295 行 / install.sh 221 行」的**计数口径现明文化**并复核：非注释行 = `trim()` 后非空且**首个非空白字符不为 `#`** 的行（按脚本读取口径先剥 BOM、按 CRLF 分行）；按此口径实测 install.ps1 **295** / install.sh **221** ⇒ 原文数值成立，任务摘要里的 296 系笔误（R1 §2.5 的 1 行记录差已消）。**install 脚本零改动**。② 任务表「probe-face 416 行」无仓内依据（实测基线 **315** 行 → 本批收口前 **573** 行）——系任务描述笔误，仓内无对应记录可订正，**如实留档于此**。
  - **验证（真实输出）**：smoke **282 → 282 项，0 失败**（**零新增 check**：五处强化均**就地扩写**既有断言的判别力条件——与 ⑧b 变异负例同款范式；故 README 的机检计数 282 无需变更）；`probe-face.mjs` **27 → 28 项机检全通过 + 11 例构造用例**（+1 = ⑧ golden id 集三向负例；机检数不降）；`scripts/probe-host.mjs --self-check` **6/6 层通过（exit 0）**；`ci-mock-face` **4/4**；`node --check` 相关文件（含新增的 `test/fixtures/host-surfaces/yml-jobs.mjs`）**0 错**；`validate-preset` **29/29 不劣化**；**两条 P2 负例真跑**（改 → 跑 → 红 → 还原 → sha256 复核一致）。**改动面 = 3 路径 + 1 新增 + 本 CHANGELOG**：`test/smoke.mjs`、`test/fixtures/host-surfaces/probe-face.mjs`、`test/fixtures/host-surfaces/yml-jobs.mjs`（新增）、`CHANGELOG.md`；`scripts/probe-host.mjs` **零改动**（P2-1 的修复面在守卫侧，脚本头注释同串按 R1 要求**保留**以证明「锚定定义行」有效）。**未 push**。
- **COMPAT-015 终批收口（REVIEW-COMPAT-014-R1 F1~F11 全量 + COMPAT-008 探针固化 + COMPAT-009 install 头部布局契约；install 脚本行为零变化）**：三条线并行收口——机检强度（F1~F9）、文档精度（F3/F10/F11）、宿主兼容面新增能力（COMPAT-008/009）。
  - **F1（P2）探测轨判据「`on` 四类事件」宣称超出实现**：`test/fixtures/host-surfaces/probe-face.mjs` ② 原只检 `  schedule:` 存在（`evaluateGate` 是 `if` 表达式纯函数、与 `on:` 无关）⇒ 删 `pull_request:` / `workflow_dispatch:` 时记录**仍全绿**（PR 门禁 / 手动随查静默消失，假绿方向）。**修复**：② 增**四键正则断言**（push / pull_request / schedule / workflow_dispatch 各存一）+ ⑦ 四键**逐一删除的变异负例**（未变红者 = 守卫对该键无判别力）。
  - **F2（P2）smoke ⑧b 接线守卫仅全文子串匹配**：把机检步骤移入 `host-latest-probe` 后字符串仍在 ⇒ 守卫仍绿而 PR 面**重新零机检**（正是 R1 F2 的失效模式本身）。**修复（两层）**：probe-face ② 增**接线自断言**（接线行 MUST 落在 `jobSection(yml,'sanity')` 段内）；smoke ⑧b 由 `includes` 改**段绑定**（本地镜像 `jobSection` 口径，理由与镜像说明见注释）+ **零落盘变异负例**（移出 sanity ⇒ 旧全文守卫仍绿而段绑定守卫必红，两值同时入断言）。
  - **F3（P3）本批新写文本内陈旧行号副本 4 处（与契约 `line` 字段自相矛盾）+ 守卫扫描面扩宽**：按契约各条 `line` 字段订正——smoke 注释 3 处（2.3 旧值 `L4374-L4377` → `L4804-L4806`；3.1 旧值 `L838` → `L978`、3.5 旧值 `L896` → `L1028` 并据实读订正为「起于**空行**」；3.8 旧值 `L2411-2421` → `L2829-2839`、6.4 旧值 `L77` → `L105`）与 CHANGELOG（2.3 同一失实）。**防复发（新增机检）**：smoke 新增 **⑩b 行号引用对账**——扫描面 = 本文件**注释行** + `CHANGELOG` **当前条目**（`- **COMPAT-015` 起），注释/条目内 `<item> L<n>` 引用 MUST 落在契约该条 `line` 字段界内（历史限定词行豁免；多段 `line` 取 min…max 为宽松界——强度边界如实披露）；**口径边界**：CHANGELOG **历史条目**不在扫描面内（各批次落地时点的记录，施加「与当前契约一致」会系统性假阳并要求改写历史；同 C-F-6「历史锚点」纪律）。判别力由「注入构造陈旧对 ⇒ 扫描器必命中」正向对照兜住（防空转）。2.13 两处旧值改为符号引用 + 「历史锚点」标注（不再承载副本）。
  - **F4（P3）判据 ①/③/⑤ 三段零负例**：probe-face ④ 各补 1 例——① 探测目录删一个 stem ⇒ exit 2 + 面归因（「探测面缺契约声明包」）+ 输入归因（「探测输入缺失」）；③ 构造快照中**非例外包**版本 ≠ `hostVersion` ⇒ exit 1 + 类别 `coverage` + 「fixtures 自洽」；⑤ 上游版本串 `0.1.5-beta.1`（track 表外）⇒ exit 1 + 类别 `version-form` + 「**重建 fixtures 关不掉**」处置文案。构造用例 7 → **11 例**。
  - **F5（P3）`inputProblems` 分支先 `exit(2)` 吞掉同轮 `version-drift`**：ci.yml 判据脚本的 input 分支改为**退出前并报**已累计 failures（新增单点 `printFailureClasses()`，两条退出路径共用），报文含「同轮另检出 N 项判据失败」+ 分类明细 + 「退出码仍取 2 = 输入类优先」——**input 类不再掩盖 drift 类**，退出码语义（A-F7）不变；probe-face ④ 增「输入异常 + 宿主新版本**同轮并报**」构造例。
  - **F6（P3）白名单机检的自指面**：③ 的 FORBIDDEN 只施加于 run 块命令面，而判据脚本体由 `node` 执行（等效命令面）⇒ 未来在其内部加 `npm`/网络/子进程调用时零信号。**修复**：probe-face ⑥ 增「判据脚本本体零 install/npx/pack/child_process/fetch/URL 命中」（6 类禁词；「零网络 / 不发命令」自述由此机检）+ 「ci.yml 探测 job 注释的**允许/禁止声明面** ↔ `ALLOWED`/`FORBIDDEN` 常量**双向对账**」（声明项逐条在注释内 ∧ 声明禁用面逐项可被常量拦截 ∧ 每条常量均有声明来源 ∧ 允许样本 ∈ ALLOWED 且不被 FORBIDDEN 命中）。
  - **F7（P3）`spawnSync` 无 `timeout`**：判据脚本若阻塞，原实现会同步挂起至 CI job 级超时且无归因。**修复**：`runNode` 加 60s `timeout` + `ETIMEDOUT`/`SIGTERM` 归类为失败项（`timedOut` 入每例报文）；⑦ 增**真跑实测**（阻塞脚本在 1.2s 上限下被终止：timedOut=true / status=null / signal=SIGTERM）。
  - **F8（P3）失败态归因与清理口径不一致**：① `die()` 原对「工具与契约结构失配」也报「输入缺失」（误导维护者去查文件可读性）⇒ 改为**归因分型**（「输入缺失」vs「工具与契约失配」，共用退出码 2，报文字首点名）；② `hostSurface.packages` 形态变化原走未捕获 `TypeError`（exit 1 + 栈回溯）⇒ 加结构守卫归因；③ 头注释称临时目录「用毕删除」但 `die()`/异常路径（发生在 `mkdtempSync` 之后）不清理 ⇒ 加 `process.on('exit')` 兜底（成功 / 失败 / 异常三路径均清理）。
  - **F9（P3）B-N2 词法缺正向对照**：原断言只有负向（契约 free-text 内 0 处现役副本），词法被误改窄仍全绿（退化为「零命中 = 无副本」的恒真面）。**修复**：smoke 新增 **⑩c 词法正向对照**——两个历史逃逸形态（「现…L…」带间隔词 / 「emit|logger(+L…)」无「现」字）MUST 被现行词法命中 ∧ 历史锚点形态（「原…L…」）MUST NOT 命中（3 例双向；与 COMPAT-012 N1 ①a / COMPAT-005 F2 注入红同款先例）。
  - **F10（P3）README 兼容矩阵歧义**：`0.1.0-rc.7` / `0.1.1-rc.2` 行的括注「该版本无仓内 fixture」紧邻 `0.1.1-rc.2` ⇒ 会被读成「`0.1.1-rc.2` 无 fixture」（仓内实存 `test/fixtures/host-surfaces/0.1.1-rc.2.json`）。**修复**：明确主语——`0.1.0-rc.7` **无**仓内 fixture（依据为 `RESEARCH`/`DESIGN` 历史实测）、`0.1.1-rc.2` **有**仓内快照。
  - **F11（P3）A-F1 收敛的取舍未留档**：R1 F1 曾列「真实 schemastery 漂移**每日可见**」为附带收益，收敛后该收益消失而文档只述「每日面 = 仅探测 job」。**修复**：README 探测轨节补「**代价**」一句（每日真实包漂移可见性随之消失；需该信号用 `workflow_dispatch` 或另立定时任务）。
  - **COMPAT-009（安装面知识规范化；install 脚本行为零变化）**：`install.ps1` / `install.sh` 头部各新增 **13 行纯注释**的「宿主布局契约」单点清单块（固定标记行 `host-contract:v1`，RB-02），逐条登记 [4.1] 布局特征名 / [4.2] 接入通道名 / [4.3] 注册通道名 / [4.4] 兜底注册通道 / [5.5] 预设目录约定。**正则对账（CI）**：probe-face 新增 ⑧——两脚本标记块存在且在头部 60 行内 ∧ 约定项键集跨脚本一致 ∧ 每项字面量片段（` + ` 分隔合取式）逐段 ∈ 契约对应 item 的 `symbol`；**负例**（变异构造，零落盘）：删标记行 ⇒ 红 / 改预设目录字面量 ⇒ 红 / 删一个约定项 ⇒ 红。**强等价证据（行为零变化）**：两脚本**非注释行逐字节等价**（install.ps1 295 行 / install.sh 221 行，`identical=True`）、删除行数 0、全部关键锚点**统一 +13 位移**（布局检测注释 / `if ($newLayout) {` / 注册插件注释 / patch 兜底注释 / 预设同步注释 逐点核对）、install.ps1 的 UTF-8 BOM 保持；契约 face 4/5 行号同步 **+13 重基**（4.1 `L136-147 / L60-70`、4.2 `L141-190 / L71-86`、4.3 `L192-251 / L88-199`、4.4 `L253-317 / L201-218`、4.5 `L319-352 / L220-245`、5.5 `L319-329 / L220-229`）+ 4.1/4.2/4.3/4.4/5.5 补「单点清单」note。
  - **COMPAT-008（探针固化 `scripts/probe-host.mjs`，新增）**：把 BUG-004/005 的排查方法论固化为**一条命令**——`--self-check`（**离线**，本仓已验证并接入 CI sanity：契约 × fixtures 能力面 **6 层**逐层对账 + 打印隔离方案与命令计划；零网络 / 零宿主进程 / 零浏览器）与 `--run`（**实机**：隔离实例起宿主（`DSH_HOME` 重定向 `tmpdir` 隔离根 + `npm_config_*` 三重定向）→ 只读 `GET` liveness + `/novel-writing/api/compat` → 无头浏览器 DOM/能力探测 → 与契约的能力 diff 报告 + 「断点层」结论）。**如实标注**：实机模式固定标记 **`REAL-RUN: UNVERIFIED`**——需运行中宿主环境（`dsh` + 浏览器/DOM），**本仓开发环境与 CI 均未执行过**（未伪造实机验证）；隔离硬校验（`DSH_HOME` 不在临时根内即拒绝执行）、不 install / 不写宿主目录 / 不触碰真实 `$HOME`；浏览器自动化依赖缺失时该层显式 `unavailable`（不伪造）。smoke 增接线守卫（脚本在仓 ∧ sanity 段内含 `node --check` 与 `--self-check` ∧ 两枚固定标记在场，防静默转「已验证」）；README 增「维护者：一条命令定位断在哪一层」节。
  - **验证（真实输出）**：smoke **279 → 282 项，0 失败**（+3：probe-host 接线守卫 / ⑩b 行号引用对账 / ⑩c B-N2 词法正向对照；⑧b 为在原条目上按 F2 改写强化——历史 279 项零回归）；`probe-face.mjs` **27 项机检全通过 + 11 例构造用例**；`scripts/probe-host.mjs --self-check` **6/6 层通过（exit 0）**、`--run` 无宿主命令时 **exit 2 且未执行任何操作**（守卫实测）；`node --check` 相关文件 **0 错**（含新增 `scripts/probe-host.mjs`）；`validate-preset` **29/29** 不劣化；`yaml.safe_load` 通过（`on` = push + pull_request + schedule + workflow_dispatch；jobs = 3）；ci.yml 判据 heredoc 提取物 `node --check` **0 错**（F5 改动后）。**改动面 = 9 路径**：`test/fixtures/host-surfaces/probe-face.mjs`、`test/smoke.mjs`、`lib/host-contract.mjs`（face 4/5 行号 +13 重基、6.4 再重基、note 与 revisions 补录）、`install.ps1`、`install.sh`、`.github/workflows/ci.yml`（F5 并报 + probe-host 接线）、`scripts/probe-host.mjs`（新增）、`README.md`、`CHANGELOG.md`。**未 push**。
- **COMPAT-014 全量收口批次（REVIEW-COMPAT-007-R1 F1~F9 + REVIEW-COMPAT-005-R2 N1~N5 + REVIEW-COMPAT-013-R1 F-1~F-6；三组逐项落地）**：
  - **A 组（ci.yml / README 面）**：**A-F1（P2）`schedule` 副作用面收敛**——`on.schedule` 属 workflow 级 ⇒ 原实现使既有 `sanity` / `host-logic` 亦进入每日**无人值守**面（含未钉版本的 `npm install --no-save @deepseek-ai/schemastery` 真实包安装与全量 smoke），而文档只把每日一次描述为探测轨频率。**收敛**：两 job 加反向 `if: github.event_name != 'schedule'`（每日面收敛为 `host-latest-probe`；push / PR / `workflow_dispatch` 仍全量——手动随查语义不变）＋ 补最小 `permissions: contents: read`（三 job 一致；两 job 仅 checkout + node、不需写权限 ⇒ **严格收窄**而非行为变更）。**A-F2（P2）判据在 PR 面零机检 → 离线机检**：判据内联于 heredoc 且该 job 被 `if` 排除 ⇒ 语法/逻辑缺陷只能等首次 schedule 暴露（既有 `extractHeredocs` 硬编码 `<<'EOF'` + 未引号 target ⇒ 不匹配新约定）。新增 **`test/fixtures/host-surfaces/probe-face.mjs`**（离线：零网络 / 零 install）并接线 `sanity`：泛化 `extractHeredocs`（引号/未引号 target + 任意 `'[A-Z_]+'` 定界符）→ 提取判据 → `node --check` + **构造 probe JSON 驱动判据真跑**（判据脚本的 `<probeDir>` 入参本身即离线输入源，无需新增在线/离线开关）；不违背「只读网络探测不进 PR 门禁」原意（检查**脚本与结构**，非宿主版本）。**A-F3（P3）CLI 代理子句②**：取 **(b) CLI 跳过子句② + 报文加代理限定语**——理由：子句②对「hostVersion 代理」不成立（仓内现存分歧实例 CLI `0.1.5-rc.1` / 子包 `0.1.5-rc.2`），选 (a) 需重建 fixtures 并手填 CLI 版本（与「fixtures 禁止手写表面」纪律及本批离线可验证性冲突，且历史两份 fixture 的 CLI 版本无从取得）；CLI 检测力由子句④版本车承担。**A-F4（P3）版本维对账**：判据增 ①b「契约 `packages` 键集 ≡ fixtures `hostVersion` 集（双向）∧ `current` ∈ 覆盖集」，smoke ① 亦改为**同时枚举 fixtures 目录**（双向）。**A-F5（P3）**：probe-face 做**正向白名单**（探测 job 命令逐条 ∈ {mkdir / npm view / cat heredoc / node / echo}）+ 禁词零命中（install/ci/npx/pack/cache/tar）+ stem↔命令一一对应 + 无越界探测。**A-F6（P3）分类处置**：fail 分类（`version-drift`/`version-form`/`coverage`/`surface`/`input`）并按类输出差异化处置——口径类红明示「**更新 fixtures 关不掉**」。**A-F7（P3）退出码归位**：逐包 try 解析探测 JSON（空文件 = `npm view` 非零退出残留）⇒ 逐包归因、**退出码 2**（原为未捕获 `JSON.parse` → exit 1 + 栈回溯）。**A-F8（P3）README 口径**：L189「出现新版本即 job 红」精确化为判据④真值（高于最新覆盖车 / 已覆盖车内推进才红；**低于最新覆盖车的中间车不判红**）+ 每日执行面 = 仅探测 job + 上游客包**新增子包**盲区披露；L184 `0.1.0-rc.7` 依据由「fixtures 快照」改指 `docs/RESEARCH.md` §3.1 / `docs/DESIGN.md`（仓内无该版本 fixture）。**A-F9（P3）**：README smoke 计数 179 → **279**，并把「随 smoke 计数变更同步」升级为**机检**（smoke 末条断言 ≡ README 声明值，含自身）。
  - **B 组（client / 契约面）**：**B-N1（P3）RB-03 触发取原始肢**——`CLIENT_PROBES` 五项 `remote-*` 显式返回 `raw`（原始服务命名空间肢）与 `ok`（raw ∧ facade 域），触发判据改读 `raw`（facade 肢仅展示）；原读合并读数会把「原始服务在场 ∧ 装配面失效 ∧ 旧 `.api` 标记在场」误报为「宿主版本低于最低支持」；smoke D1③ 增第 4 形态构造 (g)（facade 空对象 ⇒ `ok=false, raw=true` ⇒ **MUST NOT 触发**）。**B-N2（P3）行号解耦 + 守卫放宽**：1.3 / 1.10 / 1.5 / 1.7 note 与 1.4 symbol 删**现役**行号副本（1.4 symbol 的 `L39` 为陈旧副本，`line` 真值 L40）；守卫词法由 `/现\s*L\d/` 扩为「现…（≤10 字）L…」+「emit/logger/logWarn + L…」，扫描面扩至 `symbol` / `faces` / `revisions` / `regionLiterals` / `ctxGetSemantics` / `hostSurface`。**B-N3（P3）载荷自由文本清零**：`report.host.versionNote`（自由文本）→ `versionCode: 'tp4-unprobed'`（封闭码）⇒ 报告内**不再存在自由文本载体**且入机检（人类可读文案只在客户端 i18n，不出网关）。**B-N4（P3）**：契约 `clientProbes[].mode` 五项 `remote-*`：`runtime-service` → `runtime-service+raw-ns` + note 补「原始服务名在场 ∧ facade 域可用（代际判定不走 facade 形状）」。**B-N5（P3）`revisions[]` 补录 COMPAT-005**（照 COMPAT-011 先例；顶层 `clientProbes[]`/`clientProbesNote` + item 1.8 路由数 12→13 + 原因字段枚举化 + `contractProjection()`）——`[nv-compat]` 载荷 task 随末项派生为 `COMPAT-005`（断言以末项派生，自动跟随；`contractProjection()` 不需条目，R2 §7.1 裁定）。**面板真机断言（遗留）**：本环境**无宿主 / DOM**（mock react 下 `SettingsPage.view` 恒 null）⇒ **如实降级**、不伪造：以「源码挂载点 + 探针真跑 + 路由行为断言」覆盖，渲染级断言待真机/DOM 环境任务（与 R2 §8 同口径）。
  - **C 组（smoke / 契约面）**：**C-F-1**：⑫d 覆盖面标签改**动态生成**（实测 3/4/5/6 共 10 项并入 golden）——原「3/5/6」漏面 4 且与自身列举（含 4.6）矛盾。**C-F-2**：`:2012` 排除项订正为「2.2（`L97` 单行号）与 2.4（多段）」，契约 item 2.2 note 同一失实一并订正（2.3 `L4804-L4806` 属单段范围条目，旧值 `L4374-L4377` 系陈旧副本——COMPAT-015 F3 订正）。**C-F-3**：⑤a「覆盖 2 对」精确化为「**两对同源**（同行共现）⇒ 判别力等价单构造计数」并写明 golden `=== 2` 的独立作用边界。**C-F-4**：新增**判据⑥**（按目标语言取范围首行构造键、不复用 symbol 候选；配平闭合行 MUST ≤ 止；首行起构造者恰 3 项 = 4.6 `"dsh": {` / 6.1 `"peerDependencies": {` / 6.2 `"engines": {`，项数入 golden）。**C-F-5**：⑤ 注释补工具口径边界（`occ5` 纯子串计数 / 配平裸字符计数 / 方向 fail-closed / 不精确定位）。**C-F-6**：CHANGELOG COMPAT-013 条目措辞精确化——「产品代码 diff = 0」→「运行时行为与诊断载荷零变化（改动仅含 `lib/` 内契约文件 3 处描述性字段，numstat +3/−3）」；F-4 条目行号加「历史锚点」限定。
  - **P-01 实测订正（本批新增，如实留档）**：REVIEW-COMPAT-013-R1 F-4 的静态推演（「⑤b 对 4.6 / 6.2 无匹配 opener」）经**三例变异实测部分不成立**——⑤b 的候选含 tier-3 裸词**子串**，4.6 经候选 `client`（L21 `"client": {`）、6.2 经候选 `engines` **确实命中**（止改 L21 / L9 时 ⑤b 亦红）；判据⑥ 的真实覆盖增量 = 「首行键 ≠ 任何候选」与「外层构造闭合行 > 止而内层 ≤ 止」（实测 4.6 止改 **L28**：⑤b green / ⑥ RED）。契约 4.6 note 与 smoke 注释已按实测订正（不沿用 F-4 原推演）。
  - **验证（真实输出）**：smoke **276 → 279 项，0 失败**（+3：判据⑥ / A-F2 接线守卫 / A-F9 计数机检；历史 276 项零回归——B-N1/B-N2/B-N3 在原条目上强化）；`probe-face.mjs` **14 项机检全通过 + 7 例构造用例**（绿 exit 0 / 新版本红 exit 1 / CLI 代理绿 / 非 CLI 子句②红 exit 1 / 空文件 exit 2 / 版本维失配 ×2 红）；`node --check` 相关文件 **0 错**；`validate-preset` **PRESET VALIDATION PASSED**（本环境 full 模式 32 项 ok / 0 失败，与基线一致）；`ci-mock-face` **4/4**；`yaml.safe_load` 通过（on = push + pull_request + schedule + workflow_dispatch；jobs = 3）。**契约 `line` 重基**：item 6.4 `L77-113` → **`L96-137`**（本批 sanity 插入约 19 行致 host-logic 段位移；契约 1.1/1.2/1.3/4.6/5.1/5.4/6.1/6.4 的 ci.yml 行号引用同步改为**步骤/job 名锚点**，消除同类漂移面）；**`lib/client.js` 行数保持不变（4927 行）**——B-N1 改动做等行数改写，面 2/3 行号零漂移（实测：若行数变动，smoke ⑫b/判据⑤ 立即报 2.3/2.12/2.13 偏移）。**改动面 = 8 路径**：`.github/workflows/ci.yml`、`test/fixtures/host-surfaces/probe-face.mjs`（新增）、`test/fixtures/host-surfaces/ci-mock-face.mjs`、`test/smoke.mjs`、`lib/host-contract.mjs`、`lib/host-boundary.js`、`lib/client.js`、`README.md`（+ 本 CHANGELOG）。
- **REVIEW-COMPAT-005-R1 F1~F9 全量返工（P0 = RB-03 代理信号在真实旧宿主恒不触发 + 面板假绿；P1 = BC-05 载荷机检不绑定网关边界且实测载荷含仓内相对路径）**：R1 判 **NEEDS_CHANGE**（P0=1 / P1=1 / P2=2 / P3=5），本轮逐条修复并**逐条构造实证**（红 → 绿，8 项变异实测，见文末）。**改动面 = 5 代码/测试路径 + 本 CHANGELOG**：
  - **F1（P0，阻塞）探针判据错层 ⇒ 真实旧宿主上信号恒不触发 + 契约 2.5~2.9 报假绿**：五个 `remote-*` 探针原以求值于 **`makeHostApi` facade 域对象**，而 facade 在 `remote.*` 缺席时**原样返回旧表面域对象**（`legacyApi.settings/sessions/workspace/host/agentPresets`）——本仓自证的真实旧表面 fixture（`test/smoke.mjs` BUG-004 旧宿主回退用例，**域对象带方法**）在五谓词上**全部为真** ⇒ `remotePresent` 恒真、`triggered` 恒 false（文案永不出现：目标场景不可达），且面板把契约 **2.5/2.6/2.7/2.8/2.9** 五行报成「可用」（**诊断面板自身产出假绿**）。D1③ 原「触发」分句依赖**空壳域对象**构造，真实宿主不可能出现该形态。**修复**：判据下移到**原始服务名**——`makeHostApi` 同点记录 `ctx.get` 解析器（`hostServiceGet`，与 `hostLegacyApiSurface` **同源同代际**；存解析器而非快照，保留「服务后到自然接上」与 facade 惰性解析同口径），五个探针改为 **原始命名空间在场（方法面与各域 getter 同口径）∧ facade 域可用**（后者为第二重判据：防「装配面失效却报可用」）；`legacy-connection-api` 的**标记式判定按 R1 §6.2 保留不动**（方向正确）。**附带订正** `diagRbNone`（zh/en）——原「remote.* 与旧连接载体 .api 至少其一在场」与事实相反（双缺形态亦不触发），改为「触发条件 = remote.* 命名空间服务族全缺 ∧ 旧 .api 域对象表面在场；本次未同时满足」。**新增构造 (d)/(e)/(f)**：真实旧表面（域对象带方法）⇒ `triggered=true` + 2.5~2.9 行 MUST NOT 报「可用」；新表面（remote.* 五服务就位）⇒ 五项探针 `ok=true`、2.5~2.9 全 ok、`triggered=false`；双缺 ⇒ 全 false 不误报。
  - **F2（P1）BC-05 机检不绑定网关边界 + 实测载荷含仓内相对路径**：路由对 `report` 原样透传，`FACE1_UNPROBED_REASONS['1.11']` 的自由文本含 **「lib/tools.js」**⇒ 经 `report.unprobed[].reason` **出网关并在面板渲染**；而机检 payload 字段集**不含 `unprobed`**、fixture 又把 reason 净化为 `'retired'` ⇒ **盲区**（构造实证：给 probe 加 `file` ⇒ rows 投影丢弃 ⇒ D1② 仍绿）。**修复（并用两层，选择理由如下）**：**① 源侧枚举码**（R1 建议 ①）——`FACE1_UNPROBED_REASONS` → `FACE1_UNPROBED_CODES`（`retired` / `out-of-boundary-scope`），`FACES_NOT_COVERED_AT_APPLY[].reason` → `.code`（5 个面码），报告内**不再存在自由文本 reason**（`probes[]`/`unprobed[]`/`notCovered[]` 三处同改）；人类可读文案移到**客户端 i18n** `diagReason(code)`（zh/en 成对，**文案不进网关**），未知码原样回显不静默吞掉。**② 机检改为对真实路由产出整包扫描 + 正向对照**（R1 建议 ②）——新增路由行为测试：捕获真实 handler 的响应体，对**整包**（含此前盲区字段 `unprobed`/`notCovered`）跑 BC-05 扫描器，并注入 `file:'lib/client.js'` + 自由文本 `reason` 断言**扫描器必红**（判别力已证实）。**未加路由级白名单投影的理由**：源侧枚举码已从根上消除「自由文本承载路径」这一载体，再加一层白名单会二次维护字段清单（与 C-02 单一事实源相悖），而 ② 的整包扫描已使**任何**未来未登记字段的泄漏在 CI 红；故按 R1「二选一或并用」取 ①+②。
  - **F3（P2）`D_PROBE_ITEM` 与契约无等价断言（双事实源残余）**：原断言只查「是字符串且在册」，交换同面两项（2.5↔2.7）时面计数/算术/`rows`/`checks` 长度**全部不变**（面板归因错项而机检全绿）。**修复**：D1④b 增等价断言 `D_PROBE_ITEM[p.probe] === p.item` ∧ 该 item 的 `kind` 与契约对账 ∧ 映射**单射**（无两项映到同一 item）。
  - **F4（P2）D1③ 未覆盖 apply 装配路径；路由失败路径仅源码文本断言**：原三读数均在测试**自建 facade** 之后取值，而该调用本身会写模块标记 ⇒ 删掉写入点仍全绿（Developer 自查缺陷①无回归断言）。**修复**：(a) 三形态读数改为在 `runClientApply(...)` 之后**先直读** `CLIENT_PROBES[...]`（**不新建 facade**）断言 apply 装配结果，随后才用同 `getImpl`/同 `connection` 的 facade 构造报告；(b) 路由改为**行为断言**——以 fake `webServer` 捕获真实 handler、fake `req/res` 驱动，断言成功路径 200 + `report`/`contract` 字段面，失败路径（`ctx` 缺席）如实 `{ ok:false, error:'probe-unavailable' }` 且不冒泡 500；⑦ 补面板运行期探针真跑（`diagStyleProbe()` 解析真实 `NV_STYLE`：`ok=true`、`vars/rules > 0`）。**如实披露（不可达面）**：面板**渲染树**断言在本 mock 下不可达——mock react 的 `useState` 返回固定帧值且 setter 为 no-op，`SettingsPage` 的 `view`（初始 null）永不为非空 ⇒ 诊断区只在其「已加载」分支内，故该项以**源码挂载点断言** + **面板探针真跑**覆盖，不以假绿代替（已在 smoke 注释留档）。
  - **F5（P3）契约 note 内嵌行号残留旧值 → 行号解耦**：R1 指出 2.3 / 3.1 / 3.6 / 1.8 四处在重基后残留旧值（note 内嵌副本系统性偏移）。**修复**：四处 note 删除内嵌「现 L…」并改为**符号引用** + 写明「行号以本项 `line` 字段为唯一事实源」；**同口径**一并解耦面 2/3 其余 note/symbol 内嵌行号（本轮改动使它们再次失真）。**`line` 字段全量重基**（面 2/3 逐项实读复核：2.3/2.5/2.6/2.7/2.8/2.9/2.10/2.11/2.12/2.13/3.1/3.2/3.3/3.4/3.5/3.6/3.7/3.8 + 2.4 六处回退点），面 1 指向 `lib/index.js` 的 `line`/note 引用经逐点实读订正（1.4 `现 L39`→`L40`、1.5 `L36`→`L37`、1.6 调用点 `L1429/L94`→`L1457/L95`、1.9 调用点 `L1435`→`L1463`、1.10 `L154/L121`→`L155/L122`、1.3 调用点 `L169/L1366`→`L170/L1367`；1.2/1.6/1.9/1.10 note 的历史值以「原…为收口前历史值」标注，避免被读成现状）。**新增守卫**：D1⑩ 断言全契约 note 内「现 L…」形态 **0 处**（解耦不可回退）。
  - **F6（P3）死样式 / 死 i18n 键**：删 `.nv-diag-sub`、`.nv-diag-tbl code`（无引用）与 `diagStOk` / `diagStMissing` / `diagStUnprobed` / `diagNotDetected`（zh/en 各 4 键，无引用）。**重复展示（保留，理由）**：`unprobed` 汇总行与 rows 的 `unprobed` chip **信息不同**——汇总行现承载「原因码 → 文案」映射（rows 的 chip 不显示原因）；去重会丢诊断信息，故按 R1「二者选一」选择**保留汇总行**并如实说明。
  - **F7（P3）契约拼写 + 产品代码直连契约 + 同一派生式两处**：`bindow` → `browser`；新增边界导出 **`contractProjection()`**（`lib/host-boundary.js`），`lib/index.js` 不再 `import ... from './host-contract.mjs'`（依赖方向 = 产品 → 边界 → 契约，P-10 边界纪律），投影内 `revision` 复用边界既有 `CONTRACT_TASK`（= revisions 末项）⇒ **同一派生式收敛单点**。
  - **F8（P3）探针单点抛错全量丢弃 checks**：原整体 `try` ⇒ 任一探针抛错使 8 项读数全丢、面 2/3 全退化为「未覆盖」。**修复**：改**逐项 try**——单点失败降级为该项 `ok:null` + `detail='probe-error: …'`（进 unprobed），其余读数保留。新增构造断言（临时替换 `dom-phase` 为抛错实现 → 断言仅该项降级、其余 7 项保留、`checkErr` 为空，断言后复原）。
  - **F9（P3）`diagDesc` 用户可见文案硬编码仓内路径**：zh/en 两处删除 `lib/host-contract.mjs`，改为「宿主契约声明面 / the host contract declaration surface」。
  - **构造实证（红 → 绿，真 smoke 实跑，逐条留档）**：① 探针改回 facade 形状求值（复现旧判据）⇒ **D1③ 红**（`Drows=[true,…]`＝旧宿主上把 2.5 报成可用、`triggered=false`）；② 删除 `hostLegacyApiSurface` 赋值 ⇒ **D1③ + D1③b 红**；③ 删除 `hostServiceGet` 记录 ⇒ **D1③ 红**（新宿主五项探针 `Eprobes=[false×5]`）；④ 出网载荷回退自由文本 `reason`（含 `lib/tools.js`）⇒ **F2 码位断言 + D1⑥ 红**；⑤ 真实路由产出注入 `file` ⇒ **D1⑥ 红**（正向对照：扫描器判别力已证实）；⑥ `notCovered` 回退自由文本 ⇒ **D1⑥ 红**；⑦ 交换 2.5↔2.7 ⇒ **D1④b 红**（`equiv=false injective=false`）；⑧ 逐项 try 回退为整体 try ⇒ **D1⑨ 红**。全部变异经脚本「改 → 跑 → 还原」并复核工作区回到绿。
  - **验证**：smoke **271 → 276 项，0 失败**（+5 项净增：F2 码位 / D1③b / D1④b / D1⑨ / D1⑩；D1③、D1②、D1⑥、D1⑦ 为在原有条目上按 R1 要求**改写强化**——D1② 扫描面由字段子集改为**整包**并纳入 `unprobed`，D1⑥ 由源码正则改为**行为**断言，历史 271 项零回归）；`node --check` **9 文件 0 错**（含 `lib/host-boundary.js` / `test/smoke.mjs`）；`validate-preset` **PRESET VALIDATION PASSED（0 失败；本环境 full 模式计 32 项 ok）**；`ci-mock-face` **4/4**。**R1 §6 非阻断备注逐项对照**：`hostLegacyApiSurface` 标记式判定**保留**（§6.2）、`recheck`/N3/N4 诚实保持**未动**（§6.3：D1⑧/⑧b 原样通过）、`report.contract` 与 `contract` 双份元数据现状**未动**（§6.4）、`faces[1].probed` 语义**未动**（§6.5）、`smoke 271` 计数口径随本轮改为 276（§6.1 的 262+9 推导已被本轮 +5 取代，属预期变化）。
- **COMPAT-012 R1 五条 P3 收口（COMPAT-013：F-1 判据⑤ 范围完整性 / F-2 判据④ 强度边界 / F-3 披露计数更正 / F-4 契约 free-text 漂移 / F-5 判据③ 口径边界 + 严格面 golden；纯测试+契约+CHANGELOG，**运行时行为与诊断载荷零变化**——本条改动的 3 处字段（6.1 `line` / 3.5 `note` / 2.12 `symbol`）均不在任何运行时读取路径上，但文件位于打包面（`files: ["lib/"]`）且 numstat 为 **+3/−3 ≠ 0**，故**不再表述为「产品代码 diff = 0」**〔COMPAT-014 C-F-6 精确化〕）**：
  - **F-1（P3）F5d 缺「范围完整性」⇒ 新增判据⑤（⑫c，两子句）**：判据② 只要求「范围**内**至少一个候选命中」，对「起点含无关前导行 / 终点漏构造闭合行」零信号——契约 6.1 即实际触发（旧值 `L38-44`：L38 = `"dependencies": {},` 属**无关兄弟键**、L45 = `}` 为构造闭合行；①②③④ 四项对该实例**全盲**）。**⑤a 声明基数构造的全部出现**：symbol 以 `×N` 声明基数时，凡点号调用形态候选（tier 2）在**全文件**出现次数恰为 N 者，其在 `[起,止]` 内 MUST 亦为 N（实测覆盖 2 对 = 2.13 `ctx.slots.inject`×6 / `ctx.slots.register`×6；旧值 `L4422-4454` ⇒ 5 ≠ 6 ⇒ 红）。**口径收窄为实测驱动（非拍脑袋）**：该等式无差别施加于全部候选会使 **7/21 项误报**（2.3 `ctx.get` 1≠14 / 2.10 `sessions.list` 1≠6 / 2.11 `launcher.sessions` 1≠10 / 2.12 `ctx.on` 1≠2 / 3.1 `document.querySelectorAll` 2≠3 / 3.8 `document.querySelector` 1≠5 / 4.6 `cordis.patch.yml` 1≠2）——契约多数候选是「类型名 / 语义片段」，其全文件出现**本就不应**被单个范围条目包住；只有 symbol 自述基数的构造，「全部出现」才是有定义的量（防空转 = 覆盖对数 golden `=== 2`）。**⑤b 对象构造块闭合**：范围内出现的「候选键 + `: {`」声明行，其配平闭合行 MUST ≤ 止（6.1 旧值 ⇒ 闭合行 L45 > 止 L44 ⇒ 红；真值 ⇒ 绿）；全量 21 项实测不误报，**未收窄到 JSON**（JSON 与 JS 的 `键: {` 声明同构，收窄反丢 JS 面覆盖力）。**⑤b 不可被 ⑤a 取代（实测，已写入 smoke 注释防后续「简化」）**：6.1 的候选 `peerDependencies` / `schemastery` 在旧值与真值**两个**范围内的出现次数**同为 1 ≡ 1**（L38 是 `"dependencies"`、L45 是 `}`，均不含候选）⇒ 纯计数口径对**该实例无分辨力**（任务简报「增计数式判据后该实例必红」的前提经实测**订正**，故 ⑤ 以两子句落地）。**契约修正**：item 6.1 `L38-44` → **`L39-45`**（只读 `package.json` 逐行实读：L39 `"peerDependencies": {` 起、L45 `}` 闭合、L46 `}` 为根对象）。
  - **F-2（P3）判据④ 对 2.13 已真空 ⇒ 择「保留 + 标注」而非删除**：④ 为 **opportunistic**——仅当「终点行恰以点号调用起行」时生效；2.13 修复后终点 `L4461 = ))` ⇒ `callPrefix` 取 `null` ⇒ **对该条目已真空**。**处置 = 保留 ④ 并在注释如实标注其强度边界**（删除会让「终点行即下一次同构造起行」形态——旧值 `L4454` 即此类——失去唯一判据）；该条目的**持续机检力改由 ⑤a 承接**（截断 ≥1 处注册即 5 ≠ 6 ⇒ 红）。**残余缝如实披露（同次实测，未闭合）**：若只删 2.13 的**终点闭合行**（`L4429-4460`——6 处注册仍在范围内）则 ①②③④⑤ **全绿**（实测 262/0：⑤a 计数仍 6 ≡ 6、④ `callPrefix=null`、⑤b 无 `键: {` 形态构造）⇒ 该形态暂无持续机检力；根治需「构造闭合行」口径，而 JS 范围可为**合法语义片段**（2.1 `L92-94` 花括号净差 +2 / 2.3 `L4374-L4377` +1 / 3.8 `L2411-2421` +1 实测均非配平），无差别配平要求会误报此 3 项，故留档待另案。
  - **F-3（P3）披露计数更正 + 入机检**：CHANGELOG 原披露 faces 3/5/6「有**三处**范围合法起于注释行」——逐项实读**实为 2 处**（3.1 起于 `client.js:838` 的 `/**` JSDoc、3.5 起于 `client.js:896` 的 `//` 互操作说明；其余 3.8 `L2411` 函数行 / 4.6 `L17` / 5.1 `L1` / 5.2 `L16` / 5.4 `L77` / 6.1 `L39`〔修正前 `L38` 亦非注释〕/ 6.2 `L8` / 6.4 `L77` 均非注释）⇒ 订正为「**2 处**」，并新增 **⑫d golden 断言**锁定该计数（`=== 2`，断言消息内含实读清单；与 COMPAT-004 FIND-1「人工转写漂移」同款处置——把实测值入机检而非再人工转写）。
  - **F-4（P3）契约 free-text 行号/描述漂移两处精确化（只读 `lib/client.js` 逐行实读）**：① item 3.5 `note` 的 claim 让位段 `L4406-4417` → **`L4411-4424`**（L4413 `let claimHandler = null`、L4422 `window.addEventListener('dsh:split-claim', claimHandler)`、L4424 `} catch`——旧值起点偏差 **−5** 且**截断处未含 addEventListener 注册行**）；② item 2.12 `symbol` 的「单一监听器**双消费**」→ **「单一监听器三消费」**并列出三个消费者——REVIEW-COMPAT-004-R1 F2 新增 locale 分支后字段语义未同步（描述 stale）。**行号口径（COMPAT-014 C-F-6）**：本条陈述中的行号为 **COMPAT-013 落地时的实读值（历史锚点）**，非维护期副本——维护期行号以契约各条 `line` 字段为唯一事实源，free-text 不得承载现役副本（同批 B-N2 守卫已把该纪律机检化）。**机制说明**：F5d 只校验结构化 `line` 字段，**free-text 内的行号零信号**（本两处即实例）。
  - **F-5（P3）判据③ 口径边界 + 严格面防空转守卫（扩面前提）**：① `lineIsComment` 为 **JS 专属**（`//` `/*` `*` 与空行）——**YAML `#` 与 JSON（无注释语法）不在识别面内**；现无现存误判，但**未来把面 5/6 纳入 `RANGE_STRICT_FACES` 前 MUST 先按目标语言实现注释口径**（或改以「起点行是否属于所声明构造」替代注释启发式）——已在 smoke ③ 注释放档「不得盲加」；② 严格面守卫 `rangeStrict.length > 0` → **golden `=== 11`**（新增 `GOLDEN_RANGE_STRICT_ITEMS`；面 2 共 11 项——原守卫允许严格面退化到 1 项仍绿，与 `GOLDEN_RANGE_ITEMS` 的漂移探测约定对齐）。
  - **断言活性演练（红 → 绿，真 smoke 实跑，逐条留档）**：① 判据⑤ 落地后、契约未改前 ⇒ **261 passed / 1 failed（exit 1）**，报文 `bad=["6.1:⑤b peerDependencies(L39) 构造闭合行 L45 超出止 L44"]`（**证明该实例确被捕获**）；② 修正 6.1 `line` 后 ⇒ **262 passed / 0 failed**；③ 二次演练：临时回退 6.1 为旧值 `L38-44` ⇒ 同一条 FAIL 逐字复现 ⇒ 恢复 ⇒ **262/0**；④ 附带演练：临时回退 2.13 为旧值 `L4422-4454` ⇒ **⑤a 命中**（`ctx.slots.inject` 范围内 5 ≠ 声明 6，④ 亦红）⇒ 恢复 ⇒ 262/0。
  - **验证**：smoke **260 → 262 项，0 失败**（+2 项新断言 ⑫c/⑫d；历史 260 项零回归——F5d 既有四条判据行为面零削弱）；`node --check` ×5（smoke / host-contract / client / index / host-boundary）**0 错**；`validate-preset` **29/29** 不劣化。**改动面恰 3 路径**：`test/smoke.mjs`（⑫c 判据⑤ + ⑫d golden + ③④ 强度边界注释 + 严格面 golden）、`lib/host-contract.mjs`（item 6.1 `line` + item 3.5 `note` + item 2.12 `symbol`）、`CHANGELOG.md`——**运行时行为与诊断载荷零变化**（改动仅含 `lib/` 内契约文件的描述性字段——6.1 `line` + 3.5 `note` + 2.12 `symbol` ⇒ 打包面 numstat **+3/−3**；运行时只消费 `it.face===1` 的 `item/kind`、契约级元数据与 `revisions` 末项 task，均未变——原表述「产品代码 diff = 0」经 COMPAT-014 C-F-6 精确化）。**契约 `revisions[]` 未新增条目**（本轮只订正既有条目的字段值，非宿主耦合面变更——沿用 COMPAT-012 先例，避免 `[nv-compat]` 诊断载荷的归因任务随之漂移）。
- **REVIEW-COMPAT-004-R2 合并前收口（COMPAT-012：N1 强口径覆盖缝 + N2 契约范围型 line 偏移；纯测试/契约侧，产品代码零改动）**：
  - **N1（P3）收口① 强口径正则漏 `this.ctx.<id>` 形态**：R1 落地的强口径 `/(?<![.\w$])ctx\.[A-Za-z_$]/` 的 lookbehind 把**前导 `.` 一并排除** ⇒ 漏 `this.ctx.get(1)`（审证实测 `null`）；而本仓 `lib/index.js` 类方法**正在使用** `this.ctx`（`loggerOf(this.ctx)` / `emitChanged(this.ctx, …)`）——`this.ctx.<id>` 是现实的未来书写形态，一旦出现将**同时**绕过强口径（①）与补充面（①b 仅列 9 类具体名）。**修复**：改为 `/(?<![\w$])ctx\.[A-Za-z_$]/g`（同时命中 bare `ctx.` 与 `this.ctx.`）；词内/同类标识符形态（`myctx.` / `_ctx.` / `$ctx.`，前导字符属 `[\w$]`）仍排除、不误报。**双向实证入机检（smoke ①a）**：负向构造 `this.ctx.get(1)` 在 **R1 口径**下 0 命中（漏检 = 修复动因，把覆盖缝固化为反例，防口径回退）∧ **现行口径**恰 1 命中 ∧ 三类词内形态 0 误报。**真实代码面注入实证**：`lib/index.js` 现状两口径均 0 命中（**无实害**，与 R2 结论一致）；注入 `this.ctx.get('webServer')` + `this.ctx.settings` 后 R1 口径 = **0**（漏检）、现行口径 = `["ctx.g","ctx.s"]`（命中）。
  - **N2（P3）契约两处范围型 `line` 精确化（承继 R1 偏移，本次不遗留）**：item 2.12 `L4391-4397` → **`L4403-4408`**（`ctx.on('internal/service', …)` 调用节点实跨 L4403 开 ~ L4408 闭；旧值两端**均落在** BUG-004/005 注释块上）；item 2.13 `L4422-4454` → **`L4429-4461`**（6 处槽位注册段实起 L4429、止 L4461；旧起点 L4422 落在**无关行** `window.addEventListener('dsh:split-claim', …)`、旧终点 L4454 **漏第 6 处注册** L4458）。**核验方式 = 只读 `lib/client.js` 行段逐行确认**（2.12 `L4403-4408` 六行逐行读过；2.13 段内 `ctx.slots.inject(` 恰 **6** 处 = `settings.section`/`sidebar.footer.action`/`shell.overlay`×4）。
  - **smoke F5 增「范围型 line 抽核」（F5d，⑫b）**：F5a/b/c 对范围形态（`L<起>-L<止>`）仅有「形态白名单 + 首个行号 ≤ 文件总行数 + **每面 1 项**锚点」三重覆盖，而面 2 抽到的锚点是 2.1（范围精确）⇒ **终点越界 / 范围未包住所声明构造 / 边界落在注释行** 三项全无信号——2.12/2.13 即由此逃逸。新断言对**全量 21/48 项**范围条目校验：① 有效范围（起 ≥ 1 ∧ 止 ≥ 起 ∧ 止 ≤ 文件总行数——F5b 只查首个行号）；② 锚点在 `[起,止]` **范围内**命中（非「文件任意位置命中」）；③ 严格面（`RANGE_STRICT_FACES = [2]`，**11 项全覆盖**）起止行**非空行/纯注释行**；④ 终点**不截断同一构造**（终点行的行首点号调用前缀不得在终点之后再次起行）。**断言活性演练（红 → 绿，真 smoke 实跑）**：契约 line 临时回退为旧值 → F5d **FAIL** 且失配明细精确指出 `["2.12:边界落在注释/空行","2.13:终点截断(ctx.slots.inject 在 L4458 再次起行)"]`（**259 passed / 1 failed，exit 1**）→ 恢复 → **260/0 全绿**。**未纳入严格面的如实披露**：faces 3/5/6 有 **2 处**范围**合法**起于注释行（3.1 起于 `/**` JSDoc、3.5 起于 `//` 互操作说明），故边界非注释只对声明的面生效——扩面须先逐项核对边界语义（已在 smoke 注释留档，不盲加）。
  - **验证**：smoke **258 → 260 项，0 失败**（+2 项新断言；历史 258 项零回归——F5c 候选口径抽出为共用函数 `symbolCandidates` 后，FIND-1 锚点层级实测串仍为 `T3/T1/T1/T1/T1/T3`）；`node --check` ×5（smoke / host-contract / client / index / host-boundary）**0 错**；`validate-preset` **29/29** 不劣化。**改动面恰 3 路径**：`test/smoke.mjs`（F5c 候选口径提函数 + F5d 新检查 + ①强口径正则修正与 ①a 双向实证）、`lib/host-contract.mjs`（仅 2 处 `line` 字面量）、`CHANGELOG.md`——**产品代码 diff = 0**（client/index/host-boundary/tools 未触碰）。
- **REVIEW-COMPAT-004-R1 F1~F7 返工（P0 打包面阻断 + P1 locale「后到」降级 + 5 项改进）**：
  - **F1（P0，阻断）打包面缺文件 ⇒ 插件在打包安装通道完全无法加载**：`package.json` 的 `files` 只登记了 `lib/index.js`/`lib/tools.js`/`lib/client.js`，**新增的运行时依赖 `lib/host-boundary.js` 与 `lib/host-contract.mjs` 未登记** ⇒ 走 npm/pnpm 打包语义的安装通道（README 主通道 `dsh plugin add <git>`、npm 包名、`file:`）装出的插件在 `lib/index.js` 静态 import 处直接 `ERR_MODULE_NOT_FOUND`；而 `node --check`/`validate-preset`/`ci-mock-face`/`smoke` 全部在**源码工作区**执行，对打包面**零信号**，故四门禁全绿仍掩盖该缺陷。**修复**：`files` 三条精确条目**收敛为 `"lib/"`**（P-05 泛化：lib/ 内新增文件自动入包，消除同类遗漏整类；已确认 `lib/` 恰 5 个运行时文件、零杂项）。**防复发（smoke 两条新断言）**：`files` 覆盖面 ⊇ `lib/` 全部实际文件 ∧ ⊇ 入口（`main` + `exports` 去重）相对 import 的**传递闭包**，并约束 `files` 条目形态白名单（精确路径/目录前缀——出现 glob/否定式即刻红，强制显式评审）；附「`lib/` 零闭包外孤儿文件」断言（防 `"lib/"` 收编夹带未消费文件）。
  - **F2（P1）locale 服务「后到」永久降级**：A3 把 `connection`/`locale` 降为非 `inject` 服务后，`locale` 仍为 apply 期一次性快照且无自愈扳机（`degraded` 派生自 sessions）⇒ 宿主有 locale 却永久显示 zh（与 BUG-005「服务后到 ⇒ 永久降级」同类）。**修复（方案 a——与 sessions 同构）**：抽出 `snapshotLocale(ctx)`（apply 期与时序回调**共用取值口径**、零抛出），既有的**单一** `internal/service` 监听器加 `locale` 分支（重快照 + 通知已挂载组件重渲染；`store.set({})` 空 patch 仅触达 `useStore` 订阅者，零新机制）；宿主槽位标签改**渲染期惰性**取快照（不再固化 apply 期字典，与组件内 `makeT(localeSnapshot())` 同口径）。**构造断言 2 条**：① locale 后到 → 标签 `小说写作` ⇒ `Novel Writing`（同一监听器 + 清理 disposer 生效）；② 取值路径零抛出（`ctx.get` 抛错 → 保持现值、不打断宿主事件派发）。**已知边界（如实标注）**：locale 服务**撤离**时保持最后一次快照（不回退 zh，避免宿主热重载期间标签抖动）。
  - **F3（P2）宿主 `ctx.get` 语义不可复核 ⇒ 可对账化**：契约新增顶层 `ctxGetSemantics`（两形态行为 / 依赖面 / 兜底路径 / `status: unverified` + 残余风险如实披露——本环境无宿主 cordis 源码可及，不假装已证实）；smoke 新增**抛错构造断言**（`ctx.get` 抛错 → `safeGet` 兜底探测不崩（1.8 如实判缺面）+ 缺面告警可发；产品路径 `getWebServer` **透传不吞错**——两向锁定）。
  - **F4（P2）诊断载荷硬编码任务名**：`[nv-compat]` 载荷 `task` 改由契约 `revisions[]` **末项派生**（当前 = `COMPAT-004`）——后续 compat 任务沿用本层不再把缺面误标为旧任务、污染归因；smoke 断言派生值 ≡ 契约末项 ∧ 源码无 `task: 'COMPAT-004'` 字面量赋值。
  - **F5（P3）收口判据自指**：收口①由「开发者自列 9 类模式」升级为**强口径**——`lib/index.js` 代码面**零** `ctx.<标识符>` 属性访问（正则加 `(?<![.\w$])` 边界，不误命中 `service.ctx`）+ **零** `@deepseek-ai/` import（覆盖一切形态，不受「模式表是否列举到」限制）；9 类模式保留为**补充面**（覆盖 `webServer.register` / `.agent-presets` 等非 `ctx.*` 形态）。
  - **F6（P3）共享数组引用**：`lib/index.js` 改 `inject = [...hostInject]`——宿主/消费方就地改写 `inject` 不再污染边界层（`hostInject` 同时是能力探测判据 1.5 的输入）；smoke 断言非同一引用 + 内容等价 + 就地改写零泄漏。
  - **F7（P3）跨文档口径统一**：CHANGELOG / 契约（`faces[0].scope`、item 1.11 `note`）/ `lib/host-boundary.js` 文件头统一为「**lib/index.js 宿主调用 100% 收口**；`lib/tools.js`（1.11）按轴①独立评估，不在本层」，消除把收口范围读成「全服务端（含 tools 行）」的余地；smoke 断言三处文档齐备（剥 markdown 装饰后比对）+ 文档中**每一处**「100% 收口」均限定 `lib/index.js` + tools 行显式排除。
  - **验证**：smoke **248 → 258 项，0 失败**（+10 项；历史 248 项零回归）；`node --check` ×5（index/host-boundary/client/host-contract/smoke）0 错；`validate-preset` **29/29**；`ci-mock-face` **exit 0（4/4 面）**。**打包面隔离实证**（仓外 `%TEMP%` 临时目录 + `npm_config_cache`/`userconfig`/`USERPROFILE`/`XDG_*` 全部重定向 + `--ignore-scripts`；仓库工作树与宿主 checkout 零写入，探测产物已删）：`npm pack --dry-run` → tarball `lib/` **5 文件**（client/index/tools/host-boundary/host-contract）；临时项目 `npm install <tarball>` 后 `node_modules/dsh-novel-writing/lib/` 同 5 文件、包内相对 import 目标**零缺失**；**`import('dsh-novel-writing')` 成功**（`apply=function` / `inject=["settings"]` / `NovelWritingService=function`；子路径 `./client`、`./tools` 均解析到包内文件）——F1 的失效模式（`ERR_MODULE_NOT_FOUND`）已消除。
- **REVIEW-COMPAT-011-R1 FIND-1~4 收口（随 COMPAT-004 一并落地）**：
  - **FIND-1（P3 文档事实性）**：COMPAT-011 条目 C3 子条的 tier 枚举由人工转写的 `T3/T1/T1/T3/T1/T3` **订正为 smoke 实测 `T3/T1/T1/T1/T1/T3`**（face 4 / item 4.1 的最优层级实为 **T1**——中文 name 字面量 `新布局` 命中，非 T3 裸词）。已在上方 COMPAT-011 C3 子条同处订正；smoke 增 info 行输出实测串（`1.1✓T3 / 2.1✓T1 / 3.1✓T1 / 4.1✓T1 / 5.1✓T1 / 6.1✓T3`）+ **golden 断言**锁定，避免再次人工转写漂移。
  - **FIND-2（P3 文档瑕疵）**：`lib/host-contract.mjs` 头注释括号清单删除多余 `/`，并纳入 COMPAT-011 新增的 `versionExceptions` / `versionFactOrigins` / `_schema`。
  - **FIND-3（P3 承继风险 / COMPAT-004 遗留）**：`extract.mjs` 新增 **`methodNamesExcludedByKeyword`** 采集——被关键字黑名单过筛的**真实**类体顶层成员名 + **宿主原始文件行号** + 原因（行号口径 = 报原始文件行，故 `stripComments` 改为「行结构逐字节保留」的等量空白替换）；三份 fixtures **重建**，现行 fixture 实测恰三处：`cordis.DisposableList.delete@L17` / `cordis.Fiber.await@L1398` / `cordis.RegistryService.delete@L1564`（与审查报告 A3 行号**逐字一致**；历史两份 = 空数组）。契约 `_schema` 登记该字段并明确**判据优先级 `exports`/AST > `methodNames`**（methodNames 有**假阴性**风险——被过筛的真实方法会被误报缺失）。
  - **FIND-4（P3 残余缝，未触发）**：`extract.mjs` 已知边界补一行——`classBody`/`maskToClassBodyTopLevel` **不识别正则字面量、不处理嵌套模板串**（含 `{`/`}` 的正则或嵌套反引号会使花括号配平失真 → 后续顶层成员漏检或语句行首回流污染；三份 fixtures 当前未触发，根治路径 = 语法位置判别 / 现成 AST 解析器）。
  - **fixtures 重建复核（自证未扰动）**：三份 fixtures 与重建结果的结构性 diff（除新增字段外）= **恰 1 处**（`extraction.classExtraction` 文案），其余**逐字节一致**——证明「行号保留式剥注释」改造未扰动既有提取面。
  - **发现（超出本任务范围，供另案入账）**：契约 item 1.8 与 §3 底稿称「13 路由」，实测 `lib/index.js` 的 `api(...)` 注册点恰 **12**（overview/novel/chapter/gate/request/novel-create/novel-delete/file/request-done/publish/data/preset-sync）——已在 item 1.8 note 如实订正并留档；§3 底稿（`docs/research/COMPAT-001`）计数订正需另立任务。

- **fixtures 数据质量与断言强化（COMPAT-011，承接 REVIEW-COMPAT-003-R1 C1~C7 + N1~N3 全量落地；`lib/client.js` 与 `lib/index.js` 零改动）**：审查发现三份 fixtures 的 `methodNames` 混入语句/关键字（`SettingsConflictError` 误为 `['constructor','super']`），方法面判据被污染——而 fixtures 将作为 COMPAT-004/005 探测与诊断的校准基线。
  - **C1（P1）`methodNames` 关键字清零**：`extract.mjs` 的 `methodsOf` 改为三层约束——①新增 `maskToClassBodyTopLevel`：按花括号配对只保留**类体顶层**字符（方法体/块内部、字符串、模板串、注释一律置空保换行），根因是原正则把**方法体内语句行首**的 `super(`/`if (`/`for (`/`return (` 当作方法名；②`METHOD_KEYWORD_BLOCKLIST` 17 词兜底；③排除 `this.`/`obj.` 点号前缀调用。**一手对照**（宿主只读闭包 `dsh-settings@0.1.5-rc.2 lib/index.js`，类体 L92-608、导出行 L610）：`SettingsProvider` **24 → 20**（恰为主审预估的真实方法数）、`SettingsConflictError` **2 → `['constructor']`**。**三份 fixtures 重建**（重跑命令逐字留档 `extract.mjs` 头部 D 组；`npm view` 可达 → `npm pack` 12 个 tarball → `tar -xzf` → `--packed` 两份 + `--checkout` 现行闭包一份，全程只读宿主、未 install、未执行宿主代码）：关键字命中 **42/46/74 → 0/0/0**。
  - **C2（P2）`classCount`/`classAliases` 去重**：`parseModule` 新增 `declCache`（按**导出前符号名**缓存 decl 对象），使 `export { SettingsProvider as default }` 与直接声明共享同一对象、可按同一性识别「同一实现的多导出名」；`buildFixture` 改按声明对象归并，**主名优先取真实类名**（仅以 `default` 导出时才用 `default`），其余进 `classAliases`。结果：`dsh-settings` **3 → 2 类** + `classAliases = {"default":"SettingsProvider"}`（可还原类名，N3 的顾虑随之消解）；三份 fixtures `classCount` **16/16/26 → 12/12/22**、`classAliases` **0 → 4 项/份**。
  - **C3（P3）F5c 锚点断言由近恒真改实义**：候选**剥离引号后**入集 + 分级（1 引号/中文字面量 > 2 点号标识符 > 3 裸词）+ 49 词通用词表排除（`package.json`/`name`/`cordis`/`default`/`package`…）+ 断言改为「至少一个**非通用**候选命中」。此前 face 1/4/5/6 靠 `deepseek`/`cordis`/`package.json`/`name`/`peerDependencies` 恒真命中，「防 file 指错文件」不成立；现 6/6 命中且分别落在 **`T3/T1/T1/T1/T1/T3`**（COMPAT-004 FIND-1 订正：原为人工转写的 `T3/T1/T1/T3/T1/T3`，实测 face 4 由中文 name 字面量 `新布局` 命中 **T1**；该串现已由 smoke info 行输出 + golden 断言锁定，事实源 = `1.1✓T3 / 2.1✓T1 / 3.1✓T1 / 4.1✓T1 / 5.1✓T1 / 6.1✓T3`。面 5 由中文 name 字面量 `小说写作工作流` 命中——`preset` 本身**不在** `preset.yml` 内容里，原断言恰是靠通用词 `name` 蒙过）。
  - **C4（P3）「三版本实测」措辞订正 + `origin` 纳机检（二选一——采取后者并同步订正措辞）**：`remoteNamespaceServicePackages` 的值来自命令行注入的注册表事实（非提取）⇒ 契约新增 `hostSurface.versionFactOrigins`（static-extract / registry-metadata），smoke ④⑤⑥ 逐版本同时校验 `value` 与 `markers[fact].origin`（静态提取类的 origin 由 `extraction.layout` 决定），杜绝「注入值伪装成实测」；断言标签改为按来源类别如实表述。
  - **C5（P3）fixtures 内部版本一致性入机检**：新增断言「非例外包 `packages[p].version` ≡ `fixtures[v].hostVersion`」（3 版本 × 6/6/8 包）+ 契约新增 `hostSurface.versionExceptions = ['cordis','schemastery']`（独立版本族）并**双向校验**（例外表 MUST 有实据——防其腐化成万能豁免）。背景即本任务「CLI 0.1.5-rc.1 vs 子包 0.1.5-rc.2」的版本分歧教训。
  - **C6（P3）mock 面钉 1/4 → 4/4**：契约 `hostSurface.ciMock` 由单对象扩为**数组**（cordis / dsh-settings / dsh-home-paths / dsh-tools，schemastery 无 mock 装真包）；`ci-mock-face.mjs` CLI 与 smoke ⑦⑧ 循环校验不变量「mock 静态导出面 ≡ 动态 import 键集 ≡ `requiredExports[本包]` ⊆ fixture 现行宿主真实面」——第三项把「mock 导出宿主不存在的表面」这一 BUG-003 温床结构性地纳入机检（原断言只覆盖 dsh-settings）。
  - **C7（P3）输入校验与路径收敛**：`ci-mock-face.mjs` fixture 缺包/`exports` 非数组 → **结构化 exit 2**（原为 TypeError）；失败路径统一为 FAIL/exit 2；`materializeCiMocks` 新增 `resolve(abs).startsWith(resolve(tmpDir))` 包含性检查（写前第二道收敛，防 `../` 越界）。
  - **N1/N2/N3**：`origin` 按 `--packed`/`--checkout` **分流**（`tarball-static-extract` / `checkout-static-extract`）并落 `extraction.layout`——0.1.5-rc.2 实为闭包提取，原统一标 `tarball-*` 与 `source` 自述不一致；⑯ 纯数据守卫的**数组分支**补齐自有附加属性检查与 `Array.prototype` 原型校验（与对象分支对齐）；契约新增 `_schema` 说明 fixture 字段语义（含 `classAliases` 主名/别名口径与消费方读法）。
  - **诚实披露（黑名单过筛代价）**：宿主确有以关键字命名的真方法——cordis `DisposableList.delete`（L17）、`RegistryService.delete`（L1564）、`Fiber.await`（L1398，`async await()`）经黑名单一并滤除。取舍口径：`methodNames` 的消费方用于探测「我方调用的方法是否仍存在」，**伪名会造成误报缺失**，故以「零关键字」为硬口径（宁缺勿污染）；三处行号已在本条留档供 COMPAT-004 复议。
  - **验证**：smoke **230 → 231 项，0 失败**；`ci-mock-face.mjs` **exit 0（4/4 面）**；`node --check` ×4（extract.mjs / ci-mock-face.mjs / host-contract.mjs / smoke.mjs）0 错；`validate-preset` 不劣化。**断言活性演练（红 → 绿）**：临时改 fixture `hostVersion` → C5 版本一致性断言变红（失配明细列出 `0.1.5-rc.2:dsh-settings@…≠…`）→ 复原 → 231/0 全绿。

- **CI dsh-settings mock 导出面漂移修正（COMPAT-010；纯 CI mock 修正，产品代码零改动）**：COMPAT-001 宿主兼容分析（`docs/research/COMPAT-001-host-compat-analysis.md` §6.4）+ Design Review R1（F5）确证的活性缺陷——`.github/workflows/ci.yml` 的 dsh-settings mock 仍导出宿主 dsh 0.1.2-rc.1 起已删除的 `settingsNamespace` 具名导出，而真实 `dsh-settings@0.1.5-rc.2` 导出面仅 `SettingsConflictError / SettingsProvider（含 default 别名）/ redactSecrets`（宿主只读闭包 `@deepseek-ai/dsh-settings/lib/index.js` L610 权威核对）。mock 让已删除表面「复活」= 若插件代码重新引入该导入，CI 假绿而宿主实机启动崩溃——BUG-003 类事故温床。修正：mock 导出面逐项对齐真实面并保持形态忠实（`SettingsConflictError`=Error 子类〔name/code/expected/actual〕、`SettingsProvider`=cordis `Service` 子类、`default`=`SettingsProvider` 别名、`redactSecrets(schema, value)`→`{value, secrets}`；删除 `settingsNamespace` 导出），mock 处注释钉住宿主权威面位置（包路径 + 行号）。`lib/*` 产品代码零改动。
- **兼容 dsh 0.1.5 布局重构——安装脚本版本自适应（BUG-006，安装通道适配）**：dsh 0.1.2-rc.1 → 0.1.5-rc.1 重构了 profile 布局与插件注册机制（每 profile 独立目录：`package.json` + `pnpm-workspace.yaml` + 私有 `node_modules`；官方注册通道 = profile `package.json` 的 `dependencies`（`"dsh-novel-writing": "file:<插件路径>"`）+ `dsh.profile.bundles` 数组追加）。**本插件代码本身对 dsh 0.1.5-rc.1 零改动兼容**（隔离实例全链路实测启动成功），本条为安装脚本适配：install.ps1 / install.sh 自动检测布局版本（`profiles/<Profile>/package.json` 存在 = 0.1.5+ per-profile / 缺省 = 旧版全局）——**新布局** junction 接入 `profiles/<Profile>/node_modules/` + 幂等注册 profile `package.json`（dependencies + `dsh.profile.bundles`，保留既有键与键序、2 空格缩进；install.sh 注册用 `node -e`，无 node 回退 python3，两者皆无降级仅写 patch 行并提示）+ `cordis.patch.yml` insert 行兜底双保险（两条注册路径均独立有效）；**旧布局行为完全不变**（junction 到 `profiles/node_modules` + patch 行）。幂等可重复执行，不产生重复行/重复键/损坏 JSON；README 安装与兼容性说明同步。

## [0.5.1] - 2026-09-07

### 修复
- 兼容 dsh 0.1.2-rc.1：`@deepseek-ai/dsh-settings` 移除 `settingsNamespace` 具名导出，本插件导入即触发 ESM SyntaxError，`dsh web` 启动崩溃（plugin tree failed to load）。设置命名空间改为普通字符串常量 `'novel-writing'`（新版 `settings.register/get` 收普通字符串并自带命名空间格式校验），移除对该导出的依赖。（BUG-003）
- 客户端 API 表面迁移到 dsh 0.1.2-rc.1（BUG-004，三项实机报障）：
  - **症状**：①工作区对话框报「API 不可用（宿主未挂载 webServer 或插件未运行）」、设置页白屏、卡片打开/绑定/切换链全断；②点其他工作区的会话不退出工作台界面；③书目列表为空。
  - **根因**：①0.1.2-rc.1 客户端 `connection` 服务句柄不再有 `.api` 表面（实证句柄仅 isLoopback/generation/state/rpc/reconnect/registerGenerationSource/start）——宿主 API 迁移到 cordis 服务 `remote.<namespace>`（settings / session〔单数〕/ workspace / agentPresets / directoryPicker，方法为位置参数、返回 `{ok,value|error}` 无 `.result` 包装）+ `workspaces` 快照服务（getSnapshot/subscribe，无 `workspace/list` RPC），本插件 lib/client.js 内 44 处 `apiHas`/`api.<domain>.<method>` 调用面全部落空（SettingsPage 的 effect 内同步 TypeError 即设置页白屏根因）；②会话联动守卫「null 过渡仅刷新基准」把跨工作区切换的 `sessions.current` X→null→Y 形态吞掉；③`workspaceRoot` 指向的旧目录已不存在（见下方指引）。
  - **修复**：lib/client.js 新增**适配层 `makeHostApi`（单点收口，全文件唯一特性检测点）**——按域惰性 getter 每次访问重解析服务（新表面 `remote.*`/`workspaces` 优先、旧宿主 `connection.api` 域对象回退），后到服务自然接上（无轮询），对外维持插件既有调用约定 `{result:{ok,value,error}}` 与 `{ns,patch}` 单对象入参，44 处调用点零改动；`session.prompt` 按新契约自动铸客户端 `requestId`（对齐宿主 sessions 客户端 randomUUID 用法）；`host.pickDirectory/createDirectory` 的 string 返回值包装回 `{path}`；`workspace.list` 改读 workspaces 快照（pending→ready 经 subscribe 事件等待）；设置页与平台配置编辑器加 api/settings 存在性守卫（缺失走可读错误态，不再抛未捕获 TypeError）；两处会话联动守卫（管理台 NvConsole 与创作台 SplitWorkspace）改 **last-non-null 比较**（null 永不触发关闭也不刷新基准，新非空 ≠ 上一个非空才关——覆盖 X→null→Y 跨工作区与 X→Y 同工作区；豁免令牌语义不变）；`package.json` 的 `dsh.client.inject` 移除 0.1.2-rc.1 已不存在的 `@deepseek-ai/dsh-client-runtime`（保留 locale/ui-settings/api-remotes）。smoke 147 → **179 项**（新增 32 项断言：适配层新表面位置参数映射/`{result}` 包装/prompt requestId 铸造/directoryPicker 值包装/workspaces 快照等待/旧表面透传/双表面皆无降级/联动守卫 last-non-null 五形态/设置页无 api 不抛错/单点收口与包表校验）。
  - **用户侧指引（workspaceRoot 重指）**：升级后插件设置里的工作区根目录若仍指向旧路径（目录已迁移/改名，例如书稿从 `C:\Users\<你>\novels` 迁到了新位置），书目列表会为空——打开小说管理工作台控制台，点顶部「**切换 / 新建工作区…**」对话框：已配置工作区中**选中新根目录即切换**；若新目录未注册为工作区，用「选择文件夹」或「在其下新建目录」后「创建并采用」。按「一个工作区 = 一批小说」自行决定指向哪一层目录（指向含多本书的父目录则其下每本书一个 `novel-project` 均会被列出）。本修复不在代码中写死任何路径。
- **点其他工作区的会话不退出工作台（BUG-005，BUG-004 实机验收遗留根因）**：
  - **症状**：BUG-004 修复后完整重启 `dsh web` 实测症状仍在——管理工作台搜索关键词后「找到的会话」区不出现，会话联动退出也不生效（无状态点、绑定面板会话列表同样为空）。
  - **根因（活实例无头 CDP 探针实证）**：BUG-004 只让 **launcher（非 React）面**在 sessions 服务后到时刷新，**React hook 面**仍按 `apply()` 时取到的服务快照冻结。dsh 0.1.2-rc.1 中 `sessions` 服务由 `dsh-api-session-controller` 提供，其 `inject` 的 `remote.*` 命名空间要经 `dsh-api-remotes` **异步** apply（`await ctx.remote.$mount(contribution)` 循环）才挂载；本插件 `apply()` 只等同步依赖（`slots`/`connection`/`locale`）→ `ctx.get('sessions')` 在 apply 时点恒为 `undefined` → 旧 `makeSessionsHook(undefined)` 返回 **`null` 并永久冻结** → NovelDrawer / NvConsole / SplitWorkspace / BindDialog 全部 `degraded=true`：无状态点、无「找到的会话」搜索、无会话联动退出（守卫判定逻辑本身正确，是数据源恒空）。
  - **修复**：`lib/client.js` 的会话 hook 改为**稳定响应式** `makeSessionsHookReactive(ctx)`（旧 `makeSessionsHook(svc)` 冻结实现退役）——① 返回**恒定函数引用**：服务有无只改变内部解析结果，消费端 `props.useSessions !== null ? props.useSessions(sel) : …` 的条件调用形态下每帧 hook 调用次数恒定（「中途翻转不安全」的顾虑由函数身份永不变消解）；② **holder 惰性解析**：每次解析经 `ctx.get('sessions')` 取当前服务并校验 `list` 的 `getSnapshot`/`subscribe` 形状，缺席/形状不合格 → hook 返回 `undefined`（消费端既有 `?? null` / `?? []` 容错，与原降级面一致）；③ **服务生命周期重订阅**：与 BUG-004 既有监听**合并为单一** `ctx.on('internal/service')` 监听器，sessions 增减时同时刷新 launcher 引用与调用 `useSessionsH.refresh()`——已挂载实例重解析、换订阅（旧 store 退订，无悬挂订阅）、`setValue` 重渲染；④ **强制重渲染扳机**：`degraded` 判定含非 React 面 `launcher.sessions`，故服务增减时即使派生值不变也推进一次代次 state，防降级态卡死；⑤ 插件卸载沿用既有清理路径（监听 disposer 不变）。**消费端组件、联动守卫、API 适配层零改动**（smoke 回归钉：`useSessions !== null` 8 处 / `=== null` 4 处 / `degraded` 公式 3 处 / `connection.api` 代码级引用 1 处 全部原样）。
  - **验证**：`node --check` 0 错误；smoke **179 → 203 项**全绿（新增 24 项：mini-react 生命周期——缺席挂载返回 `undefined` 且不订阅、后到经 `refresh()` 自愈取到值并建立订阅、快照更新驱动重读、撤离回退 `undefined` 并退订、再次到位往返自愈、卸载无泄漏订阅、派生值不变仍重渲染、形状不合格服务按缺席处理、函数引用恒定；apply 接线与监听合并的源码面回归钉；消费端/守卫/适配层零改动回归钉）；顺带修正 BUG-004 评审遗留 F1（P2，「错误分支透传」断言把返回 Promise 的 IIFE 当布尔用 = 真空断言 → 改为先 `await` 再判定）；**活实例探针复跑决定性通过**：同一 3180 实例刷新加载新 bundle 后，控制台搜索即出现「找到的会话（44）」并渲染 30 行会话（修复前同页 `.nv-cfound-title` count=0），运行时异常与 console error 均为 0。

## [0.5.0] - 2026-08-30

### 新增
- 创作台中窗下半区左侧（与章节列表列等宽，工作台右移收窄——空出章节标题栏正下方一块）工作流控制面板（UX-059）：标题栏「▶ 开始/继续工作流」迁入中窗下半区左侧（与章节列表列等宽 chapterW 联动）；停止/继续合并单按钮按绑定会话状态切换形态（运行中 → ⏹ 停止（sessions.cancel，队列保留）/ 空闲 → ▶ 开始·继续）；新增「压缩上下文」（/compact 斜杠命令，需会话空闲）与「绑定新会话」（create → 预设 → 绑定，不自动打开）独立按钮。

## [0.4.0] - 2026-08-29

### 工作台重构（UX-006，DEC-013 五项定案）
- **挤法分栏工作区 + 小说↔会话绑定（dsh-worktable 思路，DEC-013①–⑤）**：参照 dsh-worktable 的 `[data-phase]` 会话根探测 + margin 挤压实现**固定三栏可调布局**（`shell.overlay` id=novel-split，zIndex 900 低于对话框层）——左窗（工作区行 + 书目 + 文件树，可「«」折叠）｜竖分隔线｜中窗（章节/工作流/请求/数据/发布五页签，既有 Panel 复用+切页签草稿保活）｜右窗 = 官方对话窗本体（marginLeft/marginRight+marginTop 挤法贴右，**⇄ 可换边**），两条分隔线 pointer 拖拽调宽（左窗 160–420px、对话宽 240px–余量钳制），宽度与换边 localStorage 持久化（`dsh.novel.split.v1`={leftW,chatW,chatSide}）；ResizeObserver + body 级 MutationObserver(data-phase) 重锚定（过渡态等待、无会话才关）、让位观察器（viewArea margin 被外部改写即关）、`dsh:split-claim` 共享互斥协议（与 dsh-worktable 共存兼容）、Esc/✕/再次点击关闭（过 chapterDirty 脏稿守卫）；挤法探测失败降级为不动布局+明确提示（抽屉卡片仍可切会话）。**侧栏抽屉**（`sidebar.footer.action` id=novel-drawer，order 9）：分隔线 +「📖 小说工作台」标题（⚙ 工作区=UX-005 对话框入口 / ＋ 新建）+ 书目卡片（书名/阶段·章数·字数/**绑定状态点**：工作中=accent 蓝发光/待决=黄/完成=绿/失效=红/未绑定=空心，经 `ctx.sessions.list` ObservableSnapshot 订阅叶子字段零轮询镜像；会话服务缺席降级：状态点隐藏、打开/绑定禁用并提示）。**小说↔会话 1:1 绑定**：存宿主 settings ns novel-writing 新字段 `bindings`（`z.dict(z.string(),z.string())`，overview 附带下发，客户端读现值合并写入不整表覆盖）；绑定面板按 workspace.list 分组列会话（+「新建会话并绑定」：书目录为 cwd + 预设挂载 + 启动指令）；绑定会话失效（byId 无此 id）卡片标「会话失效」引导重绑（不静默清除）；抽屉 ＋ 新建全链 = novel-create（宿主响应补 `path` 字段——本次唯一宿主契约加法）→ `sessions.create({cwd})` → `agentPresets.select('novel-writing')` → 写绑定 → `sessions.open` → 开分栏 → 发启动指令。**退役**（DEC-013①）：conversation.view「小说」标签页、conversation.input.dock LaunchBar 兜底条、sidebar novel-hud 按钮 + novel-hud-panel 浮层、store 的 pendingLaunch/launchHint/hudOpen；指令发送全面改 `api.sessions.prompt({sessionId, mode:'queue', content:[{type:'text',text}]})`（launchMsg 文案沿用：新书「开始…《书名》（目录 id）」/旧书「继续…」）。**视觉全量宿主设计令牌**（DEC-013④）：单 `<style id="novel-writing-style">` 注入、类名前缀 `.nv-`，颜色/边框/背景全 `var(--dsw-alias-*, 兜底值)`（兜底对齐 dsh-worktable styles.ts 取值），图标 emoji、状态点纯 CSS，apply 返回可逆清理（引擎关闭+监听移除+style 移除），零新依赖。smoke 67 → **83 项**（新增宿主 bindings/path 3 项 + 客户端挂载契约 13 项：ModuleLoader 注册/无 DOM 降级求值/注册面恰 5 席/退役面负断言/render 可调用/可逆清理）。

- **工作台入口重构为全屏管理控制台（UX-007，DEC-015 修正版）**：侧栏「📖 小说工作台」区块 = **纯主入口**——标题行整体可点（开/关控制台，反选语义；**移除 🔍⚙＋ 按钮组**）、书目卡片点击 = 打开控制台并聚焦该书（空态改为虚线框「打开工作台界面创建第一本小说」）；新增 **工作台控制台**（shell.overlay id=nv-console，order 25，zIndex 950，覆盖内容区不遮侧栏）：几何 = findConversationRoot bounding rect，无会话根/失效降级 = 左侧取侧栏右缘（findSidebar 向上探测，280px 兜底）+ top/right/bottom=0；ResizeObserver + body MutationObserver(data-phase) 跟随重定位（会话切换不关闭、无会话保持降级定位）；与 novel-split 互斥（开控制台先关分栏——脏稿守卫；开分栏或他引擎 dsh:split-claim 关控制台）。内容：顶部栏（📖 标题/当前工作区名/「切换 · 新建工作区…」复用 WorkspaceDialog/✕/Esc）＋ 搜索行（12px 输入 + ✕ 清除；关键词同滤小说卡片（书名/目录 id）与会话快照标题/id →「找到的会话」行，点击 = 打开会话并关控制台）＋「＋ 新建小说」右侧 fixed 320px 悬浮表单（目录名必填/书名可选默认同目录名/「创建并开始」走 launcher 既有新建链：novel-create→sessions.create({cwd})→agentPresets.select→bindings 写→sessions.open→开分栏→sessions.prompt／「仅创建」/取消；busy 禁用、错误红字）＋ 小说卡片网格（grid minmax 260px 自适应列；玻璃拟态 bg fill-l1 半透明 + border l1 + radius 10 + hover border accent/提亮 + **状态光效**：need 黄 / done 绿整卡霓虹光效、busy 蓝光 + 流光扫过；卡内 📖 书名 14px/600 + 阶段徽标 12px + 状态行 `N章 · N字 · 发布✓/✗ · 变现✓/✗ · 信号N`（11px secondary）+ **绑定状态双圆**（○○ 空心未绑定 / ●● 实心已绑定；busy = accent 蓝双圆交替脉冲 / need 黄 / done 绿 / stale 红，drop-shadow 发光）+ 最近更新时间（11px tertiary）+ **数据卡片**（卡内底部一行最近 metrics 摘要：完读率/读完率/追读/日增收藏/收益，无数据「暂无数据」）+ 卡操作（▶ 打开 = sessions.open 绑定会话+关控制台、🔗 绑定/重绑 = 既有 BindDialog（失效引导重绑）、➤ 继续工作流 = sessions.prompt 复用）；**卡片主体点击 = 切绑定会话并关控制台**（无绑定/失效 → 打开 BindDialog 提示绑定）。视觉对照 dsh-worktable 控制室卡片（玻璃渐变、状态霓虹/流光、双圆绑定），全量 --dsw-alias-* 令牌、.nv- 样式并入既有 `<style id="novel-writing-style">`（幂等+清理），数据面零新增 HTTP API（信号/metrics 经既有 overview + /api/novel 组合拉取）；smoke 83 → **86 项**（新增 nv-console 注册面 6 席 / order=25 / z-index 950 样式注入 / 抽屉无按钮组 + 标题行按钮树断言；mock react 升级为逐层执行函数组件以便组件树级断言）。
- **控制台 UI 迭代（UX-008，DEC-017 用户实机批注 5 点，纯客户端零契约变更）**：①头部——「切换 / 新建工作区…」按钮移到**工作区路径紧邻处**（✕ 经弹性留白保留最右），标题 14→15px（权重 600 不变）；②搜索行改**搜索引擎风格大药丸并移到控制台底部居中**（`.nv-cfoot` 容器行：999px 圆角 + 1.5px l2 清晰边 + focus accent 边 + fill-l2 底 + 内部左侧 🔍 14px tertiary + 输入/占位 13.5px「搜索小说 / 会话…」+ 有输入才显示的 ✕ 清除钮；行为不变——同滤小说卡片与「找到的会话」）；③删除头部下方「＋ 新建小说」实色按钮，改卡片网格末尾**「＋」虚线磁贴**（对照 dsh-worktable `.dsh-wt_presetAdd`：dashed accent + 中央 21px 加号 + hover 提亮；与卡片同网格同尺寸，不参与排序/拖动，过滤时始终显示；点开既有新建悬浮表单——表单组件不动，仅入口改磁贴）；④卡片——**删除「▶ 打开」按钮**（卡片主体点击即打开，DEC-015 口径），卡操作收敛为右下角两个 **22×22 图标钮**（🔗 绑定/重绑 · ➤ 继续工作流，hover title 提示 + stopPropagation），网格 260px→**minmax(300px,1fr)**（≥1200px 容器恰 3 列）、卡片内边距 14px、圆角 12px（玻璃拟态渐变保留）、信息行距加大（状态行 12px / 数据卡片 11.5px / meta 11px 行高 1.6）；⑤网格上方左侧加**排序控制**（「默认 / 手动」两态 pill，对照 dsh-worktable `.dsh-wt_sortBtn`；默认 = overview 顺序），手动态卡片 **HTML5 拖拽换位**（draggable + dragstart/dragover/drop 实时重排，对照其管理模式 moveTo；拖动中卡片半透明、落点 accent 虚线框），顺序持久化 `localStorage['dsh.novel.order.v1']`（novelId 数组，try/catch 异常忽略回退默认；读写仅在事件回调——渲染期零副作用），**>8 本折叠**为前 8 + 磁贴前「展开全部（N）／折叠」行（过滤后同样按可见数判定；聚焦书落在折叠尾部时自动展开再滚动）；i18n zh/en 成对新增 sortDefault/sortManual/expandAll(n)/collapse/plusTitle 并清理失用键 openBtn/bindShort。冒烟 86 → **88 项**（新增：mock 树断言——经抽屉标题行真实入口路径置 store.consoleOpen 后渲染 nv-console 组件树，断言底部搜索行/＋磁贴居网格末尾/排序两态钮/「▶ 打开」类与文本缺席；源码面——拖拽排序持久化键 + draggable + minmax(300px,1fr) + 药丸样式）。
- **控制台视觉精修（UX-009，DEC-017 落实核查 + 1400×900 实机截图诊断 4 缺陷，纯样式零行为变更）**：①头部改**左对齐组**——📖 emoji 18px + 标题 16px/600 + 工作区路径 13px secondary 同行，未设置工作区由裸「—」改行动指引「未设置工作区 — 点击选择/新建」（i18n 仅**新增** wsUnset 键，既有占位文案不动）；「切换 / 新建工作区…」改 accent 边框次填充变体（`.nv-cbtn-ws`，12px/600，hover 提亮）**紧邻路径右侧**（头部 gap 8px）；**移除 headflex 弹性留白**——✕ 经 `margin-left:auto` 保持最右。②卡片网格 `repeat(auto-fill,minmax(320px,1fr))` + gap 16px（1120px 控制台宽下恰 3 列均分无空白）；**卡片与「＋」磁贴统一 `min-height:200px`**（高度差=0）：卡片 padding 16px、圆角 14px（玻璃拟态保留）、标题 15px/600、状态行 12.5px、数据卡 12px、meta 11.5px（行高 1.6）、22×22 图标钮经 `margin-top:auto` 钉卡内右下；磁贴 1.5px dashed accent、圆角 14px、中央 26px ＋、hover accent 提亮 + `scale(1.02)` 轻缩放。③底部搜索药丸**现代搜索引擎式**——高 48px、`width:min(640px,100%)` 居中、bg-base 底 + `0 2px 10px rgba(0,0,0,.12)` 投影、focus accent 边 + `0 0 0 3px rgba(79,142,247,.25)` 光环、🔍 16px、输入/占位 15px（容器行 padding 14px 24px 18px）。④排序 pill 11→12px、排序行与网格间距 10px。冒烟 88 → **89 项**（新增 UX-009 源码面断言：nv-cbtn-ws / min-height:200px / height:48px / headflex 负断言 / wsUnset 键；minmax 断言 300→320 同步）。
- **控制台批注 4 点（UX-010，用户实机批注，客户端样式 + 会话联动行为）**：①头部 ✕ 改**菜单级醒目钮**——`.nv-console-head .nv-mini` 专有变体：28×28（box-sizing border-box）、1px border-l2、radius 8、fill-l1 底、16px/600 secondary，hover accent 边+字+提亮（特异性 (0,2,0)/(0,3,0) 覆盖裸 `.nv-mini` 20px 基型），`margin-left:auto` 保持最右。②排序 pill 12→**13px**、padding **7px 14px**、两钮 gap 6→**8px**（选中态样式不变）。③卡片紧凑+读性提升——`.nv-ccard`/`.nv-cplus` min-height 200→**180px**（等高保持）、网格 gap 16→**20px**（minmax(320px,1fr) 不变，1120 宽 − 2×20 后 3 列仍 ≥320）、字号上调一档：标题 15→**16px**、状态行 12.5→**13px**、数据卡 12→**13px**、meta 11.5→**12px**（行高 1.6 与 22×22 图标钮保持）。④**会话联动**（参照 dsh-worktable「项目打开期间切会话→自动关项目」）：控制台打开期间用户点侧栏其他会话（`sessions.list` 快照 `current` 变化）→ 自动 `consoleOpen=false, consoleFocus=null` 退出工作台界面；实现 = NvConsole 内 `useSessions` 选 `s.current` 快照 + ref 基准对比 effect，三重守卫：首次快照只记基准不关（防挂载误关）、**prev 为 null 的变化不关**（新页 sessions 快照异步就位 null→X，实测曾致控制台打开 ~0.5s 即被误关）、`launcher.pluginOpened` 一次性豁免集（launcher.open 记录本插件自发切换——卡片打开/找到会话/新建/绑定链已各自显式关控制台或走挤法降级保持打开，不因 current 变化误关）；useSessions 为 null（服务缺席）无此联动（降级）。冒烟 89 → **91 项**（UX-009 令牌断言 200px→180px 同步；新增 UX-010 ①②③ CSS 断言与 ④ current 联动源码面断言 s.current/currentSeededRef/pluginOpened）；无头探针 8 项断言全绿（✕ 28×28/1px/16px/600/最右、pill 13px/7px 14px/gap 8、卡与磁贴 180 等高/gap 20/字号一档、**行为面**：控制台开 + 真鼠标点会话行「Setting Up Project Governance」→ 控制台自动关 + 会话切换，测后恢复原 current）。
- **两级工作台架构 + 小说删除（UX-011，用户截图批注）**：工作台拆为两级——**①小说管理工作台**（侧栏「📖 小说工作台」改名「📖 小说管理工作台」entryLabel，管理控制台头部标题同步；职责收敛 = 仅管理工作区的创建/切换 + 小说的创建（＋磁贴）/**删除（新增）**与卡片化呈现）与**②小说创作工作台**（既有分栏工作区，职责不变——管理小说本身的创作与内容/状态/数据；标题栏由「📖 书名」改为「📖 小说创作工作台 ·《书名》」（新 i18n 键 creationLabel/creationTitle，zh/en 成对；阶段徽标保留；novel-split 浮层 label 同步 creationLabel，旧键 title 移除）；WorkspaceDialog「选择 / 新建小说创作工作区」标题不动）。**删除链（新能力）**：卡片右下角操作行新增第三个 22×22 图标钮 **🗑 删除**（与 🔗/➤ 平级，stopPropagation，danger hover 变体 `.nv-cico-del:hover`）——点击弹 `window.confirm('确定删除《书名》（目录 id）？将删除整个书稿目录，不可恢复。')`，确认后 POST 新宿主接口 **`novel-delete`**（body `{novel}`；`deleteProject` 校验链 P-07：`sanitizeNovelId` 与 createProject 同规并从其抽出复用——非空 ≤60/无路径分隔符与非法字符/非 `.`/`..`/点开头/非保留设备名；`resolve()` 后必须 `startsWith` 工作区根目录（路径穿越防护）；目录不存在返回 `{ok:false,error:'not found'}`；命中才 `rmSync(recursive,force)` 删**单个**书目目录并 pushVersion，成功 `{ok:true,id}`；enabled=false 走既有 503 统一守卫）→ 成功后**读-合并-整表写**清除该书绑定键（沿用 bindSession 模式——settings patch 不支持键级 undefined 删除；失败仅提示不阻断）→ consoleFocus 指向该书时清空 → 书目列表经轮询自然刷新（≤2.5s）+ 提示「已删除《书名》」；失败复用 notice 行提示「删除失败：…」；**删除不依赖会话服务（降级态仍可用）；仅用户显式确认触发、confirm 不可用 fail-closed、无任何自动删除路径**；设置页业务配置指引文案同步两级命名。冒烟 91 → **99 项**（新增：deleteProject id 校验同规 7 拒绝/拒绝后书目无恙/not found 语义/删除闭环返回值/列表+磁盘双消失/API 注册与 sanitizeNovelId 收口/客户端删除链源码面/两级命名源码面含旧键负断言）；本地 HTTP 级 E2E（真实 http.Server + registerHttp 真链路）**17/17 全绿**（含跨源 Origin 403/无 Origin 403/攻击请求零副作用/enabled=false 503/删除只影响目标目录）；无头 CDP 探针实机 9/12 过（①抽屉+控制台标题=小说管理工作台 ②🗑 CSS 22×22+danger ⑤分栏标题「📖 小说创作工作台 ·《诡异副本，先过审》」+阶段徽标+Esc 关闭+current 恢复+用户书目零变化；③④宿主 novel-delete 路由待 DSH 重启后复跑——junction 安装宿主侧须重启生效，浏览器 E2E 删除闭环脚本已备 `%TEMP%\nv-probe-ux011.mjs`）。
- **新建小说弹窗与创建链调整（UX-012，用户批注 3 点 + 落点补充，纯客户端零宿主契约变更）**：①**新建表单改居中模态对话框**——替换原右侧 fixed 320px 悬浮面板（`.nv-cform` 族退役）：全屏遮罩 `rgba(0,0,0,.45)`（点击关闭，busy 创建中禁关）+ 居中卡片 `.nv-cmodal`（`width:min(520px,100%)`、maxHeight 85vh、radius 14px、`--dsw-alias-bg-elevated→base` 令牌、投影，几何复用 WorkspaceDialog 视觉模式）+ 标题「＋ 新建小说」15px/600 + 右上 ✕；Esc 关窗（busy 禁止，bind/工作区对话框仍让位）；卡片内点击 `stopPropagation` 不冒泡关窗（探针实测捕获：缺它点「创建」会随即触发遮罩 closeCreate）。②**表单仅目录名**——删书名字段与 bookTitle 状态；`novel-create` 只传 `{ name }`（查证宿主 `createProject` L349：title 缺省/空时**默认 title=name**，卡片名=目录名直到工作流定书名，宿主语义零改动）；占位 `fayi-xiantu` 风格保留 + autoFocus。③**按钮仅「创建」「取消」**——「创建并开始」「仅创建」合并为单「创建」= 仅创建；**runCreate 的 go 自动启动链整体删除**（sessions.create→agentPresets.select→bindSession→sessions.open→ensureSplit→sessions.prompt 不再执行；bindSession 调用移除——绑定归卡片 🔗/绑定面板）；创建成功即关弹窗 + 控制台 notice「已创建《名》——点击卡片进入小说创作工作台，再点『▶ 开始工作流』启动创作」+ `selected`/`consoleFocus` 就位新书（聚焦滚动 effect 补 `novels.length` 依赖——新卡片经下轮轮询进树后补滚动）。④**创作工作台标题栏补「▶ 开始工作流 / ▶ 继续工作流」accent 实底钮**（✕ 之前；新书=completedStages 空且 0 章→「开始」，旧书→「继续」，与 launchMsgOf 同判据；13px/600 accent 底白字）：降级（sessions 缺席）→ 提示不执行；未绑定 → 标题栏 info 提示 + 弹绑定面板（同卡片 🔗 路径）；会话失效（绑定 id 不在 sessions 快照）→ 提示重绑（stale 标记）；已绑定 → 复用 `launcher.promptLaunch` 发开始/继续指令；标题栏新增 `.nv-bar-note` 三态提示条（ok 绿/err 红/info 次级灰，ellipsis）；busy 防连点。i18n zh/en 成对：删 createGo/createOnly/bookPlaceholder/createDone，增 createBtn/createDoneHint/startWorkflow/continueWorkflow/bindFirstHint（launchMsgOf 局部参数 bookTitle→title 更名避免歧义）。冒烟 99 → **101 项**（新增 UX-012 源码面两断言：居中模态+仅目录名+两钮+自动启动链退役负断言（`\b(createGo|createOnly|bookPlaceholder|bookTitle|createDone)\b`）、分栏 ▶ 钮 accent 类+两态文案+三态提示+busy 防连点+promptLaunch 复用）；无头 CDP 探针实机 16/17 过（居中几何 520px/中心=视口正中±40、单输入、按钮恰 [创建,取消] 无「创建并开始」、空名报「目录名必填」保持开、创建→弹窗关+卡片出现且**卡片名=目录名**+notice 指引、旧书钮「▶ 继续工作流」accent rgb(79,142,247)/600/13px、新书钮「▶ 开始工作流」、点击未绑定书→绑定面板弹出+提示条而非报错、✕+Esc 关闭、current 恢复、无 JS 错误、原书目零变化；唯一 BLOCKED = probe 样本清理——运行中 DSH 宿主为旧包 novel-delete 405，样本 `probe-name-*` 留存待重启后清理，禁止直接 rm）。
- **创作工作台精修（UX-014，用户实机批注 8 点含 2 真 Bug：①左窗去工作区行+书目列表 ②标题显示书名 ③文件树目录默认折叠 ④⇄ 退出 ⑤分隔线拖拽退出 ⑥⇄/✕ 调大 ⑦抽屉直达创作台 ⑧点其他会话退出 ⑨首入截断诊断）**：①**左窗收敛**——`LeftNav` 移除 `WorkspaceRootEditor`（工作区根目录行）与`LeftNavBookRow` 书目列表（两组件整体退役），仅保留当前书目 `novel-project` 文件树 + 树头改「文件」（创作台关联具体小说，不再显示工作区其他小说；工作区根目录编辑归位管理控制台顶部「切换 / 新建工作区…」对话框，设置页业务配置指引、文件头注释同步）；②标题核验保持 `📖 小说创作工作台 ·《书名》`（creationTitle，实机断言标题=书名）；③**文件树目录默认全部折叠**——`collectDirPaths` 收集全目录路径在首次拿到书目文件树时置入折叠集（`collapseInitRef` 一次性，用户展开态不被轮询刷新重置；`LeftNav` 加 `key=selectedId` 切书重挂独立初始化）。④⑤**两个真 Bug 修复**（复现取证：CDP 实测 Chrome CSSOM 把长度写回按 **≤6 位有效数字**归一化——`'1066.6875px'` 写入读回 `'1066.69px'`；用户窗口下 colW 为分数 → `applyMargin` 写出的 `contentW+'px'` 与读回值不等 → 让位观察器按**字符串全等**比较把本引擎自己的写回误判为外部接管 → `close()`——`⇄`（setChatSide→applyMargin）与聊天/左侧分隔线拖拽（setChatW/左宽→applyMargin）都触发，故点⇄/拖边线即退出）。修复：让位比较改 `sameMargin` 数值容差（`parseFloat` 差 <0.5px 视为同一；外部接管为面板宽度级差异仍触发让位）+ **重锚定重挂让位观察器**（N1 备注：syncAnchor 换新 viewArea 后观察器仍看旧节点）。⑥**⇄/✕ 28×28 醒目化**——新 `.nv-bar-ctl` 变体（尺寸/1px border-l2/radius 8/16px/600/hover accent 与管理台头部 ✕ 同型；「«」折叠钮保持小号 .nv-mini）。⑦**侧栏抽屉书目卡片点击 = 直接进入创作工作台**（跳过管理控制台）——原 console `openBook`/`openBookCreate` 整体收口为模块级 `openCtl` 单一事实源（已绑定直接 open / 未绑定走开卡自动建会话链 / 失效引导重绑 / 降级提示），抽屉卡片与控制台卡片共用同一函数（不复制第三份逻辑）；抽屉新增 `nv-drawer-notice` 提示行（自动链结果/挤法失败反馈）。⑧**创作工作台会话联动**——打开期间用户点侧栏其他会话（`sessions.list` current 变化）→ `closeWorkbench` 关闭分栏（带章节脏稿守卫；守卫与 NvConsole ⑩ 同款：首次基准/null 刷基准；**独立豁免令牌 `novelSplit.pluginOpenTokens`**——实测共用 `launcher.pluginOpened` 时管理台效应先执行吞令牌，自动链开出的分栏被 ⑧ 效应立即关掉，令牌独立互不干扰）。⑨**首入截断修复**——取证：`applyMargin` 原写 `marginTop=TITLE_BAR_H(30px)` 把对话窗整体下推（实测 scrollBody ✓ `margin-top:30px`），视口锚定布局下底部/composer 被挤出台面；修复：**挤法不再写 marginTop**（本插件浮动标题栏只覆盖 `.nv-split` 自身区域，与对话窗横向无重叠，无需下推；参照 dsh-worktable 对话窗保持整高），聊天窗保持整高、底部/composer 不溢出（实测 viewArea 底=视口底、composer 完全可见）。冒烟 107 → **113 项**（UX-013 自动链断言随 openCtl 收口更新 + 新增 6 项：左窗仅文件树负断言/树默认折叠令牌、sameMargin 容差+重挂、marginTop 不写、nv-bar-ctl 28px 双钮、openCtl 单链负断言、split 联动独立令牌）；无头 CDP 探针 26/29（实际三项探针机制修正后全绿——见下）+ 复验探针 10/11（唯一未过=探针自身「新会话」行恢复——宿主会话列表 UI 行为，`新会话` 行在切走后消失，非产品缺陷；current 停在既有「开始小说创作工作流」会话，用户「进行中」会话未触碰）；截图 `%TEMP%\nv-ux014.png`（左窗仅文件树全折叠/标题=书名/28px 钮目检通过）。
- **创建链与抽屉调整（UX-013，用户实机批注 6 点，纯客户端零宿主契约变更）**：①**工作区对话框去会话创建**——删除「在此工作区创建小说会话」按钮与 confirmCreate 全链（session.create(workspaceId)→agentPresets.select→workspaceRoot 同步→sessions.prompt(开始指令)→launcher.open；随链清理 createdId 幂等状态、done 态视图与 i18n 键 doneTitle/doneMsg/doneClose/syncFailPrefix/syncFailSuffix/createSessionBtn，START_MSG 常量同步退役），对话框**仅支持切换与新建**：选中工作区某项即切换（`api.settings.update({ns:'novel-writing',patch:{workspaceRoot:w.path}})`，成功关窗，失败提示不关——服务端下次 overview 即按新根返回书目，控制台 ≤2.5s 轮询自动刷新），新建（pickDirectory/createDirectory/workspace.create/adopt）既有流程不动；选中项下方提示行改为信息性——新增 switchHint「切换后管理工作台显示该工作区书目。」、hasSessionsHint 措辞去「确认将新建一个小说会话」只留「该工作区已有 N 个会话。」（zh/en 成对）。②**首次点卡片自动创建会话并关联**——`openBook` 重构：有绑定 → 直接进原会话（原逻辑）；会话失效 → 引导重绑（不变）；**未绑定 → openBookCreate 自动链**（busy 防连点）：a) `workspace.list` 按 `path===overview.root` 命中 → `sessions.create({workspaceId})`，未命中/服务不可用回退 `sessions.create({cwd: 根目录/书目id})`（cwd 拼接抽 `joinNovelRoot` 与 BindDialog 既有链共用：`root.replace(/[\\/]+$/,'')+'/'+id`，id 来自宿主 listNovels 已 sanitize）；b) `agentPresets.select('novel-writing')`——**失败则告知「会话已创建，但…预设挂载失败」并不绑定不打开（可手动重试）**；c) `launcher.bindSession` 合并写绑定（失败仅提示不阻断）；d) `launcher.open`+`ensureSplit`（挤法失败降级同现状：控制台保持打开+提示）；成功 notice「已为《书名》创建并关联新会话。」（autoBindDone）；**自动链不含任何 prompt**——启动指令由用户点创作台标题栏「▶ 开始/继续工作流」发送（UX-012 口径）。③**后续点击进原会话**（已绑定书走②.2 现状）。④**卡片移除 ➤ 启动工作流按钮**——`ConsoleNovelCard` 删除 ➤ 图标钮与 NvConsole.launch/promptLaunch 的卡片消费点（onLaunch prop），i18n 键 continueBtn 同步退役；**promptLaunch 保留**（SplitWorkspace 标题栏 ▶ 钮仍用）；卡操作收敛为 🔗/🗑 两钮。⑤**侧栏抽屉对齐+调宽+层次（实机测量驱动）**——根因：抽屉是 footerActions flex 子项（slot 包装 display:contents），flex 项按内容宽度自缩（实测 207px vs 工作区列表行 256px）——`.nv-drawer` 补 `width:100%;box-sizing:border-box`（与工作区列表行同宽同位，左右缘均 12px，随侧栏动态不设固定宽；wide=false mini 折叠态不变）；标题行「📖 小说管理工作台」11→**13px/600**（letter-spacing .06em 保持）且 📖 拆独立 `.nv-drawer-title-ico` **16px** 节点（与控制台标题同型 split）；卡片 title 12→**14px/600**、sub 11→**12.5px**、内边距 8/10px、圆角 8→**10px**、卡间 gap **6px**（margin 6px 0 相邻塌缩）、状态单点 8→**9px**（随卡片 +1px）；层次：分隔线 `.nv-sep` l1→**l2** 加粗、标题行 hover 提亮保持、区块底部留白 8px（`.nv-drawer` margin-bottom）。⑥**BindDialog 既有能力核验**（批注②.4「会话管理按键支持创建新会话进行关联或者关联其他已经存在的会话」落地面）——核验结论：**两条路径均已可用、未补码**：a) 「新建会话并绑定」（bindNew 钮→`api.sessions.create({cwd: joinNovelRoot(meta.root, novelId)})`→bindSession→预设挂载→（bind 面板链带启动指令）→open+ensureSplit）；b) 按 `workspace.list` 分组列既有会话（`w.sessionIds` 过滤 sessions 镜像 → 组标题「其他（未入工作区）」兜底），行点击 `finishBind(sid,false)` = 仅关联并打开（不新建、不发指令）；本次仅将 cwd 构造换用共享 `joinNovelRoot`（行为不变）。冒烟 101 → **106 项**（新增 5 源码面断言：①工作区对话框负断言 createSessionBtn/doneTitle/doneMsg/doneClose/syncFailPrefix/syncFailSuffix/confirmCreate/START_MSG 消失 + switchHint/信息性 hasSessionsHint 在位 ②卡片两钮（onLaunch/continueBtn/`const launch = async` 负断言 + 🔗/🗑 保留 + promptLaunch 仍被创作台复用） ③首次开卡自动链源码面（openBookCreate/workspaceId 命中/cwd 回退/select/bind/open + 自动链无 prompt 负断言） ④抽屉字形（width:100%/13px/📖16px/14px/12.5px/9px 点/l2 分隔线/8px 留白） ⑤BindDialog 既有能力（bindNew+createAndBind+grouped 既有行））；**顺带修复（探针实测发现的既有缺陷）**：🗑 删除链的绑定键清理——`settings.update` 为 deep-merge patch（dsh-settings mergeLayers），「读-合并-整表写」无法表达键级删除，删书后绑定键残留（探针实测 binding 保留）；改为 `api.settings.mutate({ ns:'novel-writing', ops:[{op:'unset', path:['bindings', novelId]}] })` 主路径（旧宿主无 mutate 时回退旧读-合并-整表写），冒烟 106 → **107 项**（新增 mutate unset 源码面断言）；无头 CDP 探针实机 26/27 过（唯一非通过=beforeIds 采集于预清理前的历史 probe 残留比较，探针内的「零残留」权威断言全绿）：工作区对话框无「创建小说会话」且切换 tmp↔writing 经 overview.root 变化验证后恢复、probe 书首次开卡自动链 202ms 绑定+分栏「📖 小说创作工作台 ·《probe》」+控制台互斥关+新会话 ✓ novel-writing+卡片 ●●+工作区会话计数 +1、抽屉 13/16/14/12.5/9px+卡左缘差 0px+同宽 256px、🗑 清理书+绑定键、current 恢复、零 probe 残留、无 JS 错误；探针新建的测试会话（空白、仅挂预设）无法经 UI 删除（⋮ 菜单实机无入口）——3 个会话 id 已在报告列出供人工清理。


- **创作工作台细节（UX-015，用户实机批注 5 点）**：①**标题栏加高放大**——`.nv-bar` 高 30→**38px**、内边距 10→**14px**、标题 12→**14px/600**、阶段徽标 10→**13px**（line-height 18px）、栏内 «折叠钮 20→**24px**（`.nv-bar .nv-mini` 作用域）、⇄/✕ 28×28→**32×32**（`.nv-bar-ctl` 16→**18px/600**，边框/半径不变）、「▶ 开始/继续工作流」padding 4/12→**5/14px**（line-height 18px）；`TITLE_BAR_H=38` 同步（仅驱动聊天分隔线 top/height 自身样式，几何引擎零改动——UX-014 已去 marginTop 推下，聊天窗整高不变）。②**章节列表显示章节名**——查证：`meta.json` chapters 条目无 name 字段（仅 gate/words/updatedAt/reviewScore/published/forced 等），最小改动 = 宿主 `chapterList`/`readChapter` 响应**新增 `name`**（纯增量字段，既有 API 契约不变）：`meta.chapters[*].name` 增量字段优先，缺省解析章节文件首个 `# ` 标题行并剥「第N章」前缀（中文/阿数码，「# 第1章 关于你明天的死亡，规则如下」→「关于你明天的死亡，规则如下」；无标题行/剥后为空 → `''`）；名称缓存复用 wordsCache 键策略（文件+size+mtime，2s 轮询零额外 IO）；客户端中窗左列行渲染「**第N章 · 名称 · M字**」（名称独立 span ellipsis 截断；空名称回退仅「第N章 M字」，published ✓/门禁 ⚠ 标记沿用）。③**章节列默认略窄 + 手动拖宽**——默认 180→**160px**（`CHAPTER_W_DEFAULT`，范围 **120–360px**），右缘新增 `.nv-chdiv` 分隔线（pointer 拖拽，列左缘基准），宽度持久化进 `localStorage['dsh.novel.split.v1']` 新字段 **`chapterW`**（`loadSplitSaved`/`persistSplit` 与 leftW/chatW 同结构；无存档=默认 160px）；拖拽只改组件内列宽不动 viewArea 边距——让位观察器/几何引擎不受影响。④**侧栏抽屉卡片小一档**——卡片 title 14→**13px/600**、sub 12.5→**12px**、内边距 8/10→**6/8px**、卡间 gap 6→**4px**（margin 4px 0）、状态点 9→**8px**（`.nv-card .nv-dot` 作用域——标题栏状态点基型 9px 不动）、空态 `.nv-drawer .nv-empty` 与提示行随动；抽屉标题行「📖 小说管理工作台」保持 13px 不动；**管理控制台（nv-console）卡片尺寸不动**（层级感 = 抽屉小于控制台卡片）。⑤**聊天区确认**——分栏打开后 viewArea 整高、composer 完全可见（UX-014⑨ 修复回归确认）；i18n zh/en 成对新增 resizeChlist（「拖拽调整章节列宽」/「Drag to resize chapter column」）。冒烟 113 → **122 项**（新增 9 项：宿主 chapterNameOf 提取/列表补 name/readChapter 补 name/缓存失效/meta 优先 5 项 + 客户端标题栏①/章节名②/列宽持久化③/抽屉小档④ 4 项；UX-013 抽屉字形与 UX-014 ⇄✕ 断言随新值同步）；无头 CDP 探针实机 **24/26**（协议断言 24 项全绿：①标题栏 38px/14px/600/32×32·18px ⑤viewArea 整高+composer 可见+marginTop 不写 ③默认 160px→真拖 +40px=200px、chapterW 持久化（localStorage 200）、拖后分栏仍打开、关闭重开保持 200px ④抽屉卡 13/12px/8px 点/6·8px 留白；**2 项 BLOCKED = 运行中 DSH 宿主为旧包**——`/api/novel` 响应无 `name` 字段（junction 安装，重启即新码），探针 ② 因此取不到 name；客户端对缺 name 的**负回退「第N章 M字」已实机验证**（章节行正确显示）；截图 `%TEMP%\nv-ux015.png`，探针零写残留（localStorage 还原 + 会话还原），复跑脚本 `%TEMP%\nv-probe-ux015.mjs`）。
- **小窗口聊天截断修复（UX-016，用户实机「首次进入还是截断」；Coordinator 1078×593 复现取证：引擎钳制 `hi = colW − LEFT_MIN(160) − CENTER_MIN(420)` = 798−160−420 = 218 把聊天压到 240px 下限 → hero 标题折行 + 输入框裁切；1390px 宽窗口（colW=1120，hi=540）无此问题）**：**最小预留重分配**——左窗下限 `LEFT_MIN` 160→**128px**（拖拽下限随之 128，用户可拖宽）、中窗下限 `CENTER_MIN` 420→**320px**（内容页签 320 下可滚动/折叠正常，左窗树过窄允许拖宽，无 UI 变化）、对话窗最小 `CHAT_MIN` 240→**300px**；**无存档默认** `chatW = clamp(round(colW×0.45), 300, max(300, colW−448))`（旧 34% 公式在 1078 窗口给出 271 仍被钳到下限——0.45 对话优先；上限 colW−448 保证左 128+中 320 预留；colW<748 时上下限收敛为 300）；有存档沿用已存 chatW 但同样钳到 `[300, max(300, colW−448)]`——**老存档（如 240）超界时自动收敛**防继续压窄；`setChatW`/`applyMargin` 钳制公式经共享常量自动同步（无魔法数散落），`⇄` 换边/toggle 不受影响；宽窗口（1400×900，colW=1120）默认 = clamp(504, 300, 672) = **504px**（代码未定义 480 封顶——以码为准，中窗 616px ≥ 320 无劣化）。冒烟 122 → **123 项**（新增 UX-016 源码面断言：128/320/300 常量 + `CHAT_DEFAULT_RATIO = 0.45` 默认公式 + 存档钳制共享 + 0.34 负断言）；无头 CDP 探针实机 **24/25**（唯一未过 = 探针基线会话行选择器在 fresh tab 未命中——探针自身取样项，与产品无关；其余全绿：1078×593 实测 chatW=350=clamp(359,300,350)、hero 标题 h32=单行（「探索未至之境」一行）、hero 输入框 right=1053 ≤ 1078 完整、composer bottom=330 ≤ 593、marginTop 不写、拖 chat 分隔线 ±60px 不退出且加宽被 cap 钳制/收窄被 300 守住、⇄ 换边不退出、老存档 240→收敛 300、超界 900→收敛 350；1400×900 chatW=504、管理控制台网格/抽屉/标题栏（38px + 创作台标题）无回归；截图 `%TEMP%\nv-1078-fixed.png` / `%TEMP%\nv-1400-fixed.png`，探针零写残留（localStorage 还原 + 会话还原），复跑脚本 `%TEMP%\nv-probe-ux016.mjs`）。
- **聊天宽度再宽一档 + 可见版本徽标（UX-017，用户强刷后仍见窄聊天——旧包或 300 下 hero 输入行临界溢出未分辨，Coordinator 定案两动作）**：**① 宽度重分配再一档**——`CHAT_DEFAULT_RATIO` 0.45→**0.50**、`CHAT_MIN` 300→**320px**、`LEFT_MIN` 128→**120px**、`CENTER_MIN` 320→**300px**（钳制上限随共享常量 = colW−120−300 = **colW−420**；无存档默认 = `clamp(round(colW×0.50), 320, max(320, colW−420))`，colW<740 时上下限收敛 320；存档超界收敛同按新上下限；`setChatW`/`applyMargin`/渲染钳制沿用共享常量自动同步，`⇄` 换边/toggle 不受影响）。**② 可见版本徽标**——模块常量 `CLIENT_TAG = 'v4'`（注释约定：UX-017 工作台形态；每次 UI 结构变更递增），渲染于分栏标题栏 `.nv-bar-title` 末尾与管理控制台头部标题 `.nv-console-title` 右侧各一枚 `.nv-tag`（9px/14px 行高、padding 0 5px、radius 8、fill-l1 底、tertiary 字、letter-spacing .04em；样式并入单 `<style id="novel-writing-style">`），**纯展示无任何行为影响**——用户可据此判定浏览器实际加载的客户端包版本（已知观察：分栏标题栏在极窄内容窗下因标题文本 nowrap 截断，徽标可能视觉上被裁掉；控制台头部徽标不受影响，以它为准）。冒烟 123 → **124 项**（UX-016 断言随新常量更新并补旧值负断言；新增 UX-017 源码面断言：CLIENT_TAG=v4 + `.nv-tag` 双挂载点（恰 2 处）+ 样式五要素）；无头 CDP 探针实机 **23/23 全绿**（1078×593：chatW=**378** = clamp(round(798×0.5)=399, 320, 798−420=378)、hero 输入框 right=1053 ≤ 1078 / bottom=330.5 ≤ 593、composer 底部行 4 控件（命令/访问模式/选择模型/发送▲）矩形全部可见、hero 标题单行、marginTop 不写；存档收敛：旧下限存档 300→**320**、超界 900→**378**=colW−420；1400×900：chatW=**560** = clamp(560, 320, 700)、标题栏 38px + 创作台标题无回归、输入框完整；徽标：分栏标题栏 `.nv-tag` 文本=v4 + 控制台头部 `.nv-tag` 文本=v4；截图 `%TEMP%\nv-1078-v4.png` / `%TEMP%\nv-1400-v4.png`，探针零写残留（localStorage 还原 + 会话还原），复跑脚本 `%TEMP%\nv-probe-ux017.mjs`，完整探针 JSON `%TEMP%\nv-probe-ux017-result.json`）。
- **聊天窗渲染异常真修复（UX-018，用户实机截图「exp Max ▲」右缘碎片 + 空白；根因已闭合）**：novelSplit 挤压 viewArea 只写 marginLeft/marginRight——**margin 不改变元素 content-box 尺寸**，宿主页面响应式布局（hero「探索未至之境」居中定位）靠自身尺寸变化（ResizeObserver/宽度重算）触发重排；用户真实时序「页面先以全宽挂载、后加 margin」无尺寸事件 → 不重排 → hero 保持全宽坐标（输入框中心 ~667），挤压后仅露右缘碎片 =「渲染异常」（对照时序「先挤压后挂载」——无头复现/刷新重开——页面以窄宽挂载则正常）。修复 = **双压**：`applyMargin` 在 margin 之外显式 `width = clamp 后 chatW + 'px'`（chat 钳制沿用共享常量 CHAT_MIN=320 / LEFT_MIN=120 / CENTER_MIN=300，与 `setChatW`/渲染同源）→ border-box 真变小 → 宿主必然重排，**两种挂载时序统一**；视图区原有内联 width 以本值覆盖，原值记 **`savedWidth`**（open/syncAnchor 记录，close/换根恢复——原值可能为 `''`；close 与 syncAnchor 各一处恢复）；marginTop 仍不写（UX-014⑨ 整高保持）；无根/降级分支（geom===null）不写 width。**探针驱动的引擎健壮性修正**（同链取证）：`syncAnchor` 原「仅接受 phase='active'」的过渡态等待 +「根同即快路径」会在会话切换（卡片打开链 `sessions.open` 换到目标会话）后把引擎永久卡在旧锚点——目标视图以 `phase='hero'`（初始/空会话）稳定挂载、宿主重渲染替换 viewArea 子节点时 live viewArea 永不接受挤压（实测 marker 丢失 + 样式被擦）；修正 = 重锚定与 open() 同口径**有 ok() 根即锚**（findConversationRoot 已优先 active，极短过渡残留随下一次 MO 事件重锚自愈；无根才 close）+ 快路径以 **root/header/viewArea 三元同恒**为门槛（子节点被替换走完整重锚定，让位观察器重挂按 UX-014 N1 保持）。冒烟 124 → **128 项**（新增 4 项：双压注释+记录/恢复点计数、applyMargin 写 width=chatWpx+marginTop 负断言、无根不写/覆盖恢复边界、syncAnchor hero 相位接受+三元同恒门槛+旧守卫负断言）；无头 CDP 探针实机 **47/47 全绿**——两时序 rect 收敛对比：**A 先挂载后挤压**（冷开→点会话行→等 2s→开卡）viewArea rect=［700,1078］w=**378**（挤压前 798 全宽 ⇒ border-box 真变小）、内联 width=378px/margin-left=420px、hero 输入行［717,1053］scrollWidth==clientWidth==336 完整、hero 标题单行；**B 先挤压后挂载**（冷开→直接开卡）同一几何（A/B rect 逐项一致）；**E 存档 270→320**（rect=［758,1078］w=320、ml=478px、hero 行［775,1053］完整）；C 关闭后 width/margin 恢复空串 + rect 回全宽，往返两次均压/均还原；D 拖 chat 分隔线 ±35（343↔378）与 ⇄ 换边（左/右）均不退出（sameMargin 既有）、换边后 width 仍在；截图 `%TEMP%\nv-ux018-a.png` / `%TEMP%\nv-ux018-b.png`（两时序目检一致），探针零写残留（localStorage 还原 + 会话还原），复跑脚本 `%TEMP%\nv-probe-ux018.mjs`，完整探针 JSON `%TEMP%\nv-probe-ux018-result.json`）。
- **创作台批注修正（UX-020，用户实机两问「章节标题栏单独拖动」「宽度调整边框怎么删了」修正 UX-019③）+ 章节名校准**：①**章节列拖宽条恢复**——UX-019③ 把 `.nv-chdiv` 与 `chapterW` 持久化整体退役属需求误读，恢复 UX-015③ 原实现并与 UX-019 现状融合：`CHAPTER_W_MIN/MAX/DEFAULT` = 120/360/160 常量、`loadSplitSaved`/`persistSplit` 增 `chapterW` 字段、`novelSplit` 增 `chapterW` 状态 + `setChapterW`（clamp+persist+notify，只改组件内列宽不触 viewArea 边距——让位观察器/几何引擎不受影响）、ChapterPanel 列宽改 `snap.chapterW` 内联 + 右缘 `.nv-chdiv` 分隔条（role=separator + pointer 捕获拖拽：起拖基准列宽+位移，120–360px 钳制；i18n `resizeChlist` 键恢复 zh/en 成对）、SplitWorkspace 透传 `chapterW`/`onChapterW`；**样式 = UX-015③ 原始分隔条逐字节恢复**（4px 实底 border-l2 + radius 2px + hover accent——用户对首版恢复的再设计反馈「误删了，还乱修改」→ 统一还原 UX-015③ 原样，不做样式再设计；首版误做的 6px 抓握区+1px 线已撤），`.nv-chlist overflow:auto` 直排滚动保留（UX-015③ 原样）；章节行仍仅「第N章 · 名称」（UX-019② 保留）。②**章节名校准**——查证 novel-001 真实文件：ch-004/005/018 首行带 **UTF-8 BOM**（`EF BB BF`）→ 章节名解析 `# ` 标题行失败 → 空；ch-018 另为冒号变体 `# 第18章：老福宾馆` → 旧正则剥后残留「：老福宾馆」。修复 `chapterNameOf`：标题行匹配前剥 BOM；前缀正则扩为 `第[0-9０-９一二三四五六七八九十百千零万两〇]+[章回][：: 　]*(.*)`（中文/阿拉伯/全角数码 + 章/回 + 半角/全角冒号与全角空格）；**前缀不匹配或无标题行 → ''（禁止回退取正文非标题行）**；`meta.chapters[*].name` 增量优先不变。冒烟 128 → **129 项**（新增 UX-020 章节名校准 4 断言（中文数字/冒号变体/无标题→空/BOM 容错）+ 客户端拖宽条恢复源码面 1 项（含 4px 原始样式正断言）；UX-019③ 退役负断言更新为恢复正断言）；node 直测 real novel-001 38 章：004→「质检员 7 号」、005→「考勤，还是判决」、010→「第七号放映厅」、018→「老福宾馆」（全 38 章空名=0）。
- **创作台批注修正（UX-021，用户实机截图批注「章节标题栏单独拖动，不要把中间包含章节内容作为一个整体进行拖动」「章节标题信息显示已异常：丢失章节名、包含一个小圆点、所有的拖动图标，需要默认隐藏，拖动选项卡面的位置时才能显示」）**：①**行内小圆点移除**——UX-019② 章节行加的「·」装饰分隔符（「第N章 · 名称」）用户判为异常显示，移除后行 = 「第N章 名称」（span 4px gap），空名称回退「第N章」与 ✓/⚠ 标记不变；②**所有拖动图标（可拖分隔线）默认隐藏**——三条常驻 border-l2 实线（左窗 `.nv-vdiv` / 章节列 `.nv-chdiv` / 对话窗 `.nv-chatdiv`）改默认 **background:transparent**（4px 几何、col-resize 光标、拖拽行为均不变），悬停显 border-l2 定位、按下拖动（`:active`）显 accent——「拖动选项卡面的位置时才能显示」；③章节列拖宽仍**单独生效**（chapterDividerHandler 只写 chapterW=120–360px，不触内容列/viewArea 边距，即"不要把中间作为整体拖动"）；④**章节名显示异常根因取证（宿主侧）**：novel-001 38 章现场只读核对——001-012/018 共 13 章首行带 UTF-8 BOM（旧宿主正则不做 BOM 剥除 → 001-009/011/012/018 共 12 章空名——正是截图「丢失章节名」）；014 起共 24 章为「第N章：名称」全角冒号变体（旧正则剥后残留「：」前缀，截图即「：顺延」「：清单」形态）；UX-020 已修（ba38681 宿主代码：剥 BOM + 正则含[章回]/半全角冒号/全角空格变体/中文阿数 + 无标题不回退正文行）——**本机 DSH 进程重启后 38 章名全部正确**（空名 12→0、「：」前缀 24→0）。冒烟 129 → **130 项**（UX-019② sep 断言正→负；UX-020 断言更新为三态；新增 UX-021 三条分隔线默认透明+hover/active 两态+旧常驻线负断言）；node --check 通过；README/CHANGELOG 同步。
- **创作台术语定案 + 滚动隔离（UX-022，用户红框图两张定案：「分割条」=点击拖动改页面布局的三条分隔线→常驻可见；「拖动条」=拖动滚动窗口内容的滚动条→默认隐藏）**：①**三条「分割条」恢复常驻**——UX-021 ② 把 `.nv-vdiv`/`.nv-chdiv`/`.nv-chatdiv` 误透明化（我把「拖动图标」误读为分隔线，用户红框澄清这三条是分割条），恢复 UX-015/020 原样：border-l2 常驻实底 + hover accent（4px/radius 2px/拖拽行为不变）；②**两个「拖动条」（滚动条）默认隐藏**——章节列 `.nv-chlist` + 三个内容滚动容器 `.nv-scroll`（页签外层/正文列/左窗）加 webkit 滚动条样式：track/thumb **默认透明**，容器 hover 显 border-l2、thumb hover 显 accent——「拖动/悬停时才能显示」；③**滚动隔离根因+修复（无头探针 1078×593 取证）**：`.nv-chlist` 被渲染为 **1587px 高**（章节页签包装层为 `display:block` 无高度链，`height:100%` 失效）→ 章节列不自行滚动、由外层页签滚动容器**整体带动**（滚章节列 → 正文同滑——用户「上下滑动章节名会导致中间的章节内容往上滑」）；修复 = 章节页签包装层 `height:100% + overflow:hidden`、正文列/左窗/页签外层各自 `overflow:auto`（`.nv-scroll`）+ 全链 `overscroll-behavior:contain`（滚离隔断）；④**运行时验证**：CDP 真实滚轮实测——滚轮在章节列：`.nv-chlist` scrollTop 0→**618**、正文列容器 scrollTop=**0 不变**（隔离达成）；列表内部滚动（scrollH 1091 / clientH 473）；目检：章节列右缘 4px 常驻分隔线可见、滚动条默认不可见；冒烟 130 → **131 项**（UX-021 检查改写为 UX-022 三检查：分割线常驻〔透明负断言〕/滚动条隐藏与隔离〔nv-scroll×3 + 高度链 + overscroll〕）；**兼容性声明（用户约束）**：全部改动仅作用于插件自身元素（`.nv-*`/`.nv-scroll`），零宿主选择器依赖、零宿主源码改动——宿主升级不产生兼容问题。
- **UX-022 补充（UX-023，用户实测「两个滚动条只有一个可以隐藏，而且滚动当前内容页的时候不会自动出现」）**：根因=**正文阅读区内部还有独立滚动容器**——ChapterPanel 内 `renderContent` 容器（`maxHeight:560px + overflow:auto`，无类名）与编辑 textarea 未被 UX-022 的 `.nv-scroll` 覆盖（经典滚动条常驻 + 无悬停显示）；补丁=两者加 `className:'nv-scroll'`（与正文列/左窗/页签外层合计 5 个 nv-scroll 容器）——滚动条默认隐藏、容器 hover/thumb hover 显示；smoke 断言 count 3→5（首版误写 `.className:` 多一圆点致假失败，逐段插桩定位修正）；smoke 131/131；仍然零宿主依赖。
- **滚动条行为定案（UX-024，用户两次纠正「滚动时自动显示」「停止一段时间后自动隐藏，不应解除后一直出现」）**：撤除悬停即显规则（悬停常驻违背"停止后隐藏"）；改为**滚动事件驱动**——SplitWorkspace 挂 `document` 捕获 `scroll` 监听，滚动容器（`.nv-chlist`/`.nv-scroll`）滚动时加 `.nv-scl`（thumb 显现 border-l2），**1.5s 无滚动自动撤除**（初稿 600ms，用户实测后按需调长至 1.5s；拖动拇指期间 scroll 连续→保持；`:active` 高亮保留）；卸载清监听与计时器。CDP 实测：滚动前隐藏→滚动中显现→停止 1s 隐藏→再滚再显；smoke 131/131；零宿主依赖。
- **界面优化（UX-025，用户「分割线调细一些……太突兀了」「参考比较成熟的网页端界面设计，对两级工作台界面进行一次优化」）**：①**三分隔线 4px 实底 → 1px 细线**——`.nv-vdiv`/`.nv-chdiv`/`.nv-chatdiv` 改 `width:4px` 透明抓握区 + `border-right:1px solid border-l2` + hover `border-right-color:accent`（VS Code/Linear 式：细线克制、可拖性保留——4px 命中区不变），与宿主细分隔线风格协调；②**章节列浅底侧栏感**（`fill-l1` 0.02 浅底 + 章节行 hover 过渡 0.12s）；③页签 hover 浅底 + 背景/字色/border 过渡统一（控制台卡片已有玻璃拟态/hover/过渡属成熟形态，本轮不动）。全部宿主令牌/插件自身元素/零宿主依赖；探针 computed 实测 1px/bg transparent/4px/浅底 + 目检协调；smoke 131/131。
- **区域分割线提清晰（UX-026，用户「各个区域的分割线……你看我圈出来的这个，你能看清吗？」——标题栏底边 border-l1 #262b36 近不可见，上轮「其他分割线不明显」诉求被遗漏）**：工作台**区域分割线 border-l1 → border-l2 #3a4150**——标题栏底边 `.nv-bar`、左窗右缘 `.nv-left`、工作台外框 `.nv-split` 三处；与三条 1px 细拖线（border-l2 + hover accent）同层级，区域分隔一眼可辨；拖线保持 UX-025 细线不变；smoke 131/131。
- **聊天边界两根线去重（UX-027，用户放大截图「我放大了看，这里为何是两根线」）**：UX-026 提亮外框后，外框右缘 `.nv-split` border-right 与聊天窗拖线 `.nv-chatdiv` 相距仅 ~2px、放大成两根线——修复=外框 `border-right:none`，聊天边界唯一线=1px 拖线（hover accent 保持）；外框左/上/下边线提亮保留；smoke 131/131。
- **界面设计规范调研落地（UX-028，用户「是不是可以先调研好的 web 开源项目的页面设计再优化……你自己造轮子也造不好」——P-08 检视）**：调研两个源头——①VS Code `sash.css`（分割条范本：`--vscode-sash-size:4px` 透明命中区 + 1px 可见线 + hover accent）②参考项目 dsh-worktable（`DIVIDER=4` 分割条 + 1px 分隔线体系）→ **核验结论：现有四条 1px 线（4px 抓握区 + hover accent）即 VS Code sash 规范**；滚动条 overlay 行为（滚动中显/1.5s 隐）＝成熟 OS 惯例；层级靠浅底与字重。**修正**：一处边界一条线——树窗右缘线移除（由 vdiv 拖线承担，与聊天边界同原则），消除 树|章节列 双线；分割条用 border-l2 为 user-driven 偏差（参考用 border-l1 但用户屏幕近不可见，可见性优先，已记录）。smoke 131/131。
- **连接处直角化（UX-029，用户放大「这个线的连接处怎么这么奇怪」）**：病根=单容器共享边线布局下的 `border-radius:8px`——顶部横线圆角处截断 + 右缘无框线 + 拖线偏 2-3px = 断头连接（参考项目的圆角属独立卡片布局不适用）；修复=外框 `border-radius:0`（VS Code 工作区惯例）+ 聊天拖线定位 `-2→-3`（与右缘对齐、顶部与标题栏下边线直角相接）；computed 实测+目检通过；smoke 131/131。
- **边界线体系化（UX-030，用户再次放大指认连接线并批评「没有完整的页面设计逻辑」）**：**体系化修正——VS Code sash 语义**：①**可拖分区=纯透明命中区**（vdiv/chdiv/chatdiv：无边框、无 hover 线、4px、col-resize——可见边界线与面板边缘 0 偏移，消除 1-2px 台阶）；②**可见线=面板/列自身边缘线**（`.nv-split` 全框 1px border-l2〔右缘=全高边线〕/ `.nv-left` 右缘 / `.nv-chlist` 右缘）——标题栏横线/内容线与边缘线 T 型直角相接；chatdiv 命中区 `-2` 居中纯透明。探针实测（bar 右缘 757=内容缘、border-right 757-758）+4x 放大目检（全高边线/T 相接/无台阶）；smoke 131/131。
- **滚动条悬停即显取消（UX-031，用户「鼠标放到滚动条上为什么默认显示且高亮，我只说了滚动内容的时候显示——这样会导致分割线无法选中」）**：删除 `::-webkit-scrollbar-thumb:hover` 悬停即显+高亮规则（违规：仅允许滚动内容时显示；8px 命中区紧贴边界分割线、悬停高亮干扰拖区抓取）——thumb 仅在 `.nv-scl`（滚动事件驱动）显现、拖动拇指 `:active` 高亮；悬停（不滚动）滚动条完全隐形，边界拖区可直接抓取；smoke 131/131。
- **分割线命中区与可见线齐平（UX-032，用户「分割线无法选中，但是红框的位置才能点击调整页面宽度，这个是啥意思？」）**：根因=章节面板行 `gap:12px`——拖拽命中区（chdiv 4px）与可见分割线（chlist 右缘线）相隔 12px 空档：抓线命中不了，只有 12px 外不可见 4px 区可拖（不可预期）；修复=行去 gap（命中区与列右缘齐平——VS Code sash「命中区紧贴面板边缘」）+ 内容列 `paddingLeft:12px` 保留间距；探针实测 flush 齐平 + 抓线即命中；smoke 131/131。
- **拖线选中高亮（UX-033，用户「可以拖动调整布局的线不能在选中的时候高亮一下吗？一点都不明显」）**：三个拖区（vdiv/chdiv/chatdiv）保持纯透明命中区，追加 `:hover/:active` 规则以 `box-shadow:inset 1px 0 0 0 accent` 在边界绘制 **1px 高亮线**（VS Code sash hoverBorder 范本）——悬停/按住即高亮、松手自动隐回、默认干净无布局抖动；滚动条不受影响（仍仅滚动时显现）；smoke 131/131。
- **文件显示复用章节界面（UX-034，用户批注「将文件的显示调整成复用章节显示界面」）**：文件选中态提升到 SplitWorkspace（左窗树仅选择）；**文件内容改在中窗内容列显示**——与章节共用同一 nv-scroll 阅读区（整列滚动、无 300px 截断、无左窗嵌卡样式；头部=📄 文件路径 + ✕ 关闭回章节）；切书自动复位；探针实测（点文件→内容列文件头+左窗无卡）+smoke 132/132。
- **文件视图点章节跳转修复（UX-035，用户「呈现文件内容时点章节不会跳到具体章节显示」）**：根因=内容列 fileSel 优先渲染文件、点章节仅更新章节状态；修复=`select`/`startEdit` 动作内退出文件视图（onFileClose）——文件视图打开时点任何章节/编辑即回到对应章节；smoke 132/132。
- **工作流状态迁左窗（UX-036，用户「把工作流状态迁移到左边文件树下面，将文件树的这一列分割成两部分」）**：左窗改**纵向分栏**——上=文件树（flex:1），下=**工作流状态**（阶段/门禁清单/请求列表，46% 高度 + 内部滚动 + 顶边线分隔）；中窗页签**去「工作流」**（WorkflowPanel 仅左窗渲染）；「请求」页签（发起请求表单）保留；smoke 133/133。
- **左窗工作流面板修正（UX-037，用户「为何要二分？应该根据内容调整吧」+「删除的是阶段信息，下面的阶段图标已经承载阶段逻辑了啊，你乱删什么啊」——首版误删门禁卡已纠正）**：①删除**阶段信息卡**（阶段: quality_review + 书名/题材/平台 + 章/字数——冗余；阶段逻辑由清单 ✅🔄⬜ 图标承载）②门禁/清单卡**恢复保留** ③左窗下段**内容自适应**（auto + maxHeight 55%，取消固定 46% 二分）；smoke 133/133。
- **左窗占比调整（UX-038，用户「我要的是工作流部分占比大一些」）**：左窗分栏改为**工作流 60% / 文件树 40%**（均内部滚动）；smoke 133/133。
- **工作流面板内容流式化（UX-039，用户「下面空这么多，上面还要滚动，是什么设计？」）**：清单卡取消内部滚动（19 项阶段**全部展开**）+ 容器**随内容自适应**（auto + maxHeight 78%——内容多高占多高、超限才容器级滚动、树保底）；smoke 133/133。
- **阶段徽标+统计信息迁移（UX-040，用户「移到[工作流]并加上整体统计信息，例如当前字数信息等，也调整一下呈现的效果」）**：标题栏「quality_review ○」〔阶段徽标+绑定状态点〕**迁入左窗工作流面板**并升级呈现——**阶段+统计卡**：本地化阶段名（质量审查）+ 状态点 ○ + 《书名》+ `N 章 · M 字`（整体统计）；标题栏仅保留书名；阶段逻辑仍由 ✅🔄⬜ 清单承载；smoke 133/133。
- **中窗下半区数据/发布/请求工作台（UX-042，用户「将当前红框的区域调整为工作台，将上面的数据/发布/请求页签都迁移过来」）**：中窗改**上下两个工作台**——上部=章节工作台**常驻**（章节区不再页签）；下部=**数据/发布/请求工作台**（红框区：46% 高 + 分隔线 + 页签 数据/发布/请求 迁入 + 内容滚动）；smoke 133/133。
- **底部工作台高度修正（UX-044，用户「我是刷太高了——是说太高了，你把章节名列表放出来干啥」——UX-043 方向误判修正）**：底部工作台全宽、高度 **46%→30%**（太高）；章节列表回上部区（不占全高）；smoke 133/133。
- **分隔线两端相接（UX-045/046，用户「分割线又是一样的问题」「右边连起来了，左边没有」）**：①列 padding 归零——底部工作台分隔线横贯全宽（右端接面板边线，消除 10px 悬空）；②**vdiv 改绝对定位悬浮命中区**（不占流）——中窗列紧贴左窗右缘，分隔线左端与左窗边线 **T 型相接**（两端皆连；vdiv 仍可拖/可高亮）；smoke 133/133。
- **中窗上下工作台分隔线可拖（UX-047，用户「把这个分割线也修改成可调节的……让用户有可操作的空间吧」）**：新增 `.nv-middiv` 水平分隔线（同拖线风格：4px 命中区 + hover/拖动 accent 高亮）——**下半区高度拖拽可调（120px–60% 窗口）**并**持久化**（midH 字段）；默认 30%；smoke 133/133。
- **当前布局固化为默认（UX-048，用户「把当前的布局作为默认布局吧」）**：标题栏新增 **☆ 将当前布局设为默认**——写入默认布局槽 `dsh.novel.defaults.v1`（左窗/章节列/下半区/聊天全字段）；之后无存档时回退该默认槽（否则内置）；并修复 open() 未回读 midH（持久化高度重开生效）；smoke 133/133。
- **恢复默认布局按钮（UX-049，用户「有恢复默认布局的按键吗」）**：标题栏新增 **⟳ 恢复默认布局**——`resetSplit()` 清除当前布局记忆后立即回到默认（☆ 固化槽 → 内置兜底），提示条确认；不影响 ☆ 固化的默认槽；smoke 133/133。
- **数据页字体/布局协调（UX-050，用户「调整数据页面的字体大小和布局，当前很不协调」）**：统一 **12px 基准**——表格 11→12px（行高 1.6/padding 4 8/nowrap+横向滚动兜底、表头 600）、信号行 12px/1.7；表单改**标签上置 + 输入条满宽**（对齐清朗）；页脚说明弱化 note（11.5px 次级灰）；smoke 133/133。
- **工作台视觉设计重构（UX-053，DEC-019 方向 B+A 点缀；用户「已初步优化但简陋缺设计感，保持当前基础布局，视觉呈现优化」——开源模式调研落地，P-08）**：管理台+创作台**统一设计语言**（shadcn 干净分层 + 卡片保留一层 5% 轻渐变作唯一装饰）——**①图标体系（最大杠杆）**：集中式 `NV_ICONS` 映射（21 个 Lucide 图标 path 数据，ISC 许可 `lucide-static@1.37.0` 实抓内联，文件头保留来源声明）+ `nvIcon(name,size)` 渲染助手（24×24 stroke=2 currentColor，色随外层 color 令牌——禁止 filter 改色）；全部元素级 emoji/文本图标换 Lucide 内联（📖→book-open 控制台/抽屉/卡片/标题、🔍→search、🔗→link、🗑→trash-2、⇄→arrow-left-right、⟳→rotate-ccw、☆→star、✕→x 各关闭钮、▾/▸→chevron-down/chevron-right（目录箭头展开 rotate 90° 过渡）、📄→file-text、✅🔄⬜→circle-check/refresh-cw/circle（清单 12px，done 绿/current accent/待办 tertiary）、▶→play、页签 database/cloud-upload/inbox 12px、⚠→triangle-alert（角标/章节标记/审计）、✓→check（success 色）；尺寸三档 12/14/16px；**i18n 红线（UX-052 教训）**：t() 字符串零改动——字符串内前缀 emoji（creationTitle/bindTitle/startWorkflow）按 UX-013 entryLabel 拆分渲染模式拆出（图标节点+文字节点），纯提示语 emoji 保留；**②焦点双环统一**（Vercel：`0 0 0 2px bg-base + 0 0 0 4px accent@60%`——卡片/输入/树行/章节行，搜索框 45% 软焦点变体；树行/章节行 `:focus-visible` 补位；替换旧 3px 光圈/16px 光晕/仅变边三种分散样式）；**③过渡统一**（150ms `cubic-bezier(.4,0,.2,1)`，只转 border/背景/色/box-shadow（+cplus transform），不转 padding/margin/width/height）；**④光效收敛**（`.nv-csweep` 扫光动画及 JS 渲染点删除；整卡彩色边框+22px 辉光 → 中性 border-l2 + 顶部 2px 状态条（`inset 0 2px` 状态色 70%）+ 既有状态点保持；卡片渐变峰值 .07→.05、阴影 `0 1px 6px .08`→`0 1px 3px .15`；nv-dot 微光 6px→3px 25%）；**⑤硬编码 alpha 令牌化**（5 处 `rgba(79,142,247,.x)` → `color-mix(in srgb,var(--dsw-alias-state-accent-primary,#4f8ef7) N%,transparent)` + **先 rgba 后 color-mix 双重声明兜底**（Chromium<111 回退）；hover `filter:brightness` → `color-mix(88%,black)` 底色）；**⑥字重三档 400/500/600**（700→600 树头/清单当前行）；**⑦选中态体系**（文件树行 `.nv-ft-row`/章节行 `.nv-chrow` 类化 + `[data-sel]`：选中 accent 12%/10% 底（rgba 兜底）+ 章节行 `inset 2px 0` 左边条；**行高/padding/margin/缩进几何保持原值不动**）；**⑧空态升级**（`.nv-empty` 中央 24px 淡图标——按上下文 folder-open/inbox/search + 原文案不变）；**⑨弹窗双层阴影**（`.nv-modal`/`.nv-cmodal` shadow-lv2 + `0 0 0 1px rgba(0,0,0,.25)`）；**⑩排序 pill 选中态**（accent 8% 底 color-mix+兜底）+ 徽标 pill 化（radius 8→999 + 字重 500）；**布局零改动**（38px 标题栏/48px 搜索/180px 卡高/minmax(320px,1fr)/32×32·28×28·22×22 钮/分隔条与滚动条行为全部保持——红线）；零 npm 依赖、零运行时网络加载、零宿主选择器依赖；smoke 133 → **137 项**（新增 4 项：图标体系+emoji 负断言/焦点双环/光效收敛/令牌化与选中态；UX-011/019/030 既有断言随图标化与过渡统一同步）。

- **工作台视觉 V2——真实截图差距分析驱动·主题无关自适应（UX-055，DEC-021 修订版 + DEC-022 定向授权；用户 V7 目检「变化不大」）**：V7 真实截图差距分析（无头探针截图 + 视觉子代理对比 Linear/Vercel/shadcn 参照 + 像素采样 + 代码核验）核心定案——**字阶无对比**（全 UI 无 >16px 元素，极差 1.25 倍 vs 参照 5.8 倍）/ 书卡纯文字堆叠无构成 / 稀疏时 ~70% 空白零处理 / ✓✗ 文本状态标记无色彩语义 / 正文排版无节奏。六项落地：**①字阶悬崖**——`TYPO` 单点常量（页面标题 16→20px/600 `letter-spacing -.015em`、书卡标题 16→17px/600、统计数字 18px/600、正文 15px/1.75、meta 12→11px muted、空态 28px/600）+ 全数字 `tabular-nums`（章/字/日期/百分比/章节号）；**②书卡构成重构**——Linear 式构成：monogram 首字色块（40×40 radius 10，`color-mix(accent 14%, transparent)` 派生底 + 18px/600 accent 首字，替代 📖 图标位）+ 标题区与数据区 1px 分隔线（`label-primary 8%` 派生）+ **数据 chips**（「38 章」「89232 字」「信号 N」独立 chip：`padding 2px 8px/radius 6px/500 12px`，`label-primary 6%/9%` 派生底边）+ 底部状态带（绑定双圆 + 最近更新 11px）；**③空态英雄区 + 稀疏居中**——0 本书主区居中英雄（48px book-open `accent 20%` 色 + `drop-shadow(0 0 24px accent 15%)` 光晕 + 28px「从第一本书开始」+ accent 实底「新建小说」主按钮 = 现有创建链；**隐藏网格与＋磁贴**）；1–2 本书网格内容**真居中**（DEC-022 白名单——该分支网格容器内联条件模板 `repeat(auto-fit,minmax(320px,420px))` + `justify-content:center`，轨道组不再恒满宽；≥3 本恢复标准模板 `repeat(auto-fill,minmax(320px,1fr))`；**R1 修复**：首版仅条件 `justify-content:center` 对 1fr 轨道为 no-op——无视觉交付，R1 P1-1 裁定后按 Reviewer 建议的条件模板方案修复；英雄区另修 P1-2 门控 `poll.data !== null && novels.length === 0`——加载中/轮询瞬时错误不闪现误导性空态）；＋磁贴精修（`label-primary 15%` 派生虚线 + 20px muted ＋图标 + hover `label-primary 4%` 底）；**④状态文案组件化**——`.nv-chip` 组件 + `data-tone`（ok/warn/danger/muted；状态色 tint：12% 底/25% 边/状态字，先 rgba 兜底后 color-mix）；卡片 meta 行 `发布✓/✗·变现✓/✗·信号N` → 「已发布」ok chip/「未发布」danger chip/「已开通变现」ok chip/「未开通变现」muted chip/「信号 N」；门禁卡同款 chip；发布记录行 ✓/✗ → 行尾 6px 状态点（success/danger）；`noData` 占位文案 → 「尚无数据信号」（zh）/「No signals yet」（en）（DEC-022 授权）；**除状态表达外零文案改动**（UX-052 红线）；**⑤正文阅读排版**——15px/1.75 + 段距 .75em、章节标题（阅读区顶部/正文 # 标题）16px/600 + 下 1px `color-mix` 分隔线、编辑态 textarea 同步 15px/1.75；**⑥细节统一**——抽屉书卡标题 13.5px/600、工作流清单当前阶段行 `accent 8%` 底 + 左缘 2px accent 竖条（收敛整行 600）、排序 pill 选中 + `font-weight:600`、数据表格/信号行 tabular-nums；**硬约束（DEC-021 修订版）**——全部视觉值令牌派生主题无关（亮/暗仅采样点，自定义主题随动）；表面 tint 一律 `color-mix(令牌)` 派生；**存量白 alpha 硬编码清零**（卡片渐变 `linear-gradient(135deg,rgba(255,255,255,.05)…)` → `color-mix(label-primary 4%/7%, transparent)` 渐变 + 纯 fill 兜底双声明——负断言落地）；几何红线保持（除稀疏居中白名单）；图标零新依赖（复用 NV_ICONS 21 个）；smoke 137→144（新增 ux055 六检查块；ux008/010/013/015/053 既有断言随规格演进更新）；`node --check`/`validate-preset 29/29`/`smoke 144/144` 全绿。

- **绑定会话弹窗三修（UX-057，用户截图红字反馈）**：①**「新建会话并绑定」主按钮置顶**——accent 实底按钮从会话列表尾部移到 `bindPick` 副标题之后、列表之前（弹窗内容首行——用户视线第一落点；busy/meta.loading 禁用逻辑不变）；②**只显示当前工作区的会话**——`meta.root`（overview 返回的小说工作区根）与 `workspace.list` 项 `path` 经 `normPath` 归一化比较（`/`/`\` 统一 + 大小写不敏感 + 去尾分隔符）后相等者即当前工作区，列表仅渲染该工作区 sessions（`sessionIds ∩ entries`），其余工作区的会话**完全不渲染**（用户实锤 tmp/dsh-reasoning-level 组泄露）；**回退保护**——归一化无命中（overview 失败 `root=''`、或工作区列表形态异常）保持既有全分组行为（含「其他（未入工作区）」组），不让用户无会话可选（源码注释留痕）；③**会话分组默认折叠**——展开例外集 `useState`（挂载于条件早退之前的 hook 区；open 重置 effect 一并清空为全折叠；展开键用 `normPath` 归一化路径唯一键——R1 P2-2：回退全分组下同名 basename 工作区不连带开合，`bindOther` 组用 `@others` 哨兵），分组头改**原生 button** 整行可点击切换（R1 P2-1 键盘 Tab/Enter 可达——对齐 FileTree row 惯例；`.nv-group nv-bgroup` 基底 + UA 默认样式长手法重置保留 fill-l1 底/accent 左条/12px + cursor:pointer + hover 提亮 `label-primary 4%` color-mix 派生 + 150ms 过渡；chevron-right 12px 展开 rotate 90° 过渡——复用 NV_ICONS 既有图标，与文件树 `.nv-ft-glyph` 同款交互语言）+ 折叠头右侧会话数「N 个会话 / N sessions」（i18n zh/en 成对新增 `bindSessionsCount`；**UX-052 文案红线：除该新增键外零文案改动**）；零宿主依赖/零 npm/颜色令牌派生（DEC-021）；本任务只动 BindDialog 弹窗内部与配套样式/i18n/smoke（布局零改动红线例外范围声明成立）。冒烟 145 → **146 项**（新增 UX-057 源码面断言：按钮置顶 DOM 序〔bindNew 索引位于 bindPick 之后 loading 行之前〕+ 旧尾部按钮行负断言 + normPath/回退特征串 + 折叠 useState/重置/chevron/会话数 i18n 与 `.nv-bgroup` 三件套样式）；CDP 无头探针实机验证（bindNew 在会话列表前 DOM 序 / 分组仅当前工作区无 tmp/dsh-reasoning-level 泄露 / 默认折叠→点组头展开）。

## [0.3.0] - 2026-08-29

### 客户端
- **新会话初始窗口工作台入口（UX-001）**：blank/hero 状态下 `conversation.view`（小说工作台标签）与 `composer.dock`（原预设兜底条）均不渲染，初始窗口无任何工作台入口——兜底条迁移至 `conversation.input.dock`（hero/active 均渲染，standardProps 含 inputActions），并新增「📖 小说工作台」入口行：书目列表 + ▶ 开始/继续工作流（`completedStages`/`totalChapters` 判定）+ 一键新建并开工；启动走按需挂载预设（blank 会话原位 select novel-writing 后注入），预设作为工作台附属组件对用户零感知。
- **工作台入口收敛为侧栏 + 浮层面板（UX-002）**：整行 input.dock 横幅（用户反馈喧宾夺主）移除——常驻入口改为侧栏底部「📖 小说」+ 浮层面板（书目选择/启动·继续/新建即启动，经 pendingLaunch 通道注入，输入框上方仅瞬时出现「启动指令待发送」小条）；`conversation.input.dock` 仅保留 pendingLaunch 兜底条；StudioView 主按钮防连点补全（`disabled: launching || creating`）。
- **浮层面板首次进入支持设置 workspace（UX-003）**：快速面板新增「工作区」根目录行（复用 WorkspaceRootEditor / `api.settings.update`），空态提示更新为面板内指引；WorkspaceRootEditor 补齐 API 不可用守卫（面板与工作台侧栏两消费点同受益）；陈旧 `noNovelsHint` 文案（原指向设置页）更新。
- **统一入口：选择/新建工作区 → 自动建会话（UX-005）**：侧栏「📖 小说工作台」→ 对话框（`workspace.list` 单选 + 新建：`host.pickDirectory` 选择文件夹 / `host.createDirectory` 新建子目录 + `workspace.create`）→ 确认后 `session.create(workspaceId)` → `agentPresets.select('novel-writing')` → `settings.update(workspaceRoot=工作区目录)` → `session.prompt('开始小说创作工作流')` 自动启动；成功后明确提醒进入「小说工作台」标签（宿主无视图切换 API）；失败重试**幂等**（缓存本次链 createdId，不再二次建会话）；浮层面板退位为纯状态 HUD（移除书目选择/启动/新建/工作区行）；`workspaceRoot` 与所选工作区目录一致，业务配置收敛于工作台。

### 修复
- **install.ps1 补 UTF-8 BOM（BUG-001）**：脚本原为 UTF-8 无 BOM，Windows PowerShell 5.1 对无 BOM 的 .ps1 按 ANSI(GBK) 解码，中文注释/字符串乱码触发 ParserError（The string is missing the terminator），离线通道 `.\install.ps1 -LocalPath .` 完全不可用；现于文件头补 3 字节 BOM（EF BB BF，其余字节逐字节不变），PS 5.1 解析通过、离线安装恢复；在线通道（irm + iex）头部命令已含 `.TrimStart([char]0xFEFF)`，不受 BOM 影响。
- **lib/tools.js 补 inject 声明修复预设挂载失败（BUG-002）**：UX-005 实机验证——统一入口建会话成功但「小说写作工作流」预设挂载失败，宿主报 `agent-presets: preset "novel-writing" failed to mount: failed to apply loader entry tool-novel (dsh-novel-writing/tools): cannot get property "tools" without inject`；根因是工具行模块在 apply 内 11 处 `ctx.tools.register(...)` 但未声明 inject（Cordis 强制：ctx 服务属性访问必须先在插件声明 inject）。现补 `export const inject = ['tools']`（tools 硬依赖；novel-writing 服务保持可选经 ctx.get 探测、不进 inject——未装 bundle 时本行静默激活注册 0 个工具，预设仍可挂载），并清除挂载路径上第二个潜伏阻断：`novel_state_update` 的 `latestAiPath` 参数 enum 含 null，真 dsh-tools 的 defineTool 校验拒绝（string enum 必须全字符串值），修为 `['A','B','C']`——此前被 inject 错误掩盖、从未在挂载中执行到；smoke 新增 tool 行挂载契约断言（inject 声明含 tools 且不含 novel-writing / apply 挂载注册恰好 11 个工具且名字集合精确匹配 / 可选服务缺席时静默 0 注册三路径；mock ctx 以 getter 复刻 Cordis inject 门控，本地真包与 CI 恒等 mock 两条路径均通过），堵住 CI 四道检查（语法/依赖架构/静态校验/smoke）均不经过 apply 挂载路径的防护网缺口（冒烟 60 → 67 项）。

## [0.2.2] - 2026-08-22

> 补录（2026-08-22，REL-001）：tag `v0.2.2` 发布时本段缺失，现从 commit `ef5c073` 提炼补写。

### 配置边界重构
- **业务配置归一工作台**：工作区根目录 → 工作台侧栏顶部（✎ 就地编辑——它决定书目列表本身）；平台发布模式/命令/fetchCommand → 发布页签「⚙ 平台发布配置」折叠区（使用现场就地编辑；overview 下发平台配置）。
- **设置页只留插件级开关**：启用开关 / 轮询（下限 500ms）/ 预设自动同步 / LAN 暴露（含风险警示）；页首指引业务配置位置。
- 数据页抓取指引同步改指发布页签；README/SKILL 同步配置边界说明。

## [0.2.1] - 2026-05

### 修复与加固（审计 P1 收尾）
- **宿主**：metrics 按 date 去重（同日后到覆盖）+ 滚动保留 730 条；`ensurePreset` 原子换入（staging→rename，升级中途崩溃不留半套预设）；`updateState` 增加 `allowCreate:false` 防幽灵书目（`novel_state_update` 已启用）；`sceneMatched` 阈值收紧（`max(2, 40%)`，单关键词需 ≥2 滑窗，降假阳性）；`novel_status` 可空字段按 schema 裁剪；overview 透出 `platforms` 键。
- **客户端**：设置页失败态 + 重试（不再永久「加载中」）；发布平台下拉读配置键（settings 自定义平台可见）；机器审计（字数/场景/段落/双钩子/AI 痕迹）在章节阅读页折叠展示。
- **测试**：冒烟 60 项（新增 metrics 去重、fetchCommand 适配器真实子进程、幽灵书目拒绝）。

## [0.2.0] - 2026-05

### 安全（双代理审计 P0）
- HTTP 章号收口校验（杜绝 `../` 路径穿越任意 `.md` 写入与门禁绕过）；回环判定改 `socket.remoteAddress`；POST 同源校验（Origin/Referer + JSON content-type）拒绝跨站 CSRF（隔离实测 text/plain → 403）；请求体 4MB 上限；platform 清洗；`runCommand` 异步执行（超时 kill，修 spawnSync 冻结宿主与 POSIX shell 缺失）。

### 数据正确性
- chapterList meta 键补零（已发布/门禁/审查标记恢复可见）；`total_words` 按 meta 汇总（不再恒 0）；`total_chapters`=章节文件数；第 1000+ 章进入列表；meta.json 损坏隔离改名；全部落盘原子化（tmp 随机后缀 + rename）。

### 门禁语义
- 否定句式三分类：排除型（计 drift）/ 条件型（`不得无铺垫引入` → conditional 人工复核，不计 drift）/ 需求型（requirements）；场景术语豁免（同卡必写场景要求的术语不可能是禁词）；场景解析兼容全角/无加粗漂移，格式无法解析时 **fail-closed**；`computeGate` 干跑不落盘；review 零容忍服务层强制（pass⟺100 分且无硬门禁失败）；`release_allowed` 校验下沉服务层（工具/HTTP 同一收口）；manual 模式不再标记 published。

### 客户端
- 数据录入表单 `[object Object]` 渲染修复；事件回调 hook 误用修复（新开专用会话分支恢复可用）；pendingLaunch 改 composer.dock 兜底条 + TTL + 双通道；章节轮询竞态修复（selected 入 deps、错序响应丢弃、出错清数据）；人机并发覆盖确认；脏稿三重保护（切章/切书/切页签）；HUD 关闭停轮询；pollMs 下限 500ms；文件树折叠键全路径；请求/数据面板防重复提交与数值校验；阶段清单补三审/AI 合规/可选阶段。

## [0.1.3] - 2026-05

- 预设改为**按需加载**：撤销「默认预设」设置；启动按钮三段式（已在小说预设→注入；blank 会话→原位切换；已开始的普通会话→自动新开专用会话 + 兜底发送）。

## [0.1.2] - 2026-05

- 启动按钮预设三段式与预设状态徽标（后于 0.1.3 调整为按需语义）。

## [0.1.1] - 2026-05

- 工作台四项管理能力：多书目侧栏、workspace 文件树（折叠+就地预览）、一键新建小说、一键启动/继续工作流（`inputActions` 注入）；安全文件读取（路径逃逸/私有目录/大小上限）。

## [0.1.0] - 2026-05

- 首个可用版本：宿主 `novel-writing` 服务（项目管理/看护门禁/机器审计/发布适配/数据信号/请求队列）+ 回环 HTTP API + 预设自动同步；agent 预设（Coordinator persona + 29 SKILL + 16 Agent 参考 + `novel_*` 工具行）；浏览器工作台（五页签）+ HUD + 设置页；多平台发布三级降级 + fetchCommand；依赖架构（peer-only 防双闭包）与验证管线（CI 守卫/预设校验/宿主冒烟/隔离 boot）。
