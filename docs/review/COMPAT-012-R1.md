# COMPAT-012 代码审查报告（R1 · N1/N2 合并前收口）

> **Round**：**R1**（COMPAT-012 独立任务首轮；性质 = **定向复审**——对 `REVIEW-COMPAT-004-R2` §6 两条 P3（N1/N2）的收口实效核验）
> **前轮引用**：`docs/review/COMPAT-004-R2.md`（APPROVED_WITH_NOTES，`unresolved_blockers=0`）——其 **§6 N1（P3）**「F5 强口径正则漏 `this.ctx.<标识符>`」、**§6 N2（P3）**「契约 2.12/2.13 `line` 范围偏移」及 **§8 门禁建议**（合并前可选执行 N1 一行修正 + N2 两处 line 精确化 + smoke F5 增范围抽核）= 本 commit 的动因与判据
> **审查对象**：commit `a0e515e`（"COMPAT-012: R2 复审 P3 合并前收口"，**3 路径 +93/−13**：`test/smoke.mjs` +86/−11、`lib/host-contract.mjs` +2/−2、`CHANGELOG.md` +5/−0；**产品代码 diff = 0**）
> **基线一致性（防审错对象）**：审查时 `HEAD = ea85799`（仅 `.governance/evidence-log.md` + `.governance/plan-tracker.md`）；`git diff a0e515e..HEAD` **仅 `.governance` 两文件**；工作区 `git status --porcelain` 为空 ⇒ 本报告读取的文件状态 ≡ `a0e515e` 状态
> **审查人**：Code Reviewer Agent（只读；未修改仓库任何文件，唯一写入 = 本报告）
> **审查方式**：前轮 N1/N2 逐条比对 + **修复实质核验**（正则语义构造分析 / `lib/client.js` 行段只读实读 / **旧值红态静态复现** / golden 独立复算 / 覆盖力与盲区构造分析 ≥2 项）。**smoke 未由本人复跑**（角色工具约束 + 预算，见 §7.1）
> **日期**：2026-09-12

---

## 1. 终态结论

**APPROVED_WITH_NOTES** — `unresolved_blockers = 0`。

**N1 与 N2 均为实质修复，非改标签**：

- **N1**：正则改为 `/(?<![\w$])ctx\.[A-Za-z_$]/g`（`smoke.mjs:2108`）——正是前轮 N1 的建议式；`this.ctx.get(1)` 由 **0 命中 → 恰 1 命中**，词内/同类标识符三形态（`myctx.`/`_ctx.`/`$ctx.`）仍 0 命中；且新增 **①a 双向实证**把「R1 口径漏检」固化为反例（防口径回退）——该断言具备**真实抗回退力**（见 §3.1 A2）。
- **N2**：2.12 `L4403-4408`、2.13 `L4429-4461` 与 `lib/client.js` **实读逐行一致**（2.12 恰为 `ctx.on('internal/service')` 调用节点闭区间、L4409 = `: null`；2.13 全文件 `ctx.slots.inject(` **恰 6 处**且全部落于区间内）——前轮所记「真实构造行」被独立确认，修复值 = 真实值。
- **防复发（F5d）**：新增对**全量 21 项**范围型 `line` 的四判据抽核（前轮 N2 建议的落地），且**旧值红态可由本人独立静态复现**（与演练红报文逐字一致，§3.1 B7）⇒ 该检查对 N2 两类缺陷确有分辨力。

**本轮 5 条 P3 非阻断发现**（§6 F-1~F-5），**无 P0/P1/P2**：F5d 两处判据真空/盲区（F-1/F-2）、披露数字不实（F-3）、同类漂移在 F5d 覆盖面之外残留（F-4）、判据③ 口径的扩展前提（F-5）。

硬门槛全通过（P0 = 0）；round = 1 < 3 且无 BLOCKING ⇒ 无需转 BLOCKED。

---

## 2. 硬门槛裁决表

| 门槛项 | 阈值 | 实测 | 裁决 |
|---|---|---|---|
| P0 阻塞问题数 | = 0 | **0**（§6 最高级别 P3） | ✅ |
| 5 维度全覆盖 | 100% | 5/5 逐项结论（§5.1~§5.5） | ✅ |
| 每条发现标注级别 | 100% | 5/5 本轮发现均带 P3 标签（§6） | ✅ |
| 设计一致性检查 | 已完成 | DEC-025 ①③ / P-10 / C-04 / C-05 复检（§5.6） | ✅ |
| AI 代码专项 5 项 | 全部完成 | 5/5 逐项结论（§5.7） | ✅ |
| N1/N2 修复实质核验 | 非改标签 | 逐条比对 + 行段实读 + 红态复现（§3） | ✅ |

---

## 3. 前轮 N1/N2 逐条比对（复审核心）

| 前轮 ID | 级别 | 前轮问题 | 本轮处理（commit 内容） | 独立核验（非自述） | 状态 |
|---|---|---|---|---|---|
| **N1** | P3 | 强口径 `/(?<![.\w$])ctx\.[A-Za-z_$]/g` 漏 `this.ctx.<id>`（lookbehind 连前导 `.` 一并排除） | `test/smoke.mjs:2108` 改 `/(?<![\w$])ctx\.[A-Za-z_$]/g`；新增 `smoke.mjs:2114-2127` **①a 双向实证**（R1 反例口径 0 命中 ∧ 现行恰 1 命中 ∧ 三形态 0 误报）；`lib/index.js` 现状两口径同为零命中（无实害） | **A1/B2/B3/B4**：正则语义构造推演 + `lib/index.js` 实读（`ctx.` 零出现，`this.ctx` 仅作值传递 L89/121/154）+ 注入场景推演 | ✅ **已修复（实质）** |
| **N2** | P3 | 2.12 `L4391-4397`（两端落注释块，−6）、2.13 `L4422-4454`（起点无关行、终点漏第 6 处注册，−7）范围偏移 | `lib/host-contract.mjs:113-114` 改 `L4403-4408` / `L4429-4461`；`smoke.mjs:1981-2029` 新增 **F5d** 全量 21 项范围抽核（①范围有效 ②锚点在范围内 ③严格面边界非注释 ④终点不截断） | **B5/B6/B7/B9**：client.js L4390-4474 实读逐行确认；全文件 6 处注册定位；**旧值红态静态复现与演练报文逐字一致**；golden 21 独立复算 | ✅ **已修复（实质）** |
| （N3） | P3 | 宿主侧标签刷新依赖宿主重渲染 | 本次**未涉及**（非本任务范围） | — | ⏭️ 未在本次收口范围 |
| （N4） | P3 | locale 撤离保持最后快照未入契约 | 本次**未涉及**（非本任务范围） | — | ⏭️ 未在本次收口范围 |

**本 commit 只声明收口 N1/N2，未触碰 N3/N4** ⇒ 逐条比对无遗漏、无越界声称。

### 3.1 独立核验明细（本人第一手）

**A2 — ①a 的抗回退力（关键）**：`smoke.mjs:2119-2127` 用**反例口径常量** `CTX_STRONG_R1 = /(?<![.\w$])ctx\.[A-Za-z_$]/g` 与现行 `CTX_STRONG` 对同一构造双向取值，断言 `n1R1Hits === 0 && n1NowHits === 1 && n1WordInternal.length === 0`。若有人把 `CTX_STRONG` 改回 R1 口径 → `n1NowHits` 变 0 ≠ 1 ⇒ **断言必红**。且 ①a 的判别**不依赖被审代码现状**（① 本身在两种口径下同为 0 命中、对当前代码零分辨力，分辨力由 ①a 提供并已如实注明）。**结论：防回退有效**。

**B2 — N1 新正则语义（构造推演，逐形态）**：

| 输入 | R1 `(?<![.\w$])` | 现行 `(?<![\w$])` | 判定 |
|---|---|---|---|
| `this.ctx.get(1)` | **0（漏检）** | **1（`ctx.g`）** | ✅ 修复目标达成 |
| `a.ctx.settings` | 0 | 1 | ✅ 同类形态一并覆盖 |
| `ctx.ctx.foo` | 1 | 1 | ✅ 无回退 |
| `myctx.get(1)` | 0 | **0** | ✅ 无误报（前导 `y` ∈ `\w`） |
| `_ctx.get(1)` | 0 | **0** | ✅ 同上（`_` ∈ `\w`） |
| `$ctx.get(1)` | 0 | **0** | ✅ 同上（`$` ∈ `[\w$]`） |

**B3 — 真实代码面（本人实读）**：`lib/index.js` 中 `ctx\.` **零出现**；`this.ctx` 仅三处且**均为值传递**（`L89 this.ctx = ctx`、`L121 loggerOf(this.ctx)`、`L154 emitChanged(this.ctx, …)`）⇒ `this.ctx.<id>` 形态当前确实不存在（「无实害」自述成立），而 `this.ctx` 已在本仓流通 ⇒ 该残留缝确为现实未来形态（修复动因成立）。
**B4 — 注入场景推演（复算自述的注入实证）**：注入 `this.ctx.get('webServer')` + `this.ctx.settings` 后，现行口径匹配项 = `ctx.g`、`ctx.s`（**恰 2**）、R1 口径 = 0 ⇒ 自述的 `R1=0 / 现行=["ctx.g","ctx.s"]` 在语义上成立（本人静态推演；该注入演练本身未入机检断言，见 §7.2）。

**B5 — N2-2.12 行段实读（`lib/client.js:4390-4474`）**：

```
L4402: const offServiceEvent = typeof ctx.on === 'function'
L4403:   ? ctx.on('internal/service', (name) => {      ← 声明起点
L4404:       if (name === 'locale') { snapshotLocale(ctx); rerender(); return }
L4405-4407: sessions 分支 + launcher.setup + useSessionsH.refresh()
L4408:   })                                             ← 声明终点
L4409:   : null                                         ← 恰在终点之后（与自述一致）
```
⇒ `L4403-4408` = 调用节点闭区间，**精确**；起点/终点均非注释/空行（判据③ ✓）。

**B6 — N2-2.13 行段实读**：全文件 `ctx.slots.inject(` **恰 6 处** = `L4429 / L4435 / L4446 / L4450 / L4454 / L4458`，**全部 ⊆ `L4429-4461`**；`L4429` = `ctx.slots.inject('settings.section', () => ctx.slots.register(`（段首真实注册行），`L4461` = `))`。⇒ 声明区间既**未起于无关行**（旧值 L4422 = `window.addEventListener('dsh:split-claim', claimHandler)`，实读确认属 claim 互斥段）也**未截断第 6 处注册**。**精确**。

**B7 — 旧值红态独立复现（证明 F5d 有分辨力、修复非标签式）**：
- 2.12 旧值 `L4391-4397`：`L4391`、`L4397` 实读均为 `//` 注释行 ⇒ 判据③命中（判据②因 L4397 含 `internal/service` 而通过 ⇒ ③ 是唯一判别器）⇒ 红报文应含 `2.12:边界落在注释/空行` **✓ 与演练一致**。
- 2.13 旧值 `L4422-4454`：`L4422` 非注释（③ 通过）、`L4454` 行首 = `ctx.slots.inject(` 且 `L4458` 再现同行首前缀 ⇒ 判据④命中（`end=4454` ⇒ `+4` ⇒ 报文 `L4458`）⇒ 红报文应含 `2.13:终点截断(ctx.slots.inject 在 L4458 再次起行)` **✓ 与演练报文逐字一致**。
⇒ **前轮 N2 的两处缺陷在修复前确会被 F5d 捕获；修复后（B5/B6）两处判据均通过。**

**B9 — golden 独立复算**：`lib/host-contract.mjs` 中范围型 `line`（`^L\d+-L?\d+$`）实测 **21 项**（grep 22 命中，扣除 `L26` schema 示例注释）＝ 面 2:11 + 面 3:3 + 面 4:1 + 面 5:3 + 面 6:3 ⇒ `GOLDEN_RANGE_ITEMS = 21` **准确**；`RANGE_STRICT_FACES = [2]` 覆盖面 2 全部 11 项 **准确**。

---

## 4. 独立复算证据表

| # | 复算项 | 方法 | 实测结果 |
|---|---|---|---|
| B1 | 审查对象与基线 | `git show --numstat` / `git diff a0e515e..HEAD` / `git status` | 3 路径 +93/−13（产品代码 0 改动）；HEAD 多出的 `ea85799` 仅动 `.governance` ×2；工作区干净 ⇒ 读取状态 ≡ 审查对象 ✓ |
| B2 | N1 正则语义 | 六形态构造推演（表见 §3.1） | 目标形态 0→1；词内三形态 0 ✓ |
| B3 | N1 真实面 | `lib/index.js` 实读（`ctx.` 零出现 / `this.ctx` 三处值传递） | 修复动因与「无实害」均成立 ✓ |
| B4 | 注入场景 | 语义复算（`ctx.g` + `ctx.s`） | 与自述 `["ctx.g","ctx.s"]` 一致 ✓（本人未复跑） |
| B5 | 2.12 行段 | `client.js:4402-4409` 逐行实读 | `L4403-4408` 精确，`L4409 = : null` ✓ |
| B6 | 2.13 行段 | 全文件 `ctx.slots.inject(` 定位 | 恰 6 处且全在 `L4429-4461` 内 ✓ |
| B7 | 旧值红态 | 旧 `line` 与实读内容逐项对照判据 ③④ | 与演练红报文逐字一致 ✓ |
| B8 | F5c 抽取等价 | 函数体内联版逐 token 比对 + FIND-1 golden 行 | 仅 `it.symbol → symbol` 形参差异；golden 仍 `T3/T1/T1/T1/T1/T3` ⇒ 行为等价 ✓ |
| B9 | golden 数值 | 契约范围条目独立计数 | 21 / 面 2 = 11 ✓ |
| B10 | 断言计数自洽 | diff 内 `check(` 增删计数 | 新增恰 2（①a、F5d）、零删除/改名 ⇒ 258→260 自洽 ✓（**复跑由 Coordinator 独立完成：260/0**） |
| B11 | 多文件项歧义 | 21 项 `file` 形态核查 | 数组形态仅 4.6 = `['package.json']`（长度 1）⇒ `filesOf(it)[0]` 无歧义 ✓ |
| B12 | 候选非空（判据②空转风险） | 21 项 `symbol` 候选来源核查 | 每项均含点号标识符 / 引号字面量 / 中文名 ≥1 类 ⇒ 无空候选误报 ✓ |
| B13 | `node --check` ×5 / `validate-preset` 29/29 | — | **未复跑**（自述 + Coordinator 复跑覆盖；本轮预算投入行段实读与红态复现，见 §7.1） |

---

## 5. 审查维度逐项结论

### 5.1 正确性 — 通过（余 F-1/F-2 两处判据强度问题）
N1 新正则在六形态推演下语义正确（B2），未引入词内误报；放宽后 `X.ctx.<id>` 一族**整体纳入**覆盖（含 `a.ctx.`/`ctx.ctx.`）——在 `lib/index.js` 语境中任何 `*.ctx.<id>` 属性访问都属宿主直连，方向正确（FP 方向 = 断言变红需解释，属 fail-closed，不产生掩盖）。N2 两处 line 与实读一致（B5/B6）。F5d 四判据**:①范围有效性 / ③边界非注释** 强度充分且已在旧值上验证可红；**②** 仅要求「≥1 候选在范围内命中」——对「范围含无关前导行」无分辨力（F-1）；**④** 仅在终点行恰以点号调用起行时生效，修复后对 2.13 **已真空**（F-2）。

### 5.2 安全性 — 通过（无新面）
纯断言 + 契约文本：零网络、零子进程、零 `eval/Function`、零仓库外写入；新正则均为固定长 lookbehind/无嵌套歧义量词（无 ReDoS 面）。`readFileSync(new URL('../' + filesOf(it)[0], …))` 延续既有 F5b 口径（契约内自数据、只读、无写入）；`it.file === null` 已在 `filesOf` 处理（`rangeBad` 记为「无 file」而非抛错）。无敏感数据（无凭据/路径外泄；报告仅含仓库内行号）。无 OWASP 相关新增面。

### 5.3 可维护性 — 通过（余 F-3/F-4/F-5）
设计可扩展：严格面以**声明式常量** `RANGE_STRICT_FACES = [2]` 表达，并明示「扩面须先逐项核对边界语义，不可盲加」；`GOLDEN_RANGE_ITEMS` 与 F6 `GOLDEN_NEC` 同款漂移探测约定（brittle-by-design，可接受）。披露总体充分（判据清单、旧值红态、faces 3/5/6 未纳入原因、启发式非 AST 均如实标注）——**唯数字「三处」不实**（F-3，实为 2 处，本人逐项实读）；同类漂移在 free-text 字段仍有残留（F-4）。命名清晰、职责单一（`symbolCandidates` 收敛两处口径，避免漂移，符合 C-03/单一事实源）。

### 5.4 性能 — 通过
F5d 为静态判断：21 项各做一次 `readFileSync + split('\n')` 与一次范围内 `some` 扫描、一次终点后缀 `findIndex`；最大文件 `client.js`（4488 行 × 14 项范围条目）⇒ 量级 ~10⁵ 次字符串 `includes` + ~2.5 MB 解析，**远低于 2 s**（实测量级应为数十 ms，与既有 F5/⑬ 检查同阶）。无网络/无轮询/无嵌套 N+1。可优化点（不必要）：同一文件重复读取未缓存 —— P3 级以下，不记。

### 5.5 测试覆盖 — 通过
断言净 +2 与 diff 自洽（B10）；**无断言被削弱**——diff 仅新增与抽取，F5c 的 FIND-1 golden 断言行原样保留（B8），历史 258 项零回归（Coordinator 复跑 260/0）。F5d **自带两处防空转**：`rangeItems.length === GOLDEN_RANGE_ITEMS`（防静默漏扫）与 `rangeStrict.length > 0`（防严格面空集）——后者偏弱（F-5）。判据②③④在旧值上**实测可红**（B7）⇒ 非空转检查。缺口：N1 的**真实代码面注入实证**（注入 `lib/index.js` 临时改动）仅存于 CHANGELOG/EVD 散文，未入机检断言（见 §7.2，非阻塞）。

### 5.6 设计一致性 — 通过
| 依据 | 复检结论 |
|---|---|
| DEC-025 ①③ / P-10 | 本次**未新增**任何宿主耦合字面量；F5d 直接消费 `hc.items`（**不复制条目清单**），契约仍是单一事实源 ✓ |
| 前轮判据闭合 | N1/N2 两条 P3 的**建议式**被逐字采纳（正则形式、两处 line 值、增「范围抽核」），无偏离或打折 ✓ |
| C-04 修改纯粹 | 3 路径每处均服务 N1/N2；F5c 抽取有明确复用理由（F5d 共用候选口径）✓ |
| C-05 原子性 | 单 commit 承载「同一次复审的两条 P3 收口」单元，产品代码零改动 ⇒ 无跨任务夹带 ✓ |

### 5.7 AI 代码专项 5 项 — 全部通过（余 F-3）
| 项 | 结论 | 依据 |
|---|---|---|
| mock 残留 | **无** | F5d 读真实仓库文件；`N1_NEG`/`N1_WORD_INTERNAL`/`CTX_STRONG_R1` 为**显式标注的**正/反例构造（非 mock 冒充实现）✓ |
| 硬编码 | **合理（注意 F-5）** | `GOLDEN_RANGE_ITEMS=21`（独立复算准确）、`RANGE_STRICT_FACES=[2]`（面 2 实为 11 项）——均为漂移探测 golden 且已注释理由；`rangeStrict.length > 0` 建议改 11 golden ✓ |
| 幻觉 | **未发现（唯 F-3 一处数字）** | 全部行号引用抽核为真：2.12 `L4403-4408`/`L4409`、2.13 `L4429-4461` 与 6 处注册、旧值 `L4391/L4397` 注释、旧 `L4422` 无关行、`L4458` 再现、`index.js` `this.ctx` 三处 —— **逐项实读命中**；不实项仅「三处」miscount（F-3）✓ |
| 未实现 TODO | **无** | diff 未引入 TODO/FIXME/占位断言；`console.log(' info …')` 为既有诊断惯例 ✓ |
| 过度实现 | **未超必需** | 净增 75 行 = 1 处正则（1 行）+ ①a 双向断言（14 行含注释）+ F5d（49 行，其中 25 行为判据/红态/未纳入面理由的披露注释）+ 函数抽取（净 +6）+ CHANGELOG 5 行。所谓「97 行」= 含注释与诊断输出；代码行与本次两条 P3 的收口面一一对应，无搭车功能 ✓ |

---

## 6. 本轮发现（全部非阻断，P3）

### F-1 — P3｜F5d 判据② 缺「范围完整性」，现存实例 6.1 起点含无关行即为其盲区证明

- **位置**：`test/smoke.mjs:2016`（判据②）；实例 `lib/host-contract.mjs:142`（item 6.1 `line: 'L38-44'`）。
- **事实依据**：`package.json` 实读——`L38 = "dependencies": {},`、`L39 = "peerDependencies": {`、`L44 = "@deepseek-ai/dsh-tools": "*"`、`L45 = }`（对象闭合）。声明构造（peerDependencies）真实跨 **L39-45**，而契约写 `L38-44`：起点含**无关兄弟键**、终点**漏闭合行**。F5d 判据②（`peerDependencies` 在 L39 命中）、③（面 6 非严格面）、④（L44 行首非调用前缀 ⇒ 真空）**三项全部无法察觉**。
- **影响**：非产品缺陷；维护者按 `line` 定位会多读一行并少读闭合行。属与 N2 **同类**的契约精度残留（本次范围外、非本次引入），且证明 F5d 的残余盲区已被实际触发。
- **建议**（择一，随手纳入下次改动）：①增判据⑤「范围内该构造出现次数 ≡ 全文件出现次数」（2.13 实测 `6 == 6` 非真空；回退旧值 `5 != 6` 即红，可同时覆盖 F-2 的真空）；或 ②修正 6.1 为 `L39-45` 并另行记录同类抽核结论。

### F-2 — P3｜F5d 判据④ 对修复后的 2.13 已真空（仅对旧值生效）

- **位置**：`test/smoke.mjs:2020-2024`（判据④）；`lib/host-contract.mjs:114`。
- **事实依据**：判据④ 以 `callPrefix(lines[end-1])` 取值，非点号调用起行者**跳过**。新终点 `L4461 = ))` ⇒ 对 2.13 现在的 `end` 取 `null`，**该分支不再生效**（红态仅在旧值 `L4454` 上出现，B7 已验证）。
- **影响**：2.13 的「终点不截断」在修复后**无持续机检力**——若后续把终点再度前移/后移（如 `L4429-4460`），④ 静默通过，仅 ②③ 仍有效。
- **建议**：同 F-1 判据⑤（出现次数完整性）替代/补强 ④；或至少在注释中把 ④ 标注为「opportunistic（仅当终点行以点号调用起行时生效）」以如实披露强度边界（当前注释已披露「结构性启发式」，但未点明**对已修复条目已真空**）。

### F-3 — P3｜披露数字「三处」不实：faces 3/5/6 合法注释行起段实为 **2** 处

- **位置**：`CHANGELOG.md:21`（「faces 3/5/6 有**三处**范围**合法**起于注释行（3.1 …、3.5 …）」）；`test/smoke.mjs:1990-1992` 注释（措辞为「存在合法注释行起段」并列举 2 例，未计数）。
- **事实依据**：本人**逐项实读** faces 3/5/6 全部 9 项范围条目的起止行——注释行起段恰 **2** 处：3.1 起点 `client.js:838 = /** 找到会话根容器… */`、3.5 起点 `client.js:896 = // 跨插件互操作…`；其余起止行均非注释：3.8 `L2411`（函数行）/`L2421`；5.1 `preset.yml:1-2`；5.2 `agent.cordis.yml:16`/`L216`；5.4 `L77-81`；6.1 `package.json:38`；6.2 `L8-10`；6.4 `ci.yml:77`/`L113`。（无第三处；且 `#`/JSON 注释不在 `lineIsComment` 口径内。）
- **影响**：不改变 `RANGE_STRICT_FACES=[2]` 的判定与任何门禁结果（严格面 = 面 2），但**披露数字与实读不符**——本次收口主题即「行号/范围精度」，同一文档内的计数错误应予修正。
- **建议**：改为「**2 处**（3.1 起于 `/**` JSDoc、3.5 起于 `//` 互操作说明）」；后续如需计数型披露，附一条实读命令/清单以便复核。

### F-4 — P3｜同类行号漂移在 F5d 覆盖面之外残留（free-text `note` / `symbol` 文本）

- **位置**：`lib/host-contract.mjs:121`（item 3.5 `note`）、`:113`（item 2.12 `symbol`）。
- **事实依据**（`client.js` 实读）：
  1. 3.5 `note` 写「claim 监听让位（**L4406-4417**）」，实读该段为 **L4411-4424**（`L4413 let claimHandler = null`、`L4422 window.addEventListener('dsh:split-claim', claimHandler)`、`L4424 catch`）——旧引用**起点偏差 −5 且截断处未含 `addEventListener` 注册行**。
  2. 2.12 `symbol` 写「单一监听器**双消费**：launcher 重 setup + hook.refresh」，但 `L4403-4408` 实为 **3 消费者**（`L4404 locale` 分支 = R1-F2 新增 + `L4406 launcher` + `L4407 hook.refresh`）——字段语义未随后续行为变更同步。
  3. 机制原因：F5d 只校验**结构化 `line` 字段**（B5/B6），free-text 内的行号与描述**零信号**。
- **影响**：不影响门禁与运行（`symbol` 仅作锚点文本匹配、`note` 为人读）；但属「契约实现边界/引用 ⟷ 代码」一致性残留，且与 N2 同源（同一偏移根源）。
- **建议**：**另立小任务**（或并入 COMPAT-005 诊断面板）批量收口 free-text 行号引用与 stale 描述；**不阻断本次合并**（不改判为 NEEDS_CHANGE：非本次引入、非本次声明范围、无功能影响）。

### F-5 — P3｜判据③ 的注释口径为 JS 专属 + 严格面防空转守卫偏弱（扩面前提）

- **位置**：`test/smoke.mjs:1997`（`lineIsComment`）、`:2027`（`rangeStrict.length > 0`）。
- **事实依据**：`lineIsComment = l.trim() === '' || /^(\/\/|\/\*|\*)/.test(l.trim())` ——**仅识别 JS 风格注释**；面 5/6 目标为 YAML（`#` 注释）与 JSON，即使未来纳入严格面，该口径也**不会**识别 `#` 注释行（当前面 5/6 边界行均非注释，故无现存误判）。另 `rangeStrict.length > 0` 允许严格面退化到 1 项仍绿（golden 只锁总范围条目 21 项）。
- **影响**：仅影响未来扩面的正确性前提与防退化强度，当前判定无误。
- **建议**：①若扩面，先按语言实现注释口径（或直接以「起点行是否属于声明的构造」替代注释启发式）；②`rangeStrict.length` 提为 golden（如 `=== 11`，与 `GOLDEN_RANGE_ITEMS` 同款）；③在注释中标注「严格面口径 = JS 注释语法」以防跨语言盲扩。

**P0 阻塞数 = 0；5 条发现全部 P3（非阻断）。**

---

## 7. 非阻断备注

1. **复跑范围（如实披露）**：本人**未执行**任何测试命令（角色工具约束 = 只读审查，且本轮预算优先投给行段实读与旧值红态复现）。`node test/smoke.mjs` **260/0** 引自 **Coordinator 独立复跑**（非本人复跑）；`node --check ×5 = 0`、`validate-preset 29/29`、注入实证为**自述 + 语义复算**（B4），本轮未复跑。本人独立完成的是：行段实读（B3/B5/B6）、旧值红态静态复现（B7）、正则语义推演（B2）、golden 复算（B9）、断言计数自洽（B10）、抽取等价（B8）。
2. **N1 真实面注入实证未入机检**：该演练（临时注入 `lib/index.js` → 两口径取值）**不可从仓库状态复现**（属一次性手改），仅存于 CHANGELOG/EVD。语义上已由 B4 复算确认；机检可覆盖部分已由 ①a 锁定。**建议（可选项，非阻塞）**：若希望该证据可复现，可改为对**构造字符串**断言（现有 ①a 已具备）而非要求注入真实文件。
3. **free-text 行号未纳入本次**：契约 `note`/`symbol`/`regionLiterals` 内的行号引用**不在 F5d（只查 `line` 字段）** 与任何现有断言的覆盖内（F-4 即其实例）——建议登记为后续小任务，避免下次再以 P3 形式出现。
4. **审查对象的路径级行数更正（记录用）**：本任务简报中「`test/smoke.mjs` +97/−13」与实测不符——`git show --numstat` 为 **smoke `+86/−11`**、host-contract `+2/−2`、CHANGELOG `+5/−0`，**合计 +93/−13**（简报的合计数正确，分文件行数偏高 11 行）。不影响审查结论，仅作事实澄清。
5. **本次未覆盖**：N3/N4（前轮 P3，非本任务范围）；F5a/F5b 既有判据的自身强度；`docs/review/` 之外的治理记录（EVD-089 / plan-tracker 更新）属 Coordinator 事实源，本报告不作判定。
6. **审查方式偏离声明（P-01 诚实披露）**：本报告为获取 diff 使用了**只读 git 命令**（`git show/log/status/diff`）与只读文件读取；**未执行任何写操作、未运行测试、未改动仓库任何文件**（唯一写入 = 本报告）。该使用偏离角色卡「Bash ❌」的字面约束（角色原意为禁止运行写操作/构建），在此如实登记。

---

## 8. 结论与后续

**APPROVED_WITH_NOTES（`unresolved_blockers = 0`）** —— 前轮 `REVIEW-COMPAT-004-R2` 的 N1/N2 两条 P3 **均已实质收口**：正则按建议式修正且带**抗回退双向实证**（A2/B2）；契约两处 `line` 与 `lib/client.js` 实读**逐行一致**（B5/B6）；新增 F5d 对全量 21 项范围条目构成**可红的真实防护网**（B7 红态逐字复现）。本轮 5 条 P3 均为**非阻断**（判据强度真空 F-1/F-2、披露计数 F-3、覆盖面外的同类残留 F-4、扩面前提 F-5），**无 P0/P1/P2，无新引入缺陷**。

**门禁建议**：**本 commit 可通过并合并/push**（纯测试 + 契约文本 + CHANGELOG，产品代码 diff = 0；smoke 260/0 由 Coordinator 独立复跑）。建议顺手纳入的**可选**修正：F-3 一处措辞（`三处` → `2 处`，零风险、单行）；F-1/F-2 的判据⑤（出现次数完整性）与 F-4 的 free-text 行号收口建议**另立后续任务**（避免本次合并范围膨胀）。

**后续建议任务（非本任务范围）**：①free-text 行号/stale 描述批量收口（F-4，含 6.1 `L39-45`、3.5 `note`、2.12 `symbol`）；②F5d 判据⑤ 增强（F-1/F-2）；③N3/N4（前轮 P3）择机并入 COMPAT-005。
