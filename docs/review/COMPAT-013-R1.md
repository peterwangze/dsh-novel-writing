# REVIEW-COMPAT-013-R1 — COMPAT-013（F-1~F-5 收口批次）后置代码审查

> **Round**：R1（本任务首轮）
> **前轮引用**：`docs/review/COMPAT-012-R1.md`（APPROVED_WITH_NOTES，`unresolved_blockers=0`）§6 **F-1~F-5**（全 P3）＝ 本 commit 的动因与验收项；更前置 `docs/review/COMPAT-004-R2.md`（N1/N2）
> **审查对象**：commit `561f164`（`git show --numstat`：`test/smoke.mjs` **+110/−5**、`lib/host-contract.mjs` **+3/−3**、`CHANGELOG.md` **+9/−1**，合计 **+122/−9**；恰 3 路径）
> **审查基线事实**：审查时 HEAD 已前移 `fc1dc81`（仅 `.governance/evidence-log.md` +1、`.governance/plan-tracker.md` +2/−1）——**未触碰 3 条审查路径**，故本轮结论对当前 HEAD 仍有效；工作树 `git status --porcelain` 为空
> **结论**：**APPROVED_WITH_NOTES**，`unresolved_blockers = 0`（P0=0 / P1=0 / P2=0 / P3=**6**，均非阻断、均为注释·口径·覆盖披露级）

---

## 0. 审查方法与证据边界（如实披露）

- 本轮**只读**：`read` / `grep` + 只读 git（`show --numstat`、`log`、`rev-list`、`status`）。
- **未执行任何测试或写操作**：角色工具约束禁止命令执行（不运行 smoke / `node --check` / validate-preset）。因此**门禁数值不由本轮独立复跑**——`smoke 262/0`、`node --check ×5=0`、`validate-preset 29/29`、`ci-mock-face 4/4` 采信 **Coordinator 独立复跑**（任务简报已声明）＋本轮**静态复算自洽性核验**（见 §3.7：262 = 260 + ⑫c + ⑫d；21 = 11+3+1+3+3；11 = 13−2）。
- 本报告全部行号/文本均为**本轮实读**所得，不转写 Developer 自述；凡我未读到的，标「未核」。

---

## 1. 硬门槛裁决

| 门槛项 | 阈值 | 本轮实测 | 判定 |
|---|---|---|---|
| P0 阻塞问题数 | = 0 | **0** | ✅ |
| 5 维度全覆盖 | = 100% | 5/5 逐项结论（§4.1~§4.5） | ✅ |
| 每条发现标注级别 | = 100% | 6/6 均标 P3（§5） | ✅ |
| 设计一致性 | 已完成 | 契约 6.1/3.5/2.12 与目标文件**逐行实读一致**（§3.1~§3.4）；deviation 仅注释列举（F-2） | ✅ |
| AI 代码专项 5 项 | 全部完成 | §4.6 五项逐一有结论 | ✅ |

---

## 2. F-1~F-5 逐项实质核验（非改标签）

前轮 F-1~F-5 原文语义取自 `docs/review/COMPAT-012-R1.md`（§6 与 §8），逐条比对本轮实现：

| 前轮项 | 前轮要求实质 | 本轮实现 | 核验证据（本轮实读） | 判定 |
|---|---|---|---|---|
| **F-1**（P3） | 判据② 缺「范围完整性」，现存实例 6.1 起点含无关行；建议 ①增判据⑤「范围内出现次数 ≡ 全文件次数」**或** ②修正 6.1 并另记抽核结论 | **两件都做**：新增判据⑤（⑤a 计数 + ⑤b 构造闭合两子句，`smoke.mjs:2049-2112`）+ 契约 6.1 `L38-44`→**`L39-45`** | 6.1 真值 `package.json:38` `"dependencies": {},` / `:39` `"peerDependencies": {` / `:45` `}` / `:46` `}` ⇒ `L39-45` **准确**；⑤b 报文可静态复现（§3.1）；P-01 前提订正**已在三处交付物落地**（CHANGELOG 条目、`smoke.mjs:2063-2066`、契约 6.1 note） | **已实质收口**（且诚实度高于建议：主动订正了简报里「计数式判据对该实例有效」的错误前提） |
| **F-2**（P3） | 判据④ 对 2.13 已真空；建议 ⑤ 替代/补强 **或** 注释标注 opportunistic | ④ **保留** + `smoke.mjs:1998-2006` 标注 opportunistic/真空/残余缝；机检力由 ⑤a 承接 | `client.js:4461` = `))` ⇒ `callPrefix`（`smoke.mjs:2017-2020`）取 null ⇒ 真空属实；⑤a 对 2.13 有效（截断整行 ⇒ 5≠6，§3.2） | **已实质收口** |
| **F-3**（P3） | CHANGELOG「三处」→「2 处」 | CHANGELOG 该处已改「**2 处**」+ 新增 ⑫d golden（`smoke.mjs:2121-2134`） | 注释起段实读恰 **2 处**：`client.js:838` `/**`、`client.js:896` `//`；8 项非注释起段逐项实读全部命中（§3.5） | **已实质收口**（但新注释引入 2 处列举/标签失实 → F-1/F-2） |
| **F-4**（P3） | free-text 漂移三处收口（6.1 line、3.5 note、2.12 symbol） | 三处全改 | 3.5 claim 段真值 **L4411-4424** ✓（`client.js:4413` `let claimHandler = null`、`:4422` `window.addEventListener('dsh:split-claim', claimHandler)`、`:4424` `} catch`）；2.12 **三消费** ✓（`:4404` locale 重快照 / `:4406` launcher 重 setup / `:4407` `useSessionsH.refresh()`）；2.12 范围 `L4403-4408` ✓ | **已实质收口**（副产物见 F-6） |
| **F-5**（P3） | ①③ 口径 JS 专属须注释留档；②严格面守卫 `length>0` 改 golden | ①注释 `smoke.mjs:1992-1995`；②`GOLDEN_RANGE_STRICT_ITEMS=11`（`smoke.mjs:2014`）+ 断言 `:2045` | golden 11 独立复算成立（§3.6）；F-5① 披露「现存面 5/6 边界行均非注释」经实读**属实**（5.1 `preset.yml:1` = `name:`、5.2 `agent.cordis.yml:16`、5.4 `:77` 均 `- id:`，无 YAML `#` 边界） | **已实质收口**（注释理由列举失实 → F-2） |

**结论：F-1~F-5 五条全部实质收口，无一条为改标签；简报前提订正（P-01）被正确采纳并写入三处事实源。** 本轮新增 6 条 P3 全部是本批**新写注释/口径**的准确性/披露问题（同源于 F-3 关注的「人工转写漂移」，属同类残余，非回退）。

---

## 3. 核心正确性核验（判据⑤ 逐行推演 + 实读复现）

### 3.1 ⑤b 对 6.1 的捕获真实性 —— **复现成功（逐字一致）**
- `openKey5`（`smoke.mjs:2070`）对 6.1 候选集（经 `symbolCandidates` + `COMMON_WORDS` 过滤后 = `{peerDependencies, schemastery}`；`cordis/dsh/settings/home/paths/tools` 全被通用词表剔除，`smoke.mjs:1940-1946`）中 `peerDependencies` 命中 `package.json:39`。
- 配平扫描（`smoke.mjs:2095-2103`）从 L39 `{` 计到 **L45** `}` 归零。
- 旧止 **L44** ⇒ `closeLine 45 > end 44` ⇒ 报文 `6.1:⑤b peerDependencies(L39) 构造闭合行 L45 超出止 L44` —— 与 Developer 自称报文**逐字一致** ✓。
- 真值止 **L45** ⇒ 绿 ✓。**捕获真实，非构造性绿灯。**

### 3.2 ⑤a 对 2.13 的判别力
- `client.js` 中 `ctx.slots.inject(` / `ctx.slots.register(` 全文件**各恰 6 处且同行共现**（L4429/4435/4446/4450/4454/4458，grep 实测 6 行）⇒ 全文件计数 6 ≡ 声明 `×6` ⇒ 进入 2 对；范围 L4429-4461 内同为 6 ⇒ 绿 ✓。
- 旧值 `L4422-4454` 静态复算：范围内 inject 命中 L4429/4435/4446/4450/4454 = **5 ≠ 6 ⇒ 红** ✓（与演练自述一致）。

### 3.3 残余缝（F-2 披露项）静态确认 —— **披露属实**
止改为 `L4429-4460` 时逐判据推演：① 有效 ✓；② span 内 `ctx.slots.inject` 命中 ✓（不要求全覆盖）；③ 面 2 起止非注释（L4429 代码行 / L4460 `(props) => el(BindDialog, …)`）✓；④ `callPrefix(L4460)` 因行首为 `(` ⇒ null ⇒ 真空 ✓；⑤a 仍 6≡6 ✓；⑤b 无 `键: {` opener ✓ ⇒ **全绿**，与披露一致。**披露 + COMPAT-014 立项（`plan-tracker.md:153`，依赖 `COMPAT-013✅`）处置恰当。**

### 3.4 判据④ 保留的充分理由核验
④ 对「终点行即下一次同构造起行」形态仍是唯一判据（本轮 2.13 属 opportunistic 真空，但面 2 其余项仍受 ④ 约束）⇒ 保留而非删除**方向正确**；`smoke.mjs:1998-2006` 已把强度边界写明。

### 3.5 ⑫d 逐项实读列举核验（前轮 F-3 的「实读清单」）
覆盖面 = 全部非面 2 单段范围条目 = **10 项**（面 3×3、面 4×1、面 5×3、面 6×3）。逐项实读起段行：

| 条目 | 目标文件:行 | 实读内容 | 注释？ |
|---|---|---|---|
| 3.1 | `lib/client.js:838` | `/** 找到会话根容器 … */` | **是（JS 注释）** ✓ 计入 |
| 3.5 | `lib/client.js:896` | `// 跨插件互操作：…` | **是（JS 注释）** ✓ 计入 |
| 3.8 | `lib/client.js:2411` | `function findSidebarEl() {` | 否 ✓ |
| 4.6 | `package.json:17` | `"dsh": {` | 否 ✓ |
| 5.1 | `agent-presets/novel-writing/preset.yml:1` | `name: 小说写作工作流` | 否 ✓ |
| 5.2 | `agent-presets/novel-writing/agent.cordis.yml:16` | `- id: persona` | 否 ✓ |
| 5.4 | `agent-presets/novel-writing/agent.cordis.yml:77` | `- id: skill-filesystem` | 否 ✓ |
| 6.1 | `package.json:39` | `"peerDependencies": {` | 否 ✓ |
| 6.2 | `package.json:8` | `"engines": {` | 否 ✓ |
| 6.4 | `.github/workflows/ci.yml:77` | `mkdir -p node_modules/…` | 否 ✓ |

⇒ 注释起段 **2** + 非注释 **8** = 10 = 覆盖面全量，**逐项准确**。golden `=== 2` 数值正确（缺的只是覆盖面标签，见 F-1）。

### 3.6 两个 golden 的独立复算（自洽性交叉验证）
- `GOLDEN_RANGE_STRICT_ITEMS = 11`：面 2 共 13 项，**真正非单段范围的恰 2 项 = 2.2（`line: 'L97'`，单行号无 `-`）与 2.4（多段 `'L549 / L573 / …'`）** ⇒ 13 − 2 = **11** ✓。
- `GOLDEN_RANGE_ITEMS = 21`：11（面 2）+ 3（面 3：3.1/3.5/3.8）+ 1（面 4：4.6）+ 3（面 5：5.1/5.2/5.4）+ 3（面 6：6.1/6.2/6.4）= **21** ✓（面 1 11 项 line 均无 `-` 形态，贡献 0，与 21 自洽）。
- 两个 golden 相互印证，数值无 1 项漂移。

### 3.7 断言计数自洽
diff 仅**修改** F5d 既有 `check`（`smoke.mjs:2044-2046`）+ **新增** ⑫c（`:2109`）/⑫d（`:2132`）两条 `check` + 1 条 info ⇒ 260 + 2 = **262** ✓；无既有断言被删除或削弱（`GOLDEN_RANGE_ITEMS`、FIND-1 tier golden、F5a/b/c 原样保留）。

---

## 4. 审查维度逐项结论

### 4.1 正确性 —— **通过**（余 6 条 P3，均为注释/口径级，无逻辑错误）
判据⑤ 控制流逐行读过：范围越界项在 ① 已报处 `continue`（`:2081`）避免越界取行；`closeLine < 0`（未闭合）与 `> end`（超止）分支互斥且均入 `rangeIncomplete`；`cardPairs` 防空转（`=== 2`）。⑤b 对 6.1 报文复现、⑤a 对 2.13 计数 5≠6 均静态成立（§3.1/§3.2）。**未发现逻辑/边界缺陷。**

### 4.2 安全性 —— **通过（无新面）**
- 无新增 I/O 面：仅 `readFileSync` 读仓库内文件（`smoke.mjs:2077`/`:2127`），无网络/无写/无环境变量。
- 注入面：`openKey5` 对候选做正则元字符转义（`:2084` `replace(/[.*+?^${}()|[\]\\]/g, '\\$&')`）✓，token 来自本仓契约常量，无外部输入路径。
- 敏感数据：断言仅消费行文本与计数，无凭据/路径外泄；契约/CHANGELOG 无敏感串。OWASP 面在本类构建期静态断言中 N/A。

### 4.3 可维护性 —— **通过**（余 F-1/F-2/F-5）
- ⑤a 收窄口径的注释**足以防后续「简化」**（`:2056-2068`）：给出「无差别施加将误报 7/21 项」的**具体清单** + 「只有 symbol 自述基数者『全部出现』才有定义」的判据论证 + 「⑤b 不可被 ⑤a 取代（6.1 两范围计数同为 1≡1）」的实测反例 ⇒ 简化为纯计数者会立即收到注释警告且有 golden 2 兜底。
- ⑤b **未收窄到 JSON 的理由成立**（`:2067-2068`）：JSON 与 JS 的 `键: {` 声明同构，收窄即丢 JS 面覆盖力；且当前 `openKey5` 对 21 项**零误报**（唯一匹配 opener 即 6.1，其余候选或非键名、或被通用词表剔除）⇒ 不收窄非过度实现。
- 扣分项：新写注释的事实性（F-1/F-2）与工具口径边界未披露（F-5）。

### 4.4 性能 —— **通过（静态判断）**
- 新增成本量级：外层 21 项 × 候选数（≤ ~10）× span 行数（最大 = 5.2 的 200 行）的 `split` 计数 ≈ 10⁴ 次短串操作；仅当 opener 命中才进入内层 O(文件行数) 配平扫描，**当前全量仅 6.1 命中 1 次**（其余项无 opener 匹配）⇒ 相对 smoke 既有的大量文件读取可忽略；无 O(n²) 以上风险、无 N+1。
- **未实测耗时**（见 §0 证据边界）；结论仅基于代码复杂度静态判断。

### 4.5 测试覆盖 —— **通过**（余 F-3 冗余标注 / F-4 覆盖缝 / F-5 口径）
- 计数 260→262 自洽（§3.7）。
- **④ 保留 + ⑤a 承接构成有效覆盖**：2.13 整行截断（含 inject+register 同行）⇒ 5≠6 ⇒ 红 ✓；④ 真空项已如实标注且其对其他项仍生效 ✓。
- **演练活性抽核 2 项构造核**（本轮静态复现，未执行）：6.1 报文逐字复现（§3.1）、2.13 旧值 5≠6（§3.2）⇒ 判据**非空转**。
- 残余缝（删终点闭合行全绿）**已恰当披露 + 已立项**（`smoke.mjs:2002-2006` + `plan-tracker.md:153`）✓；但同类盲区**尚有 2 个现存实例未被第五判据覆盖**（→ F-4）。
- 新增防漂移：⑫c 的 `cardPairs === 2`、⑫d 的 `=== 2`、严格面 `=== 11` ⇒ 无静默漏扫。

### 4.6 AI 代码专项 5 项

| 检查项 | 结论 | 依据 |
|---|---|---|
| mock 残留 | **无** | 无 mock/stub 引入；断言直读仓库真实文件（`readFileSync` of `client.js`/`package.json`/`ci.yml`/YAML） |
| 硬编码返回值 | **合理（1 处标注）** | 三个新 golden（`GOLDEN_CARDINALITY_PAIRS=2`、`GOLDEN_RANGE_COMMENT_START=2`、`GOLDEN_RANGE_STRICT_ITEMS=11`）均为漂移探测约定（brittle-by-design）且已注释理由；11 与 2 均经独立复算成立（§3.5/§3.6）⇒ 非取巧式硬编码。唯一弱点是 2 对的**独立性**（F-3） |
| 幻觉 API/行号 | **无**（1 处列举失实） | 本轮实读抽核 20+ 处行号/文本（package.json 8/17/29/38/39/45；client.js 838/896/2411/4403-4408/4411-4424/4429-4461；preset.yml 1；agent.cordis.yml 16/77；ci.yml 77）**全部与实读一致**；唯一失实为注释中的**排除项列举**（2.3/2.4 → 应为 2.2/2.4，F-2） |
| 未实现 TODO | **无** | 无 TODO/FIXME 残留；残余缝以「如实留档待另案」+ COMPAT-014 显式立项承接，非静默遗留 |
| 过度实现 | **在范围内，建议保留** | ⑫d 超出「一行措辞更正」的最小范围（属加固），但仍在 F-3 目标（计数入机检）内且可单点回退；其价值 = 把人工转写态改为机检态（与 COMPAT-004 FIND-1 同款处置）⇒ **保留并修正注释**比回退更优（回退会使披露计数重回无守护态） |

---

## 5. 发现列表（本轮新增，均 P3、均非阻断）

### F-1（P3｜披露准确性）⑫d 覆盖面标签「非严格面（3/5/6）」与实现不符，且与自身列举自相矛盾
- **位置**：`test/smoke.mjs:2114`、`:2120`、`:2132`（断言消息）；`CHANGELOG.md` COMPAT-013 F-3 条目。
- **事实**：`:2123-2124` 仅跳过 `RANGE_STRICT_FACES=[2]` ⇒ 实际覆盖面 = 面 **3/4/5/6 共 10 项**（含 4.6，面 4）；而注释/消息称「3/5/6」（= 9 项）；且 `:2116-2119` 的列举清单**自身已含 `4.6 L17`**（面 4）⇒ 文本内部矛盾。
- **影响**：诊断误导（面 4 条目起段变注释时会以「3/5/6」名义报红）；与 CHANGELOG 披露口径不一致。golden 数值（2）本身正确，不产生假绿/假红。
- **建议**：标签改为「非严格面（3/4/5/6，共 10 项）」，或由 `rangeItems.filter((it) => !RANGE_STRICT_FACES.includes(it.face))` 动态生成覆盖面枚举。

### F-2（P3｜注释事实性）F-5② 新 golden 的排除项列举失实：2.3 实为单段范围条目
- **位置**：`test/smoke.mjs:2012`（「13 项中 2.3/2.4 非单段范围形态」）。
- **事实**：`lib/host-contract.mjs:104` item 2.3 `line: 'L4374-L4377'` **命中** `RANGE_LINE=/^L(\d+)-L?(\d+)$/`（`smoke.mjs:2015`）⇒ 属单段范围条目；真正被排除的是 **2.2**（`:103` `line: 'L97'`，单行号无 `-`）与 **2.4**（`:105` 多段）。同 commit 的 F-2 残余缝注释（`smoke.mjs:2005`）正以「2.3 `L4374-L4377` 非配平」为例 ⇒ 两处自相矛盾。
- **影响**：golden 值 11 正确（§3.6 已独立复算），仅理由/列举错——属 F-3 同源「人工转写漂移」在本 commit 内的残留。
- **建议**：改为「13 项中 2.2（`L97` 单行号形态）与 2.4（多段）非单段范围形态」。

### F-3（P3｜判据强度披露）⑤a「覆盖 2 对」两对**非同源**，判别力等价单计数
- **位置**：`test/smoke.mjs:2109-2110`（golden 2 与消息「覆盖 2 对」）；`:2053-2060`、`:2085-2094`（⑤a 实现）。
- **事实**：`ctx.slots.inject(` 与 `ctx.slots.register(` 在 `lib/client.js` 的 6 处**同行共现**（L4429/4435/4446/4450/4454/4458，均为 `ctx.slots.inject('…', () => ctx.slots.register(`）⇒ 两 token 的出现/缺失**恒等联动**，`cardPairs=2` 源自同一 6 行。
- **影响**：不产生假绿（整行截断时两者同时 5≠6 ⇒ 红）；但「覆盖 2 对」易被读作两个独立构造受保护；golden `===2` 实际只在「仅一 token 计数漂移而另一 token 恰为 6」（如文件内多出第 7 处 `ctx.slots.inject`）时发挥独立作用。
- **建议**：注释注明「两对同源（同行共现，判别力等价单构造计数）」；若后续出现非同源 tier-2 构造再补第 3 对。

### F-4（P3｜覆盖缝，与 6.1 修前同形）⑤b 以「候选键名 ≡ 构造键名」为前提 ⇒ 4.6 / 6.2 仍处终点截断盲区
- **位置**：判据⑤b（`test/smoke.mjs:2095-2107`）对 `lib/host-contract.mjs:132` item 4.6（`package.json` **L17-29**）与 `:143` item 6.2（**L8-10**）。
- **事实**：`"dsh": {`（L17，闭合于 **L29**）与 `"engines": {`（L8，闭合于 **L10**）的键名分别为 `dsh`（在 `COMMON_WORDS` 内 ⇒ 候选被剔除）与 `engines`（候选形态为 `engines.node`，与键名不相等）⇒ `openKey5` 无匹配 opener，⑤b 对二者**不执行闭合校验**。静态推演：若 4.6 止改为 `L17-25`（或 6.2 止改为 `L8-9`），① 有效 ✓、② 锚点 `cordis.patch.yml`（L19）/`engines`（L8）仍命中 ✓、③ 面 4/6 非严格面跳过 ✓、④ 终点行首非点号调用 ⇒ 真空 ✓、⑤a 无 `×N` ⇒ 跳过 ✓、⑤b 无 opener ⇒ 跳过 ✓ ⇒ **①~⑤ 全绿**。
- **影响**：与 6.1 修前**完全同形**的盲区在契约内**仍存 2 个实例**（本轮判据⑤只闭合了 6.1）。不产生假绿以外的风险，但「⑤b 覆盖 6.1 类」的宣称范围应收窄为「候选键名与构造键名一致者」。
- **建议**：作为 **COMPAT-014 判据⑥ 的设计输入**——按目标语言提取范围内构造键名（JSON/YAML 键、JS 对象字面量键）而非复用 symbol 候选，或改为「范围内首行构造的配对闭合行 ≤ 止」口径（需同时回避 3 项合法非配平范围）；并同步收窄本批注释的宣称。

### F-5（P3｜口径披露）判据⑤ 的工具口径强度边界未披露（纯子串计数 + 裸字符配平）
- **位置**：`test/smoke.mjs:2069`（`occ5`）、`:2101`（`{`/`}` 计数）。
- **事实**：`occ5` 为**纯文本子串计数**（无词边界、不剔除注释与字符串内容）；⑤b 的花括号配平同样按裸字符计（不解析字符串/注释中的 `{`/`}`）。
- **影响**：如出现 `ctx.slots.injectExtra(`（含 `ctx.slots.inject` 子串）会令全文件计数漂移 ⇒ 该对**静默跳过** ⇒ `cardPairs<2` ⇒ **红**（fail-closed，但报文为「覆盖 N 对」而非精确定位）；配平口径对含 `{`/`}` 字面量的 JS 范围可能提前/推迟判定闭合。**当前实测零误报/漏报**（本轮静态核验 + Coordinator 复跑 262/0）。
- **建议**：在 ⑤ 注释补一行口径边界（与 F-5① 同款处置）：「计数为子串计数（无词边界、不剥注释/字符串）；配平为裸字符计数」。

### F-6（P3｜口径表述）「产品代码 diff = 0」不精确（实质零行为变化已证）
- **位置**：`CHANGELOG.md` COMPAT-013 条目「**产品代码零改动**」「产品代码 diff = 0」。
- **事实**：`lib/host-contract.mjs` 位于打包面（`package.json:30-37` `files: ["lib/"]`）且被 `lib/host-boundary.js:21` **静态 import**（运行时装载），本 commit 对该文件 numstat 为 **+3/−3 ≠ 0**。**实质零变化可证**：运行时仅消费 `it.face === 1` 项的 `item/kind`（`host-boundary.js:182-194`）与契约级元数据（`:198` `task/schemaVersion/items.length/faces.length`）及 `revisions.at(-1).task`（`:215-217`）——本轮改动的 6.1 `line` / 3.5 `note` / 2.12 `symbol` **均不在任何运行时读取路径上**，且 `revisions[]` 未新增（`[nv-compat]` 归因载荷不变）。
- **影响**：读者据「产品代码 diff = 0」可能低估改动面（打包内运行时文件的字段值确实变了）。口径沿用 COMPAT-012 先例，非本轮新引入。
- **建议**：措辞改「运行时行为与诊断载荷零变化；改动仅含 `lib/` 内契约文件的描述性字段」。**附带**：F-4 修复把新行号写入 free-text（2.12 `symbol` 的 L4404/4406/4407；3.5 `note` 的 L4413/L4422/L4424）——这些正是**下一次漂移源**，应纳入 COMPAT-012-R1 §8 后续建议①的 free-text 批量收口范围。

---

## 6. 非阻断备注

1. **简报行数口径**：简报「`test/smoke.mjs` +115」为**变更行合计**；numstat 实为 **+110/−5**（`host-contract` +3/−3、`CHANGELOG` +9/−1，合计 +122/−9 与简报一致）。与 COMPAT-012-R1 §7.4 同类，不影响结论。
2. **审查基线**：审查对象 `561f164` 之后 HEAD 前移 `fc1dc81`（仅 `.governance/evidence-log.md`/`plan-tracker.md`，未触 3 条审查路径）⇒ 本轮结论对当前 HEAD 有效；工作树 clean。
3. **「未 push」已核**：分支 `main`，`origin/main = 3442f39`，`git rev-list --left-right --count` = behind 0 / **ahead 22** ⇒ 未 push 属实、审查对象未进入远端。
4. **治理记录待更新（Coordinator 侧，非本 commit 改动面）**：`plan-tracker.md:152` COMPAT-013 行仍写「增判据后可捕获」（未采纳 P-01 订正：**计数式判据对 6.1 零分辨力**，实际由 ⑤b 捕获），且描述仅称「范围内出现次数 ≡ 全文件次数」（未体现两子句落地）⇒ 建议收口时同步，避免该行被后续当作验收依据。
5. **判据③ 未扩面的当前判断**：F-5① 要求「扩面前先实现语言口径」——本轮未扩面（`RANGE_STRICT_FACES` 仍 `[2]`），处置正确；面 5/6 现存边界行经实读均非注释（§3.5）⇒ 当前无实害。

---

## 7. 结论与后续

**APPROVED_WITH_NOTES —— `unresolved_blockers = 0`。**

前轮（`COMPAT-012-R1`）**F-1~F-5 五条 P3 全部实质收口**，且本轮最关键的前提订正（纯计数式判据对 6.1 零分辨力 ⇒ ⑤ 落地为「计数 + 构造闭合」两子句）经本轮**静态复现验证成立**：⑤b 对 6.1 旧值产生与自述**逐字一致**的失败报文，对真值绿；⑤a 对 2.13 旧值 5≠6 红。两个新 golden（11、2）经独立复算成立，断言计数 260→262 自洽，无既有断言被削弱。**P0=0，可合并/推送。**

本轮 6 条 P3 全部为**注释事实性 / 披露口径 / 覆盖缝边界**问题，无一是逻辑缺陷或安全/行为风险：
- 建议**随本次合并顺手修正**：F-1、F-2、F-3、F-5（纯注释/消息文本，单点、零风险）、F-6（CHANGELOG 一句措辞）。
- 建议**并入 COMPAT-014**（已立项，`plan-tracker.md:153`）：F-4（判据⑥ 需按目标语言提取构造键名，而非复用 symbol 候选）＋ F-6 附带的 free-text 行号批量收口。

**未由本轮独立验证的事项（如实声明）**：smoke 262/0、`node --check ×5`、`validate-preset 29/29`、`ci-mock-face 4/4` 的实际执行结果（角色工具约束禁命令行；采信 Coordinator 独立复跑 + 本轮静态自洽复算）。
