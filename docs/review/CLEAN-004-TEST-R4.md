# CLEAN-004 测试审查报告 — R4（定点返工后·轻量终审）

**ROUND**：**R4** · **REVIEW_TYPE**：**test-review（light / 定点返工终审）**
**PREV_ROUND_REF**：`docs/review/CLEAN-004-TEST-R3.md`（**BLOCKED**，BLOCKING 2 = `R3-01`/`R3-02`（均 P2）+ P3 ×6；`unresolved_blockers = 2`）+ 上下文 `docs/review/CLEAN-004-TEST-R2.md`
**审查范围（用户已裁定，不扩范围）**：只核 **R3-01 / R3-02 / R3-04 / R3-05 / R3-06 / R3-07 / R3-09（六处定点返工）+ R3-03（P2 非阻断）+ 不变量**；**未**重审整份资产
**审查姿态（重要背景）**：本轮返工的**剩余七项由 Coordinator 直接编辑完成**（QA subagent 再次失败，仅完成 R3-01 的机生成）⇒ **作者与被审对象同源**。本报告**不采信任何「已声明订正」**，逐字核对实体（文件 + 行号 + 实体读数）。
**审查者角色**：Test Reviewer Agent（只读；**唯一写入 = 本文件**；未修改任何被审文件 / 探针 / 清单 / `.governance/**`）
**仓库 / HEAD**：`D:\AI\agent\deepseek\harness\writing-workflow` / `21b100ce9ac32e534c6215e507f9835f87e0ff4c`
**日期**：2026-09-14 · **机录**：本轮 REVIEW 持久化（`review-record`）由 Coordinator 执行（审查边界禁止本 Agent 写 `.governance/**`）

---

## 0. 结论

**VERDICT: BLOCKED**（**`unresolved_blockers = 1`**：`NEW-R4-01`，P2）
**发现计数：P0 = 0 · P1 = 0 · P2 = 1（BLOCKING）· P3 = 4**

**一句话**：用户裁定的**六处定点返工 + R3-03 + 不变量 —— 8/8 逐条成立，逐字核对通过**（`R3-01` 的 (a)(c)(d) 三行现全部为本轮记录运行取值且确由脚本机读生成；`R3-02` 三小项落地；`R3-04`/`R3-05`/`R3-06`/`R3-07`/`R3-09`/`R3-03` 均按 R3 建议订正；两脚本 sha256 未变、本轮 0 次完整运行、`tally 49/42/4/3` 与「D-3 已撤回」口径未被破坏）；**但在核对 `DEFECTS §2` 的过程中，发现同一节内仍有 3 处与原件矛盾的错值格 + 4 处传播点**（其中 2 处属 R2 阻断项 `N-05`/`F-12` 的**未彻底闭合**：`D4` 的 `report-run1.json` 取值仍被写成记录运行读数；`discovery` 行的 `D4` 三格全错；`E1~E4`/`F1` 仍写 `N-A ×5` 而实体为 `PASS ×5`）⇒ **硬门槛「文档 ⇄ 实体一致 = 100%」FAIL**。
按 `agents/test-reviewer.md` 复审规则 (3) 与 `skills/test-review/SKILL.md`「超过复审 fuse 的 `NEEDS_CHANGE` 必须升级为 `BLOCKED`」：**round = 4 ≥ 3 且仍有 BLOCKING ⇒ 本轮判 BLOCKED 并升级用户裁定**。

> **成本提示（给裁定者）**：`NEW-R4-01` 的修复是 **5 处文本订正（§6 已给出逐处建议文案）**，**无需重跑探针**、不改变任何断言结论；`NEW-R4-02~05` 为 4 条 P3（各 1~2 行文本/脚本口径）。

| 维度 | 结论 | 一句话依据 |
|---|---|---|
| 1. 策略完整性 | 满足（沿用 R3 独立复核） | 三态口径、9 处 N-A 门控与谓词互斥、共享登记表 + `evalPred` 唯一判定入口、隔离/清理/回归/脱敏链 —— 本轮**未**重审（不在范围），未发现返工引入变化（探针/自证脚本 sha256 未变） |
| 2. 用例质量 | 满足（本项本轮未重审） | 49 条断言、9 谓词 19 red / 11 ok 实体未变（`report.json` + 脚本 sha256 未变即等价） |
| 3. 覆盖率 | 满足（本项本轮未重审） | 未受返工影响（`checklist` 除 D-07/§10 外未变） |
| 4. 证据充分性 | **不满足（文档面）** | 六处返工全部落地 ✓，但 `DEFECTS §2` 仍有 3 处错值格与原件矛盾（`NEW-R4-01`） |
| 5. 可复现性 | **基本满足（附 P3）** | 六处口径订正到位；`gen-defects-evidence.mjs` 真从 `report.json` 机生成且内容逐字可复现，但**文档化的 `--check` 自校验当前报 `DRIFT`（exit 3，纯 EOL 差异）**（`NEW-R4-02`）；新入仓生成器未登记进 `README §3` 清单（`NEW-R4-03`） |

---

## 1. 核验方法（全部只读；**本轮 0 次探针运行**）

| 手段 | 命令 / 读取面 | 结果 |
|---|---|---|
| 哈希 | `Get-FileHash -Algorithm SHA256`（探针 ×2、自证脚本、`DEFECTS.md`、`README.md`、`checklist`、`report*.json`、`gen-defects-evidence.mjs`） | 探针 = `DE2DE511…9065B65`；自证脚本 = `5AFB8663…408159A`；`DEFECTS.md` = `CE6D2AB9…F705185`；`checklist` = `C6FCCB03…E41650CB4D`（均与预期/README 一致） |
| README §3 全表对账 | 21 行 ×（大小 + sha256）逐项 | **20/20 非自指行与实体逐项一致**；`README.md` 行为自指（见 §2-5） |
| `report.json` 实体读数 | `ConvertFrom-Json` + `node -e` 逐字段 | `tally {49,42,4,3}`、`ok=false`、`crash=[]`、`head=21b100c`、`headDirty=false`、`05:13:45.162Z→05:16:55.181Z`（190.019 s） |
| 机生成复算 | `node gen-defects-evidence.mjs --print` **只读** + 逐行 `-ceq` 比对 | 仓内标记块 5 行与复算输出**逐字符相同**（144/197/383/604/427 字符） |
| 机生成自校验 | `node gen-defects-evidence.mjs --check`（**只读**，不改仓内文件） | **`DEFECTS-EVIDENCE-CHAIN DRIFT` / exit 3** ⇒ 归因见 `NEW-R4-02`（纯 EOL） |
| 跨运行原件对照 | `report-run1.json` / `report-rework-run.json` / `report-rework-discovery.json` / `report.json` 逐断言读 | 发现 `NEW-R4-01`（3 处错值格） |
| stdout 原件对照 | `rework-discovery-stdout.log` / `record-run-stdout.log` 逐行读 | discovery `L49 = OK D4-auto-create-book-persisted`（与 JSON 一致，反证 `DEFECTS.md:85` 三格全错） |
| 文件时间戳取证 | 4 张 PNG `CreationTime` / `LastWriteTime` 实测 | `12:17:04`（01/03/04）/`12:42:25`（02）与 `12:38:44`/`12:41:08`/`12:41:20`/`12:38:53` |
| 图像独立复核 | `read_image` 原图 + 自建 3~4× 放大裁切（`%TEMP%\r4-img`，用后删除） | 图内「失效绑定书」行带状态后缀、被测卡「孤星纪元」行**无**状态后缀 ⇒ 与 `README:81` 撤注理由相容（字级 OCR 受原图清晰度限制，见 §6-残余 7） |
| 仓库写面 | `git status --porcelain`（全量 + 产品面） + `docs/**` mtime 扫描 | 产品面（`lib/ test/ scripts/ agent-presets/ package.json README.md CHANGELOG.md`）**无输出**；`docs/**` 返工窗（>13:30）仅 3 个交付文件 + R3 报告 |
| 用户环境 | 只读 `~/.dsh/.agent-presets/novel-writing/.dsh-bundle-version` | `0.5.2`，mtime **09-12 19:51:40**（未变）⇒ 用户实例/浏览器未被触碰 |
| 残留 | `%TEMP%\clean004-*` 计数（本轮结束后） | **0**（本轮自建 1 个裁切目录已删除） |

---

## 2. 逐条核对表（用户清单 9 项 · 100% 有判定）

### 2-1 `R3-01`（P2 阻断）`DEFECTS §2` 判别证据链 — **通过**

| 核对点 | 实体读数（本轮实测） | 判定 |
|---|---|---|
| 表头自述（`DEFECTS.md:93`） | 现为 `实测值（记录运行 = 权威 \`report.json\`）`；所填 5 行取值**全部**存在且等于 `report.json` 对应字段 | **不再为假** ✓ |
| 机生成标记 | `DEFECTS.md:95` = `<!-- BEGIN: DEFECTS-EVIDENCE-CHAIN (generated by gen-defects-evidence.mjs) -->`、`:101` = END；块体 = `:96-100`（(a)(b)(c)(d)(a′) 5 行） | ✓ |
| (a)（`:96`） | 文档 `bindings.zz-first-probe = session-22721558-1eda-4029-bcfc-d2b3c40d7a5e`（HTTP 200）↔ `facts.overviewProbe.status=200`、`bindings['zz-first-probe']` 同值 | ✓ 本轮取值 |
| (b)（`:97`） | `settingsYaml 含该 id = true`（代码实测 `includes(boundId)===true`）、`boundIdOnDisk=true` | ✓ |
| (c)（`:98`） | `sessionStore["--C-Users-peter-AppData-Local-Temp-clean004-probe-WcBMlz-novels--"] = ["session-22721558…","session-e8df27a0…"]`（2 项，去重后同） | ✓ 本轮取值（**已无** `session-3c7518c9…`/`session-0c256d5a…` 等 R1 返工取值） |
| (d)（`:99`） | `workspaces["c986fb89-c663-40c4-bcfa-b954fd02738b"].sessionIds` 首项 = `session-22721558-1eda-4029-bcfc-d2b3c40d7a5e`；`updatedAt=2026-09-14T05:14:04.079Z`；`createdAt=2026-09-14T05:13:45.146Z`（差 18.933 s ⇒ 文档「19 s」✓）；跨运行副证逐字成立（`report-run1.json`：`workspaces["5a763818-0397-475b-940e-2bc6302ea268"]`、首项 `session-52f36262-4f02-4956-a979-d5efaa3a6d2e`、`updatedAt=2026-09-14T04:06:02.503Z`） | **自述为真** ✓ |
| (a′)（`:100`） | `facts.sessionAckIds` 逐字一致（`boundId` / `hostWorkspaceSessionIds` 2 项 / `source` 三面串） | ✓ |
| 生成器真伪 | `gen-defects-evidence.mjs` 只读 `report.json`（`L41-42` 载入、`L52-90` 逐格抽取），除 fixture 键名 `zz-first-probe`（键非值）外**无任何硬编码 id/时间戳**；`--print` 复算输出与仓内块**逐字符相同** | **真机生成** ✓ |
| 可复跑性 | `--check` = **DRIFT / exit 3**，但差异**仅行尾**（`DEFECTS.md` 全文 CRLF=261、bare-LF=0；块区 CRLF=6；生成体为 LF）⇒ 内容零漂移；详见 `NEW-R4-02`（P3） | ⚠ 内容一致、门禁未过（非本次返工的取值问题） |

**判定：通过**（R3-01 的原文要求「(a)(c)(d) 全部改填本轮记录运行取值 + 表头/(d) 自述不再为假 + 机生成且可复算」**逐条成立**；`--check` 的 EOL 门禁单列为新 P3）。

### 2-2 `R3-02`（P2 阻断）`README §3` 截图口径 — **通过**

| 子项 | 实体读数 | 判定 |
|---|---|---|
| ① `03` 图注「D-2 可见形态」撤下 + 写明理由（`README.md:81`） | 现文：「**原注「D-2 可见形态」已撤下（R3-02）**：R3 放大实读表明，图中带「会话失效」后缀的是 fixture 的失效绑定书目（`nn-stale-bound…`），**被测卡「孤星纪元」无状态后缀（= 已绑定）** ⇒ 本图**不构成 D-2 的视觉取证**」。我方独立 3~4× 放大复核：抽屉「失效绑定书」行末尾有状态后缀、被测卡「孤星纪元」行（`outline_writing · 3章 · 1200字`）**无**后缀 | **已撤下并写明理由** ✓（独立复核相容） |
| ② 取证窗口 ↔ 文件实体（`README.md:92`） | 文档 `12:38:44`(01)/`12:41:08`(02)/`12:41:20`(03)/`12:38:53`(04) ↔ `Get-Item` 实测**逐项一致**；`CreationTime` = 01/03/04 `12:17:04`、02 `12:42:25` ↔ 实测一致；「均落在 R1 返工运行窗口（本地 12:38–12:41）」↔ `report-rework-run.json` `04:38:34.920Z→04:41:27.904Z` ✓；「不在本轮记录运行窗口（13:13:45–13:16:55）」↔ `report.json` `05:13:45.162Z→05:16:55.181Z` ✓；原「其余三张为本轮重新取证」已明确标为**误**并订正 | **与实体一致 + 轮次归属明确** ✓ |
| ③ 「D-2 无视觉取证」显式登记（`README.md:92` 末） | 「⇒ **D-2 无视觉取证**：其证据面仅为 `C3b`/`C9` 断言与 `facts.convergence`（13/13 帧 `dotSt='stale'`、`converged=false`）及同轮 `D4`=PASS 的收敛上界」；实体：`facts.convergence.samples=13` 全 `stale`、`converged=false` ✓、`D4`=PASS ✓ | **已登记** ✓ |
| 附带断言「01 与 R1 期 sha256 逐字节相同」 | 无可复核基线 ⇒ 单列 `NEW-R4-04`（P3，非阻断） | ⚠ |

**判定：通过**（R3-02 三小项全部落地；附 1 条 P3）。

### 2-3 `R3-04` `README §6` 隔离根 — **通过**

- `README.md:181`：环境变量重定向现写 `…\clean004-probe-**WcBMlz**\*`，并在同格注明「**R3-04 订正**：原写 `v6c9fI` 系笔误，该串为 R2 前一轮运行的隔离根」。
- 实体：`report.json.isolation.root = C:\Users\peter\AppData\Local\Temp\clean004-probe-WcBMlz`，`isolation.env` 六项（`DSH_HOME`/`USERPROFILE`/`HOME`/`APPDATA`/`LOCALAPPDATA`/`TEMP`）全部落在该根下 ✓；`v6c9fI` 确为 `report-rework-run.json` 的隔离根（R1 返工运行）✓。
- 全仓检索 `v6c9fI|WcBMlz`：仅 `README.md:52`（记录运行根）、`:181`（订正后）、`:194`（清理）三处，**均为 `WcBMlz`**；`v6c9fI` 仅出现在 `:181` 的订正说明中 ⇒ **同一文档内不自相矛盾** ✓。
- 残留说明：`§7` 的清理断言（`cleanup.rootRemoved=true`、`%TEMP%\clean004-*`=0）与实体一致 —— 本轮独立复核 `%TEMP%\clean004-*` 计数 = **0** ✓。

**判定：通过**

### 2-4 `R3-05` D-2 时序下界 — **通过**

| 口径落点 | 实体文本 | 判定 |
|---|---|---|
| `README.md:22`（N-05 摘要）/`:104`（§4 表） | 均为「**≥2′06″ 未收敛、≤2′31″ 内收敛**」；`:104` 括注下界锚点 = `05:14:05.991Z → C9.reopenDiag.drawerSubs[0] 05:16:12.460Z` | ✓ 统一 |
| `DEFECTS.md:83`（跨运行表记录运行行）/`:88`②/`:89`③/`:218`（§4） | 均为「**≥ 2′06″ 未收敛、≤ 2′31″ 内收敛**」 | ✓ 统一 |
| `checklist:96`（`D-07` 行） | 「实测时限 **≥2′06″ 未收敛、≤2′31″ 内收敛**，按 R3-05 订正」 | ✓ 统一 |
| 锚点成立性（实体复算） | `facts.convergence.samples[0].t = 2026-09-14T05:14:05.991Z` ✓；`C9-width-memory` 断言 `at = 05:16:12.460Z`，其 `reopenDiag.drawerSubs[0] = "outline_writing · 3章 · 1200字 · 会话失效"` ✓ ⇒ 下界跨度 **126.469 s = 2′06.5″**（文档括注写 `2′06.5″` ✓）；`D4` `at = 05:16:37.313Z` 且 `autoCreateBound={sub:'outline_writing · 3章 · 1200字', bound:true}` ⇒ 上界跨度 **151.322 s = 2′31.3″**（文档括注写 `2′31.3″` ✓） | ✓ 锚点真实、数值可复算 |
| 原 `1′46″` 语境 | 仅出现于**订正说明**：`README.md:22`/`:120`、`DEFECTS.md:88`/`:218`、`checklist:96`（均带「原写…系下界锚点取错/低估」字样）；旧括注「`05:15:52Z` 前后 20 s 等待仍超时」已被明确标为**不可能**并给出正确窗口 | ✓ 无残留误用 |
| 附带 | 上界写法为整秒截断（`2′31″` vs 实测 `2′31.3″`）⇒ 单列 `NEW-R4-05`（P3） | ⚠ |

**判定：通过**（三文档 + 清单**统一为同一口径**，锚点与数值均实体成立；附 1 条 P3）。

### 2-5 `R3-06` `README §3` 自指哈希 — **通过**

| 核对点 | 实体读数 | 判定 |
|---|---|---|
| 互矛盾两值已删 | 检索 `35858`/`35576`/`4a8b4cc2`/`d7bd9e17`：**仅**命中 `README.md:89` 的删除声明本身（「原『35576 B / `d7bd9e17…`』与表内『35858 B / `4a8b4cc2…`』两处互相矛盾的表述**已删除**」） | ✓ 已收敛为单一口径 |
| 单一口径 + 现场实测指令 | `README.md:86` 行值 = `35979 B / 21423fe5…`，同行注明「**自指**：上列大小/哈希 = **R3 定点返工写入前**的实体读数；本次返工写入后本文件变大，**R4 复核以现场 `Get-FileHash` 为准**」；`:89` 给出命令 `Get-FileHash docs\evidence\CLEAN-004\*.md -Algorithm SHA256` | ✓ |
| `DEFECTS.md` 返工后实测 | 实体 **34037 B / `CE6D2AB9…F705185`** ↔ 预期 `34037 B / ce6d2ab9…` ✓ ↔ `README:83` 行同值 ✓ | ✓ 一致 |
| `checklist` 返工后实测 | 实体 **42669 B / `C6FCCB03…E41650CB4D`** ↔ 预期 `42669 B / c6fccb03…` ✓ ↔ `README:88` 同值 ✓ | ✓ 一致 |
| `README.md` 自身（自指） | 现场实测 **39381 B / `AE9791B8…1F31BEB932`**（与行内「写入前」值不同，属自指定义内） | ✓ 非矛盾 |
| 其余 20 行 | 20/20 与实体逐项一致（含 4 张 PNG、2 探针副本、2 stdout、6 份 report、2 前任脚本） | ✓ |

**判定：通过**（`README.md` 写入前哈希 `21423fe5…` 不可独立复核 ⇒ 记入 §6 残余 7，不是发现）。

### 2-6 `R3-07` `DEFECTS §3.2` 反驳 2 的跨运行分列 — **通过**

- `DEFECTS.md:158` 现文：`**R1 返工运行** `D4` = **PASS**（`04:41:17Z`，`byId` 命中 ⇒ 镜像含 `session-c0cf54dd`），而 `B21a`（`04:41:19Z`）声称镜像为空 —— **R3-07 订正：该 04:41 时刻属 R1 返工运行；本轮权威记录运行的 `D4` = PASS 于 `05:16:37.313Z`，两者分属不同运行，引用时不得混写**`。
- 实体：`report-rework-run.json` 的 `D4` = **PASS** @ `04:41:17.546Z`、`B21a` = PASS @ `04:41:19.806Z`（同轮）✓；`report.json` 的 `D4` = **PASS** @ `05:16:37.313Z` ✓ ⇒ **已把 `04:41:17Z` 标为 R1 返工运行，并与本轮 `05:16:37.313Z` 分列**。

**判定：通过**

### 2-7 `R3-09` `README §10` 闸门行登记 — **通过**

- `README.md:264`（§10-7）现文：「**豁免台账登记（R3-09）**：该断言在报告 `assertions[]` **无行**（闸门未进入即不产生行）；本轮**不改探针**（保「记录运行修订 = 终版」不变量），故以本局限条 + 建议任务登记，**不在报告中伪列 N-A 行**」。
- 实体核对：`report.json.assertions` 共 49 条、**id 含 `C-face` 者 = 0** ✓；`facts.cFaceGate` **不存在** ✓；`predicateStaticSites["C-face-availability"] = 1` ✓ ⇒ 三点自述与实体一致。

**判定：通过**

### 2-8 `R3-03`（P2 非阻断）机证强度登记 — **通过**

- `README.md:273`（§10-12）现文：标题即「**同源机证的强度上限（R3-03，P2 非阻断，本轮不改探针）**」；正文含「`staticCallSiteCounts()` 是**源文本计数代理**——R3 的注入④（保留一个**不可达**的 `evalPred` 调用 + 部署面**就地复制**等价表达式）下 `BINDING-OK=true / CHECK-OK / exit 0`，即『判定不再消费登记表而检查仍绿』在本机**可构造**」+「当前 9/9 站点经**人工实读**确认同源成立，但**机证强度 < 其声称**」+ **三条硬化选项**① 运行期带标记对象 ② AST 断言 ③ 显式豁免台账，并标注「**建议任务**，拟并入 CLEAN-006」。
- 判定：**如实登记「强度 < 声称」+ 含注入④可构造性 + 三条硬化选项 + 归口建议** ⇒ 与 R3-03 的建议一致。

**判定：通过**

### 2-9 不变量（硬核） — **通过**

| 不变量 | 实测 | 判定 |
|---|---|---|
| 探针 sha256 | `probe-clean-004.mjs` = `DE2DE511E89A8CCC6D08B29D529E51336079BC3CEAB4B937795DAD88D9065B65` = `de2de511…`（副本 `…final-rev-b1f1c8c0.mjs` 同值；mtime `13:13:36` 早于返工窗） | ✓ **未变** |
| 自证脚本 sha256 | `falsifiability-check.mjs` = `5AFB8663E6A9C25D3D44F2B2D5A2CA6E6BB6B3C282C9DA38982A81A26408159A` = `5afb8663…`（mtime `13:01:25`） | ✓ **未变** |
| 本轮完整运行次数 | `report.json` 仍为 `05:13:45.162Z→05:16:55.181Z`（`headDirty=false`、`crash=[]`）、hash `A6D8AE0F…`、mtime `13:16:55`；`report-rework2-run.json` 同 hash 同 mtime；目录 **22 个文件**（= R3 期 20 项 + `README.md` + 新入仓 `gen-defects-evidence.mjs`），**未新增任何 report 文件 / stdout**；`%TEMP%\clean004-*` = **0** | ✓ **0 次** |
| `tally` 口径 | `{"total":49,"pass":42,"fail":4,"na":3}`；三文档逐项一致（`README:29/282`、`checklist:252/256-257`、`DEFECTS:9/37`）；FAIL = `D2-esc`/`C3b`/`C9`/`C10`，N-A = `B5`/`C11`/`D6`（实体逐条核对） | ✓ **未被破坏** |
| 「D-3 已撤回」口径 | 全仓 `D-3` 命中逐条核读：`DEFECTS:140`（标题删除线 + 已撤回）、`:37/:242`（不计入）、`:33/:34/:106/:170/:219`、`README:6/20/83/107/118/142/299/311`、`checklist:48/258/260/262/268` —— **无一条**将其表述为产品缺陷 | ✓ **未被破坏** |
| 写面边界 | `git status --porcelain -- lib/ test/ scripts/ agent-presets/ package.json README.md CHANGELOG.md` = **空**；`HEAD=21b100c`；`docs/**` 返工窗（>13:30）仅 `DEFECTS.md`(17:36:33)/`README.md`(17:36:48)/`CLEAN-004-checklist.md`(17:36:16) + `gen-defects-evidence.mjs`(13:56) + R3 报告(13:49) | ✓ 定点返工无越界 |
| 用户环境 | `~/.dsh/.../.dsh-bundle-version` = `0.5.2`，mtime `09-12 19:51:40`（未变）；本轮只读命令未触碰 | ✓ |

---

## 3. 本轮发现列表（P0~P3；每条含文件:行号 + 实体读数 + 影响 + 建议）

| # | 文件:行号 | 级别 | 依据（实体事实） | 影响 | 建议 |
|---|---|---|---|---|---|
| **NEW-R4-01** | `DEFECTS.md:131`、`DEFECTS.md:85`、`DEFECTS.md:132`（+ 传播 `:89`③、`:102`(e)、`README.md:22`、`README.md:120`） | **P2（BLOCKING）** | 三处错值格，均与所引原件矛盾（逐字读数见下）：**① `:131`** 写 `D4-auto-create-book-persisted` = `FAIL {autoCreateBound:{sub:'…会话失效', bound:false}}`（表头 `:125` 声明「均已在**记录运行**中体现」），实体 = **PASS** `{sub:'outline_writing · 3章 · 1200字', bound:true}` @ `05:16:37.313Z`；所写值实为 `report-run1.json` 的 `D4`（**FAIL** @ `04:10:28.407Z`）⇒ **跨运行误植**（= R2 阻断项 `N-05`/`F-12` 的原样残留，`README:120` 却记「N-05 ✅ 已修复」）。**② `:85`** 跨运行表 `discovery` 行三格「`FAIL` / `bound=false` / 不收敛」全错，实体 = **PASS** @ `04:37:28.093Z`、`bound=true`；同轮 `C3b`=FAIL（`converged=false`，采样窗 `04:35:05.402Z→04:35:17.459Z` 13/13「会话失效」）但 `D4` 时刻已 bound ⇒ 与记录运行**同形态（迟滞后收敛）**，自首采样 ≤ **2′22.7″**；该轮 stdout `L49` 亦为 `OK D4-auto-create-book-persisted`（JSON 与转录一致）。**③ `:132`** 写 `E1-E4 / F1 = N-A ×5`，实体 = **PASS ×5**（`E1-short-notice-compact` @`05:16:26.733Z`、`E2-long-notice-flow` @`05:16:27.922Z`、`E3-long-notice-geometry`、`E4-notice-close`、`F1-split-claim-event`）；本运行 N-A 总数 = **3**（`B5`/`C11`/`D6`）⇒ 与 `DEFECTS.md:224`（「E/F 面本轮全部实测 PASS」）、`§0`、`README §4`、`checklist C-13~C-16` **自相矛盾**；亦与 `:87`（「`D4`=FAIL 出自 `run1`」）自相矛盾 | ① 交付文档对**自身所引原件**作出假自述（与 R3-01 同类，且为 R2 阻断项的未彻底闭合）；② D-2 的**频次**主张失效（「3 轮中 2 轮不收敛」⇒ 实际仅 `run1`（R1 期复跑、已废止修订）不收敛，记录运行与 discovery 两轮均为「窗口内 `stale` → 其后 bound」，差异在**观测时延**）；③ 破坏硬门槛「文档 ⇄ 实体一致 = 100%」；④ 移交 Developer（BUG-009）时给出与被引原件相反的状态/读数 | **A（推荐）定点返工 5 处文本（无需重跑探针）**：① `:131` → `PASS` + `{sub:'outline_writing · 3章 · 1200字', bound:true}`（`05:16:37.313Z`）并注明「`FAIL {bound:false}` 属 `report-run1.json`（`04:10:28.407Z`），不得混写」；② `:85` → `PASS`（`04:37:28.093Z`）/ `bound=true` /「采样窗 13/13 `stale`、`D4` 已 bound ⇒ ≤2′22.7″ 内收敛（同记录运行形态）」；③ `:132` → `PASS ×5`（`05:16:26.733Z~05:16:48.776Z`；走 `mm-third-probe` 未绑定卡路径，不依赖分栏重开）+ 本运行 N-A = 3；④ `:89`③、`:102`(e)、`README:22`、`README:120` 的「`run1`+`discovery` 不收敛 / `D4`=FAIL」→「仅 `run1` 全程不收敛；另两轮为迟滞后收敛」；⑤ 顺带由 QA 重述 D-2 的「非确定性」依据（**不影响 D-2 的 P2 定级** —— 记录运行自身仍为 ≥2′06″ 未兑现契约）。**B** 仅当用户明示接受「已知局限」且有另立任务承接该订正时才可（详见 §5） |
| **NEW-R4-02** | `docs/evidence/CLEAN-004/gen-defects-evidence.mjs:100,118-129`（比较逻辑）+ `DEFECTS.md`（CRLF） | P3 | `node gen-defects-evidence.mjs --check` ⇒ **`DEFECTS-EVIDENCE-CHAIN DRIFT` / exit 3**；差异**仅为行尾**：`DEFECTS.md` 全文 CRLF=261 / bare-LF=0（块区 CRLF=6），而 `buildRows()` 产出 bare `\n` ⇒ `sec.body === generated` 恒假。内容层：仓内块 5 行与复算输出**逐字符相同**（144/197/383/604/427 字符） | ① 文档化的**自校验命令当前不通过**，复核者无法区分「EOL 噪声」与「真漂移」，与 R3-01 要求「机生成 + 可复跑一致」的**可验证性**背道而驰；② 若执行 `--write`：块体写成 LF 而文件其余为 CRLF（**混合 EOL**），且写入后 `--check` 才会转绿 ⇒ 门禁形态脆弱 | 把比较改为行尾无关（`sec.body.replace(/\r\n/g,'\n') === generated`）或写入时套用文件主 EOL；并在 `README §3`/`§10` 补一句「`--check` 仅核内容、忽略行尾」 |
| **NEW-R4-03** | `README.md:64-86`（§3 表）vs 目录 | P3 | 目录内 **22 个文件**，§3 表 **21 行**（20 文件 + `README.md` 自指行）⇒ **`gen-defects-evidence.mjs` 无登记行**（其余 21 个文件逐行可对上，行匹配式为 `` | `名` | 数字 | ``） | 该生成器是本轮为消除跨运行漂移而新增的**证据生产件**，却不在哈希清单内 ⇒ 复核者无法用同一手段校验「生成器本体未被改动」（本轮由我方实测 `7909 B / 1780074057B61E1017B0A2DC0E9A9C15B927495A30B6A5C39393B1E8D291328B`） | 在 §3 表补一行：`gen-defects-evidence.mjs` = 7909 B / `1780074057B61E1017B0A2DC0E9A9C15B927495A30B6A5C39393B1E8D291328B`（说明：`DEFECTS §2` 证据链的机生成器，只读 `report.json`） |
| **NEW-R4-04** | `README.md:92`（末句「其中 `01-drawer-idle.png` 与 R1 期 sha256 **逐字节相同**」） | P3 | 该「R1 期」基线在记录面**不存在**：检索 `docs/**/*.md`、`.governance/*.md` 中 `82627`（01 现哈希前缀）与 `docs/review/CLEAN-004-TEST-R{1,2,3}.md` 中 4 张 PNG 当前 sha256 ⇒ **0 命中**；且 01 的 `LastWriteTime=12:38:44` 落在 **R1 返工窗口**（`04:38:34.920Z→04:41:27.904Z`）内 ⇒ 该文件在那一轮**曾被写盘** | 一处**不可复核的不变量断言**被表述为事实（R3-02 返工时的附带表述）；不影响四图轮次归属结论（②已成立） | 删去该分句，或补「基线出处」（如 R1 期记录的哈希文件/commit）；若无法给出基线，改述为「01 与前一版同名文件同名同尺寸（`110199 B`），本轮未再取证」 |
| **NEW-R4-05** | `DEFECTS.md:83`（及 `:88`、`README.md:22`/`:104`、`checklist:96` 同口径） | P3 | 上界写法「≤ **2′31″**」为**整秒截断**，实体跨度 = `05:14:05.991Z → 05:16:37.313Z` = **151.322 s = 2′31.3″**（同一括注内已写 `2′31.3″`）⇒ 严格上界应 ≥ 实测值（0.322 s 越界）。（该措辞源自 R3-05 的建议原文「≤2′31″」，属建议的舍入残留；下界 `≥2′06″`（126.469 s）方向保守，正确） | 极小：README 等「摘要口径」中的上界比实测紧 0.3 s，严格复核会显示「越界」（同类问题 R3 已在 `R3-05` 上定级 P3） | 统一改为 **≤2′32″**（保守）或 **≈2′31.3″**（精确），三文档 + 清单同改 |

**汇总**：`NEW-R4-01` = 1 条 BLOCKING（含 3 处错值格 + 4 处传播点）；`NEW-R4-02~05` = 4 条 P3（非阻断）。
**定级说明**：`NEW-R4-01` 判 **P2（阻断）** 与 `R3-01`/`R3-02` 的既有标尺一致 —— 「文档自述 ⇄ 所引原件矛盾」且落在**向 Developer 移交的判别面**上；而 `NEW-R4-02/03`（工具/清单面）、`NEW-R4-04`（不可复核断言）、`NEW-R4-05`（舍入）与 `R3-04`/`R3-06`（内部/自指不一致）同类，判 **P3**。

---

## 4. 硬门槛裁决

| 门槛项 | 阈值 | 实测 | 裁决 |
|---|---|---|---|
| P0 阻塞问题数 | = 0 | **0**（本轮新发现最高 P2；交付侧 D-1 P3 / D-2 P2 / P0=0） | ✅ **PASS** |
| R3 六处 + R3-03 逐条有处置判定 | = 100% | **8/8**（`R3-01`✓ `R3-02`✓ `R3-04`✓ `R3-05`✓ `R3-06`✓ `R3-07`✓ `R3-09`✓ `R3-03`✓；逐条依据见 §2，全部附实体读数与行号） | ✅ **PASS** |
| 文档 ⇄ 实体一致 | = 100% | **不成立**：`DEFECTS §2` 3 处错值格与原件矛盾（`:131` D4 状态/读数、`:85` discovery 三格、`:132` E/F 面状态）+ 4 处传播点 ⇒ `NEW-R4-01` | ❌ **FAIL** |
| 不变量 | 两脚本 sha256 未变 + 本轮 0 运行 | **PASS**：探针 `DE2DE511…9065B65`、自证脚本 `5AFB8663…408159A` 均未变；`report.json` 仍 `05:13:45.162Z→05:16:55.181Z`（hash `A6D8AE0F…`、mtime `13:16:55`）、无新增 report/stdout、`%TEMP%\clean004-*`=0；`tally 49/42/4/3` 与「D-3 已撤回」口径未被破坏 | ✅ **PASS** |
| 每条发现标注 P0~P3 | = 100% | **5/5**（`NEW-R4-01` P2 + `NEW-R4-02~05` P3，逐条带级别与依据） | ✅ **PASS** |

**裁决：BLOCKED** —— 1 项硬门槛未通过（「文档 ⇄ 实体一致」），`unresolved_blockers = 1`。
**熔断判断（T2）**：round = 4 ≥ 3 且仍有 BLOCKING ⇒ 按 `agents/test-reviewer.md` 复审规则 (3) 与 `skills/test-review/SKILL.md` 的 fuse 规则，**MUST 转 BLOCKED + escalation**，由用户裁定处置（选项见 §5）。

---

## 5. 结论可用性 + 处置选项

### 5.1 可用性

- **技术面（可直接复用的部分）**：本轮**未发现**任何新增的机制/谓词/隔离/回归缺陷 —— 两脚本 sha256 未变即证明测试资产本体在返工中**零改动**；六处定点返工**全部落地且逐字可对账**；不变量全部保持。R3 已独立复核成立的组面（三态口径、9 处 N-A 门控与谓词互斥、同源注入两向皆红、隔离/清理/真实环境零污染、回归基线、脱敏、双运行/跨运行对照、修订=终版）**依旧成立**。
- **本轮判定**：因 `NEW-R4-01`（`DEFECTS §2` 3 处错值格 + 4 处传播点，均为**文本面**），该验证资产**暂不能**作为 CLEAN-004 自动化面的「可交付记录」使用（硬门槛「文档 ⇄ 实体一致 100%」未达）。
- **完成 `NEW-R4-01` 订正后即可作为记录使用的**使用前提****：
  ① `HEAD = 21b100ce9ac32e534c6215e507f9835f87e0ff4c` ∧ `lib/client.js` 未改动（`headDirty=false`）；
  ② 期望值 = **`tally = 49 / 42 / 4 / 3`、`exit 1`**（4 FAIL = D-1 ×1 + D-2 及其下游 ×3；N-A 3 = `B5`/`C11`/`D6`）；
  ③ **与 `README §10` 的 12 条局限同读**；
  ④ D-2 的**频次**表述按订正后口径（仅 `run1` 不收敛；另两轮为迟滞后收敛），其 **P2 定级不受影响**。

### 5.2 处置选项（escalation）

| 选项 | 内容 | 成本 / 风险 | 适用 |
|---|---|---|---|
| **A. 定点返工（推荐）** | 仅订正 `NEW-R4-01` 的 **5 处文本**（§3 已给逐处建议文案）+ 可选 `NEW-R4-02~05` 四条 P3（各 1~2 行 / 脚本 1 行） | 成本极低：**无需重跑探针**、不改任何断言与计数；风险：仅文本面 | 需要保持「文档 ⇄ 原件逐项可对账」这一交付标准时（本资产的实际用途即此） |
| **B. 记为已知局限 + 另立任务** | 不改文档，把 `NEW-R4-01` 记入交付说明/BUG-009 移交单（注明「`DISCOVERY`/`D4`/`E/F` 三处以 `report.json` 及其余四份原件为准」），并由 Coordinator 记 decision/risk + 另立订正任务 | 成本最低；风险：文档内继续存在**与被引原件相反**的状态/读数，任何后续机读或人工复核都会再次撞上，且 `R2 N-05/F-12` 将长期处于「声明已修复但实际未闭合」状态 | 发布节奏极紧且明示接受上述代价时（**需用户显式裁定**） |
| **C. 接受现状（不推荐）** | 不处置 | 不建议：`NEW-R4-01` 与 R2/R3 已在 `N-04`/`N-05`/`R3-01` 上定级 BLOCKING 的同类问题（自述与所引原件矛盾），且直指 Developer 定位面 | — |

---

## 6. 残余不确定性（不因订正而消失；须与证据同读）

1. **未整轮复跑探针**（时间盒；沿用 R1/R2/R3 口径）：本轮以「原件逐字段实读 + 机生成脚本复算 + 跨运行对照 + 文件时间戳取证 + 图像放大复核 + 写面/哈希不变量」替代 ⇒ 「`C3b` 的真实根因」「各图确切轮次」仍有不可得面。
2. **镜像体内部读数仍不可得**（`facts.mirrorBodyUnreachable = {reachable:false, reason:'no-mirror-node-found', realms:1, scanned:724}`）⇒ 以渲染面为功能判据；「镜像空 vs 消费面失效」两类根因不可分（`README §10-2`）。
3. **D-2 的收敛分布仍需按新口径重述**：订正后事实为「`run1`（R1 期复跑、已废止修订）全程不收敛；记录运行与 discovery 两轮均为窗口内 `stale`、其后 bound（上界分别 ≈2′31.3″ / ≈2′22.7″）」⇒ 单轮/两轮证据不足以断言「非确定性」的统计特性；D-2 的 **P2 定级**（契约在 ≥2′06″ 内未兑现）**不受影响**。
4. **`C-face-availability` 本轮无正例**（闸门未进入 ⇒ `assertions[]` 无行）⇒ 其可失败性仅由登记表向量 + 静态绑定背书（`R3-09`/`§10-7`）。
5. **`C5` 竞态窗口未注入复现**（沿用 R1/R2 口径：多次干净运行 + 一次崩溃对照 + 机制代码）。
6. **`B21a` 覆盖口径窄**：仅 id 检索分支；按人类标题检索与「镜像 `displayTitle` ≡ 宿主 `displayTitleOf`」一致性未覆盖（fixture 下不可构造）。
7. **`README.md` 自指行的「写入前」哈希 `21423fe5…` 不可独立复核**（无历史基线；其尺寸 `35979 B` 有 R3 的独立实测佐证）⇒ 现场实测值（供对账）= **`39381 B` / `AE9791B8BC2ED2FA220872456249C8A3915A8479BDE8678AC4E712F31BEB932`**。
8. **图像字级复核的清晰度限制**：`03-console-final.png` 为低清渲染，3~4× 放大后中文/后缀能否逐字确认受限 ⇒ 我对 `README:81` 撤注理由的复核是**行级相容**（「失效绑定书」行有状态后缀、被测卡行无后缀），非逐字 OCR 确证；R3 的原始判读（`nn-stale-bound` 失效绑定书）与本轮相容，未见反证。
9. **`R3-08` 不在本轮范围**（`R3-08` 属 Coordinator 侧 `.governance/plan-tracker.md` 治理记录面）：仅作信息记录 —— `.governance/plan-tracker.md:164` 该行仍写「卡片**恒**『会话失效』」「下游牵连 `C9`/`C10`/**`D4`**」「`B21a/B21b` 需按 R2 意见重设计或撤下」，与 `report.json` 实体口径不一致；**未在本报告定级、未纳入裁决**（属治理记录修改，由 Coordinator 处置）。
10. **本轮审查的只读性**：唯一写入 = 本文件；为图像复核在 `%TEMP%\r4-img` 建过 1 个裁切目录（用后删除，复核 `Test-Path`=False）；未对 `C:\Users\peter\.dsh` 做任何写入，未关停/干扰用户 DSH 与浏览器。

---

**审查者**：Test Reviewer Agent（只读；未修改任何被审文件 / 探针 / 清单 / `.governance/**`；唯一写入 = 本文件）
**本报告路径**：`docs/review/CLEAN-004-TEST-R4.md`
**结论**：**BLOCKED**（round = 4；六处返工 + `R3-03` + 不变量 = **8/8 通过**；剩余 BLOCKING **1** = `NEW-R4-01`（P2，`DEFECTS §2` 三处错值格 + 四处传播点）；P0 = 0 / P1 = 0 / P2 = 1 / P3 = 4；`unresolved_blockers = 1`）
