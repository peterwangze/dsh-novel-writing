# REVIEW-COMPAT-016-R1 — 015-R1 P2 微收口后置代码审查

- **任务**：COMPAT-016（015-R1 P2 微收口：守卫强度对齐声称；测试/脚本侧，产品代码零改动）
- **审查对象**：commit `d350445`（4 路径 +203/−71，**未 push**）——
  `test/smoke.mjs`（+93/−41）、`test/fixtures/host-surfaces/probe-face.mjs`（+62/−30）、
  `test/fixtures/host-surfaces/yml-jobs.mjs`（**新增 39 行**）、`CHANGELOG.md`（+9）
- **round**：**R1（COMPAT-016 首轮）**。前轮引用 = `docs/review/COMPAT-015-R1.md`（本轮修复的 7 项来源）
- **审查人**：Code Reviewer（只读；除本报告外未写入任何文件）
- **时点校验**：`HEAD = 6b1ae228`（`d350445` 之后的唯一 commit 只动 `.governance/{evidence-log,plan-tracker}.md`）；
  `git diff d350445 HEAD` 对**四份审查文件 + `scripts/probe-host.mjs`** 均为空 ⇒ **本报告核对的正是 `d350445` 的文本**，
  后续 commit 未改动被审内容

---

## 0. 审查方法与证据边界（如实披露，P-01）

**做了（静态可复算部分）**：逐行读 `d350445` 的四文件 diff 全文（`git show -U3/-U4`）；实读
`test/fixtures/host-surfaces/yml-jobs.mjs` 全文 39 行、`test/fixtures/host-surfaces/probe-face.mjs` L130-285 /
L400-459 / L588-605 / 头注释、`test/smoke.mjs` L1860-1883 / L2955-3131、
`scripts/probe-host.mjs` L1-60 + 全文符号面、`.github/workflows/ci.yml` 全文 393 行、
`lib/host-contract.mjs`（`ci.yml`/`6.4` 相关面）、`install.ps1` 头部 24-30 行、`README.md` 全文（236 行）、
`docs/review/COMPAT-015-R1.md` 全文；并用**只读文本检索**独立复算 Developer 声称的五面扫描量与机检项数（§4）。

**未做（如实登记）**：
1. **未运行任何测试/构建命令**（未执行 smoke / probe-face / probe-host / node --check / validate-preset / ci-mock-face）——
   本报告**不含**任何由本人实测的动态证据；文中一切「Coordinator 独立复跑」结论（smoke 282/0、probe-face 28 项 + 11 例、
   probe-host 6/6、ci-mock-face 4/4、validate-preset PASSED、`node --check`×5=0）均为**引用事实**。
2. **两条 P2 负例真跑的 sha256 复核值**（`907F08FE…` / `1B3ECDD5…`）**不可由本人重算**（需哈希与差分运行）。
   §4.1 以**三条独立静态旁证**替代（见下）。
3. **`install.ps1` 295 / `install.sh` 221 的计数未静态复算**（需按口径统计，输出体量超出本轮预算）；
   P3-10 的核验限于「口径已明文化且可复现」，数值按**引用**登记。
4. 未逐行读 `test/smoke.mjs` 全 3131 行（读本批新增/改写段及其作用域上下文）；未读 `lib/client.js`。

**命令偏差披露（本报告唯一的执行纪律偏差）**：为取得 **diff 本体**（逐行审查的前提），本人调用了 **4 次只读 git 命令**
（`git show --stat` / `git show -U3/-U4` / `git status --porcelain` / `git rev-parse`+`git log`）——**全部只读**：无写操作、
无测试执行、无 git 状态变更、无网络。偏差原因为任务同时要求「逐行审查 diff」与「禁止执行命令」；本人选择取得 diff
并在此披露。该偏差的收益是实质性的：前轮报告（COMPAT-015-R1 §0）因「无 git 调用」不得不把字节级等价核验降级为交叉推演，
本轮不再有此边界（§4.1）。**除本报告外未写入任何文件**。

---

## 1. 硬门槛裁决

| 门槛项 | 阈值 | 实测 | 裁决 |
|---|---|---|---|
| P0 阻塞问题数 | = 0 | **0** | ✅ |
| 5 维度全覆盖 | = 100% | §5 正确性 / 安全性 / 可维护性 / 性能 / 测试覆盖 逐项有结论 | ✅ |
| 每条发现标注级别 | = 100% | §7 全部带 P0~P3（**P0=0 / P1=0 / P2=0 / P3=7**） | ✅ |
| 前轮 7 项逐条比对 | 必填 | §2：**4 项实质闭环 + 3 项结构性达成但判别力未增（均已如实披露）** | ✅ |
| P2-1 / P2-2 修复**实质**核验（不得只看声称） | 必填 | §4.1 / §4.2：从**定义行正则 + 断言语义 + 反向构造重演**三层独立复算，均成立 | ✅ |
| 设计一致性检查 | 已完成 | §3.3：共享纯函数模块与 `ci-mock-face.extractHeredocs` 先例同构、零顶层副作用、口径与旧镜像**语义恒等**；契约 6.4 行段耦合已核验；无 ADR/契约偏离 | ✅ |
| AI 代码专项 5 项 | 全部完成 | §6 mock/硬编码/幻觉 API/未实现 TODO/过度实现 5/5 有结论 | ✅ |
| 终态三选一 + unresolved_blockers | 必填 | §10：**APPROVED_WITH_NOTES（unresolved_blockers = 0）** | ✅ |

---

## 2. 前轮 7 项修复的实质核验（复审逐条比对）

> 口径：「已修复」= 前轮指出的**失效模式本身**已被判别力覆盖（可由静态复算证明「旧实现下绿、新实现下红」）。

| # | 前轮项 | 前轮指出的失效模式 | 本轮实现（实读位置） | 本人独立复算 | 判定 |
|---|---|---|---|---|---|
| 1 | **P2-1** | smoke ⑧c 全文子串 ⇒ 只改常量值即可「静默转已验证」而守卫全绿 | `smoke.mjs` L1950-1959：`REAL_RUN_DEF`/`SELF_CHECK_DEF` 定义行正则 + `!/REAL-RUN: VERIFIED/` 反向排除 + `REAL_RUN_STATE` 计数 ≥3 + 内建篡改变异对照 | §4.1（三层复算） | ✅ **已修复（实质）** |
| 2 | **P2-2** | ⑩c 正控测**副本**非部署体 ⇒ 部署词法改窄仍全绿 | `smoke.mjs` L2980-2982（**单点声明** + 部署面消费）+ L3105-3115（⑩c 引用**同一对象** + 对象同一性 + 改窄对照） | §4.2（三层复算） | ✅ **已修复（实质，内容维；消费维残余见 P3-2）** |
| 3 | **P3-1** | `deleteOnChild` 段落式跳过 ⇒ 删最后一个子键连带吞掉 `jobs:`，变异体非最小 | `probe-face.mjs` L183-194（只删目标键行 + `≥4 空格`子行）+ L542-550（**新增变异体最小性机检**） | §4.3（含「旧实现必被新机检判红」的反向复算） | ✅ **已修复（实质）** |
| 4 | **P3-4** | `jobSection` 两处镜像、无交叉机检 | 新增 `yml-jobs.mjs`（39 行，纯函数、零顶层副作用）；`probe-face.mjs` L77 与 `smoke.mjs` L47 消费同一对象 | §4.4（语义恒等 + 无残留镜像 + CI 覆盖面） | ✅ **已修复（结构维；判别力未增——如实披露）** |
| 5 | **P3-6** | 扫描面未覆盖新增文本载体（README/probe-face/probe-host） | `smoke.mjs` L3012-3092：五面扫描 + 锚点集 + 五面**扫描量下限** | §4.5（五面计数**逐个复算命中**；三面内容空面 ⇒ 判别力未增——见 P3-3/P3-4） | ⚠️ **已扩面（实质达成 R1 要求；新增面当前零匹配 → 事实增益低于表述）** |
| 6 | **P3-10** | 记录精度：295/296 差 1 且「非注释行」口径无仓内记录；「416 行」无依据 | `CHANGELOG.md` L36：口径明文化（trim 非空 ∧ 首非空白非 `#`；先剥 BOM、按 CRLF 分行）+ 295/221 复核 + 296 判定为笔误 + 416 无依据留档 | 口径文本实读可复现；数值未静态复算（§0 未做项 3） | ✅ **已闭环（口径维）** |
| 7 | **P3-11** | ⑧ golden 只锁数量 ⇒ 两脚本同步换 id 仍全绿 | `probe-face.mjs` L102（有序 id 集 `['4.1','4.2','4.3','4.4','5.5']`）+ L254-259（有序比较）+ L578-589（换项/删项/增项三向负例） | §4.6（golden ≡ `install.ps1` 实读顺序；旧失效模式必红） | ✅ **已修复（实质）** |

**结论**：前轮指定的 7 项**全部落地、无缺项**，其中 5 项（P2-1/P2-2/P3-1/P3-10/P3-11）为**判别力实质闭环**，
2 项（P3-4/P3-6）为**结构性/覆盖面达成**但**不新增当前判别力**——本轮 CHANGELOG 对 P3-4 的表述（「镜像面消除」）准确，
对 P3-6 的表述（「扩至 …」）未夸大，但**未说明「新增三面当前零匹配」**，本报告以 P3-3 补登（见 §7）。

---

## 3. 重点核验项结论（任务指定的 6 个方面）

### 3.1 正确性① P2-1 锚定实现强度 —— **锚定真绑定定义行；头注释同串确不再污染**（详见 §4.1）

- **真绑定**：`REAL_RUN_DEF = /const REAL_RUN_STATE = 'REAL-RUN: UNVERIFIED'/` 与 `scripts/probe-host.mjs` **L49 常量赋值行**逐字一致（实读）；头注释 L13 的 `**REAL-RUN: UNVERIFIED**` 形态**不匹配**该正则 ⇒ 注释保留亦不再充当「标记存在」的伪证。
- **反向排除有效**：`'REAL-RUN: UNVERIFIED'` **不包含**子串 `'REAL-RUN: VERIFIED'`（`UNVERIFIED` 中的 `UN` 阻断）⇒ 反向排除不会自伤（这是该修复能否成立的关键点，本人逐字核对，成立）。
- **引用计数余量**：`REAL_RUN_STATE` 全文 **9 处**（L49 定义 + L221/L222/L225/L252/L285/L318/L332/L344 **全部为真实输出/报告引用，无注释水份**）≥ 阈值 3，余量 6；`SELF_CHECK_STATE` **4 处**（L50 + L207/L222/L343）≥ 阈值 2。阈值取「定义 + 2 处拼接」而非「≥1」，方向正确。
- **残余**（P3-6，非阻塞）：正则未行锚定（无 `^…$`/`m`）⇒ 同一文本出现在注释中仍可满足「锚定定义行」的字面声称；计数是**代理量**（含定义行、注释亦计入）。

### 3.2 正确性② P2-2 共用 RegExp 是否真同源 —— **是同一对象**（详见 §4.2）

- **单点声明**：全文检索 `STALE_RE_NOW5|STALE_RE_EMIT5` 仅 **L2980-2981 一处 `const`**；旧副本 `LEXER_NOW15`/`LEXER_EMIT15` **已不存在**（仅存于说明注释）。
- **同源消费**：部署面 `staleNoteRefs5`（L2982）与 ⑩c 三例（L3105/L3106/L3107 `re:` 字段）引用**同一标识符**；L3109 的 `lexerShared15` 以 `===` 做**对象同一性**断言（3 项全断言入条件）。
- **作用域正确**：⑩ 与 ⑩c 位于**同一语句块**（该块自 L2960 起、L3127 `}` 止；两块间无重声明/无遮蔽）。
- **无状态风险**：两条正则均**无 `g` 标志** ⇒ `.test()` 无 `lastIndex` 漂移，多例复用同一对象安全。

### 3.3 正确性③ P3-6 扩面逻辑与五面下限 —— 逻辑成立；**三面当前内容空面**（P3-3）

- **扫描逻辑**：`scanItemLineRefs5`（L3042-3054）先过 `face()` 再 `HIST_MARK5` 整行豁免，`ITEM_LINE_REF5` 命中后经 `itemLineRange5` 取契约 `line` 的多段 min…max 宽松界；五面分别以「默认注释面（A/D）/ `face:()=>true`（B/C）」+ `base` 偏移调用 ⇒ 实现与口径声明一致。
- **误报面（当前 0）**：本人以同形正则对 `README.md` / `probe-face.mjs` 全文检索 `<item> L<n>` 形态 ⇒ **0 命中**，故扩面未引入误报；`README` L186/191 的 `0.1.x`/`§3.1` 形态因不满足 `\d+\.\d+ … L\d` 而不入选，符合预期。
- **漏报面（真实存在，P3-3）**：**新增三面（README 全文 / probe-host 注释 / probe-face 注释）当前各 0 处可判定引用** ⇒ 扩面今日**不产生判别力**，仅对**未来**文本生效。五面下限（400/5/150/30/80）只防「面被删致体量归零」，**不防「面大而内容与之无关」**；`injectedHit15` 是**面无关的合成样本**（`scanItemLineRefs5([injectedStaleRef15])` 单元素数组），故它无法为任何单面活性作证。活跃面实为**面 A（smoke 注释 15 行实引用）+ 面 B（CHANGELOG 015 条目 L54 重基清单等）**。
- **锚点漂移（P3-4）**：锚点集 `['- **COMPAT-015', '- **COMPAT-016']` 为**手工硬编码**，机检只检「锚点在位」、**不检「新增顶层条目未登记」**⇒ 与 R1 P3-6 同类的漂移面仍在（详见 P3-4 双向风险的完整表述）。

### 3.4 正确性④ P3-11 golden 有序 id 集与三向负例 —— **成立**（详见 §4.6）

- `parseInstallContract`（L245-253）**按文件顺序** push、无排序 ⇒ 有序性有意义；`installGoldenProblems`（L255-259）以 `join(',')` 做**有序全等**比较。
- golden ≡ `install.ps1` L25-29 实读顺序 `[4.1] [4.2] [4.3] [4.4] [5.5]` **逐项同位**；跨脚本一致性仍由 `keysA === keysB`（排序集合）兜住 ⇒ 单侧对 golden 覆盖两脚本的推理成立。
- 三向负例（换项 `4.4→4.5` / 删项 / 增项）**各自必与 golden 不等** ⇒ 非恒真。R1 所述失效模式（两脚本同步换 `[4.2]→[4.6]` 并写入 ∈4.6 symbol 的字面量）在新实现下 ⇒ 解析序 ≠ golden ⇒ **必红** ✅。

### 3.5 正确性⑤ P3-1 最小性机检自身是否可靠 —— **可靠，且能判别被修掉的旧实现**（详见 §4.3）

- 新 `deleteOnChild` 的最小性成立（只删目标键行 + `≥4 空格`子行；空行/0 缩进/2 缩进行一律保留 ⇒ `jobs:` 与 `on:` 其余三键不受影响）。
- ⑦ 机检两谓词（`\njobs:` 在场 ∧ 三 job 名在场且权限读数全等）**足以判别旧实现**：旧实现在删 `workflow_dispatch` 时连带删掉 `jobs:` ⇒ 第一谓词即红。**这不是自证式机检**。

### 3.6 其余：安全 / 可维护 / 性能 / 测试覆盖 / AI —— 见 §5、§6

---

## 4. 构造分析（≥2 项深挖；本轮实做 6 项）

### 4.1 P2-1 全链路——三层独立复算（**实质闭环成立**）

1. **旧守卫的可绕过性（失效模式为真）**：实读 `scripts/probe-host.mjs` **L13** 头注释含 `**REAL-RUN: UNVERIFIED**`、**L49** 为常量定义、L221/L222/L225/L252/L285/L318/L332/L344 为 8 处输出/报告引用 ⇒ 旧 `includes('REAL-RUN: UNVERIFIED')` 由注释即满足，改常量则「自述转已验证而守卫全绿」**确实成立**（R1 P2-1 的现实性已被本轮独立确认，非转述）。
2. **新守卫的判别力（对篡改变异体**静态重演**）**：变异体 = 用 `probeHostSrc.replace(REAL_RUN_DEF, "const REAL_RUN_STATE = 'REAL-RUN: VERIFIED'")` 得（正则无 `g` ⇒ 只替换首处 = L49；注释不含 `const …` 形态 ⇒ 首处即定义行）。逐项读数：`REAL_RUN_DEF.test(变异体) = false`（定义行已变）∧ `!/REAL-RUN: VERIFIED/ = false`（新值即含该串）⇒ **`realRunGuard = false` ⇒ ⑧c 必红** ✅；同时 `变异体.includes('REAL-RUN: UNVERIFIED') = true`（L13 注释仍在）⇒ `oldFullTextGreen = true` **正确地把「旧守卫仍绿」固化为断言**（即失效模式本身）。三值 `{forgedChanged, oldFullTextGreen, anchoredRed}` 全部入断言条件 ⇒ 该对照**非恒真、不可退化**。
3. **「还原」的可信度（静态旁证，替代 sha256）**：① `git diff d350445 HEAD` 对四文件 + `probe-host.mjs` **为空** ⇒ 提交内容即还原后内容；② `probe-host.mjs` **不在 `d350445` 的改动面**（`git show --stat` 仅 4 路径）⇒「probe-host 本体零改动」成立；③ `git status --porcelain` 仅 `.governance/plan-tracker.md`（M）⇒ 三份被测文件**工作区洁净、无变异残留**。三条合并 ⇒ Developer 的「改→跑→红→还原」在**提交层面无残留**（sha256 值本身仍按引用登记）。

### 4.2 P2-2 全链路——三层独立复算（**内容维闭环；消费维残余见 P3-2**）

1. **同源性**：见 §3.2（单点声明 + 同一标识符 + `===` 同一性 + 同块作用域 + 无 `g` 标志）。
2. **改窄必红（真判别器 = `hits[0]`）**：样本 `'index.js 现调用点 L170'` 在 `现` 与 `L` 之间为 `调用点 ` **4 字符**；把部署词法改窄为 `{0,3}` ⇒ 共享对象本身失配 ⇒ `lexerProbeHits15[0] = false` ⇒ 断言条件 `hits[0] === true` 破 ⇒ **必红** ✅。
3. **`narrowedNowHit5` 子条件不承担判别（P3-1）**：`narrowedNow5 = new RegExp(source.replace('{0,10}','{0,3}'))`——
   | 部署词法状态 | `narrowedNowHit5` | 是否构成判别 |
   |---|---|---|
   | **现状 `{0,10}`（绿态）** | **`false`（4 > 3，本就不命中）** | ❌ **子条件在绿态即已满足（真空）** |
   | 改窄为 `{0,3}` | `false`（`replace` 变 no-op，仍是同一窄式） | ❌ 仍满足；红来自 `hits[0]` |
   | 改宽为 `{0,20}` | `true`（`replace` no-op，宽式命中 4 字间隔） | ⚠️ 由该子条件变假而红（非「历史形态 MUST NOT 命中」的意图机制） |
   ⇒ 断言消息与 CHANGELOG 证据中的「改窄后同一正例 MUST 不再命中（`narrowedNowHit5`）」以及负例读数「`shared=true ∧ narrowedNowHit=false`」**都不是判别证据**（绿态同值）。修复的**实质成立**，但该子条件与其叙述需订正（见 P3-1）。

### 4.3 P3-1 最小性机检的反向复算（**判别力成立**）

- 新实现最小性：`src.split('\n')` 逐行——命中 `^  <key>:` 即删并进入子行区；`inTarget ∧ /^ {4}/` 即删；**其余一律 `inTarget=false` 并保留** ⇒ 目标键行 + 其 ≥4 空格子行之外**零副作用**（空行、`jobs:`、其余三键、三个 job 段全部原样）。
- 新机检对被修掉的**旧实现**是否判红（关键）：旧实现 `skipping` 只在遇 `^  [A-Za-z_]+:` 时复位；删 `workflow_dispatch` 时随后空行与 `jobs:` 均不匹配复位正则 ⇒ `jobs:` 被吞 ⇒ 变异体 `!\njobs:` ⇒ `eventNegNonMinimal` 非空 ⇒ ⑦ 记录**必红** ✅。即：**该机检正是把本项修复永久钉住的判别器**，而非「只描述现状」的装饰断言。

### 4.4 P3-4 共享模块的语义恒等与残留面（**成立**）

- **语义恒等（逐式对照旧两份实现）**：旧 smoke 版 `i > (j < 0 ? -1 : j)` ≡ 新 `i >= jobsStart(lines)`（`jobsStart = j<0 ? 0 : j+1`）——**退化情形 `i > -1` ≡ `i >= 0` 亦恒等**；段尾判据 `^  [A-Za-z0-9_-]+:\s*$` 与 `jobCmd`（trim + 剥 `run:`）逐字一致。⇒ 换实现**零行为变化**（本批 smoke/probe-face 全绿与此一致）。
- **纯净性**：`yml-jobs.mjs` 全文仅 1 个文件头注释块 + 3 个 `export function`（无顶层读取/写盘/打印/import）⇒ 满足「smoke 可直接 import 而非 import 顶层脚本」的模块纪律；已实读确认无顶层副作用。
- **残留镜像**：`ciJobSection`/`ciCmdOf` 在 smoke 中**仅存于注释**（L46），实现已删 ⇒ 镜像面消除。`probe-face` 侧 `jobsStart` 仍被 `jobGates`/`jobSection` 内部消费（合法），`jobCmd` 亦被 ②/⑦ 消费。
- **CI 语法检查清单缺该模块（P3-5，取舍判定）**：`ci.yml` L27-36 的 `node --check` 清单 8 项**不含** `yml-jobs.mjs`（实读确认）。Developer 理由经**独立核验成立**：`lib/host-contract.mjs` item **6.4** 以 `line: 'L105-146'` 记录 ci.yml mock 段，其 note 明载两次历史重基（COMPAT-014 `L77-113→L96-137`；COMPAT-015 `→L105-146`，+9），而语法检查清单在 L27-36（**位于 L105 之前**）⇒ 插入一行将使 6.4 行段位移 +1、必须重基。**判定：可接受**——模块被 `test/smoke.mjs`（`host-logic` job L148）与 `probe-face.mjs`（`sanity` job L66）**import 执行**，语法/导入错误在 CI 两个 job 内必红，缺 `node --check` 只是失去「更早更清晰的静态报错」，**不构成检测力缺口**；建议按 P3-5 后续补行 + 重基，或加「fixture 模块 ⊆ 语法检查清单」断言消除该类手工清单漂移。

### 4.5 P3-6 五面扫描量逐个复算（**Developer 声称 5/5 精确命中**）

| 面 | 声称值 | 本人只读复算 | 下限 | 复算结论 |
|---|---|---|---|---|
| smoke 注释 | 532 行 | **532**（`^\s*(//|\*|/\*)`，与 `COMMENT_LINE_RE5` 同形） | 400 | ✅ 精确一致 |
| CHANGELOG 当前条目 | 24 行 | **24**（锚点 L33 起至 L41 = 9 行；L42 起至 L56 = 15 行；`- **COMPAT-014` 在 L57 为下一条顶层 bullet） | 5 | ✅ 精确一致（**但为 015+016 两条目求和**，见 P3-7） |
| README 全文 | 237 行 | **237**（`split('\n')` 口径；文件实有 236 行 + 尾部空元素） | 150 | ✅ 一致（口径为 split 计数） |
| probe-host 注释 | 52 行 | **52** | 30 | ✅ 精确一致 |
| probe-face 注释 | 142 行 | **142** | 80 | ✅ 精确一致 |

⇒ 五个**非平凡**数字全中（含 532/142 这类不易凑巧的数字）——这是「证据来自真实读取而非转述」的强旁证（本报告据此**采纳**五面计数为已核验事实）。
另：`probe-face` 机检项数 **18 个 `record(` 调用点** − 1（`expectCase` 内，被 **11** 例构造调用）+ 11 = **28** ⇒ 与「27 → 28 项 + 11 例构造」**算术自洽** ✅。

### 4.6 P3-11 旧失效模式在新 golden 下的必红性（**闭环成立**）

`parseInstallContract` 保序（实读 L247-251）⇒ 换项后 `entries.map(e=>e.item).join(',')` = `4.1,4.6,4.3,4.4,5.5`（或增/删项形）≠ `4.1,4.2,4.3,4.4,5.5` ⇒ `installGoldenProblems` 非空 ⇒ ⑧ 红。旧实现（只比 `length !== 5`）在该变异下**仍绿**（数量不变）⇒ **新实现判别力严格更强**，且三向负例把该判别力入断言。

---

## 5. 5 维度结论

| 维度 | 结论 |
|---|---|
| **正确性** | **成立**。P2-1/P2-2/P3-1/P3-11 四项修复经**独立静态复算**证明「旧实现绿 ∧ 新实现红」（§4.1-4.3/4.6）；共享模块与旧两份镜像**语义恒等**（§4.4）；五面扫描量 5/5 精确复算命中（§4.5）；⑩b 扫描逻辑与口径声明一致、当前零误报。**三处「强度 < 叙述」的残余已单列 P3**：⑩c `narrowedNowHit5` 子在绿态即真（P3-1）、部署式消费无正控（P3-2）、⑩b 新增三面内容空面 + 锚点手工维护（P3-3/P3-4）。**无逻辑错误、无边界/资源问题**；纯测试侧改动，运行时面零变化。 |
| **安全性** | **无新增攻击面**。改动仅在测试/脚本侧：无网络调用、无新增落盘（`yml-jobs.mjs` 零副作用；smoke 既有 `mkdtemp` 临时根不变）、无子进程新增、无 secret/权限面变化；新正则均为**有界量词**（`{0,3}`/`{0,10}`/`[^。；]`、`^  [A-Za-z0-9_-]+:\s*$`）⇒ 无 ReDoS 面；`new RegExp(常量.source.replace(...))` 的输入是仓内常量（非外部输入）⇒ 无注入面。⑧c 的加固方向是**安全承诺（不得静默转「已验证」）的实现对齐**，属安全**增强**。 |
| **可维护性** | **净改善**：`yml-jobs.mjs` 职责单一（job 段切分 + run 归一）、单一事实源达成（§4.4 全链路证据）、有 `ci-mock-face.extractHeredocs` 先例背书；`probe-face.mjs` 605 行仍属「单工具三类机检」合理范围（R1 §10-2 的拆分建议在**第四类职责**到来时才触发，本批未触发）。**三处待改进**：`ci.yml` 语法清单未含新模块（P3-5，取舍可接受）、⑩b 锚点集手工硬编码（P3-4）、⑩c 子条件与叙述不一致（P3-1）。注释密度高但均承载「口径/边界/失效模式」披露，符合本仓 P-01 风格，非注水。 |
| **性能** | **可忽略**（静态判断）：⑩b 扩面新增 3 次 `readFileSync` + `split`（README + probe-host 25KB + probe-face 47KB ≈ +0.1MB，对比 smoke 既有自读 276KB 为 +43% 单项读入）与约 **987 行**面的单遍线性过滤（`face()` 先剪枝，再 `matchAll`）；`yml-jobs.mjs` 为 O(行数) 单遍切分。无嵌套扫描、无 O(n²)、无 N+1、无新增子进程 ⇒ 级联耗时增量在**个位毫秒量级**，不改变 smoke 耗时主项（既有的多例构造与子进程调用）。 |
| **测试覆盖** | **净增强，但非「五处齐增」**（如实）：**判别力实质提升** = 3 处（⑧c P2-1 / ⑩c P2-2 内容维 / probe-face ⑦ 最小性机检）+ 1 处（⑧ golden 有序 id 集，27→28）；**结构/覆盖提升但当前判别力不变** = 2 处（⑧b 消镜像、⑩b 扩面）。「零新增 check」经 diff 核对**成立**（4 处 `check(` 就地改写、无新增调用点）⇒ README 的 282 无需变更，且 README L168 声明值由 smoke **末条断言机检 ≡ 实测**（fail-closed）⇒「零新增 check 与 README 计数一致性」**成立**（282 的绝对值的静态复算不可行——`check('` 调用点 283 处含循环内调用（如 L1872 ×3）与 `if` 条件调用（L206/L208 互斥对），本报告不据此宣称；以 ⑪ 机检与 279→282→282 的历史链为据）。**覆盖缺口登记**：⑧c 的「输出面真实引用」仅由计数代理（P3-6）；golden 三向负例走**比较器直调**、未穿透 parse 层（组合推理成立，见 §9-3）。 |

---

## 6. AI 代码专项 5 项

1. **mock/硬编码残留**：**无非法 mock**。⑧c 的篡改变异体是**内存字符串**（零落盘、不替换真实文件），⑩c/⑧b 的负例是**构造样本/构造 YAML**，⑩b 的 `injectedStaleRef15` 是**合成单行**——三者都驱动**真守卫**而非替代被测对象 ✓。硬编码项均为**有意 golden**（`INSTALL_GOLDEN_ENTRIES` 有序 id 集、五面下限、锚点集、两条词法正则、两枚标记常量、`EVENT_KEYS`），符合仓内 golden 先例；**其中 3 项属「锚点需人工同步」面**（锚点集/下限/词法字面量），已由 P3-4、P3-1 登记。
2. **幻觉 API 调用**：**零命中**。逐一核对所用 API 与 Node/JS 语义一致：`RegExp.prototype.source`（含 `{0,10}` 原文，可 `replace`）、`String.prototype.replace`（**非 `g` 正则 ⇒ 只替换首处**——⑧c 变异体构造的正确性依赖此语义，成立）、`String.prototype.match`（无 `g` 返回首匹配数组；此处用 `/…/g` 返回全部 ⇒ 计数成立）、`matchAll`（要求 `g`，`ITEM_LINE_REF5` 有 `g` ✓）、`Array.prototype.flatMap`/`every`/`sort`/`join`、`readFileSync`+`new URL(..., import.meta.url)`、`[...map.keys()]` ✓；`Object.entries(...).filter().map()` 析构用法 ✓。
3. **未实现 TODO**：`test/**/*.mjs` 检索 `TODO|FIXME|XXX|HACK` = **0 命中** ✓；本批无「声明未实现」的分支（改动全部为守卫/测试逻辑）。
4. **过度实现**：**否**。+203/−71（净 +132）中注释占大头，且**逐项可追溯到 R1 id**（P2-1/P2-2/P3-1/P3-4/P3-6/P3-10/P3-11）；新增模块 39 行中 18 行为口径披露，实体仅 3 个 3-6 行纯函数，并用删旧的 14 行镜像**净抵**部分增量。**成本收益最弱的一处**是 ⑩b 五面扩面（约 40 行换取当前零匹配的三面），但该项是 R1 P3-6 的明确要求、且为未来文本载体预置防线 ⇒ 判定**非过度实现**，仅建议补 per-surface 正控使其名实相符（P3-3）。
5. **规则/契约同步（P-10）**：本批**无新增宿主耦合字面量/符号**（纯测试侧）⇒ 无需更新 `lib/host-contract.mjs`、无需补 smoke 对账断言 ✓。唯一「契约侧联动」是 **ci.yml 行号位移会触发 6.4 行段重基**——Developer 的取舍正是为回避该重基，已核验理由属实（§3.3 / P3-5），并已如实记录于 CHANGELOG（「改动面」段）✓。

---

## 7. 发现列表（P0~P3 全覆盖）

### P0（阻塞）— **0 项**

### P1（高）— **0 项**

### P2（中）— **0 项**（本轮 7 项修复未引入新的 P2 级问题；前轮 2 项 P2 未闭环项见 §8）

### P3（建议）— **7 项**

#### P3-1 — ⑩c 的 `narrowedNowHit5` 子条件在**绿态即为真**（真空断言），且其叙述被当作判别证据
- **位置**：`test/smoke.mjs` L3110-3111（构造与读数）、L3113（断言消息「改窄部署词法（间隔上限 10 → 3）后同一正例 MUST 不再命中（`narrowedNowHit5`）」）、L3114（条件）、L3115（detail 读数）；CHANGELOG L34 的负例读数引用。
- **依据**（静态复算，见 §4.2 表）：样本 `现` 与 `L` 间隔 **4 字符** ⇒ 现状 `{0,10}` 下 `narrowedNow5({0,3}).test(sample) = false` **本就为假**；改窄为 `{0,3}` 时 `source.replace('{0,10}',…)` 变 **no-op** ⇒ 该读数**仍为 false**。⇒ 该子条件与负例读数 `narrowedNowHit=false` **在绿/改窄两态同值**，不构成判别证据；真正使「改窄 ⇒ 红」成立的是 `lexerProbeHits15[0] === true`（共享对象正控）。
- **影响**：本批的主题正是「守卫强度对齐声称」（P-01 精度），此处的**证据叙述**弱于实际判别机制，属同类瑕疵的**最小残余**；不影响检测力（改窄仍必红），但会让读取证据者误判检测机制，并在**合法放宽**词法（如 `{0,10}→{0,20}`，命中面更宽）时以「改窄后 MUST 不再命中」的报文报红，归因语句误导。
- **修复建议**：删除该子条件，或改为**真判别器**——`narrowedNow5` 用**显式字面量**构造（`new RegExp(STALE_RE_NOW5.source.replace(/\{0,\d+\}/, '{0,3}'))`，与部署字面量解耦），并同时断言 `hits[0] === true` 与 `narrowedNowHit === false`；断言消息改写为「共享对象正控必命中 ⇒ 部署词法被改窄即红」。

#### P3-2 — P2-2 的**消费维**残余：常量已共享，但「部署式是否消费该常量」无正控
- **位置**：部署式 `test/smoke.mjs` L2982 `staleNoteRefs5 = contractNoteTexts5.filter((n) => STALE_RE_NOW5.test(n) || STALE_RE_EMIT5.test(n))`；⑩c 正控 L3105-3112（只对**常量对象**取样）。
- **依据**：⑩c 三例验证的是「常量对象的行为 + 对象同一性」，**不验证部署式对该常量的引用**。若有人删掉 L2982 的 `|| STALE_RE_EMIT5.test(n)` 分支（R1 P2-2 原文列举的失效形态之二「或删 emit 分支」的一种读法），则：`lexerShared15 = true` ✓、`hits[1] = STALE_RE_EMIT5.test('emit L155') = true` ✓、`narrowedNowHit5 = false` ✓ ⇒ **⑩c 仍全绿**，而 ⑩ 的 emit 形态判别力**静默消失**（`staleNoteRefs5` 少一类命中 ⇒ ⑩ 的 `length === 0` 更易满足）。
- **影响**：属「正控测对象而非接线」的通用缝；检测力损失方向为**假绿**（无人工审阅不可发现），与本批修复的家族同源但更细一层。
- **修复建议**：把部署谓词抽为命名函数（`const isStaleRef5 = (n) => STALE_RE_NOW5.test(n) || STALE_RE_EMIT5.test(n)`），`staleNoteRefs5 = contractNoteTexts5.filter(isStaleRef5)`，⑩c 增一例**对 `isStaleRef5` 本身**的正控（`isStaleRef5('index.js 现调用点 L170') === true ∧ isStaleRef5('emit L155') === true ∧ isStaleRef5('index.js 原调用点 L170') === false`）⇒ 部署式被改窄/删分支即红（同 ⑩b 的 `injectedHit` 范式）。

#### P3-3 — ⑩b 新增三面**当前零匹配**（内容空面）：面下限只防体量归零，不防「面大而内容无关」
- **位置**：`test/smoke.mjs` L3067-3077（面 C/D 读入与扫描）、L3080-3089（计数与下限）、L3078-3079（面无关的合成正控）。
- **依据**（本人只读复算）：以同形正则检索 `<item> L<n>` 形态——`README.md` **0 命中**、`probe-face.mjs` **0 命中**、`scripts/probe-host.mjs` **0 命中**；活跃面仅面 A（smoke 注释 15 行实引用）与面 B（CHANGELOG 015 条目，如 L54 的 +13 重基清单）。⇒ 扩面今日**不产生判别力**（仅对**未来**文本生效），而 `injectedHit15` 用的是**单元素合成数组**（不读任何面）⇒ 无法为任一面活性作证。
- **影响**：① 事实增益低于「扩至 …」的表述（CHANGELOG 与本报告 §2 均已如实标注，非失实）；② 若**未来**某面被整体替换为不承载 `<item> L<n>` 的文本（如 README 重写），下限仍绿 ⇒ 「面存在但当面无判别力」无信号。
- **修复建议**：为五面各加 **per-surface 正控**（向该面的行集合注入 1 行构造陈旧引用，断言命中 ≥1；一行代码/面），或至少在 detail 中登记「各面当前可判定引用数」基线（命中数入读数，长期为 0 的面才需要复核其存在意义）。

#### P3-4 — ⑩b 的 CHANGELOG 锚点集**手工硬编码**：新条目静默落在面外，且两个锚点自身未来失效即红
- **位置**：`test/smoke.mjs` L3058 `CHANGELOG_ENTRY_ANCHORS15 = ['- **COMPAT-015', '- **COMPAT-016']`；L3066 只检「锚点缺失」。
- **依据**：机检语义为 `changelogEntrySlices15.filter(s => s.start < 0)` ⇒ 只能发现**已登记锚点消失**；**新增**顶层条目（如 COMPAT-017）若不手工加锚点，其文本**静默落在扫描面外**——这正是 R1 P3-6 指出的漂移面本身（只被「本次列举」而非「机制」关闭）。反向风险：若未来对该两条目重写标题或归档 CHANGELOG 历史段，锚点消失 ⇒ **smoke 红**（fail-closed，但属「记录维护即红」的噪声）。
- **修复建议**：锚点改为**结构性派生**——取 `## [Unreleased]` 段内的全部顶层 bullet（`^- \*\*`）作为切片源，使「新增条目自动入面、归档段自动出面」，并保留「至少 1 条切片」的下限断言。

#### P3-5 — `yml-jobs.mjs` 未入 ci.yml `node --check` 清单（取舍**可接受**，建议后续补齐或加穷尽性断言）
- **位置**：`.github/workflows/ci.yml` L27-36（8 项清单，无 `test/fixtures/host-surfaces/yml-jobs.mjs`）；`test/smoke.mjs` L47 与 `test/fixtures/host-surfaces/probe-face.mjs` L77 的 import。
- **依据**：Developer 理由经独立核验**成立**——`lib/host-contract.mjs` item 6.4 `line: 'L105-146'`（ci.yml mock 段）+ 其 note 明载两次历史重基；语法清单位于 L27-36（6.4 段之前）⇒ 插一行即位移 +1、需重基 6.4（并复核 note 中的重基记录）。检测力影响：两作业均 import 该模块（`sanity` 跑 probe-face、`host-logic` 跑 smoke）⇒ 语法/导入错误必在 CI 红，**不构成缺口**，仅失去更早的静态报错。
- **修复建议**：① 后续任务补 `node --check test/fixtures/host-surfaces/yml-jobs.mjs` 并同步重基 6.4（与既往两次同法）；或 ② 在 probe-face ② / smoke ⑧b 加一条穷尽性断言「`test/fixtures/host-surfaces/*.mjs` 中被任何 CI 步骤 import 者 ⊆ `node --check` 清单」——一次投入消除该手工清单的整类漂移。

#### P3-6 — `realRunGuard` 的三处残余（均非阻塞；①③建议顺带订正）
- **位置**：`test/smoke.mjs` L1950-1958。
- **依据**：
  ① **定义行正则未行锚定**（`/const REAL_RUN_STATE = 'REAL-RUN: UNVERIFIED'/` 无 `^…$`+`m`）⇒ 若该文本出现在注释/字符串中同样满足，与「锚定**常量赋值行**」的字面声称存在缝（当前文件中不存在该形态，故为未来缝）。
  ② **引用量为计数代理**：`(src.match(/REAL_RUN_STATE/g)).length >= 3` 含定义行本身，且**注释中的出现亦计入**；实测 9 处（余量 6）⇒ 今日稳健，但对「定义常量却在输出另写字面量、同时保留 ≥3 处符号提及」的组合不敏感（需 ≥2 处定向改动方可越过，可接受）。
  ③ **`oldFullTextGreen` 对真实文件的头注释同串形成隐式保留要求**：该 conjunct 取自**真实** `probeHostSrc` 的变异体 ⇒ 若未来合法清理 `probe-host.mjs` L13 注释（去掉该串），`oldFullTextGreen = false` ⇒ **smoke 红**（fail-closed、报文可见 `forged.oldFullTextGreen`，但归因易被误读为守卫坏了）。
- **修复建议**：① 加 `^`/`m` 锚定（一行）；③ 把「头注释同串」作为**注释内固定说明**显式登记在断言消息中（已部分做到），或在注释被清理时改用**合成注释**构造该对照（不依赖仓内注释）。

#### P3-7 — 两处记录精度小项（「口径精确」为本批主题，建议顺带订正）
- **位置**：`test/smoke.mjs` L3082（面标签 `'CHANGELOG 当前条目'`，值为 015+016 两条目切片**求和** 24 行）；L3083（`'README 全文'` = `split('\n').length` = 237，文件实有 236 行内容 + 尾部空元素）。
- **依据**：本人只读复算 24 = 9（L33-41，016）+ 15（L42-56，015）；README 检索得 236 行，而 `split` 口径为 237。
- **影响**：纯读数命名/口径精度，不改变判定（下限 5 / 150 均有充分余量）。
- **修复建议**：标签改为「CHANGELOG 当前条目集（015+016）」；README 行数按 `split` 口径注明，或改用 `filter(l => l !== '')`。

---

## 8. 前轮未闭环项（carry-over，**不在本轮范围**，如实登记）

| 前轮项 | 状态 | 说明 |
|---|---|---|
| **R1 P2-3**（probe-host `--run` 浏览器层结构性不可达 + 断点层误归因） | **未处置** | R1 判定「不阻塞本批；**首次真机使用前** MUST 处置」。本轮范围 = P2-1/P2-2 + 指定 P3，未触；`scripts/probe-host.mjs` 本批**零改动**（git 证实）⇒ 该缺陷原样留存。 |
| **R1 P2-4**（`--run` 隔离硬校验恒真 + 真实 `$HOME` 未重定向，与 README 硬约束表述不符） | **未处置** | 同上。**注意**：这是**真实环境防护**相关项——首次 `--run` 真机使用前若仍不处置，将违反 M7.7 隔离举证口径（README L206 的硬约束表述当前强于实现）。 |
| **R1 P3-2 / P3-3 / P3-5 / P3-7 / P3-8 / P3-9 / P3-12** | **未处置** | 均属 probe-host 本体（P3-2/P3-3/P3-7/P3-8）、⑩b 豁免粒度与未知 item 静默跳过（P3-5）、CLI 命令面契约登记（P3-9）、`RUN_TIMEOUT_MS` 累积上限（P3-12）。⑩b 的 P3-5 与本轮 P3-3 属同一段落，但**本轮扩面未改变其豁免粒度**（仍为整行豁免 + 未知 item 静默 `null`）。 |

> 说明：上表**不计入**本轮 `unresolved_blockers`（沿用 R1 的「不阻塞」裁定；本轮为独立的微收口批次，终态以本轮 7 项修复的核验为准）。但 P2-3/P2-4 属**真机使用前的强制前置**，建议在 COMPAT-016 收尾时明确登记其处置任务，避免随迭代沉底。

---

## 9. 非阻断备注（不改变终态）

1. **「五处就地强化」的实际构成**：smoke 为 **4 处 `check(` 断言改写 + 1 处 ⑩ 输入构造单点化**，probe-face 为 ⑧ 新增 1 条 record（27→28）与 ⑦ 就地扩写；「零新增 check」经 diff 核对成立（无新增 `check(` 调用点）⇒ 与 README L168 的 282 一致（该值由 smoke 末条断言机检，fail-closed）。
2. **⑧b 的「强化」应准确理解为「消镜像」而非「增判别力」**：段绑定与「旧全文守卫仍绿 ∧ 段绑定必红」负例在 COMPAT-015 F2 已建立，本批仅把两处实现合并为同一对象 ⇒ 判别力不变、**分叉风险**消除。CHANGELOG 表述与之一致，无需订正。
3. **⑧ golden 三向负例走比较器直调**（`installGoldenProblems(ids)`）而非穿透 parse 层：但（a）`parseInstallContract` **保序**已实读确认，（b）`installGoldenProblems(installEntries…)` 把**真实解析结果**与 golden 绑定 ⇒ 组合推理完备（两脚本同步换项 ⇒ 真解析序变 ⇒ 比较器（已证判别）必红）。属可接受的纵深边界，非缺口。
4. **`README 全文 237 行`** 与文件 236 行内容的差异系 `split('\n')` 口径（尾部空元素），见 P3-7。
5. **CHANGELOG 对 P3-10 的处置是「口径明文化 + 笔误留档」**：口径规则可复现，但 295/221 的数值本人**未静态复算**（§0 未做项 3）；由于 install 脚本本批零改动、⑧ 对账不消费行数，该数值不承载任何机检语义 ⇒ 风险为零。
6. **⑩b 的 `base` 偏移仅用于报文行号**（CHANGELOG 切片传 `base: s.start`），不影响判定 ⇒ 无副作用。
7. **本批未新增任何宿主耦合字面量**（P-10 无触发面）；`ci.yml` 本文未被本批修改 ⇒ 探测轨白名单/job 门禁面**零变化**（复核：`d350445` 改动面不含 `.github/`）。
8. **`git status` 洁净度**：仅 `.governance/plan-tracker.md` 处于 M 态（属 COMPAT-016 治理记录写入，非代码残留）⇒ 无变异残留、无遗漏还原（§4.1-③）。

---

## 10. 结论与终态

**终态：APPROVED_WITH_NOTES — `unresolved_blockers = 0`**（P0 = 0 / P1 = 0 / P2 = 0 / P3 = 7）。

**判定依据**：
- **前轮 7 项全部落地**（§2），其中 **P2-1 / P2-2 / P3-1 / P3-11 四项的判别力提升经独立静态复算证明**（「旧实现绿 ∧ 新实现红」，§4.1-4.3/4.6）——本批的核心承诺「守卫强度对齐声称」**实质达成**，非仅文本对齐。
- **P3-6 的五面扫描量 5/5 精确复算命中**（532/24/237/52/142，§4.5）、probe-face 机检 28 = 17 + 11 算术自洽 ⇒ Developer 证据为**真实读取**而非转述；**「probe-host 零改动」「install 零改动」「工作区无变异残留」经 git 对象/状态独立证实**（§4.1-③）——弥补了前轮报告因无 diff 而留下的字节级边界。
- **7 项新 P3 全部为「残余缝/精度/机制」级**：无一项是假绿方向的现存缺陷（P3-1 是**叙述与子条件**不一致、真判别器在位；P3-2 需**删分支**才触发且当前 0 处现役副本；P3-3/P3-4 是扩面的名实与漂移面；P3-5 是清单遗漏且被 import 覆盖；P3-6/P3-7 是精度）。均给出 ≤5 行的修复建议，且**不影响 CI 常态面与产品行为**（本批纯测试侧、产品代码零改动）。
- **前轮 P2-3/P2-4 仍未闭环**（§8），但按 R1 自身的裁定属「真机使用前处置」且不在本轮范围 ⇒ 不构成本轮阻塞；**已明确登记**，建议随收尾任务立项，避免沉底。

**给 Coordinator 的三条处置建议（不改变终态）**：
1. **可合并/推进**：本批 4 文件可直接进入收尾（P0/P1/P2 = 0）；建议顺带处理 **P3-1**（1 行订正，消除本批主题下的最后一处「叙述 > 判别」）与 **P3-2**（抽 `isStaleRef5` 谓词 + 1 例正控，约 5 行），使 P2-2 的「同源」主张在**内容维与消费维**双双可机检。
2. **立项登记**：`ci.yml` 语法清单补 `yml-jobs.mjs` + 契约 6.4 行段重基（P3-5-①），或直接落地 P3-5-② 的穷尽性断言；二者择一即可。
3. **真机前置**：R1 的 **P2-3/P2-4**（`probe-host --run` 浏览器层归因 + 隔离硬校验/`$HOME` 重定向）MUST 在首次真机使用前处置；当前 README L206 的硬约束表述仍强于实现，属 M7.7 隔离举证口径的既存缺口（本轮未扩大也未缩小）。

**本人未运行任何测试/构建命令**（§0）；文中「Coordinator 独立复跑」结论（smoke 282/0、probe-face 28 项 + 11 例、probe-host 6/6、ci-mock-face 4/4、validate-preset PASSED、`node --check`×5 = 0）作为**引用事实**记录，供发布门禁以复跑替代。本报告的独立性建立在 §2/§3/§4 的**静态可复算**部分（含 6 项构造分析与 5 面计数复算）与 4 次**只读** git 调用所证实的对象级事实（§0 已披露）。
