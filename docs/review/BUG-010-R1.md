# BUG-010 代码审查报告 R1（Code Reviewer）

- **任务**: BUG-010 —— Code Review **round = R1**（强制后置审查：`WorkspaceDialog` 自挂 Esc / CLEAN-007 R1 C-2 同族收口）
- **前轮引用**: 无（本任务首轮）。上游来源 = `docs/review/CLEAN-007-R1.md` **C-2**（P2，`WorkspaceDialog` 无自挂 Esc ⇒ 让位后无人接手 ⇒ Esc 静默无反应）；同族既定模式 = `docs/review/CLEAN-007-R2.md`（D-1/C-1 的修复模式与纪律）
- **审查对象**: 提交 `a04ea89`（`git show a04ea89`；**5 文件 +85/−9**：`lib/client.js` +13、`scripts/probe-nv-ux012.mjs` +55/−4、`lib/host-contract.mjs` +5/−4、`test/smoke.mjs` +1/−1、`CHANGELOG.md` +11）；基线 `509d028`；`main` ahead **1**、**未 push**
- **审查方式**: 只读（`git show/diff/log/rev-parse/ls-files/hash-object`、只读文件读取、只读复跑 `node --check` / `test/smoke.mjs` / `test/validate-preset.mjs` / `probe-nv-ux012.mjs --falsifiability`、只读读取 `%TEMP%\bug010-verify\**` 与 `%TEMP%\bug010-ux012\**`）；**未修改任何被审文件**、未 commit、未写入 `C:\Users\peter\.dsh`、未终止/接管任何进程、未创建子 agent
- **唯一产出**: 本文件

## 结论

**APPROVED_WITH_NOTES** —— 修复本身**正确、最小、行为级**：`lib/client.js:4481-4486` 的自挂 Esc effect 七项要求（只关本层 / `busy` 同判据 / 同引用移除 / deps 实时 / hook 恒定 / 早退不注册 / try-catch 同款）**逐项独立复核成立**；分层语义经**挂载顺序独立验证**（`NvConsole` L5181 先于 `WorkspaceDialog` L5185 注册）＋**真跑行为读数**（`consoleKept:true`）确立；契约 4 项 / 5 处重基经**全量独立重算**（逐行等价 5/5、插入点唯一 +13、45 项零位移零改动）；四道门禁独立复跑全绿；冻结探针 `head=a04ea89 / headDirty=false / FAIL 0 / tally 49-47-0-2`、与全部可得基线**零 PASS→FAIL、零新增 FAIL**；**红态替代证据判定：充分，不需补端到端复跑**。新发现 **P0=0、P1=0、P2=2、P3=4**（均非阻塞，见 §11）。

```
unresolved_blockers=0
```

> 结构化事实源：本字段为**独立字段**（非自然语言推断），对应本报告结论 `APPROVED_WITH_NOTES`；全文不含未解决的 BLOCKING finding（P0 = 0、P1 = 0）。N-1 / N-2（P2）为**记录口径与披露事实性**项（其一订正 CHANGELOG 内一处**失实**门禁归因），按 `skills/code-review/SKILL.md` 分级（P0=0 ⇒ 可合并；P2 可遗留）不改变本结论。**被审代码（`lib/client.js`）零发现**——6 条 finding 全部落在**记录面**（`CHANGELOG.md` / 探针文件头 / 测试看护范围）。

---

## 0. 独立复跑的门禁读数（本次实跑，非转述）

| 门禁 | R1 独立复跑读数（命令/证据） | 与自述一致 |
|---|---|---|
| `node test/smoke.mjs` | **297 passed / 0 failed**（exit 0；末条 `COMPAT-014 A-F9` 声明 297 ≡ 实测 297） | ✅ |
| `node test/validate-preset.mjs` | **PASSED**（`schema-face: PASS`；skills 29/29） | ✅ |
| `node --check` 全清单 | `git ls-files` 过滤 `*.js/*.mjs/*.cjs` = **23 文件 / 0 失败** | ✅ |
| `node scripts/probe-nv-ux012.mjs --falsifiability` | **PASS**：谓词 **12** 条、向量 **red=29 / ok=12**、每条 `部署消费点=1`（≥1 达标）；`D1-esc-workspace-dialog-yield` = `red=4 ok=1 redsAllFalse=true oksAllTrue=true` | ✅ |
| 冻结探针（CLEAN-004）复跑报告 | `%TEMP%\bug010-verify\report.json`：`head=a04ea893e153e6cf2a74819f0502db89d3fcacff`、`headDirty=false`、**FAIL 0**、`tally {49,47,0,2}`、`realEnvVerdict.ok=true`（`strictDeltas={}`、`inventoryDeltas={}`）、`cleanup.rootRemoved=true` | ✅ |
| 冻结探针**无回归** | 与全部可得基线逐条 `id:status` 比对：**零 PASS→FAIL、零新增 FAIL**（明细见 §6.2） | ✅ |
| 冻结资产不变量 | `docs/evidence/CLEAN-004/probe-clean-004.mjs` sha256 = `de2de511e89a8ccc6d08b29d529e51336079bc3ceab4b937795dad88d9065b65` = 任务书锚值；`git hash-object` 在 `509d028` / `a04ea89` / worktree **三者同一**（`83987192196de6faec929b1adece37a6d2634b23`）⇒ 本提交零改动、内容前后可证 | ✅ |
| UX-012 探针绿态 | `%TEMP%\bug010-ux012\report-ux012.json`（`00:50:28→00:50:51`）：**26 条 / PASS 22 / FAIL 0 / N-A 4**，`head=509d028`、`headDirty=true`（= 已应用修复但尚未 commit 的工作树）⇒ 与 commit 时刻（`08:51:49+08` vs 运行 `08:50:28`）**时序自洽**：先跑绿态、后提交 | ✅ |
| 新断言真跑读数 | `report-ux012.json` 内 `UX012-D1-workspace-dialog-esc（BUG-010 / R1 C-2）` = **PASS**；`facts.workspaceDialogEsc = {consoleBefore:true, splitBefore:false, entryClicked:true, dialogOpened:true, dialogLabel:"选择 / 新建小说创作工作区", expectLabel:"选择 / 新建小说创作工作区", dialogWasOpen:true, after:{dialog:false, console:true, split:false}}` | ✅ 逐字段一致 |

> `report.ok=false`（冻结探针）系该探针**设计口径**（`probe-clean-004.mjs:1893`：`ok = assertions.length>0 && every(a => a.status==='PASS')`，N-A 计为非 PASS）⇒ 断言级 **FAIL=0** 才是判据；本项目既有基线同样为 `false`（R2 报告 §0 注已确认同口径）⇒ **非回归**。

---

## 1. 必核 10 项逐条结论

| # | 必核项 | 结论 | 事实依据（可复查） |
|---|---|---|---|
| 1 | 修复正确性（七项） | **通过** | `lib/client.js:4481-4486`；七项逐条见 §2 |
| 2 | 分层语义（跨层互不误伤） | **通过** | 挂载顺序独立验证 + 真跑读数 `consoleKept:true`；全文件 `'Escape'` 3→4 处且无其它缺口；见 §3 |
| 3 | 新断言真实判别力 | **通过** | 行为级（CDP 真按键）；前置合取防空真；4 条 red 向量经独立求值真 red；见 §4 |
| 4 | 红态证据充分性裁定 | **充分 —— 不需补 1 次端到端红态复跑** | 构造性判定经独立复算成立且为**确定性**结论（非概率推断）；见 §5 |
| 5 | 契约重基（4 项 / 5 处） | **通过** | 全量独立重算：4 项变更 / 45 项零位移；见 §6 |
| 6 | 回归（独立复跑） | **通过** | smoke 297/0、validate-preset PASSED、`node --check` 23/0、`--falsifiability` PASS（red 29 / ok 12 / 谓词 12） |
| 7 | 冻结探针读数复核 | **通过（含 1 条基线可得性限制）** | head/headDirty/FAIL/tally/无回归/资产 sha 全对；限制见 §6.2 注 |
| 8 | 披露项核验（P-01 三小项） | **① 事实成立但充分性有缺口 ⇒ N-2；② 处置恰当；③ 定级 P2 ⇒ N-1（并核出桶点名实质偏差 ⇒ N-5）** | 见 §7 |
| 9 | P-10 与项目原则 | **通过** | 新增耦合仅标准 DOM + 自有字段；`regionLiterals` 双向 ⊆ 仍成立；P-04/P-05/C-02/C-04 逐条见 §8 |
| 10 | AI 专项 5 项 + 真实环境纪律 | **通过** | 5 项逐一为 0；diff 内零仓库外写入；隔离与指纹一致；见 §9/§10 |

---

## 2. 修复正确性（七项逐条）

**代码事实**（`git show a04ea89 -- lib/client.js`，最终文件行号）：

```js
// lib/client.js:4481-4486（位于组件早退之前）
useEffect(() => {
  if (open !== true) return undefined
  const onKey = (e) => { if (e.key === 'Escape' && busy !== true) store.set({ entryOpen: false }) }
  try { if (typeof window !== 'undefined' && typeof window.addEventListener === 'function') window.addEventListener('keydown', onKey) } catch { /* ignore */ }
  return () => { try { if (typeof window !== 'undefined' && typeof window.removeEventListener === 'function') window.removeEventListener('keydown', onKey) } catch { /* ignore */ } }
}, [open, busy])
```

| # | 要求 | 判定 | 依据 |
|---|---|---|---|
| ① | 只关本层，不触碰 `consoleOpen` / 分栏 | **成立** | payload 恰为 `{ entryOpen: false }`（L4483）；同文件 `store.set` 的 `consoleOpen` 写入点仅 L3404（新建弹窗 Esc）/ L5198（apply 清理）——本 effect 均不经过 |
| ② | `busy` 守卫与同组件 `close()` **同一判据** | **成立（逐字同构）** | 新监听 `busy !== true`（L4483）≡ `close = () => { if (!busy) store.set({ entryOpen: false }) }`（L4495）；且 `pickWorkspace` 亦 `if (busy) return`（L4591）⇒ 三条关闭路径（Esc / 遮罩 / ✕）判据一致 |
| ③ | 卸载移除监听（**同引用**） | **成立** | `onKey` 为单次 `const`，add/remove 同引用（L4484/L4485）；闭包捕获同一常量 |
| ④ | deps `[open, busy]` ⇒ 无陈旧闭包 | **成立** | `busy` 为**原始值**入 deps（L4486）⇒ 翻转即重注册；回调内不使用任何渲染期派生值（不用 L4495 的 `close`，直接调 `store.set`）⇒ 无 TDZ 与陈旧捕获 |
| ⑤ | hook 恒定调用契约（effect 位于早退之前） | **成立** | 早退 `if (open !== true) return null` 在 **L4493**；effect 在 **L4481-4486** ⇒ 严格早于早退。`git show` 证实本组件内无更早 `return`（L4420-4492 全为 hook/派生） |
| ⑥ | `open !== true` 时早退不注册 | **成立** | L4482 与 L4493 同式 `open !== true` ⇒ 注册条件 ≡ 渲染条件，二者不可能不一致 |
| ⑦ | try/catch 与 `BindDialog` 同款 | **成立（逐字符同构）** | `lib/client.js:4484-4485` ≡ `:2820-2821`（除 `store` payload 不同）；亦与 `NvConsole:3406-3407` 同款 |

---

## 3. 分层语义（跨层互不误伤）

### 3.1 让位链完整性（全文件核实）

`'Escape'` 字面量**恰 4 处**（修复前 3 处，符合「3→4」自述）：

| 行 | 组件 | 行为 | 让位条件 |
|---|---|---|---|
| L2819 | `BindDialog` | `store.set({ bind: null })` | ——（终端消费者） |
| L3393 | `NvConsole` | `store.set({ consoleOpen: false, consoleFocus: null })` | `s.bind !== null \|\| s.entryOpen === true` ⇒ **return**（L3395） |
| L3958 | `SplitWorkspace` | `closeWorkbench(t)` | `s.bind !== null \|\| s.entryOpen === true` ⇒ **return**（L3960） |
| **L4483** | **`WorkspaceDialog`（本轮新增）** | `store.set({ entryOpen: false })` | ——（终端消费者） |

**无其它缺口**：`entryOpen` 的**全部**写入点经全文件检索确认仅 4 处——store 初值（L526）、`close()`（L4495）、`pickWorkspace` 成功分支（L4600）、本轮新监听（L4483）。除新监听外，**无任何路径**能在对话框在场时把 `entryOpen` 置 false ⇒ 「让位后无人接手」这一缺口已闭合，且未引入第二消费者。

### 3.2 正向顺序（对话框开在控制台之上）—— 成立

**独立验证（非自述复述）**：`shell.overlay` 四个注册点顺序为 `NvConsole`（L5181）→ `WorkspaceDialog`（L5185）→ `SplitWorkspace`（L5189）→ `BindDialog`（L5193）。同层监听按注册顺序派发，故控制台监听**先注册先执行**：

1. 按 Esc → `NvConsole.onKey`（L3392）执行 → `store.get()` 命中 `entryOpen === true` ⇒ **让位 return**；
2. `WorkspaceDialog.onKey`（L4483）执行 → `entryOpen:false` ⇒ 只关对话框；
3. `NvConsole` 不在 deps 上重注册（`open`/`creating`/`createBusy` 未变，L3408）⇒ 控制台保持打开。

**行为级确认**：`report-ux012.json` 的 `after = {dialog:false, console:true, split:false}` ⇒ 与 `consoleBefore:true` 同值 ⇒ `consoleKept:true`。这一读数**正是顺序正确性的判别器**：若注册顺序颠倒（对话框先执行），控制台监听随后会读到 `entryOpen:false` 而**连带关掉控制台** ⇒ `consoleKept:false` ⇒ 断言必红。故顺序风险已被真跑覆盖，非仅代码阅读保证。

### 3.3 反向顺序（对话框先于控制台）/ 连关两层 —— 无构成路径

`WorkspaceDialog` 的渲染条件为 `open = snap.entryOpen === true`（L4434）；`NvConsole` 的注册条件为 `open = snap.consoleOpen === true`（L3233）。对话框的**唯一入口**是控制台动作行 `.nv-cbtn-ws`（L3590，全文件唯一 `store.set({ entryOpen: true })`）⇒ 对话框必在控制台之后开启，`consoleOpen` 恒先为 true。**不存在**「对话框先于控制台注册」的构成路径。

其余组合亦无连关两层路径：
- **分栏在场 + 对话框开**：分栏监听（L3955）让位于 `entryOpen`（L3960）⇒ 只关对话框，分栏保持。
- **绑定面板 + 对话框同开**：二者**互斥**——均以全屏 `.nv-modal-backdrop` 承载（L2938 / L4648），当前无可达路径令 `bind !== null ∧ entryOpen === true` 同时成立。（本项为代码级论证 + 备份路径论证，非新发现。）
- **同族反向互伤**：本轮修复**未改动** §3.1 表中前三处监听 ⇒ D-1 / C-1 的既有行为零扰动（回归探针 `D2-esc-bind-yield` / `C4` 均 PASS 佐证）。

---

## 4. 新断言的真实判别力（独立核验）

### 4.1 行为级而非源码直查 —— 成立

`scripts/probe-nv-ux012.mjs` 新增段（L985-1021）经 CDP **真实** `Input.dispatchKeyEvent`（经 `pressEsc()`）驱动，观测量全部取**真实 DOM**（`.nv-modal` / `.nv-console` / `.nv-bar` 在场性）。**非**源码字符串直查：该断言不读取 `lib/client.js` 文本，仅经 `readI18n()` 取**期望文案**（与产品同源，探针既有口径，且已在文件头标注为「唯一两处源码读取面」之一）。

### 4.2 前置合取防空真 —— 成立且**双重**

```
wsEntryClicked === true && wsDialogWasOpen === true && evalPred(...)
```
- `entryClicked`（点 `.nv-cbtn-ws` 是否命中）
- `dialogWasOpen = (dialogOpened === true) && (dialogLabel === i18n.dialogTitle)`

第二条**同时**解决两个问题：① 防空真（对话框确在场才判）；② **身份区分**——工作区对话框与同族绑定面板**同为 `.nv-modal`**，仅凭类名会误断言。`i18n.dialogTitle = '选择 / 新建小说创作工作区'`（`lib/client.js:187`）而绑定面板用 `bindTitle`（L228，`🔗 绑定会话：《…》`）⇒ 严格相等是有效判别式。字面量非手工复制，由 `pick('dialogTitle')` 从产品源码提取（L416）。真跑读数 `dialogLabel === expectLabel` 逐字相符。

### 4.3 谓词 4 条 red 向量 —— 独立求值：真 red，无真空

谓词 `fn = (v) => v.opened === true && v.dialog === false && v.consoleKept === true && v.splitKept === true`（L138）。逐条代入（我独立计算）：

| 向量 | 取值 | `fn` | 判别目标 |
|---|---|---|---|
| `red` | `{opened:true, dialog:true, consoleKept:true, splitKept:true}` | **false** ✅ | 修复前实况（对话框仍在场） |
| `red2` | `{opened:true, dialog:false, consoleKept:false, …}` | **false** ✅ | 连关两层（控制台被连带关闭） |
| `red3` | `{opened:false, …}` | **false** ✅ | 前置未取得（防空真） |
| `red4` | `{opened:true, dialog:false, consoleKept:true, splitKept:false}` | **false** ✅ | 分层越界关分栏 |
| `ok` | `{opened:true, dialog:false, consoleKept:true, splitKept:true}` | **true** ✅ | 正确行为 |

`--falsifiability` 独立复跑对该谓词给出 `red=4 ok=1 redsAllFalse=true oksAllTrue=true 部署消费点=1`。**无真空**：`opened` 强制为 true 使 red3 与 ok 可分；`red2` 与 `ok` 仅差 `consoleKept` ⇒ 连关两层必红；`red4` 是 `splitKept` 的唯一 red 判别项。**注册与运行时向量一致**：`red` 向量取值 ≡ 修复前实测形态（对话框在场、控制台在场）——见 §5。

---

## 5. 红态证据充分性裁定（本轮明确表态）

**裁定：替代证据【充分】，不需补 1 次端到端红态复跑。**

Developer 未做修复前红态端到端复跑（时间盒把唯一一次全量运行给了绿态）。我对其替代证据逐层验证并要求它达到与 CLEAN-007 R2 对 C-1 采用的**同一结构判定口径**：

**(a) 静态可失败性 —— 成立。** 4 条 red 向量在 `--falsifiability` 下求值全 false（我独立复算，§4.3），且每条 ≥1 静态部署消费点 ⇒ 谓词**非恒真**、向量**非空转**。

**(b) 构造性判定 —— 我独立复算，逐环节成立，且关键性质是【确定性】：**

1. 修复前 `HEAD 509d028`：`'Escape'` 字面量**恰 3 处**（L2819 / L3393 / L3958），`WorkspaceDialog` 体内 **0 处** —— 我已以 `git show 509d028:lib/client.js` + 全文件检索**独立证实**（非采信自述）。
2. 对话框在场时按 Esc，3 处监督听的行为：
   - L2819 `BindDialog`：未挂载（`bind === null`）⇒ 无监听；
   - L3393 `NvConsole`：`s.entryOpen === true` ⇒ **return（让位）**；
   - L3958 `SplitWorkspace`：同为 `s.entryOpen === true` ⇒ **return（让位）**。
   ⇒ **零监听消费该按键**。
3. `entryOpen` 置 false 的全部路径（L4495 `close` / L4600 `pickWorkspace`）**均非按键触发**，且 `NvConsole` 的 Esc 分支（L3404）只写 `consoleOpen`/`consoleFocus`，**不触碰 `entryOpen`** ⇒ 按键后 `entryOpen` 仍为 true ⇒ 对话框**必留在场**。
4. ⇒ 修复前观测量 ≡ `{dialog:true, console:true, split:false}` ⇒ **结构同构于登记 red 向量** ⇒ 该断言在修复前**必 FAIL**。

**(c) 为何此处的「确定性」足以替代端到端复跑（与 R2 的 C-1 口径对齐）：** R2 时我要求 C-1 的红态取证，是因为其失败模式（`closeWorkbench()` 被先注册的分栏监听连带触发 ⇒ 一键关两层）**依赖运行时事件派发顺序**——属**行为学**推断，需实测锚定。本轮不同：修复前「无人接手」由**穷举监听集合 + 穷举 `entryOpen` 写入点**直接给出，第 2、3 步是**有限、封闭、可穷举**的结构事实，**不依赖任何时序或概率假设**；监听集合与写入点在 `git show` 下可完全枚举（我已枚举）。**结构确定性 ≥ 一次端到端复跑的判别力**，且不存在「复跑通过而推断有误」的反演空间。

**(d) 附带正向证据：** 绿态确经**真实**运行取得（`report-ux012.json`，`headDirty=true` = 工作树已含修复、尚未 commit，时序与 commit 时刻自洽），新断言在其中 PASS 且读数字段与自述逐字段一致（§0）。⇒ **绿态实测 + 红态构造性确定性**闭合了该修复的证据链。

**(e) 如实记录的残余限制（不改变裁定）：** 红态**无独立运行痕迹**（`%TEMP%` 下无 BUG-010 的红态 `report-ux012.json`）。若后续需要**审计留痕**（而非判别力），可低成本补跑一次 `git stash` 前后的对照；此为**取证便利性**建议，**不是**判别力缺口，故**不升级为 finding**。

---

## 6. 契约重基（4 项 / 5 处，Δ=+13）—— 全量独立重算

### 6.1 独立重算结果（≥4 处全查，实际做满 4 项 / 5 处）

| 契约项 | 旧 `line` | 新 `line` | Δ | 逐行等价 | 锚点唯一性（最终文件） |
|---|---|---|---|---|---|
| 2.3 | `L5082-L5084` | `L5095-L5097` | **+13** | ✅ 3/3 行逐字节相等 | 首 `const inject = ['slots']` 全文件命中 **1** 次 |
| 2.12 | `L5112-5119` | `L5125-5132` | **+13** | ✅ 8/8 | 首 `const offServiceEvent = typeof ctx.on === 'function'` 命中 **1** 次 |
| 2.13 | `L5149-5181` | `L5162-5194` | **+13** | ✅ 33/33 | 首 `ctx.slots.inject('settings.section'…` 命中 **1** 次 |
| 3.4 seg1 | `L4733` | `L4746` | **+13** | ✅ 1/1 | 命中 **1** 次 |
| 3.4 seg2 | `L4876` | `L4889` | **+13** | ✅ 1/1 | 命中 **1** 次 |
| 3.4 seg0 | `L925-938` | `L925-938` | **0（不变）** | ✅ 14/14 | （位于插入点之前，正确地未动） |

**独立插入点测定**（不采信 Δ 自述）：`git show` 两版逐行比对的**首个同索引差异行 = old L4475**，且 `oldFile[L] ≡ newFile[L+13]` 对**整条尾部**（L4475→文件末）**逐行成立**（无一例外）⇒ 插入点**唯一**、Δ **单段 +13**、插入块长度 **恰 13 行**。

**其余 45 项 `line` 不变 —— 我独立验证「都位于插入点之前」**：45/45 项所引用的**每一个**行号（含多段 `line` 的每一段）均 `< 4475`（违规项 **0**），且其引用内容在旧/新文件中**逐字节相同**（45/45）。⇒ 「45 项不变」不是巧合，而是**结构必然**。

### 6.2 契约结构与字段不变量（独立复算）

| 项 | 结果 |
|---|---|
| `items[]` 计数 | **49 ↔ 49**（旧/新一致）；**id 序列（face+item，含顺序）逐项相同** |
| `face` 分布 | 旧 `{1:11, 2:13, 3:8, 4:6, 5:6, 6:5}` ≡ 新（逐面相同），合计 49 |
| `line` 字段变更项 | **恰 4 项**（2.3 / 2.12 / 2.13 / 3.4）——**6 条 changed 段落全部落在这 4 项内**，无第 5 项 |
| `kind` 变更 | **0**；`necessity` 变更 **0** |
| `regionLiterals` 块 | **逐字节相同**（1840 = 1840） |
| `presetRowConfig[]` 块 | **逐字节相同**（2996 = 2996） |
| `kindEnum` 块 | **逐字节相同**（4000 = 4000） |
| `faces` 块 | **逐字节相同**（4000 = 4000） |
| item 5.3 / 5.4 / 5.6 | `L52 / L56 / L81`、`L77-81`、`L16` —— **旧新完全相同**（BUG-007 交付面零改动） |
| `revisions[]` | **追加**第 8 项 `task: 'BUG-010'`（序列：…CLEAN-007 → BUG-010）；块长 43041 → 43902 |
| `golden` 出现次数 | 7 → 8 —— **Δ 的唯一来源 = 新增的 BUG-010 revision 注记文本本身**（我逐处定位：新增处 `@10078` 位于该注记内），**不是** golden 面计数改动 |
| `lib/client.js` 行数 | 5220 → 5233（**+13**） |

**契约零扩展**：`items[]` 无新增/删除、无面变化、无 `kind`/`necessity` 语义漂移 ⇒ P-10 要求的「新增宿主耦合字面量须登记」在本提交**无触发项**（§8）。**唯一非机械变更 = `revisions[]` 注记**（设计如此，属记录面）。

**基线可得性限制（如实记录）**：任务书点名的 `%TEMP%\clean007-verify\{clean004-rework,clean004-final}\report.json` 在本次审查时**已不存在**（TEMP 已清理，现存仅 `bug010-verify` / `bug010-ux012`）⇒ 「与 clean004-rework 逐 id:status 差异 0」**无法按原路径复核**。我以**仓内归档**替代（见 §6.3），并如实标注该差异。

### 6.3 冻结探针无回归 —— 替代基线的独立比对

对**全部可得基线**做逐 `id:status` 全量比对（0.81.0 冻结探针口径 49 条断言）：

| 基线 | 差异条数 | **PASS→FAIL** | **新增 FAIL** | 差异明细 |
|---|---|---|---|---|
| `docs/evidence/CLEAN-004/report.json` | 5 | **0** | **0** | 5 条**改善**：`C10-flip-side`/`C3b-binding-convergence`/`C9-width-memory`/`D2-esc-bind-yield` FAIL→PASS；`C11-flip-back` N-A→PASS |
| `report-rework2-run.json` | 5 | **0** | **0** | 与上**逐条相同**（该文件与 `report.json` **sha256 逐字节相同** = `A6D8AE0F…`，我已验证） |
| `report-rework-run.json` | 8 | **0** | **0** | 上述 5 条 + `B21b` FAIL→PASS、`B22` N-A→PASS、`D6` FAIL→N-A |
| `report-rework-discovery.json` | 9 | **0** | **0** | 上述 8 条 + `E1-short-notice-compact` FAIL→PASS |

**判定**：**零 PASS→FAIL、零新增 FAIL** 对**每一个**基线成立（比自述范围更强）。全部差异均为 FAIL→PASS / N-A→PASS 方向的**累积改善**，可解释为 CLEAN-005 ~ CLEAN-007 已落地修复（其中 `C10`/`C11`/`C3b`/`C9` 对应 CLEAN-007 R2 已复核的 C-1 返工面，`D2` 对应 D-1）⇒ **与 CHANGELOG 陈述自洽**，非隐藏回归。

**冻结探针 N-A 与 R2 记录一致**：`{B5-found-sessions-area, D6-session-switch-close}` —— 与 `docs/review/CLEAN-007-R2.md:30` 记录的基线 N-A 集**逐项相同** ⇒ 交叉印证成立。

---

## 7. 披露项核验（P-01 三小项）

### ① 「控制台与分栏互斥」事实 + `splitKept` 充分性 —— 事实成立；充分性**有缺口** ⇒ **N-2（P2）**

**事实核验（独立）**：**成立**。`lib/client.js:3007` `if (novelSplit.active === true && closeWorkbench(t) !== true) return false` 位于 `launcher.open` 打开控制台之前 ⇒ 开控制台必先关分栏。**冻结探针** `D3-mutual-exclusion` 亦以**真行为**独立印证（`facts.exclusivity = {barOpenBefore:true, consoleOpen:true, barOpenAfter:false}`，该断言在冻结核中 PASS）⇒ 该互斥是**实测事实**，非推断。

**`splitKept` 是否构成充分覆盖 —— 我的判定：不足够。** 真跑读数 `splitBefore:false` 且 `after.split:false`，故 `splitKept = (false === false) = true` **无条件为真**，与对话框层对此层是否误伤**无关**。它**不是**恒真（`red4` 证明「误关分栏」这一改动方向**能被检出**），但**不能**检出「分栏**原本在场**时被对话框层关上」——恰是披露想兜住的那一类。

更关键的是：**探针现有 `C4`（`UX012-C4-split-state-esc-layering`）覆盖的是【绑定面板】开在分栏之上的分层，不是【工作区对话框】开在分栏之上**（C4 的 `panelWasOpenInSplit` / `panelWasClosed` 均取 `.nv-modal` 面板态）。因此披露中「分栏层状态不变由 C4 覆盖」属于**范围外引用**。

影响有限（真实风险低）：`SplitWorkspace` 的让位守卫（L3960）与 `NvConsole` 同款且**在本轮零改动**，加上修复自身只写 `entryOpen` ⇒ 误伤分栏需先改坏守卫，属独立回归面。故定 P2（记录/覆盖口径），非 P0/P1。

### ② N-1 残项（CHANGELOG 历史条目仍写「24 条」）处置恰当性 —— **判定：恰当**

**事实核验**：「24 条」在 `CHANGELOG.md` 现**仅剩 2 处**，且**都在历史条目内**（L49 CLEAN-007 F7 条目、L57 同批 R1 复审 C-5 条目）；本轮真实订正的目标面（探针文件头）**已正确改为 26**（`scripts/probe-nv-ux012.mjs:11-14`，我已逐行核对）。BUG-010 自己的条目（L41）写的是「**由**「24 条…」**订正为** 26 条」，属**引用旧值以陈述变更**，**非**现役失真。

**处置判定：恰当。** 依据充分且与我 R2 的既有口径一致：
- 历史条目是「**各批次落地时点的记录**」，施加「与当前状态一致」会**系统性假阳并要求改写历史**；
- 该纪律**已被机检化**——`COMPAT-015 F3` 的行号引用对账**显式豁免**「历史限定词行」，且 CHANGELOG **历史条目不在扫描面内**（`CHANGELOG.md:100` 明文：「CHANGELOG 历史条目不在扫描面内…同 C-F-6「历史锚点」纪律」）；`C-F-6` 条目亦有同款先例（L115）；
- 订正已有**归口**（留 CLEAN-006），未失跟踪。

**残留提醒（不升级为 finding）**：L49 / L57 同时含**陈旧向量计数**（「25 red + 11 ok」，现为 29 + 12）——与「24 条」同属该历史快照，随 CLEAN-006 一并处理即可。

### ③ 探针文件头分类桶（26 = 18 + 4 + 1 + 1 + 2）人工枚举、无机检自洽 —— **定级 P2**（归口建议见 §11 N-1）

**事实核验**：文件头（`scripts/probe-nv-ux012.mjs:11-14`）**无任何桶↔断言 id 映射**，5 个桶纯文字描述；`--falsifiability` 输出**只**校验「谓词数 / 向量数 / red 全 false / ok 全 true / 部署消费点 ≥1」，**不含**桶计数与断言总数的对账。

**可检性**：**总数 26 本身可核**——`report-ux012.json` 的 `tally.total = 26` 且我按唯一 id 独立枚举亦得 26（与 R2 的 25 条相比 +1 = 本轮新增断言，算术自洽 ✅）。但**桶的构成（18/4/1/1/2）零机检**：把任何一个桶改成 19 + 3 + 1 + 1 + 2 仍全绿。**这正是 N-1 的原始病根**（R2 时「24 = 17+3+1+1+2」的桶和与实测 25 不自洽）——**本轮修好了数字，但未消除致因**。

**且本次已可检出该口径的一处实质偏差（⇒ N-5）**：文件头把 4 条 DOM 属性读数点名为 **`A2` / `B2` / `A1b` / `A2b`**，但该 run 受 `focusMovable` 闸门记 N-A 的实为 **`A7b-focus-wrap` / `A1b-input-autofocus` / `A6-focus-restore` / `A2b-modal-semantics-full`**。即：**`A6` 与 `A7b` 未在文件头任何位置被点名**（它们才是被闸门限流的成员），而 **`A2` 与 `B2`**（语义面读数、不受闸门约束）**恰好是纯 DOM 属性读数**。根源 = 文件头把 4 个**闸门成员**与 4 个**DOM 属性读数**混为一谈：原文「4 条 DOM 属性读数（…，不可移 ⇒ N-A）」把两件事并列写在一处，导致真实受闸门者（A6/A7b）与真实读数者（A2/B2）被错误替换。因桶的构成无机检，**该偏差现场不可自证**——正是 N-1 建议的「桶↔id 显式映射 + 桶和机检」要消除的对象。

另附一处**口径歧义**：文件头括号内点名的 4 条 DOM 属性读数为 `A2 / B2 / A1b / A2b`，而 `report-ux012.json` 中恰这 4 条为 **N-A**（其余 22 条 PASS）。若「26 条 = 18+4+1+1+2」意指**总数**，则其中 4 条为 N-A ⇒ 与「按测量种类分类」口径相互缠绕，读者无法判定 26 是「PASS 22 + N-A 4」还是「26 条 PASS + 4 条 N-A = 30 观测」。建议定稿时显式声明口径。

**归口建议**：见 §11 N-1 的修复动作（① 在探针内建立**桶↔id 显式映射常量**并把「各桶计数之和 ≡ 断言条数」纳入 `--falsifiability` 机检；② 显式声明 26 是否含 N-A）。该口径与 R2 N-1 我的原始建议（「在探针内加一条自检断言『分类桶计数之和 ≡ 断言条数』」）**一致**。

---

## 8. P-10 与项目原则逐条

### P-10（宿主耦合入契约）

- **新增宿主耦合字面量 = 仅标准 DOM**：`keydown`（标准事件名）、`Escape`（标准键值）、`window.addEventListener/removeEventListener`（标准 Web API）——**均非宿主专有**，`lib/host-contract.mjs` 的 `regionLiterals`（槽位名 / 服务名 / `--dsw-alias-*` 令牌族 / 宿主 DOM selector）**不含**此类标准 DOM 面 ⇒ **无登记义务**，未登记**不是**漏登记。
- **`store.entryOpen`**：**插件自有 store 字段**（`lib/client.js:526`），非宿主导出/API 面 ⇒ 同无登记义务。
- **`regionLiterals` 双向 ⊆ 仍成立**：①我独立确认契约 `regionLiterals` 块**逐字节未变**；②`test/smoke.mjs` 的三类 region 双向对账断言（提取面 ⊆ 契约 ∧ 契约 ⊆ 提取面）随 smoke **297/0** 全绿 ⇒ 双向 ⊆ 保持成立。
- **零扩展**：`items[]` 49↔49、无面变化、`kind`/`necessity` 零变更（§6.2）⇒ 「契约零扩展」达成。

### 项目原则逐条（用户点名 4 项）

| 原则 | 判定 | 依据 |
|---|---|---|
| **P-04 测试看护** | **通过** | 真行为级探针断言（CDP 真按键）＋ 4 条 red 向量可失败性自证 ＋ 契约重基双向取证；四道门禁复跑全绿。缺口另记 N-1/N-3（P2/P3），均为**口径**而非「未做验证」 |
| **P-05 泛化性** | **通过（本案即 P-05 的正面样本）** | 修复落**正确抽象层**：不在入口处做一次性特例，而是补齐「让位链的终端消费者」这一**通用模式**（与 D-1 的 `BindDialog` 同构，两处代码逐字符同款）。且**全局排查**已做尽：全文件 `'Escape'` 4 处、`entryOpen` 写入点 4 处、四类模态（`NvConsole` / `BindDialog` / `WorkspaceDialog` / `SplitWorkspace`）**均已自洽**，**无第三处遗留缺口**（§3.1） |
| **C-02 防架构腐化** | **通过** | 无分层/依赖方向/单一事实源退化：新增监听**不新增事实源**（复用同一 `store.entryOpen`），让位判定仍由**消费方各自读 `store.get()`** 单点承担；未引入跨层直接调用（对话框不调用 `closeWorkbench`，符合分层） |
| **C-04 修改纯粹** | **通过** | 5 文件全部改动**服务于本任务**：`lib/client.js` +13 全为本修复及注释；`host-contract.mjs` 5 处全为重基 + 本轮 `revisions[]` 注记；`probe-nv-ux012.mjs` 删除 4 行 = 文件头口径订正（承接 R2 N-1，属本任务既定范围），新增 = 断言/谓词，均为本修复的看护；`test/smoke.mjs` +1/−1 = 陈旧行号副本同步（**若不改则 COMPAT-015 F3 转红**，属必要伴随）；`CHANGELOG.md` = 本条。**无顺带重构、无无关文件** |

---

## 9. AI 专项 5 项

| # | 项 | 结论 | 依据 |
|---|---|---|---|
| 1 | **mock 残留** | **0** | `lib/client.js` 新增代码无任何 stub/fake/spy；探针虽实施 CDP 请求拦截，但为**真宿主机 + 真浏览器 + 真 HTTP 路径**，且该拦截面（A9/A10/B3）**非本轮新增** |
| 2 | **硬编码返回值** | **0** | 新增监听无固定返回；`store.set({ entryOpen: false })` 为真实状态变更。**唯二考量项均清白**：① 探针 `dialogLabel` 期望值经 `pick('dialogTitle')` **从产品源码提取**（非复制字面量）；② 冻结核 `D6 EXPECTED_WRITE_PATHS` 为**声明式白名单**（冻结资产、本轮零改动） |
| 3 | **幻觉 API** | **0** | 全部 API 真实且既有：`window.addEventListener` / `window.removeEventListener`（标准 Web API）、`useEffect`/`useRef`/`useState`（宿主提供 React，`require('react')` 契约 item 2.2）、`store.get`/`store.set`（同文件既有）。**无**未声明模块、无不存在的方法名 |
| 4 | **未实现 TODO** | **0** | 新增区域无 `TODO`/`FIXME`/`XXX`/占位实现；`catch { /* ignore */ }` 为**同文件既定风格**（与 `BindDialog:2820`、`NvConsole:3406` 逐字符一致），非掩盖未实现的占位 |
| 5 | **过度实现** | **0** | 净 +13 行（含 6 行注释）严格对应 7 项要求，**无额外抽象、无预留参数、未顺带改邻近逻辑**（对比 C-04 通过）。全文件 diff 无无关增删 |

---

## 10. 真实环境纪律

- **diff 内零仓库外写入**：`git diff --numstat 509d028 a04ea89` 恰 5 路径（`CHANGELOG.md` +11、`lib/client.js` +13、`lib/host-contract.mjs` +5/−4、`scripts/probe-nv-ux012.mjs` +55/−4、`test/smoke.mjs` +1/−1），**无一**指向 `C:\Users\peter\.dsh` 或任何仓库外路径；被审 diff **不含**任何 `writeFile` / `mkdir` / `rm` / `unlink` 调用。
- **探针隔离面（冻结报告实证，非自述）**：`isolation.containment = {home:true, userprofile:true, appdata:true, localappdata:true, edge:true, tmp:true, novels:true}`；`isolation.env` 显示 `DSH_HOME` / `USERPROFILE` / `HOME` / `APPDATA` / `LOCALAPPDATA` / `TEMP` **全部重定向**至 `%TEMP%\clean004-probe-*\` 内；`plane` = 只读 npm 缓存中的 DSH 包；随机端口（`instanceUrlRedacted` 存在）。
- **真实 home 前后指纹一致**：`realEnvBefore` 记录真实 `C:\Users\peter\.dsh` 的 strict 指纹（`settingsSha` / `profileJsonSha` / `presetsSha`）；`realEnvVerdict = {ok:true, strictDeltas:{}, inventoryDeltas:{}, inventoryDeltaCount:0}` ⇒ **前后严格一致**。
- **未终止/接管任何进程**：探针隔离根 `cleanup.rootRemoved=true`（`removed: home/userprofile/appdata/localappdata/edge/tmp/novels`），未触碰用户进程；本次审查**亦未**终止任何进程。
- **冻结资产**：`probe-clean-004.mjs` 在 `509d028` / `a04ea89` / worktree **三方 blob 同一**（`8398719219…`）⇒ 「只运行不改」**已证**，且 sha256 与任务书锚值一致（§0）。

---

## 11. 发现列表

### P2
| # | 位置 | 问题 | 依据 | 影响 | 建议 |
|---|---|---|---|---|---|
| **N-1** | `CHANGELOG.md:41`（BUG-010 条目末） | **探针文件头分类桶仍为人工枚举、无机检自洽**（`26 = 18 + 4 + 1 + 1 + 2`）——总数 26 可核（`tally.total=26`，我独立枚举同得 26 ✓），但**桶的构成零机检**：改成 `19+3+1+1+2` 仍全绿。**这正是 R2 N-1 的原始病根**（桶和与实测不符）——本轮修好数字，**未消除致因**。另：桶口径歧义（括号点名的 `A2/B2/A1b/A2b` 恰为该 run 的 **4 条 N-A**，读者无法判定 26 是否含 N-A） | `scripts/probe-nv-ux012.mjs:11-14`（无桶↔id 映射）；`--falsifiability` 输出仅校验谓词/向量（我复跑确认）；`report-ux012.json` `tally={26,22,0,4}` | 「如实披露」的桶描述仍可能在下次改动后静默失真，且外部审计无法机检该口径 ⇒ R2 N-1 有复发风险 | ① 在探针内建立**桶↔断言 id 显式映射常量**；② 把「各桶计数之和 ≡ 断言条数」纳入 `--falsifiability` 机检（≥1 部署消费点）；③ 显式声明 26 条是否含 N-A。成本低，与既有「每条 ≥1 静态部署消费点」纪律同构 |
| **N-2** | `scripts/probe-nv-ux012.mjs:1008-1012`（新增段如实披露注释）；`CHANGELOG.md:37` | **`splitKept` 在真跑中为同值不变式（`splitBefore=false` ⇒ 无条件真），且其覆盖兜底被引到范围外的断言**。披露称「分栏**在场**时的 Esc 分层由探针 `C4` 与冻结探针 `B17` 覆盖」——但：① **`B17` = 新建弹窗态（`.nv-cmodal`）**，非分栏层（`probe-clean-004.mjs:974`：`escLayer1 = ({modal: '.nv-cmodal'..., console})`）；② **`C4` 覆盖的是【绑定面板】开在分栏之上**（`panelWasOpenInSplit` / `panelWasClosed` 取面板态），**不是【工作区对话框】开在分栏之上** | `report-ux012.json` `facts.workspaceDialogEsc.splitBefore=false`；冻结报告 `B17` 事实字段；`probe-nv-ux012.mjs:1052-1054` | **两处交叉引用失实** ⇒ 读者会错判该分层面已被行为级覆盖，而实际无任何探针验证「工作区对话框开在分栏之上」的 Esc 分层。真实风险低（`SplitWorkspace` 让位守卫亦含 `entryOpen` 且本轮零改动；`red4` 证明改动方向可检出），但披露口径不可留失实 | ① 订正 `B17` 引用为实际覆盖面（或改引真正覆盖分栏分层的断言）；② 或补一条「分栏在场 + 工作区对话框开其上 ⇒ 一次 Esc 只关对话框」的行为级断言（路径可构造：先建分栏再无绑定卡开控制台，成本与 C4 同量级）；③ 若维持同值不变式口径，MUST 明写「本断言不检出分栏在场时被误关」 |

### P3
| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| **N-3** | `test/smoke.mjs:691-698` | 早退顺序检查（`ordered()`）的**目标为 `useModalFocus(open, wsdlgRef)`**，未纳入本轮新增的 Esc effect。好在因该 effect 使用 `store`（模块级）而**未**捕获 `close`（L4495，早退后 const），把它移到早退之后仍会因 `close()` 只在渲染期可见而触发**真 TDZ ReferenceError**（React 状态更新异常 ⇒ 探针 catch 路径 ⇒ 断言红）⇒ **实际可检出，非漏洞**。仅断言**作用域与其注释口径**（「三处 hook 均在组件早退之前」）未覆盖新 hook | 把注释口径限定为该断言实际覆盖的 hook，或把 `useEffect(() => {\n        if (open !== true) return undefined\n        const onKey` 串一并通过 `ordered()` 检查，使契约表述与实际判别一致 |
| **N-4** | `%TEMP%\bug010-verify\report.json` | 冻结核复跑报告仅落在**仓外 TEMP**，而仓内 `docs/evidence/CLEAN-004/report.json` 仍是 `head=21b100ce`（tally 42/4/3）的旧副本 ⇒ 「冻结探针复跑读数」在**仓内无痕**、外部审阅者无法按仓内路径复核。本轮资产未被修改（三方 blob 同一，已证） | 与既有留痕纪律对齐，将本次 `report.json` 归档入 `docs/evidence/`（或明示该报告仅存 TEMP 且给出路径） |
| **N-5** | `scripts/probe-nv-ux012.mjs:11-14` | **桶口径双重失实（已核出具体偏差）**：① 文件头点名的 4 条 DOM 属性读数为 `A2` / `B2` / `A1b` / `A2b`，而该 run 受 `focusMovable` 闸门记 N-A 的实为 **`A7b-focus-wrap` / `A1b-input-autofocus` / `A6-focus-restore` / `A2b-modal-semantics-full`** ⇒ **`A6` 与 `A7b` 在文件头全无点名**（它们才是闸门成员），`A2` / `B2` 则不受闸门约束；根源 = 把「闸门成员」与「DOM 属性读数」两件事并列于同一括号；② 括号内「不可移 ⇒ N-A」使 26 无法判定是否含 N-A（读作「22 PASS + 4 N-A」时桶和 26 成立，读作「26 PASS + 4 N-A = 30 观测」时 `tally.total` 应为 30） | 把闸门成员与读数分类**分开陈述**；补齐 `A6` / `A7b` / `A2b` / `A1b` 的闸门口径点名；显式声明 26 条为计入 N-A 的总数（PASS 22 / N-A 4）；并入 N-1 的桶↔id 映射机检后由机检守住 |
| **N-6** | `test/smoke.mjs:2159`（本轮改动的注释行） | 注释仍将该处断言的标定写作 `COMPAT-015`，而同文件末条 `COMPAT-014 A-F9` 才是计数同步断言；本轮改动仅同步陈旧行号副本（必要且正确） | 顺手订正该注释的断言归属标注，避免审计时归因错位 |

> **发现分级口径**：P0 = 阻塞合并；P1 = 强烈建议本轮修改；P2 = 建议修改、可作为遗留项，**不阻塞合并**；P3 = 讨论/建议，不要求修改。本轮 **P0 = 0、P1 = 0** ⇒ 依 `skills/code-review/SKILL.md`「P0 = 0 且 P1 = 0 ⇒ 合并」；N-1 / N-2 建议紧随本地 CLEAN-006 批次收口。

---

## 12. 五维度结论（硬门槛项）

| 维度 | 结论 | 依据 |
|---|---|---|
| **1 正确性** | **通过** | 逻辑正确（只关本层 / `busy` 同判据 / 同引用移除）；边界条件（`open !== true` 早退不注册、卸载清理、`busy` 实时）；并发安全（同层监听按注册顺序派发，顺序经挂载序独立验证 + 真跑读数确证；无跨层共享可变状态新增）；资源管理（监听随卸载移除，且从不在早退后注册） |
| **2 安全性** | **通过（N/A 面已说明）** | 无输入校验/注入/敏感数据/权限面变化——本修复为**封闭的 UI 状态切换**，不引入外部输入、不做 I/O、不落盘、不涉权限；无硬编码密钥（AI 专项 §9 同结论） |
| **3 可维护性** | **通过（含 N-3 P3）** | 命名（`onKey`/`wsdlgRef`）与既有同款式一致；新增 effect 6 行 ≪ 50 行阈值；与 `BindDialog:2817-2822` 逐字符同构 ⇒ **零形态分叉**，可复用既有心智模型；注释充分且经逐条核对**与代码一致**（结论、deps、hook 位置陈述均属实）。唯一缺口 = smoke 早退断言的作用域口径（N-3） |
| **4 性能** | **通过** | 无算法引入（O(1) 条件判断）；单监听器，随 `open`/`busy` 变化才重注册（非每渲染）；无循环内 I/O、无 N+1、无大数据结构；对既有渲染路径零新增开销 |
| **5 测试覆盖** | **通过（含 N-1/N-2 记录面项）** | 核心路径有测试（**真行为级**：CDP 真按键 + 真实 DOM 观测，独立于 smoke 源码面断言）；边界测试（`busy` 中 Esc 不关由既有 `D1-busy-bind-panel`/`A10` 同族覆盖；前置未取得由 red3 防空真）；错误路径（`open!==true` 不注册 / N-A 如实标注不渲染成 PASS）；可失败性自证（4 red + 1 ok，`--falsifiability` PASS）。**判别力缺口** = 分栏在场态（N-2）与桶口径机检（N-1），均已如实披露 |

---

## 13. 硬门槛裁决

| 门槛项 | 阈值 | 实测 | 判定 |
|---|---|---|---|
| P0 阻塞问题数 | = 0 | **0** | **PASS** |
| 5 维度全覆盖 + 每条发现带级别 | = 100% | 5/5 维度有结论（§12）；6 条发现全部标注 P0~P3（§11） | **PASS** |
| 设计一致性（P-10 / 契约零扩展 / 不变量） | 已完成 | P-10 无触发项且 `regionLiterals` 双向 ⊆ 成立；契约 49↔49 零扩展；不变量（冻结资产三方同一、`regionLiterals`/`presetRowConfig[]`/`kindEnum`/`faces` 逐字节不变）全部成立 | **PASS** |
| AI 专项 5 项 | 全部完成 | mock / 硬编码 / 幻觉 API / TODO / 过度实现 **逐项为 0**（§9） | **PASS** |
| 红态证据充分性 | 明确表态 | **充分 —— 不需补 1 次端到端红态复跑**（构造性判定经独立复算成立且为**确定性**结论；§5） | **PASS** |

**结论：APPROVED_WITH_NOTES**（`unresolved_blockers=0`；P0=0、P1=0、P2=2、P3=4）

---

**证据索引（可复查路径）**：`git show a04ea89`（含 `--stat` / `-- lib/client.js` / `-- lib/host-contract.mjs` / `-- scripts/probe-nv-ux012.mjs`）、`git show 509d028:lib/client.js`、`git show {509d028,a04ea89}:lib/host-contract.mjs`、`git diff --numstat 509d028 a04ea89`、`git rev-parse/hash-object docs/evidence/CLEAN-004/probe-clean-004.mjs`、`git ls-files`（23 js/mjs 清单）、`lib/client.js:526 / 1416 / 3007 / 3229 / 3233 / 3392-3408 / 3590 / 3955-3965 / 4420-4495 / 4588-4606 / 4648 / 5181-5194`、`lib/client.js:187 / 228`（i18n `dialogTitle` vs `bindTitle`）、`lib/host-contract.mjs`（`items[]` / `revisions[]` / `regionLiterals` / `presetRowConfig[]` / `kindEnum` / `faces`）、`scripts/probe-nv-ux012.mjs:11-24 / 128-144 / 396-419 / 614 / 620 / 656 / 985-1021 / 1052-1054`、`test/smoke.mjs:684-706 / 2156-2160`、`test/smoke.mjs`（297/0 实跑）、`test/validate-preset.mjs`（PASSED）、`node --check`（23/0）、`scripts/probe-nv-ux012.mjs --falsifiability`（谓词 12 / red 29 / ok 12）、`CHANGELOG.md:33-43 / 49 / 57 / 93 / 100 / 115`、`README.md:169`、`docs/evidence/CLEAN-004/{probe-clean-004.mjs,report.json,report-rework-run.json,report-rework2-run.json,report-rework-discovery.json}`、`docs/review/CLEAN-007-R1.md`（C-2）、`docs/review/CLEAN-007-R2.md:29-35 / 177 / 229`、`%TEMP%\bug010-verify\report.json`、`%TEMP%\bug010-ux012\report-ux012.json`。
