# 代码审查报告 — UX-055 横幅守卫终版（R4 快审）

| 元信息 | 值 |
| --- | --- |
| Task | UX-055（P1，v0.4.0）— 交付验证修复：标题栏居中横幅碰撞守卫（A-prime 终版） |
| 审查轮次 | **R4**（快审，窄聚焦 R3 后增量；前轮 = R3 报告 `docs/review/UX-055-R3.md`，结论 APPROVED_WITH_NOTES/unresolved_blockers=0） |
| 审查对象 | `docs/review-UX-055-R4.diff`（168KB 累计全量；R3 后增量 = 横幅守卫三轮迭代终态）+ 当前 `lib/client.js`（3946 行）/`test/smoke.mjs` 交叉核验 |
| 背景事实 | Coordinator 三主题探针交付验证发现真实碰撞（title[327,644]/banner[480,740]/launch[652,765]）；三轮迭代（A 未生效 → B 固定阈值漏判 → A-prime 精确测量）；Developer 三场景 + Coordinator 三宽度（1600/968/2400/2560）双探针实证通过 |
| 审查者约束 | 只读审查 + 报告写入；未运行命令；验证事实（node --check / 29/29 / 144/144）为任务上下文提供 |

---

## 一、结论（round=R4）

```
结论: APPROVED_WITH_NOTES
unresolved_blockers: 0
P0: 0   P1: 1   P2: 0   P3: 3
```

**结论理由**：A-prime 逻辑正确性、中间态残留清理、hook 顺序恒定、范围纪律、断言质量逐项核验通过；三轮修复迭代的实证（三场景 + 三宽度探针）与代码事实一致。保留 **1 项 P1 备注**（同书书名变化源未入重测触发——低概率、有缓冲、修复成本一行；建议本轮修或记遗留计划，非阻塞）与 3 项 P3（非阻断）。

---

## 二、R3 后增量审查

### 2.1 A-prime 逻辑正确性（协议第 1 项）—— 通过

| 检查点 | 结论 | 证据 |
| --- | --- | --- |
| leftClear = 书名 span 自然宽 | ✅ scrollWidth 语义正确（flex 裁剪/ellipsis 下 scrollWidth 仍为内容自然宽，任意书名长度适配——注释明确该设计意图） | client.js:2934 |
| 右簇 min-left（无重叠过滤） | ✅ A 根因②修复：min(r.left of launch/ctl/note, width>0 过滤)——最短 left 天然覆盖与横幅重叠的控件；`rightStart===Infinity`（右簇全空）→ true 不误伤 | 2937-2943（无 `r.left > b.right` 过滤残留，grep 0 命中） |
| 安全边距 M=8 | ✅ 与 `.nv-bar` gap 8px 对齐 | 2924 |
| 降级无循环 | ✅ `visibility:hidden` 元素保留布局/rect → 测量与 RO 持续（碰撞解除自动恢复）；visibility 不改变尺寸 → 不触发 RO 死循环 | 3147 + 3836 CSS（`[data-hidden=true]{visibility:hidden}`）+ 583-585 注释 |
| 监听清理零泄漏 | ✅ RO disconnect + window resize removeEventListener，双 catch 包裹 | 2958-2961 |
| deps 完备性 | ⚠️ **P1-1**（见下）——四源覆盖三源：窗口/几何（RO bar + resize）、banner 内容宽（阶段名/统计随轮询更新 → RO banner）、书名切换（scrollWidth 变不触发 RO → deps novelId）；**缺「同一 novelId 下书名文本变化」** | 2962 |
| 早退守卫 | ✅ `snap.active !== true \|\| 任一 ref null → return undefined`（关闭态/首帧不测量） | 2923 |

**P1-1（备注，非阻塞）**：同一 novelId 下书名文本变化（真实路径：打开创作台 → ▶ 启动工作流 → 工作流定书名 → `current.title` 经轮询更新 → t-text 内容变 → **scrollWidth 变但 t-text 元素尺寸不变**（flex 分配）→ RO(title) 不触发；banner 不含书名 → RO(banner) 不触发；novelId 不变 → deps 不触发）→ 该窗口期内碰撞漏测（守卫目标同形态缺陷低概率复现）。缓冲因素：工作流运行中章/字/阶段统计变化会经 RO(banner) 触发重测（非确定性）。建议：deps 追加 title 文本值（如 `current?.title`）或在 t-text 挂字符级观察——一行级修复；若记录遗留计划亦可。**注：采纳修复时 smoke.mjs:841 的 deps 精确串断言需同步。**

### 2.2 中间态残留清查（协议第 2 项）—— 清干净

- **B 方案常量**：`BANNER_MIN_WIDTH` / `bannerNarrow` —— client.js **grep 0 命中**（代码无残留）+ smoke 负断言（845-846）交叉核对 ✓。
- **A 方案死代码**：①barRef 未绑定根因——现代码 `{ ref: barRef, className: 'nv-bar' }` 已绑定（3128）✓；②「r.left > b.right」过滤形态——代码 0 残留 + 注释明确标注「那是上轮 bug 的形态」（2936）✓；三 refs（barRef/bannerRef/titleTextRef）全部绑定（3128/3145/3137）✓；无未用 ref。
- 方案 B 的说明注释保留（2906）——历史记录性质，非死代码，可接受。
- 唯一注意：**CHANGELOG/README 未同步横幅守卫说明**（R4 增量零文档行）——P3（发布卫生：UX-055 条目可补一行「交付验证修复：标题栏横幅碰撞防护」；不阻断）。

### 2.3 hook 顺序恒定 + 早退守卫（协议第 3 项）—— 通过

- 新增 hooks（barRef/bannerRef/titleTextRef/bannerCollides/effect，2918-2962）位于组件 hooks 群（既有 hooks 至 2902 之后）**无条件区域**——组件渲染早退 `if (snap.active !== true) return null` 在 **3028**（所有 hooks 之后）→ hook 顺序恒定、mockReact 单帧求值兼容 ✓。
- effect 内早退 `return undefined` 不触 hook 顺序 ✓；所有 hook 调用无条件 ✓。
- `setBannerCollides` 仅在 effect 内 measure 调用（非渲染期 setState）✓；值不变时 React bail-out 无性能问题 ✓。

### 2.4 范围纪律（协议第 4 项）—— 通过

- R3 后增量 = client.js（2906-2962 守卫 + 3128/3137/3145-3148 绑定/类化 + 3836 CSS 1 行 + 注释）+ smoke.mjs（818-846 新增 1 check + 标题串）——**无其他夹带**（无常量抽取/无样式其他改动/无文档变更/无 i18n 变更）。
- 与 R3 终态及既有开放项（R1 P2-3 死键、P3-A~D 等）零交集 ✓。

### 2.5 smoke 断言质量（协议第 5 项）—— 可接受（P3 评定）

- 新 check（smoke.mjs:824-846）混合：**行为契约**（`data-hidden`/`visibility:hidden`/`nv-bar-banner` 类化——对终端语义，好）+ **历史根因锁定**（`{ref: barRef, className:'nv-bar'}` 绑定、BANNER_MIN_WIDTH/bannerNarrow 负断言——针对 A 根因①/B 残余，值得精确锁定）+ **实现细节**（`title.scrollWidth + M`、`querySelectorAll` 选择器串、`}, [snap.active, snap.novelId])` deps 精确串）。
- 评定：该测试环境无 DOM（源码级静态断言），行为级验证由 Coordinator 三宽度探针承担（已实证通过）——**对重构容忍度低为环境约束下的合理取舍，非阻断**；P3 备注：①deps 精确串（841）与 P1-1 修复强耦合（改 deps 即需同步）；②建议未来演进（若引入 DOM 测试环境）将实现细节断言重定向为行为断言（`data-hidden` 呈现 + 探针几何）——发布卫生/测试基建语境。

---

## 三、发现列表（R4）

### P0（0 项）· P2（0 项）

### P1（1 项，备注）
- **P1-1** deps 缺口：同一 novelId 下书名文本变化（工作流定书名）不触发重测——见 2.1；建议 deps 追加 title 文本值（一行）+ smoke 841 断言同步；或记录遗留计划。

### P3（3 项）
- **P3-1** CHANGELOG/README 未记录横幅守卫（发布卫生：UX-055 条目补一行，可选）。
- **P3-2** smoke 断言对重构容忍度（环境约束下合理；未来 DOM 测试环境可重定向为行为断言）。
- **P3-3**（延续 R3 P3-4）渲染树级「已加载 0 本」断言建议后续补（维持前轮备注）。

---

## 四、硬门槛裁决（R4 快审）

| 门槛项 | 阈值 | 结果 |
| --- | --- | --- |
| P0 阻塞问题数 | =0 | ✅ **0** |
| 5 维度全覆盖（本增量窄面） | =100% | ✅ 正确性（2.1 逻辑链）/ 安全性（纯前端测量，无新增面）/ 可维护性（2.2/2.4/2.5）/ 性能（RO/resize 既有模式、无轮询新增）/ 测试覆盖（2.5 + 探针实证引用） |
| 每条发现标注级别 | =100% | ✅ P0×0 / P1×1 / P2×0 / P3×3 |
| 设计一致性检查 | 已完成 | ✅ UX-040 居中横幅定案保持（`position:absolute; left:50%` 未动，diff L847 断言锁定）+ DEC-021/022/023 无冲突（守卫为行为逻辑，无视觉值新增） |
| AI 专项 5 项 | 全部完成 | ✅ 增量无 mock/硬编码/幻觉 API/TODO/过度实现（守卫为既有测量模式复用） |
| 结论可复查 | =100% | ✅ 每条指向 client.js:L# / smoke.mjs:L# |

**最终结论：`APPROVED_WITH_NOTES`，`unresolved_blockers=0`。** 复审链终态保持（R3 通过 → R4 快审通过）；P1-1 为非阻塞备注（建议本轮一行修复或记录遗留计划），P3×3 非阻断。

---

*审查依据：R4 diff 全量 + lib/client.js（2906-2962 / 3125-3170 / 3742-3758 / 3836）+ smoke.mjs（818-848）+ 任务上下文提供的探针实证事实。未运行命令。*
