# 发布审查报告 — REL-006 v0.5.2（R2 复审）

- **任务 ID**：REL-006（P1，v0.5.2 发布，BUG-006 安装通道适配承载）
- **round**：**R2**（复审轮）
- **前轮报告引用**：`docs/review/REL-006-R1.md`（≡ `.governance/review-REL-006.md`，245 行，2026-09-12，结论 `NEEDS_CHANGE`，硬门槛 3 PASS / 2 FAIL〔H1·H2〕，findings 9 条 P0×0 / P1×4 / P2×3 / P3×2）——本报告为 M7.4 step 4.6 的**强制复审轮**，逐条比对前轮 9 条 findings，不绕过前轮直接给结论。
- **审查时间**：2026-09-12（发布同日，返工轮之后）
- **审查员**：Release Reviewer Agent（只读；除本报告两份落盘外未修改任何发布文档/代码/tag；**无 Bash 权限**）
- **绑定 SKILL**：`release-review`
- **被审对象（R2 事实基准）**：

  | 对象 | SHA / 值 | 说明 |
  | --- | --- | --- |
  | 返工提交 | `792030c`（5 文件 **+156/−13**） | release-notes +60/−11｜checklist +12/−3｜rollback-plan +92/−1｜feature-flags +4/−2｜plan-tracker +1（里程碑补 `v0.5.1` 行） |
  | 证据固化提交 | `914725f8` | 新增 `docs/release/evidence/rel006-drill-20260912/`（15 件 / 94.9 KB）+ tracker REL-006 行更新 |
  | **当前 tag 对象** | `793dde78`（annotated `v0.5.2`）→ peel **`914725f8`**（= HEAD） | 旧对象 `7ca99d6d` → peel `e1f25df8` **已删除**；**仍未 push**（远端 main = `3442f396`，远端无该 tag）【C】 |
- **结论**：**`NEEDS_CHANGE`**（硬门槛 **4 PASS / 1 FAIL**〔H1〕；前轮 9 项 = 已修复 **8** / 未修复 **1**〔F4 留痕面〕/ 新引入 **0**；本轮新发现 **4** 条 N1~N4 = P2×2 / P3×2）
- **push 前置判定**：**不可 push**（前置未满足，见 §4）——但补齐 2 项**纯文档留痕**后即可 push，无内容性返工。

---

## 0. 证据基准与能力边界（P-01）

| 标记 | 含义 |
| --- | --- |
| 【文档】 | 本次实际读取的文件 + 行号（可复查） |
| 【C】 | Coordinator 实测提供（本 Reviewer **无 Bash 权限，无法复跑**；按任务书内联事实引用） |
| 【待验证】 | 文档自述但本轮无独立证据者——**不计入 PASS 依据** |

**审查员能力边界（如实声明）**：本轮为**只读文档 + 仓内证据交叉核对**；未执行任何命令、未跑门禁、未复算 diff、未检查 git 对象。凡 SHA/远端/tag/门禁结果均以【C】引用，未独立复现。仓内可读面 = 本轮实际读取的 6 份发布文档 + 证据目录索引与结构化结论（`README.md` 42 行、`drill-summary.json` 114 行）。

---

## 1. 前轮 9 条 findings 逐条比对（MUST 逐条，不得绕过）

| # | 前轮 finding（严重度） | 前轮复审验证点 | R2 裁定 | 依据 |
| --- | --- | --- | --- | --- |
| **F1** | 回滚方案未演练（H2 FAIL）｜P1 | 演练输出（命令 + 实际结果 + 幂等 diff 为零）**或** decision-log 风险接受条目 | **已修复** | 演练已执行且**证据在仓内可读**：`rollback-plan` §6 L78「✅ 已执行（隔离环境，2026-09-12 返工轮）」+ §7 L84-169 全量记录；`checklist` §C.2 L36 同步；证据目录 `docs/release/evidence/rel006-drill-20260912/`：`drill-summary.json` 7 leg（A1~A5 / B1 / B2）**全部 `Exit: 0`**、有效版本序列 `null→null→0.5.2→0.5.2→0.5.1`、`LegacyVer` `0.5.1→0.5.2→0.5.1`、旧布局 B1/B2 `SnapLines=86` 一致；`README.md` L27-31 幂等 diff A1→A2=0 / A3→A4=0 / A4→A5=0 / B1→B2=0、`realPathLeak=0`【文档】；原始输出 `drill-log.txt`（4496 B）与 9 份 `snap-*.txt` 已随 `914725f8` 入仓【C】。**残留**：`release-notes` §8 清单表步骤三行**未同步**（仍写「⚠️ 未执行」）⇒ 计入本轮 **N2**（该项不影响 F1 实质闭合） |
| **F2** | push 未执行 + 推送范围可见面变化未声明/授权｜P1 | 发布记录中的推送范围声明 + 用户授权留痕（或切分方案） | **已修复** | 声明：`release-notes` **§3.1.1 L52-61**（「`git push origin main v0.5.2` 将同时把本机领先远端的 **44 commits**（含 COMPAT-002~016 的 v0.6.0 线工作树内容）推送至远端主线」+「属发布决策，须用户授权」+「**先补齐审查项、后 push**」纪律）+ **§9-1 L201-203** 复述；授权留痕：`.governance/decision-log.md` **L36 DEC-027** ②「**授权 Coordinator 重试 push**（用户侧已授予 `workflow` scope）」+ 执行序「补齐 → 重定 tag → R2 复审 → push → `--lineage-mode released` 核验」【文档】。push 未执行**符合**既定执行序（R2 之后），非缺陷 |
| **F3** | `check-release` 门禁不可用（21 issues）⇒ 发布就绪无可复查机检 PASS｜P1 | SYSGAP 登记条目 ID + 替代判据的裁定记录 | **已修复** | ①登记：`risk-log.md` **L8 RISK-002** 扩展段（`verify_workflow.py` **L7181** 默认读插件仓 `project/CHANGELOG.md`、无 `--changelog`、另行期望插件仓 `1.0.0 依赖链`/`REQ-059~064`/插件自家测试路径）+ **L11 RISK-005** + **L12 RISK-006**（同族）；②裁定：`decision-log.md` **L37 DEC-028**「本仓『发布就绪』替代判据集合（`check-release` 跨根缺陷期内的**正式口径**）」a)~h) 全部条目化（含 g) Release Reviewer 终态 = APPROVED 或 APPROVED_WITH_NOTES(unresolved_blockers=0)；h) 未执行项如实标注、**禁止**以「未执行」充「已验证」；废止条件 = 上游修复后重跑 PASS）【文档】；③**未包装为 PASS**：`checklist` §G L73 仍记 **FAILED — 21 issue(s) / 不可用**，L75 明文「状态维持 `FAILED`…**不得改写为 PASS**」【文档】——合规 |
| **F4** | tag 树边界三重不一致（不含工件 / 含 v0.6.0 线 / 范围仅靠声明）｜P1 | **重定后的 `git for-each-ref refs/tags/v0.5.2`（新 target SHA）** 或用户确认条目 + `--release-commit` 最终采用的 SHA **与记录一致** | **未修复（留痕面未同步）** | **处置动作已执行**（DEC-027 ①：tag 重定至「含全部发布工件的提交」）⇒ 新 tag peel = `914725f8`，其树 ⊇ 三件 `docs/release/*-0.5.2.md` + 演练证据 15 件 ⇒「**发布工件不在 tag 树内**」这一**可机检不一致已消除**；「tag 树含未发布 v0.6.0 线」按 DEC-027 选项①由**声明**承担（结构性，仍不可机检），`rollback-plan` §2 路径 B 保留精确剥离通道【C/文档】。**但**：发布工件与记录**仍陈述旧 tag 身份**（`checklist` L6 `对象 7ca99d6d… → peel e1f25df8…`；`rollback-plan` L5 同 + §7.1 L95 演练引用值同；`release-notes` §7 R7 L135 `tag 对象 7ca99d6d → peel e1f25df8`、§10 L231 同；`plan-tracker` L177「SHA 见 EVD-097 补录」）⇒ **记录值与 tag 实际 target 失配**，且 `7ca99d6d` 作为 tag 对象**已随删 tag 消失**（`for-each-ref` 不再返回）⇒ R1 明示的验证点「与 `--release-commit` 最终取值一致」**未满足**。详见 **N1** |
| **F5** | 唯一交付面（安装通道）未对**被发布树**安装冒烟；清单 §D.1 的 ✅ 实为 PARTIAL｜P2 | 隔离安装冒烟输出（隔离根 / 命令 / 结果）**或** risk-log 等效接受条目 | **已修复** | `checklist` **§D.1 L44** 已改为「✅（口径已订正，F5）」并给出：隔离根 `%TEMP%\rel006-drill-20260912`、对 `git archive v0.5.2` **导出树**执行安装/重装/回滚往返、有效版本正确 + 幂等 diff=0，且**明文写出订正说明**（原 ✅ 的证据面错位 = 证据来自 BUG-006 交付内、早于发布树，证据对象 ≠ 被发布树；现由对**被发布树**的实测替代）；措辞合规 = **「隔离环境安装冒烟（环境变量重定向至临时目录）通过」**，**无**无限定语的「真实安装」【文档】。**残留**：`release-notes` §8 清单表步骤四行仍沿用旧口径（「BUG-006 既有隔离实例实证」）⇒ 计入 **N2** |
| **F6** | 版本里程碑表缺 `v0.5.1 发布` 行（R3 历史遗留）｜P2 | 里程碑表含 v0.5.1 行，或显式延期条目 | **已修复** | **本轮实读** `.governance/plan-tracker.md`：**L190** `| v0.5.1 发布 | v0.5.1 | 2026-09-07 | 已发布（历史缺口补录，F6） |`，**L191** `| v0.5.2 发布 | v0.5.2 | 2026-09-12 | 达成（REL-006，本地 annotated tag v0.5.2——未 push，RISK-004 待授权） |`；路线图 **L177** v0.5.2 行「已发布」含范围声明【文档】（前轮【待验证】项已由本轮实读关闭）。**残留**：L191 的「RISK-004 待授权」与 DEC-027 已授权不一致 ⇒ 计入 N4 |
| **F7** | `archive.py migrate --auto --dry-run` 解析已发布版本 = 0 个（SYSGAP-001 家族边缘）｜P2 | SYSGAP 条目（含本项）或修复后的 dry-run 真实输出 | **已修复** | **DEC-028 L37** 事实段明列「F7 同族：`archive.py` 解析已发布版本 = 0 个（8 个 tag 视而不见）」并纳入替代判据裁定范围；`checklist` §G L72 保留真实输出「跳过（已发布版本数解析 0<2；exit 0）｜不阻断（SYSGAP-001 家族边缘，无数据损失）」【文档】。（`risk-log` L8 行内该子项因行文过长在本次 grep 输出被截断，未逐字复核——已由 DEC-028 条目覆盖，不影响裁定） |
| **F8** | 发布文档交叉引用陈旧（「§7（R1~R7）」实为 R1~R8）｜P3 | 两处引用与 §7 实际条目数一致 | **已修复** | `checklist` **§B.4 L27** =「发布记录 §7（**R1~R8**）」；`release-notes` **§8 L183** =「§7（**R1~R8**）」——两处均已修正，且 §7 表格实际含 R1~R8（L129-136）【文档】 |
| **F9** | `feature-flags` §2 取证命令为相对引用、事后不可复现｜P3 | §2 取证命令含绝对 SHA 且复算结果 = 4 文件 | **已修复** | `feature-flags-0.5.2.md` **§2 L17** = `git diff e1f25df8^ e1f25df8 --name-only`（**绝对 SHA**），结果 4 文件（`CHANGELOG.md` / `package.json` / `.governance/plan-tracker.md` / `docs/review/REL-006-release-notes.md`）、零 `lib/**`；**L19** 另加「取证时点 / 订正时点」说明与「任何时点复跑结果恒为上述 4 文件」的可复算承诺【文档】。**待验证**：该命令的实际复算（R2 无 Bash；`release-notes` §10 返工轮日志 L237 记同结果【C】） |

**统计：已修复 8 / 未修复 1（F4）/ 新引入 0**（前轮 9 项中无一项被本次返工引入新缺陷）。

---

## 2. 前轮 §5「复审（R2）必查项」五项结论（MUST 覆盖）

| # | 必查项 | 结论 | 依据 |
| --- | --- | --- | --- |
| ① | 补强证据在仓内**可读** | **满足** | `docs/release/evidence/rel006-drill-20260912/`（15 件 / 94.9 KB，随 `914725f8` 入仓）【C】；本轮**实际读取**该目录 `README.md`（索引：`drill-log.txt` / `drill-summary.json` / `version-flip-table.json` / 前·后 fingerprint / `snap-A1..A5/B1/B2/leg1` / `isolate-env.ps1` / `run-drill.ps1`）与 `drill-summary.json`（7 leg 结构化结论）——**结论可与索引逐项对齐**，非转述【文档】 |
| ② | 清单 §C.2 / §D.1 / §F 结论与补强后事实一致（**不得**「✅ 已验证」而无演练输出） | **部分满足** | §C.2 L36 =「✅ **已执行（隔离环境）**」**且**同格给出演练要素（隔离根 / 取树方式 / 版本序列 / 幂等 diff / 已知残留）⇒ 无「✅ 而空证」；§D.1 L44 = ✅ + 被发布树隔离实测 + 订正说明 ⇒ 一致；**§F L63 不一致**：仍写「条件 = ①push **待用户授权**（RISK-004，本机凭证缺 `workflow` scope）」，而 DEC-027 已裁定授权 + 用户已授予 scope（**口径陈旧**，方向为保守而非夸大）——另 **清单头部 L6 tag 身份**与 tag 实际 target 不符（N1）。**注**：同族错位在本轮**迁移到 `release-notes` §8**（步骤三行「⚠️ 未执行」与其同文件 §4.1「已执行」自相矛盾）⇒ N2 |
| ③ | 发布文档交叉引用已修正（F8） | **满足** | 见 §1 F8（两处 R1~R8） |
| ④ | `feature-flags` §2 取证命令已改绝对 SHA（F9） | **满足** | 见 §1 F9（`e1f25df8^ e1f25df8` + 时点说明） |
| ⑤ | 结论终态字段自洽 | **满足（本 round 裁定 = `NEEDS_CHANGE`）** | 按前轮同款口径：`unresolved_blockers` 字段**仅在 `APPROVED_WITH_NOTES` 终态下**为通过判据（R1 §5 ⑤）。本 round 非该终态 ⇒ 不以 `unresolved_blockers=0` 主张通过；机读输出 `review-verdict` 中 `unresolved_blockers = 2`（= N1 / N2 两项**须先修后 push** 的阻断项）以保持字段自洽与可机检 |

---

## 3. 硬门槛裁决表（H1~H5，逐项 PASS/FAIL + 证据）

| # | 硬门槛项 | 阈值 | 裁决 | 证据与理由 |
| --- | --- | --- | --- | --- |
| **H1** | 发布检查清单**全部 PASS** | = 100% | ❌ **FAIL** | 前轮 4 项驱动中 **§C.2（回滚已验证）与 §D.1（唯一交付面证据面）已闭合**（§1 F1/F5）；**残留 2 项非 PASS**：① **§G L73** `check-release` = `FAILED — 21 issue(s)` / 门禁不可用（诚实保留、未包装为 PASS；已由 **DEC-028 a)~h)** 提供**正式替代判据口径**，故该项性质 = **外部工具不可用 + 已裁定替代**，非本版交付缺陷）；② **§F L63** 决策口径 =「有条件发布」且条件描述（push 待授权）**已过期**，未同步 DEC-027（N4）。**另**：清单头部 L6 记录的 tag 身份与事实不符（N1）⇒ 在该记录未校正前**不能证明「全部 PASS」**。**N/A 项**（监控/告警/量化指标）经核为诚实的结构性不适用，**不构成 FAIL**（沿用 R1 口径）。⇒ **未达 100% PASS** |
| **H2** | 回滚方案存在且**已验证** | = 已验证 | ✅ **PASS** | 方案存在且命令级（`rollback-plan` L20-51 三路径）；**已验证**：§6 L78/§7 L84-169 隔离演练（路径 A 往返 `v0.5.1 → v0.5.2 → 回滚 v0.5.1`，6 次 install 全 `exit=0`，有效版本 `0.5.1/0.5.1/0.5.2/0.5.2/0.5.1`；幂等 A1→A2=0、A3→A4=0、**回滚 A4→A5=0**、旧布局 B1→B2=0；`realPathLeak=0`）+ **原始输出入仓**【文档/C】。**残留如实登记**：闭环 A2→A5 diff=4（`profiles/web/node_modules` 链接 + profile `package.json` 280→408 B 不被 v0.5.1 清理）⇒ 语义边界「**回滚 = 版本回退，非 DSH_HOME 状态还原**」已写入 §6 L80 / §7.4 L142 / `checklist` §C.2 L36 / 证据 `README.md` L29——**如实、不掩盖**。**演练对象与被发布树的一致性**：演练用 `git archive v0.5.2`（当时 peel = `e1f25df8`），重定后 peel = `914725f8`；二者之间仅 `792030c`（4 份文档 + plan-tracker）与 `914725f8`（证据目录 + tracker）⇒ **`install.ps1`/`install.sh` 内容无变化**（依据【C】文件清单，非独立复跑）⇒ 演练对**安装面**的代表性仍成立；其身份引用失配另计 N1 |
| **H3** | CHANGELOG 用户视角完整 | 关键段全部覆盖 | ✅ **PASS** | 沿用 R1 结论（`[0.5.2]` 段 1 条 BUG-006、症状链路 + 兼容边界、`[Unreleased]` 保留 v0.6.0 线全量条目、移动纯粹性、版本三方一致）；本轮返工仅动 4 份文档 + tracker【C】，**无新反证** |
| **H4** | breaking changes 已标注 | = 100%（与 diff 对照） | ✅ **PASS** | breaking 面 = **空集**（`792030c` 5 文件 = 4 份 `docs/**` + `.governance/plan-tracker.md`；`914725f8` = 证据目录 + tracker【C】；零 `lib/**`、零 `install.*`、零 CI）⇒ 覆盖 0/0；条目内已声明「旧布局行为完全不变」，且新证据 **B1→B2 `diff=0`** 提供**命令级**旁证（强于 R1 时的声明） |
| **H5** | Feature Flag 关闭验证 | 全部通过 | ✅ **PASS** | 本版零 flag（`feature-flags` §2 取证 0 命中 + 零 `lib/**` 改动）；F9 订正后取证命令**可复算**（绝对 SHA）；既有 kill switch 未触及，`smoke` **282 passed / 0 failed**【C】 |

**裁决汇总：4 PASS / 1 FAIL（H1）** ⇒ 按角色定义「任一 FAIL 即阻断」⇒ 结论**不得为 `APPROVED` / `APPROVED_WITH_NOTES`**。

---

## 4. tag 边界与 push 前置条件判断（专门裁决）

### 4.1 重定后 tag 是否已消除**可机检偏差**：**部分消除（1/2 项消除，1 项按 DEC-027 由声明承担；另新增 1 项留痕失配）**

| 前轮 F4 的三重不一致 | R2 状态 | 依据 |
| --- | --- | --- |
| ① tag 树**不含**三件发布工件 | ✅ **已消除**（可机检） | 新 tag peel `914725f8` ⇒ tag 树 ⊇ `docs/release/{release-checklist,feature-flags,rollback-plan}-0.5.2.md` + `docs/review/REL-006-release-notes.md` + 演练证据 15 件【C】。**此即 R1 建议的零成本首选路径（选项①）** |
| ② tag 树**含**未发布 v0.6.0 线（COMPAT-002~016） | ⚠️ **仍存在（结构性）**，已按 **DEC-027 ①**「tag 重定为含全部发布工件的提交」**显式接受**，范围口径仍以**声明面**承担（`release-notes` §3.2 + 路线图 L177 行 + `checklist` §B 分层机检）；**声明面不可机检这一事实未改变** | 【文档/ C】 |
| ③ tag ↔ CHANGELOG ↔ package.json 版本三方一致 | ✅ 成立 | `0.5.2` 三处一致（R1 已裁 + 本轮无改动该三处）【文档/C】 |
| ④ **新增**：发布记录中的 tag 身份与 tag 实际 target **失配** | ❌ **未消除**（N1） | `checklist` L6、`rollback-plan` L5 / §7.1 L95、`release-notes` §7 R7 L135 / §10 L231、`plan-tracker` L177 均指向 **旧对象 `7ca99d6d` → peel `e1f25df8`**；实际 = **`793dde78` → peel `914725f8`**【文档/C】 |

**裁定**：重定动作**本身正确且已达成 R1 选项①的机检目标**（工件入 tag 树）；但 **R1 为 F4 设定的验证点含「与 `--release-commit` 最终取值一致」这一留痕面要求，该面未同步** ⇒ F4 不能判「已修复」，且该失配是**可机检的自述—事实错位**（任何复核者按文档值执行 `for-each-ref`/`--release-commit e1f25df8` 即得 mismatch）。因 `7ca99d6d` 已随删 tag 不再由 `for-each-ref` 返回，**旧值不可能事后成立**，必须改写。

### 4.2 push 前置条件判断：**不可 push**（2 项阻断 + 1 项口径陈旧；均为**纯文档留痕**，无内容性返工）

| 前置项 | 状态 | 依据 |
| --- | --- | --- |
| tag 树 ⊇ 全部发布工件 | ✅ 满足 | §4.1 ① |
| 用户授权（DEC-027 ② + `workflow` scope） | ✅ 满足 | DEC-027 L36 ② 与 RISK-004 L10「用户 2026-09-12 已授予 `workflow` scope 并授权 Coordinator 重试」【文档】 |
| R1 纪律「先补齐审查项、后 push」 | ❌ **未满足** | 补强项 4/4 已落地，但 **F4 留痕未同步（N1）** 与 **`release-notes` §8 自相矛盾（N2）** 未闭合 ⇒ 若此刻 push，**发布记录将与 tag 事实永久失配**（此后修正须走 revert + 新版本号，即 R1 明确警告的「固化不可逆事实」） |
| 发布就绪替代判据（DEC-028 a~h） | ❌ **g) 未满足** | g) =「Release Reviewer 独立审查终态 = APPROVED 或 APPROVED_WITH_NOTES(unresolved_blockers=0)」——本 round 终态为 `NEEDS_CHANGE` ⇒ 替代判据集合**尚未通过**，故**不得**以 DEC-028 支持 push（另有 N1/N2 实质缺陷） |
| 口径一致（§F / plan-tracker 授权态） | ⚠️ 陈旧 | N4：`checklist` §F L63、L5/L7 与 `plan-tracker` L191 仍写「push 待授权 / RISK-004 待授权」 |

**结论**：**不可 push**。补齐 N1、N2（并顺手同步 N4 口径）后即可 push——届时 DEC-028 g) 由 R3 终态承接，push 后须即刻补 `check-release --version 0.5.2 --require-changelog --lineage-mode released --release-commit **914725f8**`（**必须使用新 tag target**，不得使用文档中现存的 `e1f25df8`）。

---

## 5. 新发现（4 条：P2×2 / P3×2）

### N1 — 发布工件/记录中的 **tag 身份未随重定同步**（自述—事实机检失配）｜**P2**（新引入·返工轮处置动作的留痕面）
- **现象**：tag 已重定为 `793dde78` → peel `914725f8`，但 5 处文档仍陈述旧身份 `7ca99d6d` → peel `e1f25df8`；该旧 tag 对象**已删除**。
- **证据（行号）**：`release-checklist-0.5.2.md` **L6**（发布提交 `e1f25df8`；tag 对象 `7ca99d6d…` → peel `e1f25df8…`）；`rollback-plan-0.5.2.md` **L5** + **§7.1 L95**（演练前 `rev-parse v0.5.2` = `7ca99d6d…` → peel `e1f25df8…`）；`REL-006-release-notes.md` **§7 R7 L135**（"tag 对象 `7ca99d6d…` → peel `e1f25df8…`"，EVD-097 补录值）+ **§10 L231**（返工轮 rev-parse 值）+ **L240**（「未执行任何 tag 命令」——已被 DEC-027 执行推翻）；`plan-tracker.md` **L177**（"SHA 见 EVD-097 补录" ⇒ 指向旧值）；实际值 = `793dde78` → `914725f8`【C】；`evidence-log.md` **L268 EVD-097** 亦记旧值（历史行，需以新证据行 supersede）。
- **影响**：①F4 的验证点（记录与 tag target 一致）**机检失败**；②`--release-commit` 取值口径歧义——若按文档取 `e1f25df8`，released 模式核验将与 tag target 不符；③R1 警告的「push 后固化」在此项上**尚未固化**（仍未 push）⇒ 修复成本极低。
- **修复建议**：在返工/收尾提交中统一改写为「发布提交 `e1f25df8`（版本 bump）+ 返工提交 `792030c` + 证据固化提交 `914725f8`；**tag `v0.5.2` 对象 `793dde78` → peel `914725f8`**（= 当前 HEAD）」，并在 EVD 追加一条 supersede 记录（`7ca99d6d` 已删除的说明）。
- **R3 复审验证点**：`checklist` L6 / `rollback-plan` L5·L95 / `release-notes` L135·L231 / `plan-tracker` L177 全部改为 `793dde78 → 914725f8`，与 `for-each-ref` 结果一致。

### N2 — `REL-006-release-notes.md` §8 清单表两行**与前文自相矛盾**｜**P2**（新引入·返工轮只改了 §4.1 未改 §8）
- **现象**：同一文档内，§4.1（L86/L88）与 §6（L121）已声明两项**已执行**，而 §8 清单表仍写「未执行 / 旧口径」。
- **证据**：`release-notes` **L185**「步骤三｜回滚方案已验证（测试环境执行）｜⚠️ **未执行**｜§4.1 如实标注（非运行态系统；步骤确定性）」 **vs** **L88**「回滚演练…**已执行（隔离环境，2026-09-12 返工轮）**」+ **L121**「状态：**已定义、已演练（隔离环境）**」；`release-notes` **L188**「步骤四｜发布后验证清单｜✅｜三件套（§4）+ **用户侧实机使用（BUG-006 既有隔离实例实证）**」 **vs** `checklist` **§D.1 L44** 已订正为「被发布树隔离实测」（F5）。
- **影响**：单一发布工件**同时主张「未执行」与「已执行」** ⇒ 未来复核者无法判定发布事实（违反 P-01）；正是 R2 必查 ② 所禁的「自述—事实错位」同族（本次方向为**漏更新**，未夸大）。
- **修复建议**：§8 步骤三行改为「✅ 已执行（隔离环境，2026-09-12 返工轮，见 §4.1 / `rollback-plan` §7）」；步骤四行改为与 `checklist` §D.1 同款口径（被发布树隔离安装冒烟 + 限定语）。
- **R3 复审验证点**：§8 两行与 §4.1 / §6 / `checklist` §C.2·§D.1 措辞一致，无「未执行」残留。

### N3 — `rollback-plan` §7.5 关于工件存放的陈述已被 `914725f8` 推翻｜**P3**（新引入）
- **现象**：**L158**「工件位于系统临时目录，**非仓库内资产——它们不随提交入仓**；本 §7 与 `release-checklist` §C.2/§D.1 为仓库内可读的记录面」；而证据目录已随 `914725f8` **入仓**（15 件 / 94.9 KB）【C】，`evidence/.../README.md` L3 自述「由 Coordinator 于 2026-09-12 **固化入仓**」。
- **影响**：R2 必查 ① 的「仓内可读」在**事实层已满足**，但 `rollback-plan` 的陈述仍指向「不入仓」⇒ 两处自述互斥，弱化证据可发现性。
- **修复建议**：改写为「工件已固化入仓：`docs/release/evidence/rel006-drill-20260912/`（原 temp 副本可能被清理）」。
- **R3 复审验证点**：§7.5 与证据目录 README 的存放口径一致。

### N4 — 发布边界/授权口径陈旧（`§F`、清单头、plan-tracker、路径 C 范围）｜**P3**（新引入）
- **证据**：`checklist` **L5**（发布类型「未 push —— RISK-004」）、**L7**（「条件 = push **待用户授权**；本机凭证缺 `workflow` scope」）、**§F L63** 同；`plan-tracker` **L191**（「未 push，RISK-004 **待授权**」）；`rollback-plan` **§2 路径 C L47**（`revert --no-edit e1f25df` + 标题「撤销本版发布动作」——重定后发布动作链 = `e1f25df8` + `792030c` + `914725f8` 三提交，单 revert 已不足以撤销全部发布工件/证据）【文档】 **vs** DEC-027 L36（已授权 + 已授予 scope）与 RISK-004 L10（已更新为「进入待执行」）。
- **影响**：口径**保守陈旧**（非夸大），但会使复核者按已废弃的前置条件判断 push 就绪度；路径 C 的适用面描述与当前提交结构不再字面一致。
- **修复建议**：三处改为「push 已授权（DEC-027），按执行序待 R2/R3 通过后执行」；路径 C 补注「撤销需覆盖 `e1f25df8`+`792030c`+`914725f8`（或 `reset` 到 `v0.5.2` 之前）」。

---

## 6. 结论（三选一，唯一）

> **结论：`NEEDS_CHANGE`**

**理由**：硬门槛 5 项中 **H1（发布检查清单 100% PASS）FAIL** ⇒ 按角色定义「任一 FAIL 即阻断」，**不得判 `APPROVED` / `APPROVED_WITH_NOTES`**。**不判 `BLOCKED`** 的依据：① 全部残留缺陷为**文档留痕面**（tag 身份失配、§8 自相矛盾、存放/授权口径陈旧），**不涉及** CHANGELOG 分层、版本三方一致、tag 树内容、演练事实、安装行为或代码面；② 未 push ⇒ **零远端可见面、零固化**，修复为**零成本的一次文档提交**；③ 前轮 4 项 exit criteria **实质均已落地**（演练证据入仓 / tag 重定为含工件提交 / push 范围声明 + 授权 / SYSGAP+替代判据裁定），发布工件主体质量合格且**全程无造假、无包装、无虚假绿灯**（`check-release` 至今保持 `FAILED/不可用`，未改写为 PASS——合规亮点）。故退回补齐 **N1、N2**（建议顺带 N3、N4）后重 spawn **本 Reviewer（round R3）**复审。

**顺带裁定（避免误读）**：
- **重定 tag 的实质成果成立**：tag 树已消除「发布工件缺失」这一**可机检**不一致（R1 选项①达成），范围项按 DEC-027 由声明承担；**本轮判 NEEDS_CHANGE 不否定该成果**，仅要求**留痕同步**。
- **演练与 M7.7 事件处置合规**：隔离演练证据入仓可读、闭环残留如实登记、措辞严守「隔离环境安装冒烟（环境变量重定向至临时目录）」；首轮真实环境误写事件**如实披露 + 已清理 + 已加 fail-closed 守卫**，机写 `EVD-098` + `RISK-006`（含纠正措施），——按 M7.7「真实环境必防护 + 逐条上报 + Coordinator 机写证据」链路闭合，未发现掩盖或降格记录。

### 转 `APPROVED` 需补齐的内容（2 项必做，缺一不可；2 项建议）

| # | 补什么 | 对应 finding | 可接受方式 |
| --- | --- | --- | --- |
| 1（必做） | 同步 tag 身份到发布工件/记录（5 处 + EVD supersede），使记录与 tag target `914725f8` 一致 | F4 / **N1** | 一次文档提交（Release agent） |
| 2（必做） | 修正 `release-notes` §8 步骤三/四行，消除「未执行 ↔ 已执行」自相矛盾 | **N2** | 同上 |
| 3（建议） | 工件存放口径（§7.5）改为「已固化入仓」 | N3 | 同上 |
| 4（建议） | push 授权态口径同步（`§F`/清单头/plan-tracker/路径 C） | N4 | 同上 |

### 复审（R3）必查项
① 上述 5 处 tag 身份改写后与 `for-each-ref refs/tags/v0.5.2` 一致（`793dde78 → 914725f8`）；② `release-notes` §8 与前文一致、无「未执行」残留；③ `checklist` §G 仍如实保持 `FAILED/不可用`（**不得**因替代判据而改写为 PASS）；④ push 已执行时，附 `--release-commit 914725f8 --lineage-mode released` 的真实输出；⑤ 若届时判 `APPROVED_WITH_NOTES`，REVIEW 输出 MUST 含 `unresolved_blockers=0`。

---

## 7. Reviewer 自检

- [x] 前轮 9 findings **逐条比对**（已修复 8 / 未修复 1 / 新引入 0），未绕过往轮直接给结论。
- [x] 前轮 §5 五项必查**全部覆盖**（①满足 ②部分满足并指出 §F 不一致 + §8 迁移 ③满足 ④满足 ⑤满足，字段自洽已声明）。
- [x] 硬门槛逐项裁决（4 PASS / 1 FAIL），FAIL 即阻断，**未**因「流程看起来完整」放行。
- [x] 事实依据红线（P-01）：全部结论可回溯至【文档】行号或【C】内联事实；无法独立复跑者已标【待验证】（F9 的复算、risk-log L8 截断子项）；**未以推测填充**。
- [x] 发布事实优先：`check-release` 状态仍记 **FAILED/不可用**（未包装为 PASS）；演练残留（闭环 diff=4）与 M7.7 真实环境事件**均如实计入判断**。
- [x] 只读约束：未修改任何发布文档/代码/tag；写入仅限本报告两份（`.governance/review-REL-006-R2.md` + `docs/review/REL-006-R2.md`）。
- [x] 未与用户交互（无 AskUserQuestion）。

---

<!-- review-verdict: {"task_id":"REL-006","round":"R2","verdict":"NEEDS_CHANGE","prior_round_report":"docs/review/REL-006-R1.md","hard_gates":{"total":5,"pass":4,"fail":["H1_publish_checklist_100pct"]},"prior_findings":{"F1":"fixed","F2":"fixed","F3":"fixed","F4":"not_fixed","F5":"fixed","F6":"fixed","F7":"fixed","F8":"fixed","F9":"fixed","fixed":8,"not_fixed":1,"newly_introduced":0},"new_findings":{"N1":"P2","N2":"P2","N3":"P3","N4":"P3","total":4},"unresolved_blockers":2,"push_ready":false,"push_blockers":["N1_tag_identity_record_mismatch","N2_release_notes_section8_self_contradiction"],"lineage_release_commit":"914725f8","next_round_required":true} -->
