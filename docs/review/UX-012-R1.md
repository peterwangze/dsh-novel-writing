# UX-012 R1 代码审查报告（Code Reviewer）—— 回溯补审

- **task**: UX-012 ｜ **round**: R1 ｜ **date**: 2026-09-13 ｜ **reviewer**: Code-Reviewer
- **审查类型**: **retrospective（回溯补审）**——非交付时点常规审查；交付当时未执行 Code Review（见下"债务来源"）
- **基线 commit（双点声明）**:
  - **交付点 = `f0a45a4`**（`UX-012: 新建弹窗居中放大（模态520px）+仅目录名…`，2026-08-29 00:52:25 +0800；`git log --oneline --all --grep=UX-012` 唯一命中）
  - **审查点 = HEAD `21b100c`**（`git rev-parse HEAD` 实读＝`21b100ce9ac32e534c6215e507f9835f87e0ff4c`）
  - 交付点→审查点跨度 **130 commits**（`git log --oneline f0a45a4..HEAD | Measure-Object -Line` = 130），其中 UX-013~UX-060 连续重构同一批代码路径 ⇒ 本审查对象按任务书要求 = **UX-012 引入的逻辑在当前 HEAD 的存续状态**，而非仅当时快照。
- **债务来源**: `check-governance` **Check 21「Review Debt / Spawn Gap」** FAIL（父级给定输出：`1 product-code task(s) with review debt: - UX-012`）；与 EVD-028 自述一致——该证据条 `review.conclusion = "PENDING-CODEREVIEW"`（`git show f0a45a4:.governance/evidence-log.md` 实读）。本次审查为清偿该债务的唯一复审。
- **工作树一致性（审查前提，实读核验）**: `git status --porcelain` 仅治理文件（`.governance/**`、`AGENTS.md`）与 `docs/review/**` 落笔（父级在途工作），**无产品代码漂移** ⇒ 审查对象 = 当前 HEAD 的 `lib/client.js` / `lib/index.js` / `test/smoke.mjs` / `lib/host-contract.mjs` 磁盘内容。
- **本次审查的读命令（全部只读）**: `git rev-parse/status/log/show/--stat/-S`、`node --check ×2`、`node test/smoke.mjs`、`node test/validate-preset.mjs`、`node -e`（i18n 键集机检 / 死键扫描 / SplitWorkspace `novels` 使用面比对 / smoke 断言标签集提取）。**未**写仓库、**未** `git commit`、**未**对 `C:\Users\peter\.dsh` 写入、**未**触碰运行中的 DSH 实例、**未**复跑历史探针（理由与残余见 §4-F7）。唯一写入 = 本报告文件 `docs/review/UX-012-R1.md`。
- **前轮**: 无（R1）。本报告不引用任何交付者自述结论；存续性判定全部由 `git log -S`（pickaxe）与 HEAD 实读行号支撑。

---

## 0. 独立复跑的硬门禁读数（本审查自己跑的，不是转述）

| 门禁 | 命令与结果（实跑） | 判定 |
|---|---|---|
| 冒烟 | `node test/smoke.mjs` → `SMOKE DONE: 291 passed, 0 failed`（exit 0） | **PASS**（与任务书期望基线 291/0 一致） |
| 预设 | `node test/validate-preset.mjs` → `PRESET VALIDATION PASSED（schema-face: PASS）`（exit 0） | **PASS** |
| 语法 | `node --check lib/client.js` → 0；`node --check lib/index.js` → 0 | **PASS** |
| i18n 成对性 | 独立机检（自写提取器，非转述）：`zh keys=149 / en keys=149 / only-zh=[] / only-en=[] / dup=[]` → **PAIRED=true** | **PASS**（另注：任务书提及的 `KEYS-NOT-PAIRED` 标识在插件与仓库**均不存在**——`grep -r "KEYS-NOT-PAIRED"` 于 `<plugin_root>` 与仓库两侧 0 命中；仓库的 i18n 成对口径是 smoke 内逐键计数断言，见 §5 第 4 项） |
| UX-012 断言存续 | `git log -S "ux012 create modal missing" -- test/smoke.mjs` → **仅 `f0a45a4` 一次**（交付后 130 commits 内未再增删改）；HEAD 直读 `test/smoke.mjs:663` 命中 | **PASS**（未被删除/放宽） |
| 退役负断言 | `git log -S "nv-bar-launch" -- lib/client.js` → `f0a45a4`（引入）→ `75455e0`(UX-055) → **`d59f688`(UX-059) 删除** | **PASS**（迁移有据、无残留） |

---

## 1. 回溯审查口径（本报告的判定规则）

1. **审查对象 = 存续状态**：UX-012 引入的逻辑在 HEAD 是否仍正确、是否被正确复用。
2. **取代即 N-A**：交付点若已被后续任务取代/退役，标 `N-A（被 UX-xxx 取代）` + 给出替代点位置，**不计为缺陷**（任务书第 3 条）。
3. **归属显式化**：每条发现标注「UX-012 引入 / 承袭既有 / 后续任务引入 / 仓库既有约定」——避免把邻近任务（UX-059/UX-060/BUG-007）的问题记到 UX-012 账上，也避免放过 UX-012 自己的残留。
4. **事实依据红线**：每条结论指向可复查事实（文件:行、命令输出、pickaxe 结果）；无法证实者标 `未验证`，不以推测充事实。
5. **今日相邻改动不越界**：BUG-007（`agent-presets/…/agent.cordis.yml` persona 键）与 UX-060（`.nv-notice`/`.nv-bar-note` 形态裁决）已各自独立审查通过，本报告仅在其与 UX-012 逻辑产生**交互**处记录（见 §3 维度 1 与 §4-F5、§5 第 10 项）。

---

## 2. 存续性判定表（UX-012 交付点 × HEAD `21b100c`）

| # | UX-012 交付点 | HEAD 判定 | 替代点位置 | 事实依据（行号/命令） |
|---|---|---|---|---|
| ① | 新建**居中模态**：全屏遮罩 + 居中卡片 `min(520px,100%)`、`max-height:85vh`、radius 14、令牌投影 | **仍存续（逐字未改）** | — | `lib/client.js:3529-3553`（`.nv-cmodal-backdrop`/`.nv-cmodal`/`-head`/`-title`）、CSS `:4768-4772`；`git log -S "nv-cmodal-backdrop" -- lib/client.js` **仅 f0a45a4** |
| ①b | 遮罩点击关 / Esc 关 / **busy 期间禁关** | **仍存续** | — | `:3530` `onClick: closeCreate`；Esc 效应 `:3234-3248`（busy 分支 `:3240-3243`）；`closeCreate` busy 守卫 `:3270` |
| ①c | 卡片内 `stopPropagation`（防点「创建」冒泡→立即自关，交付时探针实测缺陷的修复） | **仍存续** | — | `:3534` + 注释 |
| ② | 表单**仅目录名**；`novel-create` 仅传 `{name}` | **仍存续** | — | `:3344` `apiJson('/novel-writing/api/novel-create', { name })`；smoke `:658` 断言同字面量 |
| ②b | 宿主 `title` 缺省 = `name`（交付时的查证结论） | **仍存续（宿主侧未改动）** | — | `lib/index.js:357` `typeof title === 'string' && title.trim() !== '' ? title.trim() : clean`；展示层兜底 `lib/index.js:290`（`listNovels`） |
| ③ | 按钮**仅「创建」/「取消」**；自动启动链退役（`sessions.create→预设→绑定→open→split→prompt`） | **仍存续** | — | `:3546-3549` 两钮；smoke `:659-662`（含 `sessions.create({ cwd: r.path })` 与 `bindSession(r.id` 两条负断言） |
| ④ | 创作台**标题栏**「▶ 开始/继续工作流」accent 实底钮 | **N-A（被 UX-059 取代）**——位置已退役 | **UX-059**：中窗下半区**工作流控制条** `.nv-wfctl-btn[data-mode=go]` | `git log -S "nv-bar-launch"`：f0a45a4 引入 → 75455e0(UX-055 视觉调整) → **d59f688(UX-059 删除)**；HEAD 渲染点 `:4045-4050`；smoke 迁移负断言 `:677`、`:706-707` |
| ④b | ④ 的**逻辑内核**：三态（降级不执行 / 未绑定→提示+弹绑定面板 / 会话失效→提示重绑）+ busy 防连点 + 复用 `launcher.promptLaunch` | **仍存续（迁移至控制条，语义未降级）** | `:3868-3928`（`.nv-wfctl`）| 判据 `:3870-3873`；三态分支 `:3887-3897`；空闲→`promptLaunch` `:3919-3927`（UX-059 另加 `try/finally`）；smoke `:674-675` |
| ④c | `.nv-bar-note` 三态提示条（ok 绿/err 红/info 次级灰） | **仍存续（承载面经 UX-060 扩展，非替换）** | +UX-060 `.nv-notice` 流式面（长文本/窄栏碰撞升级） | 渲染 `:4140-4142`、CSS `:4802-4804`；UX-060 升级判据 `:3735-3741` + 流式面 `:4172-4179`；smoke `:676` 断言 `.nv-bar-note` 保留 |
| ⑤ | i18n 失用键清理（删 `createGo/createOnly/bookPlaceholder/createDone`，增 5 键） | **仍存续** | — | 独立机检 149/149 成对、0 孤儿、0 重复；4 个退役键在 `lib/ test/ agent-presets/ README CHANGELOG` 面的引用仅剩 smoke 负断言 `:660` 与 CHANGELOG 史录（无产品代码残留） |
| ⑥ | smoke 99→101（新增两条 UX-012 源码面断言） | **①原文存续；②被 UX-059 取代为更强后继** | `ux059 wf ctl missing`（smoke `:664-678`）+ `:679-721` | `git log -S`：`ux012 create modal missing` **仅 f0a45a4**（HEAD `:663` 仍在，从未被改）；`ux012 split launch missing` 由 **d59f688(UX-059) 删除**（后继断言数更多且含退役负断言 ⇒ 非放宽） |
| ⑦ | 无头探针 `nv-probe-ux012` 16/17 + 目检截图（EVD-028 行为级验收） | **资产未入仓（不可复跑）** | 后继先例：`scripts/probe-nv-bar-geometry.mjs`（UX-060 起入仓） | `git show --stat f0a45a4` 无 `scripts/` 路径；`git ls-files \| Select-String "ux012\|probe.*012"` 0 命中 ⇒ 该探针非版本化资产（详见 F7） |

**存续性小结**：UX-012 的 ⑦ 个交付点中，**5 个逐字/语义完整存续**（①②③⑤ + ④b/④c 的逻辑内核），**1 个位置被 UX-059 取代**（④，按任务书规则标 N-A、不计缺陷，其逻辑内核 ④b 已迁移承接），**1 个证据资产未入仓**（⑦，见 F7）。**无任何交付点属"静默丢失"**。

---

## 3. 五维度逐项结论

### 维度 1：正确性 — **PASS（0 阻塞；F1 P2、F3 P3）**

| chk | 结论 | 事实依据 |
|---|---|---|
| 逻辑正确 | **PASS** | 新建链收敛为「校验空名 → `POST novel-create {name}` → 关窗 + `selected/consoleFocus` 就位 + notice 指引」（`:3337-3357`），无分支遗漏；③ 的退役是全量删除（smoke `:661-662` 两条负断言锁定 `sessions.create({cwd})`/`bindSession` 不再出现）；`isNewBook` 判据（`:3873`：`completedStages` 空 ∧ `totalChapters===0`）与 `launchMsgOf`（`:1444`）**同判据**，无第二事实源 |
| 边界条件 | **PASS** | 空名：`:3340` 前置拦截（`t('nameRequired')`，不发起请求）；`createBusy` 重入：`:3338` + 全部按钮/关闭路径 `disabled`/守卫（`:3547-3548`、`:3270`、`:3240-3243`）⇒ 5 条关闭路径（✕/取消/遮罩/Esc/Enter）**无一路可在请求在飞时重复提交或关窗**；创建失败：`:3352-3353` 错误留在弹窗内可读，`finally` 复位忙碌态 `:3354-3356`。**1 条既有边界**见 F1 |
| 并发安全 | **PASS** | 无新增共享可变状态；`launchWorkflow` 的 `wfBusy`（UX-059 后由主/次/绑定三按钮共享）用 `try/finally` 兜底复位（`:3925-3927`），**UX-012 原版无 finally**（异常将永久锁死 busy）——该弱点已由 UX-059 修复，非现役缺陷 |
| 资源管理 | **PASS** | Esc keydown 监听在 cleanup 移除（`:3247`）；聚焦滚动 effect 无监听器/定时器（`:3254-3264`，返回 `undefined`）；`sessionsById` 订阅经 `makeSessionsHookReactive` 的 `disposed/mounted.delete/detach()` 三重解挂（`:1412-1416`）⇒ **无订阅泄漏**；UX-060 新增的 `ResizeObserver` 亦有 `disconnect`（`:3710`） |

### 维度 2：安全性 — **PASS**

| chk | 结论 | 事实依据 |
|---|---|---|
| 输入校验 | **PASS** | 客户端 `trim` 后空名前置拒绝（`:3339-3340`）；服务端 `sanitizeNovelId`（`lib/index.js:339-348`）拒绝空/超 60 字/`<>:"\|?*\\/` 路径分隔符与非法字符/`.`/`..`/点开头/Windows 保留设备名（`con/prn/aux/nul/com1-9/lpt1-9`） |
| 注入防护 | **PASS** | 目录名不拼接任何 shell/SQL 面；**路径穿越不可达**：分隔符在 `sanitizeNovelId` 即被拒，且 `novelDir`（`:131-136`）二次拒绝 `/`、`\`、`.`、`..`；错误文本经 React 文本子节点渲染（`:3550` `el('div', …, createNotice)`）自动转义，**无 `innerHTML`/`dangerouslySetInnerHTML`**；回显面（`novel already exists: ${clean}`）中的 `clean` 已通过 `<>` 拒绝，双保险 |
| 敏感数据 | **PASS** | UX-012 全 diff 零密钥/token/凭据字面量；`nv-cmodal` 族无外部资源引用 |
| 权限检查 | **PASS** | `novel-create` 路由（`lib/index.js:1333-1335`）经既有 `registerHttp` 统一守卫（CSRF/503 禁用守卫，EVD-027 已验），UX-012 未改动该路由；删除路径的确认风控（UX-011）未被 UX-012 触碰 |

### 维度 3：可维护性 — **PASS（F2 P2、F4/F5/F6/F9 P3）**

| chk | 结论 | 事实依据 |
|---|---|---|
| 命名可读 | **PASS** | `closeCreate` / `runCreate` / `createBusy` / `launchWorkflow` / `barNotice` / `isNewBook` 均达意；`launchMsgOf` 形参 `bookTitle`→`title` 的更名（交付 diff）消除了「书名 vs 卡片名」歧义——**但留下死参，见 F9** |
| 函数长度 | **PASS** | `runCreate` ≈ 20 行、`closeCreate` 1 行、`launchWorkflow` ≈ 45 行（UX-059 增「停止」分支后），均低于 50 行建议线；无超长函数 |
| 重复代码 | **PASS（针对 UX-012）** | UX-012 **减少**重复：删除 `runCreate(go)` 自动启动链的第三份创建链拷贝（创建链拷贝数 3→2）。**残余 2 份属 UX-059 引入**（`bindNewSessionCtl` 内联复制 `openCtl.autoCreate` 三段），见 F2（已按归属单列） |
| 注释质量 | **PASS** | UX-012/UX-059 关键决策均有可复核注释（`:3232-3233` busy 禁关理由、`:3251-3252` 补 `novels.length` 依赖理由、`:3332-3336` 仅目录名 + 宿主 title 缺省依据、`:3533` stopPropagation 探针实测理由、`:4137-4139` 三态/迁移/流式面归属）；`bindNewSessionCtl` 的「复用」措辞与实现不符（F2 内含建议） |

### 维度 4：性能 — **PASS（F4 P3）**

| chk | 结论 | 事实依据 |
|---|---|---|
| 避免不必要循环 | **PASS** | UX-012 未引入循环；`.nv-ccard` 查找为单次 `querySelector`（`:3257`） |
| 数据结构 | **PASS（1 条 P3）** | `sessionsById` 选择整张 `byId`（`:3667-3669`）——订阅面宽于所需，同型点均为单叶选择，见 F4；`novels.length` 作 deps 标量（`:3264`）避免整表引用比较 |
| 懒加载 | **PASS** | 模态为条件渲染（`creating === true`，`:3529`）——关闭态零节点；控制台/分栏均为 active 才渲染 |
| 批量操作/轮询 | **PASS** | UX-012 未新增轮询；`novels.length` 入 deps 的代价 = 每轮 overview 刷新最多一次 `scrollIntoView`（注释 `:3251-3252` 已说明设计意图），无 O(n²)、无循环内 I/O |

### 维度 5：测试覆盖 — **PASS（F7 P3）**

| chk | 结论 | 事实依据 |
|---|---|---|
| 核心路径有测试 | **PASS** | 源码面：smoke `:655-663`（模态 5 族类名 + 520/85vh + 仅传 `{name}` + 两钮 + 4 条退役负断言）；`node test/smoke.mjs` 实跑 **291/0** |
| 边界测试 | **PASS（部分）** | 空名文案 `nameRequired` 有源码面/文案断言；`createBusy` 禁关/防重入为源码面 + 交付时探针行为验证；**行为级断言未入仓，见 F7** |
| 错误路径测试 | **PASS（部分）** | 服务端 `sanitizeNovelId` 拒绝链有既有机检（UX-011 引入的 7 类拒绝断言仍在 HEAD 通过）；客户端 `createFailPrefix` 分支为源码面覆盖 |
| 覆盖率达标 | **PASS（按仓库口径）** | 本仓库以 `node --check` + `validate-preset` + `smoke` + CI 为防护网（P-04）；三者在 HEAD 全绿（§0），且 smoke 末条自断言 `README 声明 291 ≡ 实测 291` |

---

## 4. 发现列表（每条含文件:行号 / 级别 / 事实依据 / 影响 / 修复建议 / 归属）

> **P0 = 0，P1 = 0**（硬门槛通过）。以下 9 条均为非阻塞项；**其中仅 F4/F9（P3）与 F5/F7（P3）的可归因部分属 UX-012 自身**，F1 为承袭既有的引擎侧边界，F2 为 UX-059 引入，F3/F6/F8 为既有形态/记录口径。

| # | 位置（文件:行） | 级别 | 事实依据 | 影响 | 修复建议 | 归属判定 |
|---|---|---|---|---|---|---|
| **F1** | `lib/index.js:356-357`（`createProject`）；`lib/index.js:282-284`（`listNovels`） | **P2** | `createProject` 先 `mkdirSync(join(dir, PROJECT_DIR), {recursive:true})`（:356）再 `updateState`（:357）；`updateState→writeState→atomicWrite`（:210-211、:108-117）在 `writeFileSync(tmp)`/`renameSync` 失败（ENOSPC/EACCES/EROFS）时抛错并清理 **tmp**，但**不回滚已建目录**；`listNovels` 对读不到 `state.json` 的目录 `continue` 跳过（:283-284） | 低概率 I/O 失败下遗留 `<root>/<name>/novel-project` 空目录：该目录**在 UI 中不可见**（无卡片 ⇒ 无 🗑 可点），而同名重试被 `existsSync(dir)`（:355）拒为 `novel already exists` ⇒ 用户无 UI 自愈路径，且错误文案误导（"已存在"实为残骸） | 另立任务（C-04：顺带发现问题另立任务）：`createProject` 失败时 `rmSync` 回滚本次自建目录（或用 try/catch 包裹 mkdir 后的全部步骤）；同时把"残留目录不可见"纳入 `listNovels` 诊断或删除入口 | **非 UX-012 引入**——`git show f0a45a4:lib/index.js` 的 `createProject` 已为同序（mkdir→updateState），UX-012 仅去掉 `title` 入参；属引擎侧既有边界 |
| **F2** | `lib/client.js:3965-3994`（`bindNewSessionCtl`）vs `:1549-1581`（`openCtl.autoCreate`） | **P2** | `bindNewSessionCtl` 注释自称「复用 openCtl.autoCreate 链 a-d」，实为**内联复制** a/b/c 三段（`workspace.list` 命中 root→`createArg`→`sessions.create`→`agentPresets.select('novel-writing')`→`bindSession`，:3979-4007），与 `openCtl.autoCreate:1549-1581` 逻辑等价、仅提示面不同（`barNotice` vs `ctx.notice`） | 创建链存在**两个事实源**：后续修改（如新增 `requestId`、改预设名、改 cwd 回退策略）须同步两处，漏改一侧即行为分叉且无测试可察（smoke 仅断言字面量存在）。即 CLEAN-004 ⑤ A-1 所指「抽 `launcher.createAndStart` 收口」的具体载体 | 另立 CLEAN 任务：抽 `launcher.createSessionFor(novel, ctx, { open })` 单一入口（返回 `{ok, sessionId, error}`），`openCtl.autoCreate` 与 `bindNewSessionCtl` 共用；并订正该注释（「复用」→「同型内联，待收口」） | **UX-059 引入**（`d59f688`），**非 UX-012**——UX-012 实际把创建链拷贝从 3 份降到 2 份 |
| **F3** | `lib/client.js:3270`（`closeCreate`）vs `:3348`（成功路径） | **P3** | `closeCreate = () => { if (createBusy !== true) { setCreating(false); setCreateNotice('') } }`——✕（:3538）/取消（:3548）/遮罩（:3530）/Esc（:3241）四路共用，**均不复位 `dirName`**；仅创建成功路径 `setDirName('')`（:3348）。`setDirName` 全仓仅 3 处（:3083 声明、:3348 成功清空、:3543 onChange） | 取消后重新打开弹窗，上次输入的目录名仍留在输入框；用户可能误读为"已保存"状态。无功能破坏（`autoFocus` 仍在，可覆盖输入） | 若有意保留草稿 ⇒ 在 `closeCreate` 补一行注释声明该意图；否则在 `closeCreate` 内一并 `setDirName('')`（与 Esc 分支同改） | **UX-012 承袭既有形态**（交付前内联 cancel 同为 `setCreating(false); setCreateNotice('')`，UX-012 未改变该语义） |
| **F4** | `lib/client.js:3667-3669` | **P3** | `sessionsById = props.useSessions((s) => (s.byId !== null && typeof s.byId === 'object' ? s.byId : {}) ?? {})`——返回**整张 byId 映射**；同型消费点均为单叶选择：`:2874`/`:2980`/`:4240` 用 `s.byId[boundId] ?? null` | 任一会话条目变化即触发 SplitWorkspace 重渲染（含标题栏/工作台/控制条子树的 selector 比较），订阅面宽于实际所需（仅需 `byId[boundId]`）。当前会话规模下无实测卡顿（未验证性能数值） | 改为叶选择：`props.useSessions((s) => (boundId !== null ? (s.byId?.[boundId] ?? null) : null))`，与三处同型点对齐；`boundEntry` 随之直接取用 | **UX-012 引入**（该 hook 为 UX-012 新增，交付 diff 可见） |
| **F5** | `lib/client.js:3529-3533`（挂载点）+ `:4649`（`.nv-console{position:fixed;…;z-index:950;overflow:hidden}`） | **P3** | 模态节点是 `.nv-console` 的**直接子节点**（组件单返回 `el('div', {className:'nv-console'}, head, body, foot, modal)`：`:3403-3413` 起、`:3554` 收；`.nv-cfoot` 与模态同为 8 空格同级子项） | **当前行为正确**：`position:fixed` 后代的包含块是视口，`.nv-console` 无 `transform/filter/contain/will-change` ⇒ 不受其 `overflow:hidden` 裁剪；`z-index:1000` 落在 950 栈上下文内 ⇒ 天然让位于 `z-index:1000` 的 bind/工作区对话框（与 Esc 让位 `:3239` 一致）。**但该正确性依赖隐式前提**：一旦后代改动给 `.nv-console` 加上 `transform/filter/contain`，全屏遮罩会被裁剪并重锚到控制台盒（静默布局破裂，smoke 源码面断言无法察觉） | 在该 CSS 行补注释固化前提（「.nv-console 不得引入 transform/filter/contain——.nv-cmodal-backdrop 依赖视口为包含块且不受 overflow 裁剪」），或把模态提升为 `shell.overlay` 级独立挂载点以消除隐式耦合 | **UX-012 的设计选择**（居中模态改为挂在控制台内）；风险为潜在/未来向 |
| **F6** | `lib/client.js:3529-3553` | **P3** | `.nv-cmodal` 无 `role="dialog"` / `aria-modal="true"`；无焦点陷阱、关闭后无焦点归还（Tab 可逸出到背后控制台）；已有部分 a11y 处理：输入框 `autoFocus` + `aria-label`（:3542）、✕ `aria-label`（:3538）、✕ 用 `nvIcon('x',12)` 而非文本 | 键盘/读屏用户体验：模态语义未声明，弹窗打开时焦点可移出模态（WCAG 2.4.3/2.1.2 风险）。不影响功能正确性 | 与既有 `.nv-modal`（:4462）**一并**补 `role="dialog"` + `aria-modal="true"` + 关闭焦点归还触发元素；另立 a11y 任务统一处理，避免只改一处造成形态分叉 | **仓库既有约定**（既有 `.nv-modal` 同样无 role/aria-modal）——非 UX-012 单点偏差 |
| **F7** | `test/smoke.mjs:655-663`（UX-012 源码面断言）；无对应入仓探针 | **P3** | UX-012 的**行为级**验收（居中几何 520px/遮罩关/Esc 关/busy 禁关/单输入/恰两钮/卡片名=目录名）来自 `nv-probe-ux012` 无头探针 + 目检（EVD-028），但：`git show --stat f0a45a4` 无 `scripts/` 路径、`git ls-files` 无 `ux012`/`probe.*012` 资产 ⇒ **该探针未版本化**，本次无法复跑（只读边界内不重建）；smoke 仅做**源码面字符串存在性**断言，不驱动行为 | 行为回归无法被 CI 捕获——例如重新引入「卡片点击冒泡→遮罩自关」这一交付时实测过的真缺陷（:3533 注释记录），smoke 仍会全绿。仓库对探针资产已有成熟先例（UX-060 的 `scripts/probe-nv-bar-geometry.mjs`、BUG-007 的 `scripts/isolated-preset-mount.mjs`） | 按 `scripts/probe-nv-bar-geometry.mjs` 的既有形态，把弹窗行为断言（遮罩关/Esc 关/busy 期间禁关/恰两钮/请求体仅 `{name}`）固化为入仓探针，并在 `README` 与 smoke 计数处登记（P-04 测试看护） | **UX-012 的证据资产缺口**（交付时仓库尚未形成探针入仓约定——先例是 UX-060 建立的；本条按"事实记录 + 改进建议"给出，不作 UI 缺陷计） |
| **F8** | `.governance/evidence-log.md` EVD-028（`git show f0a45a4:.governance/evidence-log.md`）+ 任务书转述 | **P3** | **记录口径订正**：任务书称「EVD-028（**本地 HTTP E2E 17/17** + smoke 101/0 + 目检通过）」；实读 EVD-028 结构化事实为：`node --check` / `validate-preset` / `smoke 101 passed 0 failed` / **「无头探针 nv-probe-ux012 + 截图目检 → 16/17（blocked=宿主旧包 405）」**。EVD-028 **不含** HTTP E2E 记录——`HTTP E2E 17/17` 是相邻条 **EVD-027（UX-011）** 的验收（`git show f0a45a4:.governance/evidence-log.md` 中 EVD-027 行含 `本地 HTTP E2E（真实 registerHttp 链路）… 17/17`） | 仅影响口径，不影响结论：UX-012 的验收强度为「smoke 101/0 + 探针 16/17（1 项 blocked = 运行中宿主旧包 `novel-delete` 405，probe 样本清理待重启）+ 目检」，与 plan-tracker UX-012 行、CHANGELOG 记述一致（均写 16/17）。若沿用「17/17」会高估 UX-012 的验收面 | 无需改动代码；后续引用 EVD-028 时按实读口径（冒烟 101/0 + 探针 16/17 + 目检）表述 | **记录/转述口径**（非代码、非 UX-012 交付缺陷） |
| **F9** | `lib/client.js:1442-1444`（`launchMsgOf`） | **P3** | 形参 `forceNew` 与判据 `forceNew === true \|\| …`（:1444）**不可达**：全仓 `launchMsgOf` 仅剩 1 个调用点 `:1496` `launchMsgOf(novels, novel.id, novel.title)`（3 参 ⇒ `forceNew === undefined`）；既有的唯一 4 参调用 `launchMsgOf(novels, r.id, r.title, true)` 随自动启动链在 UX-012 被删除；`promptLaunch` 的两个调用点（`:2703`、`:3921`）亦均为 3 参 | 死参 + 死分支：不影响行为（新书判定由 `n.completedStages/totalChapters` 兜底，与 `isNewBook` :3873 同判据且行为等价），但属**退役动作未做完整性收敛**——删了调用点却留下形参与分支，后续读者会误判存在"强制新建"语义 | 删除 `forceNew` 形参与 `forceNew === true \|\|` 分支（:1442、:1444），保留 `n`-based 判据；同步核对注释「沿用 launchMsg 逻辑」（:1441） | **UX-012 引入**（退役清理不彻底——唯一可明确归因于 UX-012 自身的新增残留） |

**级别计数**：P0 = **0** ｜ P1 = **0** ｜ P2 = **2**（F1、F2）｜ P3 = **7**（F3、F4、F5、F6、F7、F8、F9）｜ 合计 **9** 条，**每条均带级别**（硬门槛第 3 项达标）。

---

## 5. 必查 10 项逐条结论

| # | 必查项 | 结论 | 事实依据 |
|---|---|---|---|
| 1 | **创建链完整性** | **PASS**（1 条既有边界 F1） | ①`{name}` 约定在 HEAD **成立**：`:3344` 传 `{ name }`，smoke `:658` 同字面量断言且实跑绿。②`title` 缺省=name **有事实依据**：`lib/index.js:357` `title.trim() !== '' ? … : clean`；展示层 `listNovels:290` 同款兜底（`info.title` 空 ⇒ `id`）；两处在 `f0a45a4` 与 HEAD **同源未改**。③错误路径有防护与反馈：重名 `existsSync`→`novel already exists`（:355）、非法名/路径穿越由 `sanitizeNovelId`（:339-348）+ `novelDir`（:131-136）双重拒绝、均经 `apiJson`（`:562-566` 取响应 `j.error`）→ `catch` → `setCreateNotice(t('createFailPrefix') + errOf(e))`（:3353）红色文案落弹窗内可读；**反馈文案为服务端英文技术串拼接本地化前缀**（如「创建失败：novel already exists: x」），属仓库既有口径（`delFailPrefix` 等同型），未计缺陷。**残留边界**见 F1（半成品目录，非 UX-012 引入） |
| 2 | **弹窗行为** | **PASS**（F3/F6 为 P3） | **模态语义**：遮罩 `:3530` + stopPropagation `:3534` + Esc `:3236-3245`（bind/entryOpen 对话框优先让位 `:3239`）；**busy 期间禁关/禁重入**：`closeCreate`（:3270）、Esc 分支（:3240-3243）、两钮 `disabled`（:3547-3548）、`runCreate` 重入守卫（:3338）——**五路穷尽**，交付时探针实测的「冒泡自关」缺陷修复仍在位（:3533-3534）。**焦点处理**：`autoFocus`（:3542）+ `aria-label`（:3538/:3542）在位；无焦点陷阱/焦点归还是既有 `.nv-modal` 同族形态（F6，P3）。**关闭后状态复位**：`creating/createNotice` 复位；`dirName` 仅成功路径复位（F3，P3）。**i18n 键成对性**：独立机检 **149/149 成对、0 孤儿、0 重复**（zh 162-338 / en 340-517 区段） |
| 3 | **`promptLaunch` 三态与 busy 防连点** | **PASS**（HEAD 下仍被正确复用；消费点已按预期迁移） | **复用成立**：唯一实现在 `launcher.promptLaunch`（`:1494-1504`，含 `apiHas('sessions','prompt')` 守卫 + 全路径 try/catch + `{ok,error,msg}` 返回），HEAD 有 **2 个消费点**（`:2703` 绑定面板带启动、`:3921` 控制条主按钮）——任务书所述「UX-059 后迁移」属实：**标题栏消费点已删（`d59f688`），控制条消费点承接且语义等价**（`:3868-3928`）。**三态判据未降级**：降级 `:3887`（提示不执行）/ 未绑定 `:3889-3892`（info 提示 + 弹绑定面板 `bind:{novelId, stale:false}`）/ 失效 `:3894-3897`（err 提示 + `stale:true` 重绑）。**吞异常检查**：`promptLaunch` 不抛（catch→`errOf(e)`），`launchWorkflow` 用 `try/finally`（`:3920-3927`）——`ok!==true` 分支给 `promptFailPrefix + (apiError|error) + promptFailHint`，**有用户可见反馈**，无静默失败路径。**busy 防连点**：`wfBusy` 三按钮共享 + 早退守卫（`:3885`）。残留：**UX-012 原版缺 `try/finally`**（异常将永久锁死 busy）——已由 UX-059 修复，非现役缺陷；`launchMsgOf` 死参见 F9 |
| 4 | **i18n 清理（是否误删/回归）** | **PASS** | ①**无误删**：4 个退役键（`createGo`/`createOnly`/`bookPlaceholder`/`createDone`）在产品/测试/预设/文档面的引用**仅剩** smoke 负断言 `:660` 与 CHANGELOG 史录，无任何残留 `t('…')` 调用。②**无后续任务复用后回归**：独立机检 zh/en **149/149 成对**；UX-012 新增 5 键（`createBtn`/`createDoneHint`/`startWorkflow`/`continueWorkflow`/`bindFirstHint`）**全部有 ≥1 调用点**（死键扫描：`t()` 调用数分别 1/3/3/2 + `bindFirstHint` 2）⇒ **无孤儿键**。③**`KEYS-NOT-PAIRED` 口径说明**：该标识在插件 `<plugin_root>`（`grep -r` 全树）与仓库（`test/`）**双侧 0 命中，不存在此检查项**——本报告以其**等效独立机检**（zh/en 键集双向差集 + 重复键 + 死键）替代，结论 = 未成对键 **0**、重复键 **0**、孤儿键 **0** |
| 5 | **测试覆盖（当时与现状）** | **PASS**（1 条 P3） | **当时**：smoke 99→101 = 追加 2 条（`git show f0a45a4 -- test/smoke.mjs` 新增 2 个 `check(`）；EVD-028 记录 `101 passed 0 failed`（历史读数属记录事实，只读边界内无法回检出 `f0a45a4` 工作树重跑，**标注为记录值**）。**现状**：`ux012 create modal missing`（`:663`）**逐字存续且交付后从未被改动**（`git log -S` 仅 f0a45a4）；`ux012 split launch missing` 由 **UX-059 `d59f688` 删除**，但由**更强后继**承接（`ux059 wf ctl missing` `:664-678` + `:679-721`，断言数更多且含 `.nv-bar-launch` 退役**负断言**）⇒ **非放宽、非静默删除**。**当前基线实测**：`node test/smoke.mjs` = **291 passed / 0 failed**、`node test/validate-preset.mjs` = **PRESET VALIDATION PASSED（schema-face: PASS）**——与任务书期望一致。**缺口**见 F7（行为级断言未入仓） |
| 6 | **AI 专项 5 项** | **全部完成**（见 §6） | mock 残留 0 / 硬编码返回 0 / 幻觉 API 0 / 未实现 TODO 0 / 过度实现 0（含 1 条"退役不彻底"的交叉项 F9） |
| 7 | **安全** | **PASS**（F1 为数据安全方向 P2，非本任务引入） | **路径注入**：目录名经客户端 trim + 服务端 `sanitizeNovelId`（`:339-348`，拒 `<>:"\|?*\\/`、`.`、`..`、点开头、保留设备名、>60 字）+ `novelDir`（`:131-136`）二次校验 ⇒ 穿越/注入不可达（`f0a45a4` 与 HEAD 同规）。**宿主 API 调用面输入校验**：UX-012 **净删除** 4 处宿主调用（`sessions.create`/`agentPresets.select`/`sessions.open`/`settings.update`），仅保留 `sessions.prompt`（经 `apiHas` 守卫 + `{mode:'queue', content:[{type:'text',…}]}` 形状固定）⇒ 调用面**收窄**，校验面无新增缺口。**失败数据安全**：状态文件走 `atomicWrite`（tmp+rename，`:108-117`）⇒ **无半写状态文件**；残留风险限于 F1 的空目录（非 UX-012 引入）。**注入/XSS**：错误文本经 React 文本子节点渲染（:3550）自动转义，无 `innerHTML` |
| 8 | **设计一致性（P-10 契约纪律 + `launcher.*` 收口）** | **PASS** | **P-10**：UX-012 新增宿主耦合字面量 = **0**。其 DOM 类族（`.nv-cmodal*`、`.nv-bar-note`）为**插件自有前缀**，按契约 `regionLiterals` 提取口径**规则③「自有前缀 `.nv-` 一律排除」**（`host-contract.mjs:182`）**不入册**；UX-012 唯一触及的宿主字面量是 CSS 令牌 `--dsw-alias-bg-elevated`（用于 `.nv-cmodal`），**已在册**——`host-contract.mjs:208`（`regionLiterals.cssTokens`）与 **item 3.4**（`:143`，其 note 明确记载「第 12 个 `--dsw-alias-bg-elevated` 与 `--dsw-shadow-lv2` 出自 NV_STYLE 的 `.nv-cmodal` 段」）。UX-012 未新增宿主 API ⇒ `items[]` 无新增面；`sessions.prompt` 调用**已在册**（item 2.6 `:127` `remote.session（create/prompt/cancel…）`、`svcDomains` `:193`）。**机检**：`regionLiterals` ≡ 源码提取面的双向 ⊆ 对账随 smoke 291/0 绿 ⇒ 无漏登记。**`launcher.*` 收口**：UX-012 未另立新链，改为**复用**既有 `launcher.promptLaunch`（`:1494`）并在其内保留唯一文案事实源 `launchMsgOf`（`:1442`）⇒ 方向正确。**架构债**：创建链拷贝由 3 份降为 2 份（UX-012 自身**未留债**）；残余 2 份 = UX-059 的 F2（已按归属单列，建议另立 CLEAN 任务收口 `createSessionFor`） |
| 9 | **可维护性/性能** | **PASS**（F4/F5/F9 为 P3） | **长函数**：`runCreate`≈20 行、`launchWorkflow`≈45 行，均 < 50 行建议线（维度 3）。**重复代码**：UX-012 方向为**净减少**（删第三份创建链）；残余见 F2（UX-059）。**轮询/订阅泄漏**：`sessionsById` 订阅经 `makeSessionsHookReactive` 卸载三重解挂（`:1412-1416` `disposed`/`mounted.delete`/`detach`）；Esc 监听 cleanup 移除（`:3247`）；UX-060 的 `ResizeObserver` 有 `disconnect`（`:3710`）；聚焦滚动 effect 无监听器（`:3264`）⇒ **无泄漏**。**deps 正确性**：`:3264` 补 `novels.length` 属必要（新卡片经下轮轮询进树，布尔 `dataLoaded` 不翻转——注释 `:3251-3252` 已说明），无缺失依赖告警。**性能**：见维度 4（F4 订阅面偏宽，P3） |
| 10 | **证据一致性（EVD-028 vs 可核验事实）** | **PASS（含 1 条口径订正 F8）** | ①**smoke 101/0**：EVD-028 记录属实，与交付 diff「新增 2 断言」（99→101）自洽；HEAD 对应载体（`:655-663`）存续。②**探针 16/17 + 目检**：EVD-028、plan-tracker UX-012 行、CHANGELOG（`L171`）**三处一致**均为 **16/17**，且唯一 blocked 已明示为「运行中 DSH 宿主为旧包 `novel-delete` 405，probe 样本 `probe-name-*` 留存待重启清理，禁止直接 rm」——属**环境性 blocked**、非产品缺陷，记述如实。③**订正点**：任务书转述的「**本地 HTTP E2E 17/17**」不存在于 EVD-028——该读数是**相邻条 EVD-027（UX-011）**的验收（HTTP E2E 17/17）；UX-012 无 HTTP E2E 记录（详见 F8）。④**行号漂移**：EVD-028 引「`createProject` L343-353 / `listNovels` L282」，HEAD 已漂至 `:351-361` / `:290`（130 commits 后的正常漂移，`git show f0a45a4:lib/index.js` 实读证实**交付时实质正确**）⇒ 记事实、不追责。⑤**审查状态自述**：EVD-028 `review.conclusion = PENDING-CODEREVIEW` 与 Check 21 债务**吻合**——本报告即其清偿记录。**未验证项（如实标注）**：历史 101/0 与 16/17 为记录值，只读边界内未回检工作树重跑；F4 的性能影响无实测数字 |

---

## 6. AI 代码专项 5 项（逐项结论）

| # | 专项 | 结论 | 事实依据（针对 UX-012 引入且在 HEAD 存续的代码） |
|---|---|---|---|
| 1 | **mock 残留** | **0（PASS）** | `lib/client.js` 全文件 `grep -i "mock\|stub"` 仅 1 命中 = `:5007` 注释（述"测试经注入的 react mock 直测生命周期"，属测试设施说明，非产品代码路径）；UX-012 存续面（`:3337-3357` 新建、`:3529-3553` 模态、`:3868-3928` 控制条、`:4140-4142` 提示条、CSS `:4768-4804`）**零** mock/假数据/占位实现；模态文案全部来自 `t(...)`，数据全部来自 `apiJson`/宿主快照 |
| 2 | **硬编码返回值** | **0（PASS）** | 无伪造绿/写死结果：`runCreate` 无返回值、其 notice 派生自**真实响应**（`:3351` `t('createDoneHint', r.title ?? r.id)`，`r.title` 源自服务端 `state.project_info.title`，`lib/index.js:360`）；失败走 `errOf(e)`（`:3353`）。`launchWorkflow` 的 ok/err 由 `promptLaunch` 的真实 `r.ok` 决定（`:3922-3924`）。`r.title ?? r.id` 为**防御性兜底**（服务端缺字段时退化为目录名），非硬编码；无 `return {ok:true}` 式桩 |
| 3 | **幻觉 API** | **0（PASS）——且调用面净收窄** | UX-012 **删除** 4 处宿主调用（`sessions.create({cwd})`、`agentPresets.select`、`sessions.open`+`ensureSplit`、经 `bindSession` 的 `settings.update`），存活面仅**复用既有** `launcher.promptLaunch`→`sessions.prompt`（`:1494-1504`）。逐项对账 `host-contract.mjs`：`remote.session（create/prompt/cancel…）` **item 2.6 `:127` 在册**、`remote.agentPresets（select）` **item 2.9 `:130` 在册**、`svcDomains` `:193` 含 `remote.session`/`remote.agentPresets` ⇒ **无未登记/不存在的宿主方法名**；`apiHas(...)` 守卫（`:1495`）与 UX-059 的 `apiHas('sessions','cancel')`（`:3903`）均以契约为依据 |
| 4 | **未实现 TODO** | **0（PASS）** | `lib/client.js` 全文件 `grep "TODO\|FIXME\|XXX"` **0 命中**；UX-012 存续路径无占位分支、无"暂未实现"文案、无空实现函数；模态的全部交付功能（居中/遮罩关/Esc 关/busy 禁关/单输入/两钮/notice 指引）均为**已实现且可达**的分支 |
| 5 | **过度实现** | **0（PASS；1 条交叉项 F9）** | 无投机抽象：UX-012 **净删代码**（`lib/client.js` 交付 diff 为 `+223/−82` 区间的收敛式改动，删 go 分支 ≈ 40 行）；未引入未使用的配置项/开关/兼容分支；模态几何复用既有令牌族与 `WorkspaceDialog` 视觉模式（**未自造轮子**，P-08）。JSON 形状、class 命名、hook 用法均沿用仓库既有范式（`el()`/`t()`/`useSessions` 条件调用契约见下）。**交叉项**：F9（`forceNew` 死参 + 死分支）严格属"**退役清理不彻底**"而非过度设计——列为 P3，已计入发现列表 |

**`useSessions` 条件调用核查（防误判）**：UX-012 新增的 `props.useSessions !== null ? props.useSessions(sel) : …`（`:3667-3669`）**不是**新引入的 hook 违规形态——`makeSessionsHookReactive`（`:1361-1424`）的注释以契约定明「**函数引用恒定**：无论服务是否就位都返回同一个函数——消费端条件调用形态下每帧 hook 调用次数恒定」（`:1348-1351`），且同型用法在 HEAD 有 6 处先例（`:2642`/`:2874`/`:2980`/`:3098`/`:3108`/`:3809`/`:4240`）；`useSessions` 由 slot 注册期单点注入（`:4962`/`:4971`/`:4979`/`:4983`），组件生命周期内 `null` 性不变 ⇒ **符合既有架构约定，不构成发现**。

---

## 7. 硬门槛裁决

| 门槛项 | 阈值 | 实测 | 判定 |
|---|---|---|---|
| P0 阻塞问题数 | = 0 | **0** | **PASS** |
| 5 维度全覆盖 | = 100% | **5/5**（正确性 / 安全性 / 可维护性 / 性能 / 测试覆盖，§3 逐维度均给出结论 + 事实依据） | **PASS** |
| 每条发现标注 P0~P3 | = 100% | **9/9**（F1-F9，每条含级别 + 文件:行 + 事实依据 + 影响 + 修复建议 + 归属） | **PASS** |
| 设计一致性检查 | 已完成 | **已完成**——P-10 契约纪律（新增宿主耦合字面量 0；`--dsw-alias-bg-elevated` 已在册 item 3.4/`:208`；提取口径规则③排除自有 `.nv-` 前缀 `:182`；`sessions.prompt` 在册 item 2.6；smoke 双向 ⊆ 对账 291/0 绿）+ `launcher.*` 收口方向 + **HEAD 存续性判定表**（§2，含 2 处 `git log -S` pickaxe 定证） | **PASS** |
| AI 代码专项 5 项 | 全部完成 | **5/5**（mock 残留 / 硬编码返回 / 幻觉 API / 未实现 TODO / 过度实现，§6 逐项结论） | **PASS** |

### 结论：**APPROVED_WITH_NOTES**

- **依据**：硬门槛 5/5 全通过、**P0 = 0、P1 = 0**；UX-012 的 7 个交付点在 HEAD 的存续性经逐点机检**无静默丢失**，唯一被取代项（④标题栏启动钮）由 UX-059 明确迁移且**逻辑内核（三态/busy/promptLaunch 复用）完整承接**，按任务书规则标 N-A 且不计缺陷。
- **本审查清偿的债务**：`check-governance` Check 21 中 UX-012 的 review debt（EVD-028 `PENDING-CODEREVIEW`）——本次为 R1 **通过终态**，无 unresolved blocker ⇒ 可结束复审链。
- **非阻塞遗留项（9 条，建议另立任务；不影响本结论）**：
  - **P2 且需另立任务**：F1（半成品目录回滚，承袭既有引擎边界）、F2（创建链双事实源收口 `createSessionFor`，UX-059 引入）。
  - **P3 归 UX-012 自身**：**F9（删死参 `forceNew`，最易修且最应修）**、F4（`sessionsById` 收窄为叶选择）、F5（固化 `.nv-console` 不含 transform/filter/contain 的前提）、F7（弹窗行为断言入仓，对齐 UX-060 探针先例）。
  - **P3 归既有形态/记录**：F3（`closeCreate` 是否复位 `dirName` 需声明意图）、F6（对话框 `role/aria-modal` 与焦点归还，与 `.nv-modal` 一并处理）、F8（EVD-028 口径订正：无 HTTP E2E，实为冒烟 101/0 + 探针 16/17 + 目检）。
- **重复造轮子核对（P-08）**：UX-012 复用既有 `launcher.promptLaunch`/`launchMsgOf` 与既有 `.nv-modal` 视觉模式、宿主令牌族，**未自研**；模态几何沿既有 `WorkspaceDialog` 模式 ⇒ 无重复造轮子。

```
unresolved_blockers=0
```

> 结构化事实源：本节 `unresolved_blockers=0` 为**独立字段**（非自然语言推断），对应 §7 结论为 `APPROVED_WITH_NOTES`；全文不含未解决的 BLOCKING finding（P0=0、P1=0）。
