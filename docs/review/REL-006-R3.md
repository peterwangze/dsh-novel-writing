# 发布审查报告 — REL-006 v0.5.2（**R3 终审轮**）

- **任务 ID**：REL-006（P1，v0.5.2 发布，BUG-006 安装通道适配承载）
- **round**：**R3**（终审轮；前轮 R1 = `NEEDS_CHANGE` → 返工 R1；R2 = `NEEDS_CHANGE` → 返工 R2 留痕订正）
- **前轮报告引用**：
  - `docs/review/REL-006-R1.md`（≡ `.governance/review-REL-006.md`，245 行，结论 `NEEDS_CHANGE`，硬门槛 3 PASS / 2 FAIL〔H1·H2〕，findings P0×0 / P1×4 / P2×3 / P3×2 = 9 条）
  - `docs/review/REL-006-R2.md`（169 行，结论 `NEEDS_CHANGE`，硬门槛 **4 PASS / 1 FAIL**〔H1〕，前轮 9 项 = 已修复 8 / 未修复 1〔F4 留痕面〕/ 新引入 0，本轮新发现 **N1~N4** = P2×2 / P3×2；push 前置判定 = 不可 push）
- **审查时间**：2026-09-12（返工 R2 之后）
- **审查员**：Release Reviewer Agent（只读；本轮未修改任何发布文档/代码/tag；**无 Bash 权限**——凡 SHA/远端/tag/门禁数值均按【C】引用，未独立复跑）
- **被审对象（R3 事实基准）**：

  | 对象 | SHA / 值 | 说明 |
  | --- | --- | --- |
  | 返工 R2 提交 | `66f0213`（4 文件 **+40/−27**） | `release-notes` +42/−?｜`checklist` 9 行｜`rollback-plan` 10 行｜`plan-tracker` 6 行【C】 |
  | R2 机录提交 | `ef23ed3` | R2 review-record 机写（**不在 tag 树内**）【C】 |
  | **tag `v0.5.2` 实际身份** | annotated tag 对象 **`793dde78`** → peel **`914725f8`** | 身份判据 = `git for-each-ref refs/tags/v0.5.2`（**唯一权威口径**）【C】 |
  | 远端 | `refs/heads/main` = `3442f396`；**远端无 `v0.5.2`**；**push 未执行** | DEC-027 执行序中 push 位于 R3 之后 ⇒ 本轮**非缺陷**（见 §5）【C】 |
  | 门禁终态 | `node --check` = **12 files, 0 failures**（exit 0）｜`validate-preset` = **PRESET VALIDATION PASSED**（29/29，exit 0）｜`smoke` = **282 passed / 0 failed**（exit 0，末条断言自校「声明 282 ≡ 实测 282」） | 本次 Release 复跑 + Coordinator 独立复跑，**两次一致**；覆盖对象 = 返工 R2 落盘后工作树【C】 |
  | N1 的 EVD supersede | **EVD-099**（明载「删除旧 tag `v0.5.2`（对象 `7ca99d6d` → peel `e1f25df8`），新 annotated tag `v0.5.2` = 对象 `793dde78` → peel `914725f8`」）；EVD-097 作为历史行保留原读数 | 【文档：evidence-log L272 命中 EVD-099 且含返工 R1 + 证据固化 + tag 重定内容（行尾截断，逐字 tail 未读完 ⇒ 该子项按【C】）】 |
- **结论**：**`APPROVED_WITH_NOTES`**（硬门槛 **5 PASS / 0 FAIL**；R2 四条 N1~N4 = **已修复 4 / 未修复 0 / 新引入 0**；本轮新发现 **2 条 N5~N6，均 P3、均非阻断**）｜**`unresolved_blockers` = 0**
- **push 前置判定**：**可 push**（前置逐项满足，见 §5；须由 Coordinator 执行，且 push 后 MUST 立即以 `--release-commit 914725f8` 做 released 模式核验）

---

## 0. 证据基准与能力边界（P-01）

| 标记 | 含义 |
| --- | --- |
| 【文档】 | 本轮实际读取的文件 + 行号（可复查） |
| 【C】 | Coordinator 实测提供（本 Reviewer **无 Bash 权限，无法复跑**；按任务书内联事实引用，不主张为独立复现） |
| 【待验证】 | 文档自述但本轮无任何证据者——**不计入 PASS 依据** |

**本轮读取面（9 次工具调用）**：`docs/review/REL-006-R2.md`（全 169 行）、`docs/release/release-checklist-0.5.2.md`（80 行）、`docs/release/rollback-plan-0.5.2.md`（171 行）、`docs/review/REL-006-release-notes.md`（252 行）、`docs/release/feature-flags-0.5.2.md`（30 行）、`.governance/plan-tracker.md` L140-199、`.governance/decision-log.md`（DEC-027/DEC-028 命中行）、`.governance/evidence-log.md`（EVD-097/098/099 命中行）、`.governance/risk-log.md`（RISK-002/004/005/006/007 命中行）。
**能力边界如实声明**：未执行任何命令、未跑门禁、未复算 diff、未检查 git 对象；发布文档的**内部一致性**由本轮实读承担，**外部事实面**（SHA/远端/门禁）由【C】承担。

---

## 1. R2 四条新发现 N1~N4 逐条比对（MUST 逐条）

| # | R2 发现（严重度） | R2 要求的验证点 | R3 裁定 | 证据（行号） |
| --- | --- | --- | --- | --- |
| **N1** | tag 身份未随重定同步（自述—事实机检失配）｜P2 | `checklist` L6 / `rollback-plan` L5·L95 / `release-notes` L135·L231 / `plan-tracker` L177 全部改为 `793dde78 → 914725f8`，与 `for-each-ref` 一致 | **✅ 已修复** | ①`release-checklist-0.5.2.md` **L6** = 发布提交 `e1f25df8` + 返工链 `792030c → 6f0027b → 914725f8`；**tag `v0.5.2` 对象 `793dde78…` → peel `914725f8…`**（DEC-027 重定后）；**L7** 新增 supersede 行：「重定前旧对象 `7ca99d6d…` → peel `e1f25df8…` 已随 DEC-027 重定删除（`for-each-ref` 不再返回）——记录值以本行新身份为准」②`rollback-plan-0.5.2.md` **L5** = 新身份 + 旧值「已随重定删除，supersede 留痕」；**§7.1 L97** = 演练时读数标注「**重定前**取值，历史留痕」，同格给出「**当前身份（DEC-027 重定后）= 对象 `793dde78…` → peel `914725f8…`**」+ 演练代表性依据（`git diff --name-only e1f25df8 914725f8` 对 `install.ps1`/`install.sh` = 0 改动）③`REL-006-release-notes.md` **§7 R7 L135** = 新身份（DEC-027 重定后，= 返工链末端证据固化提交）+ supersede 段；**§10 L231** = 保留原始读数并**显式标注**「〔历史留痕〕该读数为 DEC-027 重定前取值」+ 指向重定后身份；**L240** = 订正为「R1 返工轮内（重定前）未执行任何 tag 命令；其后 DEC-027 已由 Coordinator 执行 tag 重定」；**L246** = 新增「返工 R2 轮」行，新身份全串 + 说明「一律写 peel `914725f8` = 返工链末端而**不写**『= 当前 HEAD』」④`.governance/plan-tracker.md` **L156**（REL-006 行）= 「本地 annotated tag **`v0.5.2` 对象 `793dde78` → peel `914725f8`**（DEC-027 重定后；旧对象 `7ca99d6d` 已随重定删除）——**未 push**（**已授权**…）」；**L177**（路线图）= 同；**L191**（里程碑）= 同⑤EVD supersede 由 **EVD-099** 承接，EVD-097 保留为历史行【文档/ C】 |
| **N2** | `release-notes` §8 清单表两行与前文自相矛盾｜P2 | §8 两行与 §4.1 / §6 / `checklist` §C.2·§D.1 措辞一致，无「未执行」残留 | **✅ 已修复** | **L185**（步骤三）现为「✅ **已执行（隔离环境，2026-09-12 返工轮）**」+ 依据（§4.1 / §6「已定义、已演练（隔离环境）」/ `rollback-plan` §7：A1 `v0.5.1` → A3 `v0.5.2` → A5 回滚 `v0.5.1`；6 次 install `exit=0`；幂等 A1→A2 / A3→A4 / 回滚 A4→A5 / B1→B2 均 `diff=0`；闭环 A2→A5 `diff=4` 残留如实登记）+ **「N2 订正」**说明；**L188**（步骤四）现为「三件套（§4）+ 本版**唯一交付面（安装通道）的被发布树隔离实测**——**隔离环境安装冒烟（环境变量重定向至临时目录）通过**」+ **「N2 订正」**说明（原「BUG-006 既有隔离实例实证」证据对象 ≠ 被发布树）。**残留「未执行」字样复核**：仅剩 L5「push 未执行」、L90「CI 未运行」、L133（R5 原始报文节选中的 `[FAIL] release docs` 历史输出）——**均为真陈述或历史原始报文，非自相矛盾** ⇒ 满足验证点 |
| **N3** | `rollback-plan` §7.5「工件不入仓」陈述被 `914725f8` 推翻｜P3 | §7.5 与证据目录 README 存放口径一致 | **✅ 已修复** | `rollback-plan-0.5.2.md` **L160** = 「**存放口径（N3 订正）**：工件**已固化入仓**——`docs/release/evidence/rel006-drill-20260912/`（15 件工件 94.9 KiB + `README.md` 索引；目录实测 16 文件 / 98.4 KiB；随证据固化提交 `914725f8` 入仓，提交面 16 files / +1281）；**原 temp 副本可能被系统清理**，仓内副本为可复查面」——与证据目录 `README.md` L3「由 Coordinator 于 2026-09-12 **固化入仓**」一致，且与 R2 §2 ① 认定的「仓内可读」事实一致 |
| **N4** | 发布边界/授权口径陈旧（`§F`、清单头、plan-tracker、路径 C 范围）｜P3 | 三处改为「push 已授权（DEC-027），按执行序待 R2/R3 通过后执行」；路径 C 补注撤销范围 | **✅ 已修复** | ①`checklist` **L5** = 「本地发布（… **未 push**；**push 已授权**（DEC-027，用户已授予 `workflow` scope），按执行序待 R2/R3 审查通过后由 Coordinator 执行）」；**L8** 结论条件① 同款；**§F L64** 同款（「条件 = ①push **已授权**（DEC-027…；RISK-004 已进入「待执行」）」）②`plan-tracker` **L191** = 「未 push；**push 已授权（DEC-027）**，按执行序待 R2/R3 审查通过后由 Coordinator 执行」；**L177** 同；**L156** 同（注：tag/push 原受 RISK-004 阻塞「**已解除**」）③`rollback-plan` **§2 路径 C L47-48** = 撤销命令改为 `revert --no-edit 914725f8 6f0027b 792030c e1f25df8`（重定后 4 提交逆序）+ `reset --hard e1f25df8^` 变体；**L51** = 「**撤销范围补注（tag 重定后，N4）**」明写单 `revert e1f25df` 已不足、须逐个 revert 或 reset 到 `v0.5.2` 之前④`release-notes` **L116**（路径 C 补注引用 `rollback-plan` §2）、**L129 R1**、**L201** 均记「push **已授权**（DEC-027）」 |

**统计：已修复 4 / 未修复 0 / 新引入 0**（R2 四条全部闭合，且**无一项被返工 R2 引入新的自相矛盾**）。

---

## 2. R2 §6「复审（R3）必查项」五项复核（MUST 覆盖）

| # | 必查项 | 结论 | 依据 |
| --- | --- | --- | --- |
| ① | 5 处 tag 身份改写后与 `for-each-ref refs/tags/v0.5.2` 一致（`793dde78 → 914725f8`） | **✅ 满足** | §1 N1：`checklist` L6-7、`rollback-plan` L5/§7.1 L97、`release-notes` R7 L135/L231/L240/L246、`plan-tracker` L156/L177/L191——**全部位置的「当前主张」= `793dde78` → `914725f8`**，与 `for-each-ref`【C】一致；旧值 `7ca99d6d` 仅以「重定前 / 已随重定删除」标注形态存在，**无任何位置再将其陈述为当前身份** |
| ② | `release-notes` §8 与前文一致、无「未执行」残留 | **✅ 满足** | §1 N2（L185/L188 + 两处「N2 订正」；残留字样均为真陈述/历史报文） |
| ③ | `checklist` §G 仍如实保持 `FAILED/不可用`（**不得**因替代判据改写为 PASS） | **✅ 满足** | `checklist` **L74** = 发布就绪行 `**FAILED — 21 issue(s)**（exit 1）｜**不可用（跨根期望源缺陷）**…**不包装为 PASS**`；**L76** = 「**状态维持 `FAILED — 21 issue(s)` / 门禁不可用，不得改写为 PASS**」；**L80** = 替代判据集合**明文「不构成 `check-release` 的替代 PASS —— 该门禁仍记 `FAILED`」**；`release-notes` **L194** 门禁诚实声明同款 ⇒ 无虚假绿灯 |
| ④ | push 已执行时，附 `--release-commit 914725f8 --lineage-mode released` 的真实输出 | **条件未触发（N/A 本轮）** | push **未执行**（远端 main 仍 `3442f396`、远端无 tag【C】），且该状态符合 DEC-027 执行序（…→ R2 → **R3** → push → released 核验〔decision-log L36〕）⇒ 本项**不构成本轮缺陷**；Round 3 的替代判定项 = **push 前置是否满足**（§5）⇒ **满足**。**强制后续**：push 后 MUST 立即执行该命令并把原始输出写入证据行（fail-closed；不得以 candidate 口径代替，`release-notes` L200 已明写「**必须使用重定后 tag target `914725f8`——不得使用旧值 `e1f25df8`**」） |
| ⑤ | 若届时判 `APPROVED_WITH_NOTES`，REVIEW 输出 MUST 含 `unresolved_blockers=0` | **✅ 满足** | 本报告结论段与机读块均显式写出 **`unresolved_blockers = 0`**；同时满足 **DEC-028 g)**（「Release Reviewer 独立审查终态 = APPROVED 或 APPROVED_WITH_NOTES(unresolved_blockers=0)」，decision-log L37）⇒ 该替代判据集合**本轮整体通过** |

---

## 3. 硬门槛裁决表（H1~H5，逐项 PASS/FAIL + 证据）

| # | 硬门槛项 | 阈值 | 裁决 | 证据与理由 |
| --- | --- | --- | --- | --- |
| **H1** | 发布检查清单**全部 PASS** | = 100% | ✅ **PASS** | R2 的三项 H1 失败驱动**全部闭合**：①`§C.2` L37 = 回滚**已验证**（隔离环境，含演练要素与残留如实登记）；②`§D.1` L45 = 被发布树隔离实测 + 口径订正说明（措辞「隔离环境安装冒烟（环境变量重定向至临时目录）通过」，**无**无限定语的「真实安装」）；③`§F` L64 决策口径已同步 DEC-027（N4 修复）；④清单头部 **L6/L7** tag 身份与 tag 事实一致（N1 修复）。**逐节复核**：§A L16-19 全 ✅｜§B L25-28 全 ✅（+L30 分层机检）｜§C L36-39 全 ✅｜§D L45-48（二项 N/A 为**诚实结构性不适用**，L46-47 明文「插件无生产监控面（如实标注，不伪造）」，沿用 R1/R2 口径**不计 FAIL**）｜§E L54-58 全 ✅/替代口径｜§F L64 Go+条件已同步｜§G = 三项门禁全绿（`node --check` 12/0、`validate-preset` 29/29、`smoke` 282/0）+ 归档 dry-run 不阻断 + **`check-release` 保持 `FAILED/21 issues` 不可用**。**对 §G 该项的裁决（明示豁免，非放行）**：其性质 = **外部工具跨根期望源缺陷**（`verify_workflow.py` L7181 默认读插件仓 `project/CHANGELOG.md` 且无 `--changelog`；另期望插件仓 `1.0.0 依赖链`/`REQ-059~064`/插件自家测试路径）——**非本版交付缺陷**，已由 **DEC-028 a)~h) 正式替代口径** + RISK-002/RISK-005/**RISK-006**/SYSGAP-001 登记承接，且**状态诚实保留为 FAILED/不可用（未包装为 PASS）**；DEC-028 g) 由本轮终态（§6）承接 ⇒ **该单项不构成 H1 的交付面 FAIL**。⇒ **清单达 100% PASS（含 1 项已裁定豁免 + 2 项诚实 N/A）** |
| **H2** | 回滚方案存在且**已验证** | = 已验证 | ✅ **PASS** | 方案命令级三路径（`rollback-plan` L20-51）；**已验证**：§6 L80 + §7 L84-171（路径 A 往返 `v0.5.1 → v0.5.2 → 回滚 v0.5.1`，6 次 install `exit=0`，`realPathLeak=0`，幂等 A1→A2=0 / A3→A4=0 / **回滚 A4→A5=0** / 旧布局 B1→B2=0）+ 原始输出**已入仓**（`docs/release/evidence/rel006-drill-20260912/`，§7.5 L160 N3 订正后自述与事实一致）。**代表性**：演练取树与被发布树在安装面等价（§7.1 L97：`install.ps1`/`install.sh` 0 改动）⇒ 对安装面成立。**残留如实登记**（不掩盖）：闭环 A2→A5 `diff=4`（新布局注册项不被 v0.5.1 清理）⇒ 语义边界「**回滚 = 版本回退，非 DSH_HOME 状态还原**」已写入 §6 L82 / §7.4 L144 / `checklist` §C.2 L37 / `release-notes` §6 L121，并升格为 **RISK-007**（risk-log L13）——**如实、有登记、有触发条件** |
| **H3** | CHANGELOG 用户视角完整 | 关键段全部覆盖 | ✅ **PASS** | 沿用 R1/R2 结论（`[0.5.2]` 段 1 条 BUG-006：症状链路 + 兼容边界；`[Unreleased]` 保留 v0.6.0 线全量条目；移动纯粹性；版本三方一致）；`checklist` §B L25-30 分层机检在仓（`[0.5.2]` contains BUG-006 ∧ ¬COMPAT-；`[Unreleased]` COMPAT- ×70 ∧ ¬BUG-006）；返工 R2 = 纯文档 + 治理记录（4 文件，零 `CHANGELOG`/`package.json` 改动【C】）⇒ **无新反证** |
| **H4** | breaking changes 已标注 | = 100%（与 diff 对照） | ✅ **PASS** | breaking 面 = **空集**：返工 R2 `66f0213` 4 文件 = 3 份 `docs/**` + `.governance/plan-tracker.md`；`ef23ed3` = 机录；零 `lib/**`、零 `install.*`、零 CI【C】⇒ 覆盖 0/0；条目内声明「旧布局行为完全不变」另有**命令级旁证** B1→B2 `diff=0` |
| **H5** | Feature Flag 关闭验证 | 全部通过 | ✅ **PASS** | 本版零 flag：`feature-flags` §2 L16-17 取证（`lib/**` 0 命中 + `git diff e1f25df8^ e1f25df8 --name-only` 恰 4 文件、零 `lib/**`，**绝对 SHA 可复算**）；§3 L27-28 既有控制面（`enabled` 总开关 / 逐平台 `enabled`）**本版行为零变化**；`smoke` **282/0**【C】 |

**裁决汇总：5 PASS / 0 FAIL** ⇒ 无硬门槛阻断；结论可判 `APPROVED` 或 `APPROVED_WITH_NOTES`（本轮取后者，理由见 §6）。

---

## 4. 结构性事实的专门裁定（三项，纳入判断）

### 4.1 tag 自引用悖论（Coordinator 裁定已入档）—— **接受该裁定，无需再动 tag**

**事实**：tag peel = `914725f8`（树 ⊇ 三件发布工件 + 演练证据 15 件），而其后的提交（R2 机录 `ef23ed3`、返工 R2 `66f0213`）**不在 tag 树内**【C】。**因此任何「tag 树内文档声明自身 tag 身份」的写法都不可能自洽**（tag 创建先于其后的提交）。
**裁定（本轮采纳）**：tag 身份**唯一权威口径 = `git for-each-ref refs/tags/v0.5.2`**；**不再为追补文档而重定 tag**（否则构成无限回退，且每次重定都会产生新的「其后提交」）。该裁定已写入 `plan-tracker` L156 与 `rollback-plan`。
**本轮合规核验**：全仓发布文档**无一**声称「tag 树包含其后提交」或「tag peel = 当前 HEAD」——`release-notes` L247 甚至**显式禁止**该写法（「一律写 peel `914725f8` = 返工链末端而**不写**『= 当前 HEAD』」）⇒ **留痕形态正确**。
**R3 结论**：该悖论**不构成本版缺陷**（结构性、无法通过文档消除），且**已有正确留痕**；不得以此为由要求重定 tag 或再修一轮。

### 4.2 `release-notes` §10 命令级日志保留原始读数 —— **判定：合格形态，不需改写**

**事实**：§10 L231 保留重定前 `rev-parse` 读数（`7ca99d6d` → `e1f25df8`），紧邻给出「〔历史留痕〕该读数为 DEC-027 重定前取值」并在 L246（返工 R2 行）给出重定后身份。
**判定依据（按「当前主张是否与 tag 事实一致」）**：①该表为**执行日志（历史面）**，保留原始读数符合「不得抹掉历史」与命令级日志纪律（P-01：不得以事后值替换当时读数）；②**当前主张面**已由 L231 标注 + L246 新行与 tag 事实一致；③旧值被明确标注「重定前」并给出替代指针 ⇒ **不产生「同一文档同时主张两个当前值」的歧义**（这正是 N2 被否的形态，此处不成立）。
**R3 结论**：**该形态达标，无需改写成其他形态**。若下游要求更强可机检性，唯一可加的形态是在 §10 表头加一行「本表为历史执行日志；**当前 tag 身份以 §7 R7 与本表末行为准**」——属**可选增强，非阻断项**。

### 4.3 push 未执行 = 既定执行序的结果 —— **非缺陷；判定项为「push 前置是否满足」**

DEC-027 执行序（decision-log L36）：补齐 → 重定 tag → **R2** → **R3** → push → `--lineage-mode released` 核验（`--release-commit` **必须 = `914725f8`**）。push 位于 R3 之后 ⇒ 本轮「push 未执行」**不是缺陷**，且**不构成 DEC-028 h) 的「以未执行充已验证」**（该核验被显式排程于 push 之后）。判定见 §5 = **前置全部满足**。

---

## 5. push 前置条件判断（专门裁决）

| 前置项 | 状态 | 依据 |
| --- | --- | --- |
| tag 树 ⊇ 全部发布工件（R1 F4 机检面） | ✅ 满足 | tag peel `914725f8` 树含三件 `docs/release/*-0.5.2.md` + `docs/review/REL-006-release-notes.md` + 演练证据 15 件【C】；`checklist` L7 + `plan-tracker` L156 留痕 |
| tag 身份留痕与 `for-each-ref` 一致 | ✅ 满足 | §1 N1（11 处当前主张全为新身份） |
| 用户授权 + `workflow` scope | ✅ 满足 | **DEC-027 ②**（decision-log L36「**授权 Coordinator 重试 push**（用户侧已授予 `workflow` scope）」）+ RISK-004 L10（2026-09-12 已授予 ⇒ 状态进入「待执行」） |
| R1 纪律「**先补齐审查项、后 push**」 | ✅ 满足 | R1 9 项 + R2 4 项**全部闭合**（R2 §1 = 8/9 已修复 + N1~N4 本轮 = 4/4 已修复）；本轮硬门槛 5/5 PASS；无 BLOCKING 未闭合 |
| 可见面声明（push 将公开 v0.6.0 线内容） | ✅ 满足（附 1 项非阻断精度项 = N5） | `release-notes` §3.1.1 L54-61 + §9-1 L201-202（44 commits 含 COMPAT-002~016；属发布决策、已授权、先补齐后 push） |
| DEC-028 替代判据集合（a~h） | ✅ **整体满足**（g) 由本轮承接） | a) 12/0 ✅ b) 29/29 ✅ c) 282/0 不劣化 ✅ d) 版本三方一致 ✅ e) 分层机检 ✅ f) 发布工件四件齐备 ✅ **g) 本轮终态 = `APPROVED_WITH_NOTES(unresolved_blockers=0)`** ✅ h) 未执行项如实标注 ✅ |
| 门禁终态（released 前最后读数） | ✅ 满足 | 12/0 + 29/29 + 282/0，两次独立复跑一致【C】 |
| `--release-commit` 取值口径唯一 | ✅ 满足 | **`914725f8`**（`release-notes` L200 明写「必须使用重定后 tag target `914725f8`——**不得使用旧值 `e1f25df8`**」；L129 同） |

> **判定：可 push。**
> **执行约束（MUST）**：①执行者 = **Coordinator**（Release Agent 硬边界不执行 push，`rollback-plan` L5/L51、`release-notes` L166）；②命令 = `git push origin main v0.5.2`；③push 后**立即**执行 `python "<plugin>/infra/verify_workflow.py" check-release --version 0.5.2 --require-changelog --lineage-mode released --release-commit 914725f8`，**原始输出入证据行**（fail-closed）；④push 证据行中**记录实际推送 commit 计数**以闭合 N5；⑤push 后 `rollback-plan` 路径 C **失效**（L53 已声明），此后仅可 revert + 新版本号。

---

## 6. 本轮新发现（2 条，均 P3，**均非阻断**）

### N5 — push 范围声明的 commit 计数「44」为早期读数，未随治理提交增长更新｜**P3**（非阻断·精度）
- **现象**：`release-notes` §3.1.1 L54 与 §10 L215 记「本机领先远端的 **44 commits**」，EVD-097 亦同；而其后的两个提交（R2 机录 `ef23ed3`、返工 R2 `66f0213`）**不在 tag 树内亦未计入该计数**【C】⇒ push 时的实际计数 ≥ 46。
- **影响**：**仅计数精度**——该声明的**实质**（push 将一并公开未发布的 v0.6.0 线内容：CI 探测轨、宿主契约/边界层、fixtures、诊断面板等）**不因此改变**；授权面（DEC-027）与决策性质（发布决策）均不受影响。**不阻断 push**。
- **需改写的形态（明确要求，非只判缺陷）**：不改写历史行；在 **push 证据行（EVD）** 记录**实际推送计数与 `git log --oneline <remote-main>..HEAD` 实测**，或将 L54 计数标注为「≥44（**随治理提交增长**，以 push 时实测为准）」。二者任一即闭合。

### N6 — `rollback-plan` 路径 C 的 **revert 变体**未覆盖 tag peel 之后的治理提交｜**P3**（非阻断·精度）
- **现象**：§2 路径 C L47-51 定义「本版发布动作链 = `e1f25df8` → `792030c` → `6f0027b` → `914725f8` 共 4 提交」，但 tag peel 之后另有 `ef23ed3`（R2 机录）、`66f0213`（返工 R2）两提交【C】；若走 **revert 变体**，撤销后仍残留这两条治理/审查提交。
- **影响**：**仅 revert 变体**——同格的头部命令 `git reset --hard e1f25df8^`（L48）会丢弃**全部后代**（含该两提交），**完整且正确**；且路径 C 为**本地未 push 的应急通道**，非发布主路径。**不阻断 push**。
- **需改写的形态**：在 §2 路径 C 补一句「**tag peel 之后的治理/审查提交**（如 R2 机录、返工 R2）不在 tag 树内：`reset` 变体一并丢弃；走 `revert` 变体须追加 `revert ef23ed3 66f0213`」——**可选在下次触碰该文件的提交中完成，或 push 后并入收尾记录**。

> **新发现统计：2 条（P3×2）；P0/P1/P2 = 0**。两条均**不影响任何发布事实**（tag 身份、tag 树内容、版本三方一致、CHANGELOG 分层、门禁终态、回滚已验证、授权与可见面声明），**均非 BLOCKING**。

---

## 7. 结论（三选一，唯一）

> **结论：`APPROVED_WITH_NOTES`**
>
> **`unresolved_blockers` = 0**

**理由**：
1. **硬门槛 5/5 PASS**（§3）。R2 唯一 FAIL（H1）的三项失败驱动（§C.2 / §D.1 / §F + 清单头 tag 身份）**全部闭合**；仅存的「非 PASS 单格」= `check-release` 外部工具不可用，**性质为工具跨根缺陷、非交付缺陷**，已由 DEC-028 a)~h) 正式裁定替代并以诚实状态（`FAILED/不可用`）保留，**未包装为 PASS**。
2. **R2 四条新发现 4/4 已修复**（§1），且**无新引入**；R2 §6 五项必查 4 项满足、1 项（released 模式输出）因执行序未触发且**前置已满足**（§2、§5）。
3. **DEC-028 g) 由本轮终态承接**（`APPROVED_WITH_NOTES(unresolved_blockers=0)`）⇒ 替代判据集合**整体通过**，发布就绪在本仓正式口径下成立。
4. **不判 `NEEDS_CHANGE`**：无未闭合阻断项；本轮 2 条新发现均为 P3 精度项（§6），且**均已给出明确的、可在 push 证据行或收尾提交中一次性完成的改写形态**——以再修一轮为由阻断发布不符合「round=3 终审轮」定位，也与其影响面（不触及任何发布事实）不匹配。
5. **不判 `BLOCKED`**：**无 BLOCKING 未闭合** ⇒ **不满足 M7.4 T2（round≥3 仍 NEEDS_CHANGE）触发条件**，亦无外部阻塞（用户授权已有 DEC-027；scope 已授予；tag 与门禁均已就位）。**特别建议**：Coordinator **不得**将本任务转为 `BLOCKED`。
6. **发布事实面未被「流程看起来完整」替代**：`check-release` 保持真实 `FAILED`；回滚残留如实登记为 RISK-007；M7.7 真实环境误写事件（EVD-098 + RISK-006）如实披露并已加 fail-closed 守卫；演练措辞严守「隔离环境安装冒烟（环境变量重定向至临时目录）通过」——**未发现虚假绿灯、未发现以声明替代实测**。

### 残余（非阻断，如实登记）
| # | 项 | 处置归属 |
| --- | --- | --- |
| 1 | **RISK-004**（push 待执行） | Coordinator 按 §5 执行 push + released 核验后关闭 |
| 2 | **RISK-007**（回滚=版本回退，非 DSH_HOME 状态还原） | 语义边界已如实入档；「状态还原级回滚」超本版范围 |
| 3 | RISK-002 / RISK-005 / RISK-006（上游跨根缺陷 / hook 静默 no-op / 隔离举证事件） | 沿用 DEC-028 + DEC-027③（上游仅登记）；RISK-006 观察窗 = 下次隔离演练 |
| 4 | **N5 / N6**（P3 精度项） | push 证据行 / 收尾提交一次性闭合（§6 已给形态） |
| 5 | COMPAT-017（`--run` 真机面，用户侧首次真机前 MUST 处置） | 已入 plan-tracker L157，属 v0.6.0 线，不属本版 |

---

## 8. Reviewer 自检

- [x] 头部声明 `round: R3` + 前轮引用（`REL-006-R1.md` / `REL-006-R2.md`）。
- [x] **R2 四条 N1~N4 逐条比对**（每条 = 已修复 / 未修复 / 新引入 + 行号证据），统计 4/0/0。
- [x] **R2 §6 五项必查逐条复核**（4 满足 + 1 条件未触发〔前置已满足〕）。
- [x] **round=3 特殊规则遵守**：无 BLOCKING 未闭合 ⇒ 未判「再修一轮」，亦未建议转 `BLOCKED`；并**主动建议不得转 BLOCKED**（T2 不触发）。
- [x] 硬门槛 H1~H5 逐项裁决 + 明示豁免（`check-release` 外部工具不可用，未包装为 PASS）。
- [x] push 前置逐项判定（§5）= **可 push** + 5 项执行约束（含 `--release-commit 914725f8`）。
- [x] 三项结构性事实（tag 自引用悖论 / §10 保留历史读数 / push 未执行）**均纳入裁定**，且对前两项给出「判定为合格形态 + 明确不需改写（含可选增强形态）」。
- [x] 事实依据红线（P-01）：结论全部可回溯至【文档】行号或【C】；无法独立复跑者已标【C】/【待验证】；**未以推测填充**。
- [x] report-first：9 次读取 + 1 次写入（≤10 Read / ≤12 调用）。
- [x] 只读约束：写入仅 `docs/review/REL-006-R3.md`（**未**写 `.governance/review-REL-006-R3.md`，该路径留给 review-record 机录）；未修改发布文档/代码/tag；未与用户交互。

---

<!-- review-verdict: {"task_id":"REL-006","round":"R3","verdict":"APPROVED_WITH_NOTES","prior_round_report":"docs/review/REL-006-R2.md","prior_rounds":["docs/review/REL-006-R1.md","docs/review/REL-006-R2.md"],"hard_gates":{"total":5,"pass":5,"fail":[],"waived":["check-release_external_tool_unavailable_adjudicated_by_DEC-028"]},"prior_findings":{"R1":{"fixed":8,"not_fixed":1,"newly_introduced":0},"R2_N":{"N1":"fixed","N2":"fixed","N3":"fixed","N4":"fixed","fixed":4,"not_fixed":0,"newly_introduced":0}},"new_findings":{"N5":"P3","N6":"P3","total":2,"blocking":0},"unresolved_blockers":0,"push_ready":true,"push_blockers":[],"push_constraints":["executor=Coordinator","post_push_required: check-release --lineage-mode released --release-commit 914725f8","record_actual_pushed_commit_count_in_evidence"],"lineage_release_commit":"914725f8","tag_identity":{"object":"793dde78","peel":"914725f8","authority":"git for-each-ref refs/tags/v0.5.2"},"next_round_required":false} -->
