# CLEAN-004 — 实机浏览器验证清单（按 HEAD `21b100c` 重裁）

**任务**：CLEAN-004（P2，目标版本 v0.5.3）· **作者**：QA Agent · **日期**：2026-09-13
**仓库**：`D:\AI\agent\deepseek\harness\writing-workflow` · **HEAD**：`21b100c`（UX-060）
**证据目录**：`docs/evidence/CLEAN-004/`（探针源码、`report.json`、截图）。证据索引见 `docs/evidence/CLEAN-004/README.md`。

> **本清单的性质**：原始 CLEAN-004 行（`.governance/plan-tracker.md` L73）写于 **UX-006/UX-007 时代（v0.4.0）**；
> 此后 UX-008~UX-060 连续重构（两级工作台、控制台/抽屉、挤法分栏、底部工作台、notice 双形态…），清单大量条目
> **已陈旧或语义漂移**。本清单**按当前 HEAD 重新裁剪**：每条给出「面 / 验证方式 / 期望结果 / 判据来源 / 状态」，
> 并在文末给出 **①~⑤ 原始条目的逐项判定**（仍有效 / 被 X 取代 / 已废弃 + 依据）。

---

## 0. 验证方式与状态口径

| 记号 | 含义 |
|---|---|
| **自动** | 由隔离实例探针 `docs/evidence/CLEAN-004/probe-clean-004.mjs` 驱动真实 UI 自动断言（真点击/真指针拖拽/真键盘事件/真 HTTP 轮询）。判定为「真」需 `report.json` 中对应断言 `status=PASS`。 |
| **用户目检** | 需要真实用户实例（真实工作区、真实会话、真实 API Key）才能判定的面；由用户在浏览器执行（见 §4 用户侧最小清单）。 |
| **PASS** | 自动化断言成立（附命令/退出码/输出摘要）。 |
| **FAIL** | 自动化断言不成立 ⇒ 记入缺陷（见 `DEFECTS`）。 |
| **N-A** | 该面在当前环境**无法构造**（不是"通过"）：如实标注原因，转入用户侧清单或未覆盖风险。 |

**验证环境**（每次运行都可复核）：隔离根 `%TEMP%\clean004-probe-<rand>\`（`DSH_HOME`/`USERPROFILE`/`HOME`/`APPDATA`/`LOCALAPPDATA`/`TEMP` 全部重定向 + prefix+sep 包含性校验）；宿主平面以**只读 junction** 接入；无头 Edge + CDP；随机端口；真实 `~/.dsh` 全程只读（前后指纹逐项一致）。用户正在运行的 DSH 实例与浏览器**未被触碰**。

---

## 1. 面 A — 管理台（侧栏抽屉）

| # | 面 | 验证方式 | 期望结果（可判真伪） | 判据来源 | 状态 |
|---|---|---|---|---|---|
| A-01 | 管理台/抽屉 | **自动** | 抽屉渲染全部书目卡（fixture 10 本，卡数 = 10）；标题行 `aria-label` = `entryLabel`；插入符号（`.nv-drawer-caret`）在位 | `lib/client.js:L2902`（`NovelDrawer`）/ `L2867`（`DrawerBookCard`）/ `L2916` / `L2938` | **PASS** |
| A-02 | 管理台/抽屉 | **自动** | 抽屉宽度 = 满行（实测 ≈256px），书目卡左缘与抽屉左缘对齐（偏差 ≤1px，UX-013 批注⑥） | `lib/client.js:L2927` / 样式 `.nv-drawer`（`client.js:L4528`） | **PASS** |
| A-03 | 管理台/抽屉 | **自动** | 点抽屉标题行 → 控制台**打开**（反选语义：再点关闭） | `lib/client.js:L2858`（`toggleConsole`）/ `L2931` | **PASS** |
| A-04 | 管理台/抽屉 | **自动** | 抽屉卡**点击 = 直达创作台**（不再打开管理台）——本 HEAD 行为 | `lib/client.js:L2949-2957`（`openCtl.open`） | **PASS**（见 C-01 链） |
| A-05 | 管理台/抽屉 | **用户目检** | 图标栏模式（侧栏折叠为窄栏）下抽屉退化为单 📖 入口，点击开/关列表 | `lib/client.js:L2919-2925` | 待目检（自动化未覆盖：需宿主侧栏折叠态） |

## 2. 面 B — 控制台（管理台全屏浮层）

| # | 面 | 验证方式 | 期望结果（可判真伪） | 判据来源 | 状态 |
|---|---|---|---|---|---|
| B-01 | 控制台/几何 | **自动** | 控制台四边 ≡ 会话内容区 rect（偏差 ≤1.5px）；无会话根时才降级为侧栏右缘/280px | `lib/client.js:L3075`（`NvConsole`）/ `L3170-3230`（几何效应）/ `L988`（`findConversationRoot`）/ `L3200`（280 兜底） | **PASS**（本实例命中会话根分支） |
| B-02 | 控制台/几何 | **用户目检** | **降级分支**：宿主无会话视图（`findConversationRoot()===null`）时控制台左侧 = 侧栏右缘、top=0、右/下=视口边缘 | `lib/client.js:L3199-3210` | 待目检（自动化未能构造该 DOM 态） |
| B-03 | 控制台/头部 | **自动** | 头部 = 标题「小说管理工作台」+ 工作区路径 + 「切换 / 新建工作区…」+ ✕（`aria-label=关闭`） | `lib/client.js:L3414-3424` | **PASS** |
| B-04 | 控制台/搜索 | **自动** | 关键词滤小说卡（「孤星」→ 仅 1 卡）；＋磁贴过滤时恒显示；卡数 ≤8 时无折叠行 | `lib/client.js:L3160-3167` / `L3507` | **PASS** |
| B-05 | 控制台/搜索 | **自动** | 无匹配 → 空态 `.nv-empty`「没有匹配的小说或会话」，卡数 0 | `lib/client.js:L3467` | **PASS** |
| B-06 | 控制台/搜索 | **自动** | 清除钮 → 关键词清空 + 恢复 8 卡（折叠态） | `lib/client.js:L3521-3523` | **PASS** |
| B-07 | 控制台/搜索 | **自动**（R2 重设计后）+ **用户目检** | 关键词命中会话 → 「找到的会话（N）」+ 会话行（标题 + 预设标记 ✓），点行 = 切会话并关控制台 | `lib/client.js:L3427-3439` / `L3430`（行渲染）/ `L3432`（点击行为）/ `L3394-3397`（命中口径 `displayTitle ?? id`） | **R2 重定**：检索前提改为「**实际会出现在 `displayTitle ?? id` 的串**」（宿主已认可 id 的 `session-…` 展示串 / 镜像实测 `displayTitle`）⇒ 自动化可判（`B21a`/`B21b`）；**原文的「隔离实例的会话镜像未回收自建会话 ⇒ 本环境无法构造」与「自动化 FAIL（缺陷 D-3）」两说均已撤下**（R2 N-03：D-3 撤回——原关键词 `孤星` 与 `displayTitleOf = title ?? cwd 名 ?? id` 无交集，空结果属**预期行为**）。用户侧复核项 **U-2** 保留（真实实例的人读口径） |
| B-08 | 控制台/网格 | **自动** | 10 本 → 折叠前 8 卡 + 折叠行「展开全部（2）」+ ＋虚线磁贴 | `lib/client.js:L3165-3167` / `L3500-3505` / `L3507-3512`；`CONSOLE_COLLAPSE_N=8`（`L955`） | **PASS** |
| B-09 | 控制台/网格 | **自动** | 3 列均分（`grid-template-columns` 三轨等宽 ≤0.1px；同行 3 卡） | `lib/client.js:L3478` + 样式（`client.js` `.nv-cgrid`） | **PASS** |
| B-10 | 控制台/网格 | **自动** | ＋磁贴最小高度 = 180px，卡片 ≥180px（等高下限） | `lib/client.js` 样式 `.nv-cplus` / `.nv-ccard` min-height 180 | **PASS** |
| B-11 | 控制台/网格 | **自动** | 卡片结构：monogram 首字 + 阶段徽标 + 数据 chips（章/字/发布/变现/信号）+ 数据行 + 更新时间；8 卡 ×（2 图标钮 + 绑定双圆） | `lib/client.js:L3033-3060` | **PASS** |
| B-12 | 控制台/网格 | **自动** | 旧形态**负断言**：卡片无「▶ 打开」钮（UX-008④）、无「➤ 启动工作流」钮（UX-013②.4） | `lib/client.js:L2972`（注释）/ `L3055-3060`（仅 🔗/🗑） | **PASS** |
| B-13 | 控制台/网格 | **自动** | 图标钮 22×22（UX-008④ 尺寸收敛） | `lib/client.js:L3056-3059` + 样式 `.nv-cico` | **PASS** |
| B-14 | 控制台/排序 | **自动** | 排序两态（默认/手动，`data-on` 互斥）+ 手动态卡片 `draggable=true` | `lib/client.js:L3361-3392` / `L3443-3444` / `L3486` | **PASS** |
| B-15 | 控制台/弹窗 | **自动** | ＋磁贴 → 居中模态（水平/垂直居中 ≤2px）+ 单输入 autoFocus + 「创建/取消」 | `lib/client.js:L3529-3553` / `L3337-3357`（`runCreate`） | **PASS** |
| B-16 | 控制台/弹窗 | **自动** | 空目录名点「创建」→ 表单错误「目录名必填」，弹窗不关（前端守卫，不发请求） | `lib/client.js:L3340` | **PASS** |
| B-17 | 控制台/Esc | **自动** | Esc 在弹窗态**只关弹窗**（控制台保持打开） | `lib/client.js:L3234-3248`（含 `creating` 分支） | **PASS** |
| B-18 | 控制台/Esc | **自动** | Esc 在**绑定面板**态：`NvConsole` 的 Esc 处理**让位**（`bind !== null` 直接 return）——但 `BindDialog` 自身**无 Esc 关闭** ⇒ 面板不关 | `lib/client.js:L3239`（让位）/ `L2626+`（`BindDialog` 全文无 Escape 监听：`grep -n Escape lib/client.js` 仅 L3237/L3772） | **FAIL**（缺陷 D-1） |
| B-19 | 控制台/数据 | **自动** | 卡片状态双圆语义：失效绑定 → `data-bound=stale` | `lib/client.js:L2966-2969`（`BindDualDot`）/ `L2983` / `L1428`（`statusOfEntry`） | **PASS** |
| B-20 | 控制台/绑定 | **自动** | 失效卡 🔗 → 绑定面板打开（标题/「为本书选择一个会话」/「新建会话并绑定」三面齐备），且**在控制台之上** | `lib/client.js:L2626`（`BindDialog`）/ `L3495-3498`（🔗 消费点）/ `L2784`（模态半径） | **PASS** |
| B-21 | 控制台/互斥 | **自动** | 控制台 ↔ 分栏互斥：打开控制台（抽屉标题行）→ 分栏自动关闭 | `lib/client.js:L2852-2856`（`openConsole`）/ `L1124-1128`（`novelSplit.open` 关控制台） | **PASS** |

## 3. 面 C — 分栏（创作工作台）

| # | 面 | 验证方式 | 期望结果（可判真伪） | 判据来源 | 状态 |
|---|---|---|---|---|---|
| C-01 | 分栏/打开 | **自动** | 未绑定卡点击 → `openCtl` 自动链（`sessions.create` → 预设 → 绑定 → 打开）→ 分栏打开；控制台自动关 | `lib/client.js:L1530-1588`（`openCtl`）/ `L1575-1581` | **PASS** |
| C-02 | 分栏/标题 | **自动** | 标题 =「📖 小说创作工作台 ·《孤星纪元》」 | `lib/client.js:L4088-4089` + i18n `creationTitle`（`L166`） | **PASS** |
| C-03 | 分栏/挤法 | **自动** | 对话窗 `viewArea` 被写入 `marginLeft/Right` + 显式 `width`（UX-018 双压）；分栏右缘 ≡ viewArea 左缘（≤2px） | `lib/client.js:L1071-1081`（`applyMargin` 调用）/ `L3050+`；`L1124-1130` | **PASS** |
| C-04 | 分栏/标题栏 | **自动** | 标题栏四控件齐备：⇄ 换边 / ☆ 设为默认 / ⟳ 恢复默认 / ✕ 关闭（`aria-label` 齐备） | `lib/client.js:L4100+`（`.nv-bar-ctl` 簇） | **PASS** |
| C-05 | 分栏/三面 | **自动** | 左窗（文件树 + 17 阶段工作流清单，当前阶段行唯一）+ 中窗（章节列/正文/数据·发布·请求 3 页签）+ 三条常驻分隔线（`.nv-vdiv`/`.nv-middiv`/`.nv-chdiv`） | `lib/client.js:L4188+` / `L4219` / `L3559-3619`（三分隔线 handler） | **PASS**（记录运行 + 独立复跑；`final4` 曾在该点读到分栏自发关闭 ⇒ 已裁定为**时序 artifact**，探针已加存活闸门——见 `docs/evidence/CLEAN-004/DEFECTS.md` §3） |
| C-06 | 工作流面板 | **自动** | 下半区控制条：主按钮（`data-mode=go/stop`）+「压缩上下文」+「绑定新会话」（UX-059） | `lib/client.js:L4020-4061` | **PASS** |
| C-07 | 分栏/分隔线 | **自动** | 拖拽竖分隔线 → 左窗宽增大且 `dsh.novel.split.v1.leftW` 同步（与实测差 ≤2px） | `lib/client.js:L3559-3575` / `L946-950`（`persistSplit`）/ `L887` | **PASS** |
| C-08 | 分栏/关闭 | **自动** | 标题栏 ✕ → 分栏关闭且**挤法还原**（`marginLeft` 复空、对话窗回满） | `lib/client.js:L1152+`（`close`）/ `L3867+`（`closeWorkbench`） | **PASS** |
| C-09 | 分栏/分隔线 | **自动** | 关闭后重开 → 左窗宽按存档恢复（宽度记忆，偏差 ≤2px） | `lib/client.js:L925-945`（`loadSplitSaved`）/ `L1063-1065` | **FAIL**（记录运行实测：`{reason:'分栏重开失败', settleToBound:false, reopenAttempt.subBefore:'…会话失效', reopenDiag.bindPanel:true}` —— 分栏重开走失效重绑分支，**D-2 的下游**；见 `DEFECTS.md` §2/§4） |
| C-10 | 分栏/换边 | **自动** | ⇄ → `chatSide` right→left 且持久化；挤法边距换侧；**分栏不自关**（UX-014 `sameMargin` 容差回归） | `lib/client.js:L975-985`（`sameMargin`）/ `L1109-1123`（让位观察器）/ `L3602-3619` | **FAIL**（记录运行实测 `{sideAfter:{chatSide:'right', ml:'', mr:'', splitOpen:false}}` —— 分栏未重开 ⇒ 无 ⇄ 可点；**D-2 的下游**） |
| C-11 | 分栏/换边 | **自动** | ⇄ 再点 → 回到 `chatSide=right`（两态可逆；**须先确认 ⇄ 确实被点击**） | 同上 | **N-A**（R1 返工：原为空真 PASS ⇒ 谓词加 `flipBackClick === true` 前置，未点击时记 N-A；本轮分栏未重开 ⇒ 前置不成立。见 `DEFECTS.md` §4） |
| C-12 | 分栏/脏稿守卫 | **用户目检** | 有未保存章节修改时：切书/关分栏前弹确认（`dirtyConfirm`/`dirtyCloseConfirm`），取消则不切换 | `lib/client.js:L1592-1603`（`confirmSwitchSelect`）/ `L222`（文案） | 待目检（自动化未覆盖：`window.confirm` 原生模态无法在无头 CDP 下稳定断言） |
| C-13 | notice | **自动** | 短提示（≤24 字符）走**紧凑单行承载**：在场 ∧ `data-form=row` ∧ 单行 ∧ 高 ≤32px ∧ `title` 置空 ∧ 无溢出——UX-060 短提示零变化 | `lib/client.js:L4140-4142`（`.nv-bar-note` chip）/ `L4177`（流式面 `data-form` 短文本 ⇒ `row`）/ `L891-896`（`NOTICE_INLINE_MAX=24`，其后修订） | **PASS**（R1 返工：前置改走未绑定卡路径后**首次取得实测** —— `E1` 实测 `{cls:nv-notice, form:row, lines:1, h:29, text:已恢复默认布局, titleAttr:""}`。**注**：实测形态为 `.nv-notice[data-form=row]` 紧凑单行，而非旧清单写的「标题栏内 chip」——按语义（紧凑单行）判定） |
| C-14 | notice | **自动** | 长提示 = 标题栏下方 **in-flow 流式面**（`data-form=block`；不在标题栏内；多行；`pre-wrap` 不截断；含关闭钮；`title` 置空；首尾文案逐字）——UX-060 | `lib/client.js:L4164-4245`（流式面）/ `L4805-4815`（样式）/ `L3687-3711`（形态判定） | **PASS**（R1 返工后首次实测：`E2` 实测 `{form:block, lines:4, close:1, titleAttr:"", whiteSpace:pre-wrap}`；取证截图 `02-long-notice-block.png`） |
| C-15 | notice | **自动** | 长提示与标题/控件交集 = 0；位于标题栏之下；不覆盖下方首屏内容 | 同 C-14 | **PASS**（R1 返工后首次实测：`E3` 实测 `{interTitle:0, interCtlMax:0, y:39, barBottom:39, belowContent:true}`） |
| C-16 | 共存互斥 | **自动** | 分栏打开路径派发 `dsh:split-claim`（`detail.id=novel-writing`）占用声明（dsh-worktable 互操作协议面） | `lib/client.js:L1045-1056` | **PASS**（R1 返工后首次实测：`F1` 实测抓到 `claims` 含 `novel-writing`——探针在「关 → 等消失 → 开」窗口内监听 `dsh:split-claim`；前置改走未绑定卡路径） |
| C-17 | 共存互斥 | **用户目检 / 未覆盖** | 与 dsh-worktable 同装时的**双向**互斥（对方 `.ta_splitClose` 让位 + 占用声明被对方消费） | `lib/client.js:L1048-1050`；对方插件选择器 `.ta_splitClose` | **N-A**（本机 profile 未安装 dsh-worktable，无法构造；见「未覆盖风险」） |

## 4. 面 D — 降级与失效重绑

| # | 面 | 验证方式 | 期望结果（可判真伪） | 判据来源 | 状态 |
|---|---|---|---|---|---|
| D-01 | 数据/绑定 | **自动** | 绑定指向不存在会话 → 卡片/抽屉卡呈「会话失效」（`data-bound=stale` / 副文案含「会话失效」） | `lib/client.js:L1428`（`statusOfEntry`）/ `L2880-2881` | **PASS** |
| D-02 | 数据/绑定 | **自动** | 失效卡 🔗 → 绑定面板（`stale: true` 分支） | `lib/client.js:L1535`（`openCtl.open` stale 分支）/ `L3495-3498` | **PASS** |
| D-03 | 降级态 | **自动** | 会话服务可用态：卡片渲染绑定双圆（服务缺席时该面隐藏） | `lib/client.js:L3148`（`degraded` 判定）/ `L3052` | **PASS** |
| D-04 | 降级态 | **用户目检** | `sessions` 服务整体缺席：双圆隐藏、打开/绑定禁用 + 提示「会话服务不可用…」 | `lib/client.js:L3148` / `L236`(i18n `sessionsMissing`) / `L3496` | 待目检（自动化未能构造"服务缺席"实例） |
| D-05 | 降级态/会话联动 | **用户目检**（自动化 `D6` 在本环境记 **N-A**） | 分栏/控制台打开期间用户点宿主侧栏其他会话 → 自动关闭（UX-010④/UX-014⑧；BUG-004 last-non-null 基准） | `lib/client.js:L3802+`（分栏联动）/ `L3101-3131`（控制台联动）/ `L799-800`（`shouldCloseOnCurrentChange`） | 见 §4 用户侧（隔离实例侧栏无可切换会话行）。**R2 N-07 订正**：记录运行的 `D6` 原件为 **FAIL（旧谓词）**，**终版谓词**下 `hostRowsBefore=0` ⇒ N-A；两者口径不同不得混写——见 §10 的逐运行标注 |
| D-06 | 降级态 | **用户目检** | 挤法失败（无会话根）时保持现状 + 提示「未找到当前会话视图…」 | `lib/client.js:L1541` / `L1560` / i18n `openFailHint`（`L235`） | 待目检 |
| D-07 | 数据/绑定 | **自动** | 自动链建会话 + 写绑定后，卡片状态应在会话语义上收敛（不长期停在「会话失效」） | `lib/client.js:L1575`（`bindSession`）/ `L1428`（`statusOfEntry` 只按镜像判定）/ `L3147` | **FAIL**（见缺陷 **D-2**；实测时限 **≥2′06″ 未收敛、≈2′31.3″ 内收敛**——下界按 R3-05 订正、上界按 R5 NEW-R5-02 由 `≤2′31″` 改为实测值；非确定性：`run1` 全程不收敛） |

## 5. 面 E — 请求 / 数据 / 发布 / 工作流面板（观感面）

| # | 面 | 验证方式 | 期望结果 | 判据来源 | 状态 |
|---|---|---|---|---|---|
| E-01 | 数据面板 | **自动**（结构性） | 中窗 3 页签（数据/发布/请求）；数据页签含「暂无信号（入库后自动检测）」空态 | `lib/client.js:L4064-4081` / `L1913+`（`DataPanel`） | **PASS**（页签存在）；**内容面**（真实信号清单的渲染/去重/空态切换）无自动化落点 ⇒ **R2 N-07 收口**：已登记为 `R-16`（见 §9.1）+ 并入 **U-11** 判据 |
| E-02 | 发布面板 | **用户目检** | 平台配置就地编辑（`platforms` 来自 overview）+ 发布记录表 + 状态点 | `lib/client.js:L1989+`（`PublishPanel`）/ `L4836-4837`（状态点样式） | 待目检 |
| E-03 | 请求面板 | **用户目检** | 请求清单（待处理/已完成）+ 提交请求表单 | `lib/client.js:L2221+`（`RequestPanel`） | 待目检 |
| E-04 | 工作流面板 | **自动** | 17 阶段清单 + 当前阶段唯一高亮（`data-cur=true`）+ 门禁 chips（发布/变现） | `lib/client.js:L1891-1901` / `L4838-4839` | **PASS** |

## 6. 面 F — 设置页 / 诊断（回归面）

| # | 面 | 验证方式 | 期望结果 | 判据来源 | 状态 |
|---|---|---|---|---|---|
| F-01 | 设置页 | **用户目检** | 「小说写作」设置区可打开、无报错 | `lib/client.js:L2268+`（`SettingsSection`）/ `L4958+`（注册） | 待目检 |
| F-02 | 设置页/诊断 | **用户目检** | 「诊断」区渲染能力报告（面 1 实测 + 未覆盖项如实），无写入路径 | `lib/client.js:L2351+`（`buildDiagReport`）/ `L4854+`（样式） | 待目检 |

---

## 7. 原始 CLEAN-004 条目判定（①~⑤）

> 逐项给判定 + 依据。**⑥（UX-012 补审）不在 QA 范围**（另有 Code Reviewer 并行承接），本清单不涉及。

### ① 控制台全链路（原 6 项）

| 原始子项 | 判定 | 依据 |
|---|---|---|
| 侧栏标题行开/关（反选） | **仍有效** | `client.js:L2858` `toggleConsole`；自动化 A-03 **PASS** |
| 搜索滤小说 + 找 session | **已取代**：「滤小说」仍有效；「找 session」行渲染 + 命中链路在 R2 返工后**可自动化判定** | A/B-04 **PASS**；B-07 **PASS**（`B21a`/`B21b`/`B22`，R2 重设计前提后实测命中） |
| ＋新建表单（创建并开始 / 仅创建） | **被 UX-012 取代**：表单仅剩「目录名 + 创建/取消」，**无「创建并开始」**（自动启动链已删） | `client.js:L3332-3357`（`runCreate`）/ `L3529-3553`；B-15/B-16 **PASS** |
| 卡片状态光效 / 数据卡片 | **仍有效**（语义细化）：`data-glow`（busy/need/done）+ 状态 chips + 数据行 | `client.js:L2984`（glow）/ `L3001-3049`；B-11/B-19 **PASS** |
| ▶ 打开 = 切绑定会话 + 进分栏 | **被 UX-008④ 取代**：`▶ 打开` 钮**已删**，卡片主体点击即打开（并被 UX-014⑦ 收敛进 `openCtl` 单链） | `client.js:L2972` 注释 / `L3014-3027`（主体 onClick）/ `L1530`；B-12 负断言 + C-01 **PASS** |
| 🔗 绑定 / 失效重绑 | **仍有效** | `client.js:L3495-3498` / `L1535`；B-20/D-01/D-02 **PASS** |
| ➤ 继续工作流 | **被 UX-013②.4 + UX-059 取代**：卡片 ➤ 钮已删；启动动作迁至创作台**下半区工作流控制条**主按钮（`data-mode=go`） | `client.js:L3058` 注释 / `L4042-4050`；B-12 负断言 + C-06 **PASS** |

### ② 分栏（原 4 项）

| 原始子项 | 判定 | 依据 |
|---|---|---|
| 分隔线拖拽 + 宽度记忆（`dsh.novel.split.v1`） | **仍有效**（范围扩大：新增 `.nv-middiv` 中窗上下分隔线与 `.nv-chdiv` 章节列分隔线，且 `midH` 一并持久化） | `client.js:L887`（键）/ `L925-950` / `L3559-3619`；C-07 **PASS**、C-09 **FAIL**（见 §3 表；R1 F-07 订正：本条原写「C-07/C-09 PASS」与 §3 状态相反） |
| ⇄ 换边 | **仍有效** | `client.js:L3602-3619` / UX-014 `sameMargin`（`L975-985`）；C-10 **FAIL**（分栏未重开 ⇒ 无 ⇄ 可点）、C-11 **N-A**（R1 返工：加「⇄ 确被点击」前置后不再冒充空真 PASS——见 §3 表；R1 F-07 订正：本条原写「C-10/C-11 PASS」与 §3 状态相反） |
| Esc / ✕ / 反选 | **仍有效**（Esc 分两级：`closeSplit` 关闭 + 绑定/新建弹窗让位；✕ 按钮在标题栏 `.nv-bar-ctl`） | `client.js:L241`（`closeSplit` 文案）/ `L3772`（`onKey`）/ `L3239`（控制台让位）；C-08 **PASS**；**绑定面板自身缺 Esc** ⇒ 缺陷 D-1 |
| 脏稿守卫 | **仍有效** | `client.js:L1592-1603` / `L222`（文案）；C-12 转用户目检 |

### ③ 降级（原 3 项）

| 原始子项 | 判定 | 依据 |
|---|---|---|
| sessions 缺席 | **仍有效**（`degraded` 统一判定；双圆隐藏、打开/绑定禁用） | `client.js:L3148` / `L3052` / `L3496`；D-03 **PASS**（可用态）；缺席态转目检 |
| 挤法失败 | **仍有效**（`ensureSplit` 重试 12 次后 `onFail` → `openFailHint`；控制台保持打开） | `client.js:L1506-1521` / `L1541`；D-06 转目检 |
| 控制台几何无会话根 | **仍有效**（`mode:'fallback'`：左侧 = 侧栏右缘 / 280px 兜底） | `client.js:L3199-3210`；B-02 转目检 |

### ④ 与 dsh-worktable 共存互斥

**判定：仍有效，但本环境只能验证单向协议面。** 依据：`client.js:L1045-1056`（打开分栏时点掉对方 `.ta_splitClose` + 派发 `dsh:split-claim`）；`C-16` **N-A**（声明面——分栏打开路径在该运行未取得 ⇒ 无 PASS 级证据；R1 F-07 订正：本条原写「`C-16` **PASS**」，与 §3 表的 N-A 相反）。**双向互斥需同装 dsh-worktable** ⇒ 本机 profile 未安装 ⇒ **N-A**，列入 §10「未覆盖风险」与用户侧清单。

### ⑤ 评审备注处置评估（A-1/B-1/C-1~C-3/F-1~F-3/H-1/H-2）

> 来源：UX-007 评审（EVD-021，Code Reviewer `bf2e7c08`，APPROVED-WITH-NOTES）。

| 备注 | 判定 | 依据（按当前 HEAD 核对） |
|---|---|---|
| **A-1** 抽 `launcher.createAndStart` 收口三份拷贝 | **已废弃（问题本身消失）** | 三份「建会话+挂预设+绑定」拷贝已随 UX-012（新建弹窗去自动启动）与 UX-014⑦（`openCtl` 单链）合并为**一条**：`client.js:L1530-1588`；`launch/promptLaunch` 仅剩创作台控制条一个消费点（`L3947+`） |
| **B-1** 对话框 Esc 让位 | **部分未落实 ⇒ 缺陷 D-1** | 让位契约在 `NvConsole`（`L3239`）与绑定/工作区浮层之上成立，但 `BindDialog` **自身没有 Esc 监听**（全文 Escape 仅 `L3237`/`L3772`）⇒ 绑定面板打开时 Esc 无效（B-18 **FAIL**）；新建弹窗路径正常（B-17 **PASS**） |
| **C-1** 守卫式 hook 注释 / stub | **被取代（已内化为注释契约）** | hook 恒调用的理由已就地注释：`client.js:L2979-2980`（`ConsoleNovelCard`）、`L2873-2874`（`DrawerBookCard`）、`L2660+`（`BindDialog` 注释「hook 恒定调用（open 判空移入渲染）」） |
| **C-2** selector 重建噪音 | **被取代（不再适用）** | 当前离线：`useSessions` 订阅走 `sessions.list` 叶子字段（`client.js:L1339+`）+ 组件内 selector 常量（`L2981`），未见"每次渲染重建 selector 对象"的调用点 |
| **C-3** 聚焦选择器单引号 | **已废弃（实现已换法）** | 聚焦改走 DOM 查询 `document.querySelector('.nv-ccard[data-nv-id="…"]')`（`L3257`）+ `scrollIntoView`（`L3259`），原"聚焦选择器"形态不存在 |
| **F-1** smoke 断言收紧：z-index 类归属 | **被取代** | 卡片不再有 `▶ 打开` 钮（B-12 负断言）；层序断言已由 smoke 覆盖 `zIndex 900/950/1000`（`client.js:L4565`/`L4648` 注释 + smoke 断言） |
| **F-2** smoke 断言收紧：null 白名单 | **已废弃（对应路径消失）** | 原"null 白名单"针对已退役的卡片路径；当前卡片控制台渲染有 hero/空态/网格三分支（`L3456-3478`），断言随 UX-055 重写 |
| **F-3** smoke 断言收紧：卡片路径树断言 | **被取代** | 原「卡片路径树」面已随 UX-014①（左窗去书目列表）退役；当前树断言针对 `.nv-ft-row` 文件树（`L2080+`） |
| **H-1** 实机行为面清单 | **仍有效 ⇒ 本清单即落实** | 本文件 §1-§6 即 H-1 要求的"实机行为面清单"；自动化子集由 `probe-clean-004.mjs` 实证 |
| **H-2** 未用 i18n 键清理 | **FAULT（存在未用键）** | 自动化扫描 `scan-i18n-dead-keys.mjs`：zh/en 各 150 键、**各 6 键零消费引用**（`refresh`/`platform`/`submitRequest`/`lightAudit`/`chapter`/`add`，zh `L170-183`、en `L347-360`）；`retired` 为 `diagReason` 枚举码（在用，非死键） |

---

## 8. 用户侧最小清单（≤10 条，可复核）

> 前置：**重启 DSH**（使 BUG-007 的 `config.prefix` 修复经 `ensurePreset()` 重同步生效、UX-060 长告警面生效）。
> 判据列写明"看什么/量什么"，任一条不成立即回报缺陷。

| # | 操作步骤 | 期望结果 | 判据 |
|---|---|---|---|
| U-1 | 侧栏底部「📖 小说管理工作台」标题行点一下，再点一下 | 控制台开 / 关（反选） | 标题行点击 = 全屏管理台出现/消失；不遮侧栏 |
| U-2 | 控制台底部搜索框输入书名片段 | 卡片被过滤；清空后恢复；输入会话标题片段 → 出现「找到的会话（N）」行，点行 = 切到该会话且控制台关闭 | 卡片数随关键词变化；会话行点击后控制台消失 |
| U-3 | 点网格末尾「＋」虚线磁贴 → 只填目录名 → 创建 | 弹窗居中；空名点创建报「目录名必填」；成功即关窗 + 顶部提示 + 新卡进入网格 | 空名不关窗（`client.js:L3340`）；成功后卡片出现（≤2.5s，overview 2.5 s 轮询 `L3142`）。**R1 F-15 订正**：「新卡聚焦」在 UX-012（去自动启动链）+ UX-055 后无判据来源（`grep -n consoleFocus` 仅为「切书聚焦」，非「新建后聚焦」）⇒ 已从期望中删除；如需保留该期望，须先给出 `lib/client.js` 判据行。 |
| U-4 | 点一张**未绑定**卡 | 自动建会话并绑定 → 进入创作台（分栏）；卡片状态由「未绑定」转为已绑定（**不应长期停在「会话失效」**） | 分栏标题 =「📖 小说创作工作台 ·《书名》」；侧栏抽屉该卡副文案不含「会话失效」 |
| U-5 | 在创作台里拖左窗右侧竖分隔线；关闭（✕）后从侧栏卡片重开 | 宽度记忆保持（重开后左窗宽与拖后一致） | 目视宽度一致；`localStorage['dsh.novel.split.v1'].leftW` 与实测一致 |
| U-6 | 点标题栏 ⇄（换边）；再点一次 | 对话窗在左/右之间切换且分栏不关闭 | 对话窗位置切换；分栏仍在 |
| U-7 | 打开创作台后，改一节内容但不保存，点 ✕ | 弹确认；选「取消」则不关闭 | 原生 confirm 出现；取消后分栏仍在 |
| U-8 | 有未绑定卡时点 🔗（绑定会话）；在弹出面板上按 Esc | 面板应关闭（Esc 未生效即为缺陷 D-1） | 按 Esc 后面板消失 |
| U-9 | 创作台下半区控制条：点「▶ 开始/继续工作流」 | 绑定会话收到启动指令（无预设 persona 报错）；长错误文本以**标题栏下方整行可读**呈现（含关闭钮），短提示仍在标题栏内 | 长错误不被截断、不压在标题栏/横幅上；短提示保持单行 chip |
| U-10 | 若同装 dsh-worktable：分别从两侧开分栏 | 同一时刻只允许一方分栏（另一方让位） | 打开我方 → 对方关闭；打开对方 → 我方关闭 |
| U-11 | 折叠宿主侧栏（图标栏模式）后点 📖；并依次打开 发布 / 请求 / 设置「小说写作」/ 设置「诊断」四区 | 折叠态仍能开/关书目列表；四区可打开、无报错 | 折叠态入口可点；四区渲染无 `apiError` 文案 |
| U-12 | 构造/等待「无会话视图」与「会话服务缺席」两态（如：宿主会话视图未就绪时开控制台；profile 级禁用 sessions 服务后重启） | 控制台降级几何（左=侧栏右缘、右/下=视口边缘）；双圆隐藏 + 打开/绑定禁用 + 提示「会话服务不可用…」；挤法失败时提示「未找到当前会话视图…」 | 几何与提示逐条对照；禁用态按钮不可点 |
| U-13 | 分栏/控制台打开期间，点宿主侧栏**另一个会话** | 分栏/控制台自动关闭（UX-010④） | 点击后两者消失 |
| U-14 | 创作台下半区点「绑定新会话」 | 复用自动链建会话并绑定；面板状态收敛（不再「未绑定/会话失效」）；出现「已为本书绑定新会话 …」提示 | 副文案不含「会话失效」；提示文案出现 |

> **U-1~U-14 的覆盖映射**：U-1~U-10 覆盖主路径与 3 个目检项（`C-12`→U-7、`C-17`→U-10、`D-05`→U-13）；
> U-11~U-14 为 **R1 F-05 新增**，承接 §10.1 的 9 个「待目检」面与 6 类零覆盖行为面（突破原「≤10 条」上限的理由：原上限
> 建立在「主路径 ≥10 项」的假设上，而 R1 审计发现 15 个面**无任何落点**；拆分为 U-11~U-14 四条可执行项后总条目 14 条）。
> 若用户希望压缩，可只执行 U-11 与 U-14（对应残余风险最高的 R-10/R-11 面）。

---

## 9. 未覆盖风险（R1 F-05 补写；§0 L22 / ④ §7 / `C-17` 三处引用的小节）

> **性质**：本节是 §0 口径「N-A/目检项 → 转入用户侧清单**或未覆盖风险**」的落点，也是 ④（§7）与 `C-17` 引用的目标。
> **纪律**：每条给出「面 / 为何未覆盖 / 归口（用户项或任务）/ 残余风险」。**不得**用「覆盖较全面」之类笼统表述替代。
> **R1 F-05 修复范围**：① 补本节（原为悬空引用）；② 9 个自标「待目检」面逐个落点（§8 的 U-1~U-14 或下表已列明原因）；
> ③ 补登记零覆盖行为面（「绑定新会话」动作面 / 空书目 / 窄窗口 / 多书 >8 二次展开 / 无会话根 / sessions 服务缺席）。

### 9.1 自动化未覆盖面 → 归口表

| # | 面 | 判据来源 | 为何未覆盖（实测原因） | 归口 | 残余风险 |
|---|---|---|---|---|---|
| R-01 | `A-05` 图标栏（侧栏折叠）模式：抽屉退化为单 📖 入口 | `client.js:L2919-2925` | 无头实例无「侧栏折叠」持久态；折叠由宿主 UI 手势驱动，探针不改宿主布局状态 | **U-11**（用户目检） | 折叠态下入口是否仍可点开——低 |
| R-02 | `B-02` 控制台几何**降级分支**（`findConversationRoot()===null` → 侧栏右缘 / 280px 兜底） | `client.js:L3199-3210` | 需「宿主无会话视图」DOM 态；隔离实例启动即有会话内容区（`[data-phase]` 子节点）⇒ 无法在不 mock 产品代码的前提下构造 | **U-12**（用户目检） | 降级几何写错会整屏错位——中（有用户项兜底） |
| R-03 | `D-04` `sessions` 服务**整体缺席**（双圆隐藏 / 打开-绑定禁用 / `sessionsMissing` 提示） | `client.js:L3148` / `L3052` / `L2893` / `L4034` / `L236` | 隔离实例的宿主平面必然提供 `sessions` 服务（`useSessions !== null` 恒成立）⇒ `degraded` 分支**不可达**；请求见 `CLEAN-001` 类「宿主平面裁剪」能力 | **U-12**（用户目检；另建议另立探针能力任务，见 §10.3） | 降级提示与禁用在真机上未被验证——中 |
| R-04 | `D-05` 分栏/控制台打开期间**切换宿主会话** → 自动关闭 | `client.js:L3802+` / `L3101-3131` / `L799-800` | 需要「可切换的宿主会话行」；本次运行 `button[class*="sessionRow"]` 实测 0 行（`facts.hostSessionRowsAtB21.withText=0`）⇒ 无可点目标 | **U-13**（用户目检；`D6` 断言在本环境为 N-A） | UX-010④/BUG-004 联动面未被自动化覆盖——中 |
| R-05 | `D-06` 挤法失败（无会话根）保持现状 + `openFailHint` | `client.js:L1541` / `L1560` / `L235` | 与 R-02 同因（无「无会话根」态） | **U-12** | 失败分支提示文案未验证——低 |
| R-06 | `E-02` 发布面板（平台配置就地编辑 / 发布记录表 / 状态点） | `client.js:L1989+` | 需真实发布数据与平台配置；fixture `workflow-state.json` 无发布记录 | **U-11** | 面板渲染与落库一致性未验证——中（数据面） |
| R-07 | `E-03` 请求面板（请求清单 + 提交表单） | `client.js:L2221+` | 需真实请求数据；提交动作会写工作区（探针不写入 fixture 之外的语义面） | **U-11** | 提交路径未验证——中（数据面） |
| R-08 | `F-01` 设置页「小说写作」区 | `client.js:L2268+` | 设置页由宿主 shell 提供，探针未驱动宿主设置路由 | **U-11** | 注册与渲染未验证——低 |
| R-09 | `F-02` 设置页「诊断」区（能力报告 + 未覆盖项如实） | `client.js:L2351+` | 同上 | **U-11** | 「未覆盖项如实」这一自述面无人复核——中 |
| R-10 | 「**绑定新会话**」按钮的**动作行为**（不是存在性） | `client.js:L3968-4006`（`bindNewSessionCtl`）/ `L4057-4060`（按钮） | 自动化仅断言按钮**存在**（`C6`：文案/mode/disabled）；「点击 → 复用 `openCtl.autoCreate` 链 → 面板收敛 + `bindNewDone` 提示」**无任何断言**——而这是 BUG-007 三处失败面之三（三处中「继续工作流→U-9」「persona 挂载→U-9」已有落点，**本项无**） | **U-14**（用户目检）＋ §10.3 建议另立任务 | BUG-007 同族路径之一零覆盖——**中高** |
| R-11 | **空书目（0 本）**：hero 空态分支（48px 图标 / 28px 大字 / 副文案 / 主按钮） | `client.js:L3456-3478`（`zeroBooks` 分支；P0-0 R2 明确 hero 与网格互斥） | fixture 恒 10 本；构造 0 本需另建一套 fixture 并重跑全部依赖 10 本的断言（`B4/B7/B8/B10/B12b/D4` 均以 10 本为基线） | §10.3 建议另立任务 | 首启用户（0 本）看到的正是这一分支——**中高** |
| R-12 | **窄窗口 / 视口变化**（resize 后几何与挤法复算） | `client.js:L1084-1085`（`ResizeObserver`）/ `L3170-3219`（`measure`） | 视口恒 `1400×900`（`Emulation.setDeviceMetricsOverride`）；未做第二视口与 resize 序列 | §10.3 建议另立任务（同族 UX-060 探针已有两视口先例可复用） | 窄屏（如 1078×593——用户报障截图尺寸）下的挤法/notice 几何未验证——**中** |
| R-13 | **多书 >8 的「展开全部（2）」二次展开** | `client.js:L3500-3505` / `L3165-3167` | 仅断言折叠行文案与磁贴存在（`B8`），**未点「展开全部」**验证展开后 10 卡落位与折叠态回收 | §10.3 建议并入探针后续修订 | 展开路径（含 `expanded` 状态回退）未验证——低中 |
| R-14 | **脏稿守卫**（`window.confirm` 原生模态） | `client.js:L1592-1603` / `L222` | `window.confirm` 在无头 CDP 下无法稳定断言（会被自动 dismiss 或挂起） | **U-7**（用户目检，**唯一**有落点的目检项之一） | 已有用户项——低 |
| R-15 | 与 `dsh-worktable` 的**双向**互斥（对方让位 + 占用声明被对方消费） | `client.js:L1045-1056`；对方选择器 `.ta_splitClose` | 本机 profile 未安装 dsh-worktable ⇒ 无法构造对端 | **U-10**（用户目检） | 协议面单向已验证（声明派发），双向未验证——低中 |
| R-16 | `E-01` **数据面板内容面**（真实信号清单：有信号时的行渲染/去重、无信号时的 `emptySignals` 空态切换） | `client.js:L1913+`（`DataPanel`）/ `L4064-4081`（页签与容器） | 自动化只断言**页签存在**（结构性）与空态文案；fixture `workflow-state.json` 无真实信号数据，且信号由入库动作产生（写工作区语义面）⇒ 探针不构造 | **U-11**（用户目检；判据已补「数据页签内容面」） | R2 N-07 指出的**零落点面已闭合**——数据面板内容无自动化覆盖，误读「PASS」会高估覆盖——低中 |

### 9.2 与 §8 用户侧清单的映射（缺口 → 用户项）

| 新增用户项 | 对应未覆盖面 | 期望（可判真伪） |
|---|---|---|
| **U-11** | R-01 / R-06 / R-07 / R-08 / R-09 / **R-16** | 折叠侧栏入口可开列表；发布/请求/设置「小说写作」/「诊断」四区可打开且无报错；**数据页签内容面**：有信号时逐条渲染（无重复/无截断），无信号时显示「暂无信号（入库后自动检测）」 |
| **U-12** | R-02 / R-03 / R-05 | 构造/等待「无会话视图」与「会话服务缺席」两态，核对：控制台降级几何（左侧=侧栏右缘、右/下=视口边缘）、双圆隐藏、打开/绑定禁用并提示「会话服务不可用…」、挤法失败提示「未找到当前会话视图…」 |
| **U-13** | R-04 | 分栏/控制台打开时点宿主侧栏**另一个会话** → 两者自动关闭（UX-010④） |
| **U-14** | R-10 | 创作台下半区点「绑定新会话」→ 面板状态收敛（不再「未绑定/会话失效」）且出现「已为本书绑定新会话 …」提示 |

### 9.3 建议另立任务（不属本任务写锁范围，仅登记）

1. **探针扩展：空书目 fixture 变体**（R-11）——独立 fixture 根（0 本）跑一个精简场景集（hero 三分支 + ＋磁贴 + 创建链），避免与 10 本基线断言互相污染。
2. **探针扩展：多视口与 resize 序列**（R-12）——复用 `scripts/probe-nv-bar-geometry.mjs` 的两视口驱动，追加 `1400×900 → 1078×593 → 恢复` 的挤法/notice 几何复算断言。
3. **探针扩展：降级态构造**（R-03）——需要「宿主平面裁剪」能力（如 profile 级禁用 `dsh-api-session-controller`），属装配层改动，宜与 F-10（探针共享模块抽取）合并立项。
4. **`R-13` 展开全部二次展开**断言（低风险，可并入下一次探针修订）。
5. **F-10（R1 P3）探针重复装配抽取**：`docs/evidence/CLEAN-004/probe-clean-004.mjs`（约 1600 行）与 `scripts/probe-nv-bar-geometry.mjs`（590 行）的 `class Cdp` / `bootChild` / `resolvePlane` / `realFingerprint` / `containment` **逐函数各存一份**。本任务**不重构**（`scripts/**` 在写锁之外），建议并入 CLEAN-006 的 F-6 范围统一处理。

---

## 10. 状态汇总（本次自动化实测）

见 `docs/evidence/CLEAN-004/README.md` 与 `report.json` 的 `tally`；逐条状态见上文各表。
**R2 返工记录运行（2026-09-14 13:13:45→13:16:55 本地 = `05:13:45.162Z`→`05:16:55.181Z`，`probe-clean-004.mjs`，退出码 1，190 s，HEAD `21b100c`，无 crash，隔离根已清理）**：
`tally = { total: 49, pass: 42, fail: 4, na: 3 }`（`report.json` = `report-rework2-run.json`，sha256 `a6d8ae0f…`；探针 sha256 `de2de511…` = 终版，运行后未再编辑）。

| 计数 | 值 |
|---|---|
| 自动化断言总数 | **49**（`report.json.tally.total`；含新增 `FALSIFIABILITY-PROOF` 机证断言） |
| PASS / FAIL / N-A | **42 / 4 / 3**（`report.json.tally`，R2 权威记录运行） |
| FAIL 归属 | 产品缺陷 `D-1`（P3，Esc）×1；产品缺陷 `D-2`（P2，绑定收敛迟滞）本身 ×1（`C3b`）及其下游 ×2（`C9`/`C10`）——见 `DEFECTS.md`。**`D-3` 已撤回**（R2 N-03）⇒ 不再计入 |
| N-A 归属 | `B5`（宿主真实会话行 = 0 ⇒ 前置未取得；**原为空真 PASS**）、`C11`（⇄ 未被点击；**原为空真 PASS**）、`D6`（宿主无会话行 ⇒ 切换动作不可构造） |
| 阻塞/关键缺陷数 | **0 / 0**（P0 = 0 ⇒ 不阻断；P2 ×1 = D-2、P3 ×1 = D-1；**D-3 已撤回**） |
| 空真/弱断言 | **0**：`B5`/`D3`/`B21a`/`B21b`/`C11`/`B12b`/`D5`/`E1`/`C-face-availability` 九条谓词全部取自**共享登记表** `PREDICATE_REGISTRY`，逐条机证（**19 反例向量全红 / 11 正例向量全真**）+ 源码级部署绑定 9/9 + 注入式两向核对（见 `README §9`） |
| R2 N-01/N-03 成果 | `B21a`/`B21b` 前提重设计后**实测命中**（`foundTitle='找到的会话（1）'`、`rows=[{t:'novels'}]`），`B22` 首次取得观测并 PASS ⇒ 原「镜像未反映宿主会话」主张（D-3）撤回 |
| 回归基线 | `node test/smoke.mjs` = **291 passed / 0 failed**（exit 0）；`node test/validate-preset.mjs` = **PRESET VALIDATION PASSED**（exit 0） |
| 真实 `$DSH_HOME` 零写入 | `realEnvVerdict.ok = true`（`strictDeltas = {}` ∧ `inventoryDeltaCount = 0` ∧ `pointsIntoIsolation = false`） |
| 残留清理 | `%TEMP%\clean004-*` = **0**（运行期证据目录已删；`ux060-*` 未触碰，见 `README §7`） |
| 缺陷 | 见 `docs/evidence/CLEAN-004/DEFECTS.md`（本任务**只报不改**） |

> **旧值（供对照）**：R1 期 `tally = 48 / 34 / 6 / 8`（`report-run1.json`）；R1 返工运行 `49 / 40 / 6 / 3`（`report-rework-run.json`——**已被 R2 否证**：含 `B21a` 伪 PASS 与 D-3 假阳性）。
> **R3 复跑期望**：`tally = 49 / 42 / 4 / 3`、`exit 1`（若 `PASS` 更多或 `FAIL` 更少，先核对 HEAD 与 `lib/client.js` 是否被改动——本任务断言针对 `21b100c`）。
