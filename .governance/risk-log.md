# 风险记录

用于记录 workflow 执行过程中的风险、阻塞和缓解动作。

| 编号 | 识别日期 | 风险描述 | 影响 | 概率 | 等级 | 缓解措施 | 触发条件 | 责任人 | 状态 | 最后更新 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RISK-001 | 2026-08-22 | CI 首跑曾失败（schemastery mock 无法覆盖 .default()/.union 链式 API），修复刚合入（fdf4edd），CI 稳定性待观察 | 后续提交的回归信号可信度下降；若 CI 再失败可能掩盖真实回归 | 中 | 中 | 观察最近 3 次 CI 运行结果；若再次失败，升级为 P0 任务根因分析 | 最近 3 次 CI 运行中出现非文档性失败 | Coordinator | 已关闭 | 2026-08-22 |
| RISK-002 | 2026-08-22 | check-governance 在 root_divergence=true 部署形态下存在跨根误报（Check 28c 以插件仓库 dogfood 数据为期望；Check 18c/18d 把需求矩阵行与已完成任务误判为活跃 P0/P1）+ 部分扫描路径指向宿主不存在的位置（project\e2e-test-project、docs\release） | 宿主项目治理健康信号失真（27 issues 中 25 个 FAIL 均为误报）；每次运行 check-governance 需人工甄别，长期可能掩盖真实问题 | 高（确定复现） | 中 | 短期：误报清单固化于 DEC-007/EVD-001，check 结果解读时对照豁免；中期：向插件仓库（project_management_workflow）报告 issue 并跟踪修复；修复后重跑基线复核 | 每次运行 check-governance 产生同类 FAIL | Coordinator | 打开（2026-09-12 复核：danger-full-access 会话 CLI 全量可达后复跑 check-governance——历史 27 issues 中确证存在真实缺陷〔表行未转义管线/EVD-028 字段缺失/执行包结构缺口〕并已修复 20→8；确证误报剩余=Check 13 交叉引用 token 扫描〔93 项含 'UTF-8'/'P-08'/'FIX-082' 等非任务 token〕等；SYSGAP-001 上游报告仍待执行——**2026-09-12 新增同根因缺陷（Release Agent 独立发现 #2，附行号/实测）**：`check-release` 期望源跨根——`verify_workflow.py` **L7181** `changelog_path = changelog_path or (ROOT / "project/CHANGELOG.md")` 默认指向**插件仓自身** CHANGELOG 且该子命令**无 `--changelog` 参数**（argparse L23371-23388）⇒ 对宿主仓检查**结构性不可达**；另以 `ROOT` 为基期望插件仓「1.0.0 依赖链」/`REQ-059~064`/插件自家测试路径/插件 `docs/reviews/`（L6253/L6258-6262）。REL-006 实测 `--lineage-mode candidate` = **FAILED 21 issues**（其中 `changelog` FAIL 报文称本仓缺 `## [0.5.2]`——而本仓 CHANGELOG 确有该段）⇒ **stage-release「check-release PASS」在本仓不可用**；替代口径 = 三件套实跑（node --check/validate-preset/smoke 282）+ Release Reviewer 独立审查 + 人工裁决；**同族第三项（REVIEW-REL-006-R1 F7）**：`archive.py migrate --auto --dry-run` 解析**已发布版本 = 0 个**（仓内实有 8 个 tag：v0.2.0~v0.5.1）⇒ 归档触发判定恒为「跳过」（首次迁移/发布强制/task 增量/90 天兜底四类触发器均不生效）、长期致治理热文件膨胀与归档策略失效（无数据损失）；修复前以**人工判据**替代归档触发检测） | 2026-09-12 |
| RISK-003 | 2026-08-28 | UX-006 挤法布局引擎依赖宿主 DOM 约定：`[data-phase]` 会话根探测 + viewArea=children[1] 结构假设 + margin 挤压——DSH web 前端升级可能改变 DOM 结构使分栏失效（参考项目 dsh-worktable 同款依赖，其 README 亦记载宿主升级致插件失效先例） | 宿主升级后小说工作台分栏布局失效（打开无布局调整或错位）；不影响书目数据与绑定数据（文件系统+settings 持久） | 中 | 中 | 探测失败降级：findConversationRoot 返回 null 时不做布局调整，抽屉卡片仍可切换绑定会话（sessions.open 独立于布局）+ 友好提示；互斥协议 dsh:split-claim 兼容 dsh-worktable 共存；实机验证清单覆盖开/关/切会话/多书切换回归 | 宿主升级后打开工作台无布局变化或渲染错位 | Coordinator | 打开（2026-09-12 复核：兼容性架构演进已落地该风险的机械化看护——DOM 面入契约 item 3.8/findSidebar + 面 3 探针（COMPAT-005 诊断面板 D1）+ probe-face 对账 + 降级兜底保持；**残余 = 用户侧实机验证未做**〔无运行中宿主〕⇒ 待用户实机后关闭） | 2026-09-12 |
| RISK-004 | 2026-09-12 | 推送受阻：本机 git 凭证（OAuth App）**缺 `workflow` scope**，GitHub 拒绝推送任何修改 `.github/workflows/ci.yml` 的提交（本轮 COMPAT-003/010/014/015 等含 ci.yml 变更）⇒ 40 commits 滞留本地 | 演进成果与治理记录未上远端（单点副本）；CI 探测轨等 CI 变更无法生效 | 高（确定复现） | 中 | 用户侧动作择一：(a) 用含 `workflow` scope 的凭证/SSH 手动 push；(b) 授权后 Coordinator 重试；(c) 保持本地（不推荐——单点副本） | 再次 `git push origin main` 被拒 | 用户/Coordinator | **已解除（2026-09-12）**——**push 已成功**：`3442f39..872c9c7`（56 commits）+ `[new tag] v0.5.2`（远端 tag 对象 `793dde78` ≡ 本地）；**通道 = SSH over 443**（`ssh://git@ssh.github.com:443/peterwangze/dsh-novel-writing.git`），**绕开** OAuth 令牌的 workflow-scope 限制（该限制不适用于 SSH 密钥）。**根因留档**：HTTPS 路径 `credential.https://github.com.helper = !gh auth git-credential` ⇒ 取 gh 令牌，`gh auth status` 实测 scopes = `admin:public_key, gist, read:org, repo`（**终端侧无 `workflow`**），而浏览器授权页显示 "Update github action workflows" 已在现有授权中——两处读数不一致，因 SSH 通道已解除阻塞**未再深挖**（如实留痕）；**副产物**：探测 SSH 时 `~/.ssh/known_hosts` 由无到有（103 B，仅含 `[ssh.github.com]:443` ED25519），**指纹经与 GitHub 官方公布值逐字符核验一致**（`SHA256:+DiY3wvvV6TuJJhbpZisF/zLDA0zPMSvHdkr4UvCOqU`）；**纠正留痕**：`git push --dry-run` 在 scope 缺失时仍回显成功 ⇒ **不可作为 scope 探针**（GitHub 于 ref 更新阶段拒绝）。详见 EVD-100 / EVD-101 | 2026-09-12 |
| RISK-005 | 2026-09-12 | **治理 hook 静默 no-op**（Release Agent 独立发现 #1，附行号/实测）：`.git/hooks/post-commit` **L321**（Step 5b 锁清理）/ **L354**（Step 5c scope-creep 检测）硬编码 `python3 -c`，而同文件 Step 4b write-guard（**L230-233**）有 `command -v python` → `python3` 回退；本机**无 `python3`**（仅 `python` 3.14.3，`python3 --version` 报不存在）⇒ 该两步被 `2>/dev/null \|\| true` 吞掉、**永不执行** | ①`.governance/agent-locks.json` 的**锁自动释放失效**（只能靠人工/`check-governance` 发现）②**并发写冲突检测失效**（SCOPE CREEP 告警不出——旁证：REL-006 在 CHANGELOG 被 COMPAT-016 持锁状态下提交，hook 零告警）⇒ 多 agent 并行时同文件并发写可能**静默发生** | 高（确定复现） | 中 | 短期：Coordinator **手工维护** locks（REL-006 派发前已手工 acquire、完成后手工 release）+ 每次派发前人工核对 `active_tasks`/`file_locks`（本轮已按此执行）；中期：上游修——插件仓 `infra/hooks/post-commit` 两处改为与 L230-233 同款解释器回退（或统一 `python -c`）；**本仓 `.git/hooks/*` 为安装副本，本地改会与上游分叉且重装即覆盖 ⇒ 属插件仓改动，需用户授权**；登记入 SYSGAP-001 家族。**2026-09-12 用户裁定（DEC-027）＝上游缺陷仅登记、不改插件仓** ⇒ 本仓以「Coordinator 手工维护 locks（派发前 acquire / 完成后 release / 每次派发前核对 `active_tasks`+`file_locks`）」为**正式替代控制**（非临时过渡） | 再出现「持锁状态下提交而零 SCOPE CREEP 告警」或多 agent 同文件并发写 | Coordinator | 打开（缓解已落地，观察窗 = 下一次隔离演练重跑） | 2026-09-12 |
| RISK-006 | 2026-09-12 | **M7.7 真实环境误写事件**（REL-006 隔离演练首轮，EVD-098 已机写）：PowerShell `$HOME` 只读自动变量冲突 ⇒ `$home` 未生效、`DSH_HOME` 被设为真实 `C:\Users\peter` ⇒ `install.ps1` 在真实 profile 根写入 3 项（`profiles/`、`.agent-presets/novel-writing/`、`settings.yaml`，共 48 文件 + 1 junction） | ①用户真实 profile 被插件安装面意外写入（本次 3 项均为新建、已**完全清理**、`~/.dsh` 13 项指纹未变、根目录零残留）②同类脚本退化若发生在**修改/删除型**操作（卸载、覆盖配置、清理旧版）上，可直接损坏用户数据与配置 ⇒ 属「隔离举证失败」类风险，非本次发布缺陷 | 低（已发生 1 次；根因确定、后果可逆、清理可验证） | 中 | ①**已落地**：`$home`→`$dshHomeIso` 重命名；`Set-IsoEnv`/`Get-TreeSnapshot` fail-closed 路径断言（`DSH_HOME` MUST 以隔离根为前缀否则 throw）；逐行**真实路径泄漏检测器**（输出出现真实 home 前缀即 FAIL 中止）；调用侧 `$ErrorActionPreference='Stop'`（同类静默赋值失败将直接终止）②**强制步骤**：演练前 fingerprint + 演练后逐项对比（含真实 `~/.dsh`）③后续 COMPAT-017 收口时评估把「隔离演练脚本 MUST 含路径断言 + 泄漏检测」纳入 smoke/脚本守卫（P-10 家族）④插件仓与本仓 `.git/hooks` 不改（DEC-027 裁定） | 再次出现隔离演练写入真实路径 / 任何 `DSH_HOME` 未以隔离根为前缀即执行安装 | Coordinator | 打开 | 2026-09-12 |
| RISK-007 | 2026-09-12 | **回滚语义边界**（REL-006 隔离演练实测发现，EVD-099）：v0.5.2 → v0.5.1 回滚**不等于 DSH_HOME 状态还原**——v0.5.2 新增的新布局注册项（`profiles/web/node_modules` 链接 + profile `package.json` 280→408 B）**不被 v0.5.1 清理**，闭环 A2→A5 diff=**4**（有效版本回退正确 `0.5.2→0.5.1`，幂等 diff 均 0） | 用户按 `rollback-plan-0.5.2.md` 回滚后，DSH_HOME 内可能残留 v0.5.2 时期的新布局注册项 ⇒ 若后续再装回 v0.5.2 或排障时以「目录内容」为判据，可能与预期不符；**有效版本（判据主面）在演练中已被证明正确** | 中（确定性复现，影响面限于 DSH_HOME 注册项残留） | 低 | ①已在 `docs/release/rollback-plan-0.5.2.md` 与 `release-notes` §5 如实登记为**语义边界**（回滚=版本回退）②回滚方案的验证步骤以**有效版本读数**为主判据（非目录 diff）③长期：若需「状态还原级回滚」，须由 v0.5.2+ 提供卸载/清理路径（**新任务候选**，超出本版范围） | 用户执行回滚后以目录内容为判据 / 后续版本引入新布局注册项 | Coordinator | 打开 | 2026-09-12 |

## 使用规则

- 阻塞项必须同步登记为风险。
- 风险变化应同步回写主计划。
- 风险关闭前必须明确缓解结果。
